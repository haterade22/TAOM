# Mike's decisions (2026-09-24), taken one at a time

| # | Decision | Answer | Follow-through |
|---|---|---|---|
| 1 | Plans 011, 016, 017 are blocked on edits to protected files (`.claude/settings.json`, `Directory.Build.props`). Make the Step 0 edits on their own branches and run them through execution, deep review and Codex? | **Yes, all three** | Orchestrator makes each Step 0 edit in the plan's worktree only (never the main tree), via the config-protection override, then executes and reviews; nothing merged or pushed |
| 2 | Crash capture after 006 (6 shims, live toggle): default and toggle? | **Keep ON, keep the toggle** | No change to 006 |
| 3 | 006 allowlist entry 6 (`BannerlordTableauManager_RequestCharacterTableauSetup`, likely dead in v1.5.3) | **Swap to `RenderTargetComponent_OnPaintNeeded`** | Follow-up commit on `improve/006-*`, test first, then a convergence review |
| 4 | 006 bridge `HandleOrPassThrough`: one exit that always preserves the throw site | **Apply** | Follow-up commit on `improve/006-*`, test first |
| 5 | 006 bridge Harmony priority | **Leave at 400** | None (docs already say 400) |
| 6 | 006 repeat-suppression log cadence (powers of ten) vs an added 60 s time floor | **Keep powers of ten** | None |
| 7 | 008 skip banner in `notify-test-results.sh` goes to stderr, which the session never sees | **Remove the hook change** | Follow-up on `improve/008-*`: revert the hook edit and `tools/test_hooks.sh` section 7c; rely on dotnet's `Skipped:` count and the strict runsettings |
| 8 | 008 `TreatNoTestsAsError` in `binding-gate.runsettings` | **Add it** | Done with D7 and D9 in `37306bca`: strict gate 368/0/0, a zero-match filter now exits 1, suite 10,245 passed / 2 known failures |
| (moot) | 008 `if: !cancelled()` on the self-hosted gate step | not asked | 010 deletes that job; its `csharp.yml` already guards both test steps (`csharp.yml:60,73`) |
| 9 | 008 game-folder resolver order | **Env vars first** (as built) | None |
| 10 | 009: a `[HarmonyPatch]` naming a missing engine type makes Harmony's category cache throw, so all 84 categories fail | **Isolate the broken class** | Follow-up on `improve/009-*`: build the category cache class by class, skip only the broken class; test first |
| 11 | 009 startup failure notice is literal English | **Localize it** | Follow-up on `improve/009-*`: `{=KEY}` strings + the `/localize` pipeline |
| (none) | 012 | no decision beyond filing its issue | |
| 12 | 014 `_lossAnnouncedFor` shown-once latch never cleared | **Clear on reset and discharge** | Follow-up on `improve/014-*`, test first |
| 13 | 014 load reset sits behind the co-op authority gate | **Move it above the gate** | Follow-up on `improve/014-*`, test first |
| 14 | 014 a save with no Enlistment data normalizes the previous session's record | **Clear the store first** | Follow-up on `improve/014-*`, test first |
| 15 | 014 leave-on-arrival offer latch never cleared at the end of a stop | **Once per stop** | Follow-up on `improve/014-*`: clear the latch when the stop ends; test first |
| 16 | 014 `SubModule.OnGameEnd` calls `ResetSessionCaches` to release the dead campaign's objects | **Add it now on 014** (not the recommendation) | Follow-up on `improve/014-*`: one call in `SubModule.OnGameEnd` (single-owner file; another SubModule hunk at merge) |
| 17 | The June `impl-*` branches | **Port through the pipeline** | New `improve/` branches: cherry-pick `cfc47206` (002 NaN guard), `4310aa6e` + `4bc520a1` (005), `6eb5955c` (003); rewrite 001's SpecialResources reset fresh; deep review + Codex each; then delete `impl-001/002/003/005` |
| 18 | How the CHANGELOG works | **Generate it at `/release`** from commit bodies | New plan: `/release` compiles the section from `git log` since the last tag; change AGENTS.md "CHANGELOG every session" and `check-changelog-changed.sh`; archive the current file |
| 19 | Three rules that disagree with the code | **Amend all three** | On a branch: ADR-007 gains a protected-virtual boundary-seam exception with conditions; the interface rule becomes "adapters always, a service only when faked or with a second implementation" (ADR-002, lens 1, feature-builder); the hook-interface layer becomes conditional (AGENTS.md, csharp-patterns, review-reference). ADRs are protected: Mike approved |
| 20 | Repo weight (3.27 GiB pack, no LFS) | **No change** | None |
| 21 | Order of Battle "Auto-Assign is a Phase-1 stub" button | **Wire Auto-Assign** (feature, own plan); still delete the other unreachable scaffolding | New feature plan for `HeroAutoAssigner`; a cleanup branch for `IEditorSceneAdapter`, `EditorCacheRebuild/Caching` and the reserved config fields (ARCH-02) |
| 22 | Does anything reload TAOM in-process? | **No**: Mike stops the debugger, rebuilds, and presses Play, which restarts the game process | Drop the `ResetForUnload` requirement for new patches (record it in the rule and the sweep test's doc); existing methods stay until touched |
| 23 | The desktop's slow mode (launch profiles attach the mixed managed-and-native debugger) | **Managed-only default profile + a separate mixed profile** | Small change to `Main/Properties/launchSettings.json` on a branch; Mike's next Play doubles as the test (PatchShield pass 2 should fall from about 69 s to about 2.5 s in `diag.log`) |

| 24 | 006 (#650): add the mission combat callbacks back to the crash-capture allowlist? | **Add the combat set back** | Follow-up on `improve/006-*`: trace from the engine which mission callbacks dispatch into TAOM mission code (melee and missile hits, agent removed, and the like) and add them, test first |
| 25 | 007 (#651): PatchShield exclusion scope | **Keep the whole `ManagedCallbacks` namespace** | None |
| 26 | 007/006: `CrashReportPatchHelper.HandleAndSwallow` fallback paths return the raw exception | **Fix on the 006 branch** | Follow-up on `improve/006-*`: route each fallback through `RethrowStackPreserver.PreserveForRethrow`, test first |
| 27 | 007 (#651): diag.log label and PatchShield's `_shielded` counts | **Both** | Follow-up on `improve/007-*`: widen the label at `Dependencies/SubModule.cs:281`; split `_shielded` into seen and attached so the summary counts are true; test first |
| 28 | 1.4.5 ports | **All six player-facing branches** (006, 007, 009, 012, 014, 015) | After each merges on 1.5.x: a port branch from `bannerlord-1.4.5`, conflicts resolved against 1.4.x, staged for the laptop to build, test and commit |
| 29 | 011 (#654): GitHub ruleset on the release branches | **No ruleset** | None; the local hook is the only guard |
| 30 | 011 (#654): the local force-push hook's branch scope | **Only the two trunks** (`bannerlord-1.5.x`, `bannerlord-1.4.5`) | Plan 011 executes with the exact names instead of `bannerlord-*` |
| 31 | 015: dropping the z cells widens scan results between grid rebuilds (every extra agent is inside the live radius) | **Keep the wider, correct results** | None; the docs and `CollectInRadius_PointMovedVerticallySinceTheBuild_IsJudgedOnItsCurrentPosition` already describe and pin it |
| 32 | 015: four warg tree nodes resolve services in field initializers (a service locator outside Intentional Patterns) | **Inject through `WargBehaviorTree`** | Follow-up on `improve/015-*`: the tree builder resolves once per tree and passes the services to the four node constructors; test first |
| 33 | 015: `BoneCheckDuringAnimation.Tick` (A) fetch the skeleton after the hit-window test, (B) read progress once | **Both, proved in the owed warg battle** | Follow-up on `improve/015-*` with D32; no unit test reaches `Tick`, so the in-game warg Custom Battle is the proof |
| 34 | 019: the no-settlement, no-camp-frames path in `BesiegerCamp_GetSiegeCampPartyPosition_Patch` still defers to vanilla | **Close it as unreachable** | None; the log line stays as the tripwire |
| 35 | 019: a partial `KingdomSiegeMessages` JSON entry shows a blank button or empty text | **Per-field fallback to `DefaultMessages`** | Follow-up on `improve/019-*`, test first |
| 36 | 019: the scope extension into `.claude/skills/build-fix/SKILL.md` | **Keep it** | None |
| 37 | 009 (D11): may the executor run the paid translator (`tools/translate_with_claude.py --module TAOM --sync-ids --apply`, 12 languages) for the new keys? | **Yes** | The 009 follow-up runs it on its branch for the new keys only; explicit paid-dispatch authorization per AGENTS.md. Done by the orchestrator: only the 5 new keys (a scoped run of the branch's own translator functions; the stock CLI would also have translated unrelated rows), 12 languages, about $0.14, committed `b6cb6ff5` |
| 39 | 013: narrow each Bash hook's prefilter to the word it gates (`commit` for the six commit gates and suggest-compact, `push` for validate-push, `no-verify` for block-no-verify) | **Apply both narrowings** | Follow-up on `improve/013-*`; section 5 payloads updated; hook suite green. Plan 011 also edits `validate-push.sh` (D38), so 011 executes on top of 013's tip |
| 40 | 013: a payload with a `\u` escape could hide the gated word from the prefilter (fail-open) | **Route `\u` payloads to the parser** | Same 013 follow-up; a hook-suite row with an escaped word proves it |
| 41 | Found by 013's review (pre-existing MED): `mark-verification-run.sh` never marks the canonical `dotnet test` command (strips `dotnet` as an env prefix; splits inside quotes) | **Fix it inside plan 011** | 011 makes its Stop reminder visible, so it ships with a working marker; RED hook-suite row first |
| 42 | Found by 013's review: `suggest-compact.sh` (PreToolUse on every call) and `notify-test-results.sh` print only to stderr on exit 0, which reaches nobody | **Delete both, inside plan 011** | 011 removes the two registrations from `.claude/settings.json` (D1 approval), the scripts, their hook-suite sections and catalog rows |
| 43 | 006: D24 stopped safely (combat callbacks arrive off the main thread; a bridge capture is not marked off-thread, so the crash service would run collectors and UI on a worker thread) | **Mark off-thread, then add the 10** | Second follow-up on `improve/006-*`: the bridge sets `OffMainThreadDataKey` when `MissionThreadGuard.IsOnMainThread` is false; then the 10 traced callbacks join the allowlist (cap raised); test first; attach cost from the log line |
| 44 | 010: delete the unused SandBoxCore reference (build parity proven on 1.5.3; 1.4.8 unchecked) | **Delete on 1.5.x** | Follow-up on `improve/010-*`; the 1.4.5 CI port re-checks against 1.4.8 |
| 45 | 010: move `RequiresGame` from the `Patch86HideoutBossFightBindingTests` class to its one game-bound method | **Move it, prove on the RefAsm replay** | Same 010 follow-up; restore the class tag with a reason if either method fails on the reference assemblies |
| 46 | 010: point the CI unit step's `BANNERLORD_GAME_DIR` at `refasm-game` to recover some of the 24 skipped tests | **Try it, keep only what's green** | Same 010 follow-up; keep only if skips drop with no new failure, else record the numbers and revert |
| 47 | 006 (follows D43): the executor stopped because `MissionThreadGuard` knows the main thread only after the first mission tick | **Reuse `AppDomainExceptionHook`'s boot-time main-thread id** | Third 006 follow-up: expose that id (recorded in `Subscribe()`, `AppDomainExceptionHook.cs:42`) to the bridge, mark off-thread captures, then add the 10 combat callbacks |
| 48 | 018: what "fail closed" means at campaign start (Patch37 swallows the runner's rethrow; the loading step retries) | **Decide when it matters** | Recorded as a precondition in plan 018's Maintenance notes: the first `OwnsSaveData` module's migration settles it first. No code now. (018's CHANGELOG entry, a deviation from the plan's scope, stands: AGENTS.md asks for one every session) |
| 49 | 021: the seam conditions as decided (ids and value types only; one engine call per seam) would flag today's reviewed seams (`RefugeService.SpawnRefugeParty`, `IsCampReady(CampState)`) | **Looser variant** (ids, value types and TAOM types, never a sealed TaleWorlds class; one engine operation per seam, no TAOM decision logic) | Written into ADR-007 in 021's Step 0 commit `1cdf8eb0` |
| 50 | 2026-09-25: "commit, push and merge everything that is ready" into `bannerlord-1.5.x` (not 1.4.5); bypass mode for 011, 016, 017, 020, 021 | **Done** | 19 reviewed branches merged in an integration worktree (build and full suite after every merge), CI replayed locally, 69 engine-bound tests tagged `RequiresGame`, pushed as `e9b28e2a`; first hosted C# CI run green; 11 issues closed. Protected Step 0 edits made under a brief `-none` override window (the hook sees no session id), closed after each round |
| 38 | Found by 013's review (pre-existing HIGH): `validate-push.sh:54` reads only the first line, so a force push after a `cd` line passes | **Fix it inside plan 011** | Plan 011's execution scans every line of the command, two-line RED test first |
| 51 | 2026-09-25, plan 016: which Serena version `.mcp.json` pins (016 as executed pinned the v1.7.0 release, a step back from the unpinned `main` in use) | **"The newest version of Serena"**: pin `main` at `7a2968335f2198b966864de1ce3655c8e485a653` (`serena-agent` 2.0.0.dev0, committed 2026-09-24; newest release is still v1.7.0 of 2026-08-09) | Checked before applying: `uvx --from git+https://github.com/oraios/serena@7a296833... serena start-mcp-server --help` exits 0 and lists `--context` and `--project`; `ide-assistant` still maps to `claude-code` there. Applied on the 016 branch after its deep review finishes (`.mcp.json:8` and its CHANGELOG line) |
| 52 | 2026-09-25, plan 005 item A: BUTR's GitHub Packages credential inside three gitignored vendor archives (`Dependencies/.vendor-source/`) | **Leave them as they are** | The checklist line from plan 005 (credential sweep over vendored drops, archives included) is the guard; plan 005 item A closes as accepted |
| 53 | 2026-09-25, plan 005: tell BUTR their public source ships the credential | **Don't tell them** | Their public source, their credential |
| 54 | 2026-09-25, plan 005: file the public GitHub issue | **File it with the credential part trimmed** | The issue covers the faction-map injection fix and the checklist line; it does not name the archives. Note: trunk already names them (CHANGELOG, the 005 review record, audit notes), never the value |
| 55 | 2026-09-25, plan 021 review O1: ADR-007's seam rule covers only `protected virtual` members, but the cited services use private lookup helpers only seams call (`RefugeService.FindParty`/`FindHero`/`FindTroop`, `SupplyOrderService.FindHero`, `WardenService.FindHero`) | **Count them as seam body** | One sentence in ADR-007 after condition 4, mirrored in the Standards lens: condition 1 applies to the calling seam's signature, not the helper's. Applied on the 021 branch |
| 56 | 2026-09-25, plan 021 review O2: four seams carry TAOM decision logic against condition 2 (`SupplyOrderService.ChargePlayer`, `RefugeService.FindNearestHostile`, `WardenService.CompanionsInMainParty`, `WardenService.MintCompanionFromTroop`) | **Refactor them now** | New plan 026 (`improve/026-seam-decision-logic`, worktree `wt-026` from `31cc629f`): behaviour-preserving move of the logic into the services, tests first; ADR-007's "each tested through one subclass" becomes "test subclasses" on the 021 branch (RefugeServiceTests has two) |
| 57 | 2026-09-25, plan 026 scope: the final 021 convergence pass found three more seams with filter or routing logic (`SupplyOrderService.RefundConsumption` with `RestoreVolunteerSlots`, `RefugeService.ReleasePeacePrisoners`, `CampService.DistanceToNearestFortification`) | **Add all three to plan 026** | The orchestrator's read adds `RefugeService`'s twin of the fortification search (`DistanceToNearestFortification`/`...From`, private `DistanceToNearestFortificationFromPosition`, the same filter and nearest pick), handled with CampService's as one item |
| 58 | 2026-09-25, plan 016 review: Serena's editing tools bypass every Edit/Write hook (hooks match tool names), like the nine git/filesystem MCP write tools already denied | **Deny all but memory** | Applied on the 016 branch in shared `.claude/settings.json`: the 15 tools Serena 7a296833 marks can-edit other than its memory tools (`create_text_file`, `replace_content`, `replace_in_files`, `delete_lines`, `replace_lines`, `insert_at_line`, `replace_symbol_body`, `insert_after_symbol`, `insert_before_symbol`, `rename_symbol`, `safe_delete_symbol`, `execute_shell_command`, `jet_brains_move`, `jet_brains_safe_delete`, `jet_brains_rename`); `write_memory`, `edit_memory`, `rename_memory`, `delete_memory` stay allowed. Not covered: the optional beta `serena_repl` (not marked can-edit, not enabled) |
| 59 | 2026-09-25: the `bannerlord-1.4.5` ports (decision 28's list, and 016's untrack and pins) | **Mike ports from `bannerlord-1.5.x` himself, on the laptop** | The orchestrator stages no 1.4.5 port branches; decision 28's port-branch task is withdrawn |
| 60 | 2026-09-25, plan 011 review: also block a push that deletes a trunk (`--delete`, `:branch`)? | **Force pushes only** | `validate-push.sh` stays as decision 30 scoped it |
| 61 | 2026-09-25, plan 011 review: nine PreToolUse gates are registered for Bash only, so a PowerShell git command skips them | **New plan to cover PowerShell** | Plan 027: register the gates for PowerShell too, each gate's parsing checked for PowerShell syntax, test rows per shell |

Pending: see FOR-MIKE.md for the open design questions from the reviews.
