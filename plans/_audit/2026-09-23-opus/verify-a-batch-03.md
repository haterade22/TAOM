# Verify batch A-03: adversarial check of ARCH-04, ARCH-03, ARCH-07

Checker: fresh adversarial verifier. All reads at `b2e387db` via `git show b2e387db:<path>` or
`git grep ... b2e387db` (HEAD has moved to `4b5662b2`; the working tree is not used for evidence).
No build, no test.

## ARCH-04: Extract parked NativeSkinFixes; keep NavalTravel parked

- **Outcome:** CONFIRMED (impact LOW; a direction item that needs Mike's yes, as the finding says).
- **Re-read at `b2e387db`:**
  - `Main/SubModule.cs:716-739`: install branch commented out ("PARKED 2026-07-08 (user decision)"); `:728` says "RE-ENABLE: uncomment the install branch below AND flip the MCM default". `:2121-2124` still calls `NativeSkinFixesInstaller.Uninstall()`, which returns at once when not installed (`NativeSkinFixesInstaller.cs:152-154`, `if (!HasInstalled) return;`).
  - Nothing else loads the DLL: `git grep -n -E "NativeHookLoader|TAOM\.NativeSkinFixes\.dll|LoadLibrary" b2e387db -- Main` hits only the five feature files, the `SubModule.cs:718` comment and `THIRD-PARTY-LICENSES.txt`; the only `Install` caller is the comment at `SubModule.cs:736`; `Main/_Module/SubModule.xml:73` lists only `TAOM.dll`. MinHook has no other consumer (`git grep -l -i MinHook` over `Main`).
  - Sizes: `git ls-tree -l` gives `TAOM.NativeSkinFixes.dll` 190,976 B and `MinHook.x64.dll` 16,384 B. Managed: 5 files, 474 lines (62+62+63+113+174). Native: 15 `.cpp/.h/.ps1` files, 1,521 lines (MinHook excluded). NavalTravel: 11 `.cs` files, 868 lines; tests 236+156 = 392 lines.
  - `Signatures.h:29` "AUTHORED FOR BANNERLORD v1.4.6 (2026-06-30)"; `docs/features/native-skin-fixes.md:131-140` states the patterns are unverified since and must be re-authored before re-enabling.
  - CI step `.github/workflows/build.yml:81-102` (static-CRT check) targets only this DLL. Lens `1-standards.md:17` item 9b is C++ only; the repo holds no other `.cpp/.h` outside MinHook and this port (`git ls-tree -r` filter returned nothing), so its only live target is this port.
  - NavalTravel: model commented at `SubModule.cs:1038-1044`, Patch54/57 at `:1747-1761`, `IoC.cs:112` plus `NavalTravelIoC.cs` (three singletons); snapshot rows `patch-targets.md:197-199`, `gamemodel-bases.md:179-185`. `HarmonyPatchBindingTests` enumerates every `[HarmonyPatch]` type by reflection (`:83-92`), so Patch54/57 are checked on each bump.
- **Refutation attempts that failed:** no second loader, no SubModule.xml native entry, no other C++ consumer of the lens or CI gate. The parking is decided (BRIEF), but the brief explicitly makes its carrying cost fair game, and the finding does not challenge the park itself; it proposes a reversal only with Mike's approval. The vendored-DLL trap row is an allowlist, not a mandate to ship.
- **Partly known already:** the "not re-enable-ready" half is recorded in `docs/features/native-skin-fixes.md:131-140`. What is new is that the in-code instruction (`SubModule.cs:728-729`) and the MCM hint (`TaomSettings.cs:138`, "re-enabling is a code change (uncomment ... + flip this default)") contradict that doc.
- **Corrected evidence (minor, not load-bearing):** the posture row is `TAOM.Tests/Features/Mcm/SettingRequireRestartPostureTests.cs:39` (the finding drops `Features/`). The CI step spans `build.yml:81-102` (the finding says `:85-99`). The NSF hint also opens "Currently DISABLED", so it is as honest as NavalTravel's about being off; only its re-enable recipe is wrong. Of NavalTravel's three toggles only the master (`TaomSettings.cs:992`) says "Currently DISABLED". Commits since `141b749` depend on path scope: `Main/Features/NativeSkinFixes` 2 (`c3dde6b9`, `4b3f30a2`), `Dependencies/...NativeHooks` 4 (adds `0f348a84`, `2ea360d6`), the test 2 (`0fc9b8c1`, `c3dde6b9`); all incidental sweeps, which supports the claim. `git grep -l -i NavalTravel` over docs, `.claude` and `.ai` gives 26 files, not 27.

## ARCH-03: the "patch to hook interface to service" rule describes 11% of patches

- **Outcome:** CONFIRMED (impact LOW; doc policy plus two optional folds), with corrected evidence below.
- **Re-measured at `b2e387db`** (`git archive b2e387db Main TAOM.Tests ...` into the scratchpad; scripts `arch03.py`, `arch03b.py` there):
  - 217 `Main/**/*.cs` files contain `[HarmonyPatch` or `HarmonyPatchCategory` (213 once comments are stripped). 24 of them name an `IOn*` interface (`grep -rln "\bIOn[A-Z]" | xargs grep -l HarmonyPatch`), so 11% holds. My own coarse split of the rest: 116 reach a non-adapter TAOM interface, 4 only an adapter, 73 no TAOM interface (the finding's 113/3/77 is within heuristic noise; 26 of my 73 call an `Initialize(` or `IoC.Resolve<Concrete>`, which matches the finding's own caveat).
  - 19 `IOn*` interfaces (listed by the script). `Substitute.For<IOn` in `TAOM.Tests`: 0; test classes implementing an `IOn*`: 0.
  - `AGENTS.md:36` reads "Patch, model or behavior → hook interface → service → adapter" as written.
  - Forwarders re-read: `SpecialResources/Hooks/RecruitmentResourceGateHook.cs:14-15` is one expression-bodied forward to `ISpecialResourceService.CanAffordRecruit`; its only consumer is `RecruitmentVM_RecruitGate_Patch.cs:23,26` (the ExecuteDone patch reaches it only through that class's static `EvaluateCart`), wired at `SubModule.cs:1707`. `FactionMap/Hooks/CultureStageViewFinalizeHook.cs:5-9` calls two statics on `CultureStageViewCreatedHook`. `BannerInjection/Hooks/BannerEditorDoneHook.cs:16-26` is two `MarkAsPlayerModified` calls, a null check and two log lines. No test file names any of the three (grep over `TAOM.Tests`).
  - `PartyUpgradeResourceCheckHook.cs:14-41` is mostly forwards over the 192-line `ISpecialResourceService.cs`, as stated. Side note verified: `CultureStageViewTickHook.cs:15` does `AccessTools.Field(viewInstance.GetType(), "GauntletLayer")` per tick; `CultureStageViewCreatedHook.cs:70` does the same lookup once.
- **Refutation attempts that failed:** no ADR, rule or review-reference line makes the hook layer optional; `docs/adrs/002-thin-entry-points.md` never mentions hooks, and the "Known exceptions" list in `.ai/review-reference.md:176` only blesses `IoC.ResolveAll<T>()` for multi-hook dispatch. Not in the BRIEF's decided tradeoffs (ADR-007 covers adapters, not hooks).
- **Corrected evidence:**
  - The mandatory wording is not only `AGENTS.md:36`. The same chain is stated at `.ai/review-reference.md:215` ("One-liner ... -> IHookInterface -> Service") and `:328` ("Patches are thin entry points, delegate ALL logic to services via `IHookInterface`"), `.ai/review-reference.md:389-399`, `.claude/rules/csharp-patterns.md:11-22`, `docs/ai-includes/architecture.md:247` and `docs/ai-includes/decompiled-code-analysis.md:99,279`. The fix sketch edits only `AGENTS.md:36`, which would leave five sources contradicting it; the policy edit must cover all of them.
  - The `/deep-review` Standards lens does not demand the hook layer: `.claude/skills/deep-review/lenses/1-standards.md:12` item 5 asks only that entry points "delegate to services". So the friction the finding describes comes from the shared `.ai` review reference and the rules file, not from the Claude review gate.
  - `IOn*` implementers are 15 classes, not 13 (script list: the 14 a single-line `grep -E "class \w+[^{]*:[^{]*\bIOn"` finds, plus `TroopWeightDisplayHook`, whose five interfaces sit on a multi-line base list at `TroopWeight/Hooks/TroopWeightDisplayHook.cs:39-44`). Which two the finding dropped is not knowable from its text; the count does not change the conclusion.

## ARCH-07: duplication verdicts (fold row VMs, skill maps, three LotrIssue methods; LotrIssue base-class trap)

- **Outcome:** CONFIRMED for the three recommended folds (impact LOW). Two supporting claims are wrong and are corrected below: the "a `LotrIssueBase` is save-breaking" warning is overstated, and the Elephant/Mumakil reject rests on a ratio that counts comment lines.
- **Re-read at `b2e387db`** (scratchpad extraction; script `arch07.py`):
  - `jscpd-digest.md:3` gives 76 clones, 1,114 lines, 0.69%, as quoted.
  - **Row VMs:** `diff SupplyGoodRowVM.cs SupplyTroopRowVM.cs` (79 lines each) differs only in the doc comment (`:8-10`), the class name (`:12`), the constructor name (`:18`), the label string (`:25`) and the variable name (`:27`). Added in `2244fed5` (2026-08-22), after `141b749` (2026-06-13): "introduced" holds. The prefab uses separate `ItemTemplate`s (`TaomSupplyOrderScreen.xml:226`, `:310`), but both bind the same names (`@Label` at `:234`/`:318`, `@QtyText` at `:254`/`:338`). The engine resolves bindings by property name on the runtime type (`TaleWorlds.Library.ViewModel.cs:241-254` caches properties per `GetType()`; `:451-453` `GetPropertyValue(string name)`, installed v1.5.3 via `taom-src`), so the "separate item templates" comment does not require two types. Cost the finding omits: `TAOM.Tests/Features/SupplyLines/SupplyOrderPrefabBindingTests.cs:35-41` and `:181-183` map each list to its VM type and need the same edit.
  - **Skill maps:** `CareerMenuService.cs:30-62` and `NarrativeMenuBuilder.cs:18-50` are byte-identical after trimming indentation (18-entry `SkillMap`, 6-entry `AttributeMap`); jscpd's `:30-64`/`:18-52` range also takes in the differing constructor line. They are the only two `Main` files with more than one `DefaultSkills.X` reference (18 each, every other file 1). The second clone (`ResolveSkills`, `CareerMenuService.cs:332` / `NarrativeMenuBuilder.cs:133`) has drifted: only `NarrativeMenuBuilder` logs a warning on a null skill.
  - **LotrIssue methods:** after whitespace normalisation, `GetFrequency` (11 lines), `CanPlayerTakeQuestConditions` (19) and `EnsureDef` (4) are identical in all three Issue classes: 34 lines per copy, so 68 redundant lines ("about 70" holds). `Tx` and `GetIssueEffectAmountInternal` differ (the `ITEM` variable; `SettlementSecurity` vs `SettlementLoyalty`), as stated. File sizes 297/419/366 match.
  - **FactionMap probe:** `PossibleTypeNames` plus `FindCultureStageViewType()` repeat at `:15-29` in `CultureStageView_Constructor_Patch.cs`, `_Finalize_Patch.cs` and `_Tick_Patch.cs`.
- **Save mechanism, now verified against the installed DLL** (`taom-src` resolved it this run): `TaleWorlds.SaveSystem.Definition.MemberTypeId.cs:9` `SaveId = (TypeLevel << 8) + LocalSaveId`; `TypeDefinitionBase.cs:20-33` `GetClassLevel` counts depth to `object`; `TypeDefinition.cs:176-177` keys each `[SaveableField]` by `GetClassLevel(fieldInfo.DeclaringType)`; `TaleWorlds.SaveSystem.Load.FieldLoadData.FillObject` silently skips a saved id with no matching definition (no throw, the field stays default).
- **Corrected evidence (the trap is narrower than stated):**
  - The quest fields are not at risk. They sit on `*LotrIssueQuest : QuestBase` (`CombatLotrIssue.cs:142-150`, `DeliverGoodsLotrIssue.cs:165-173`, `DeliverPersonnelLotrIssue.cs:143-150`), which an `IssueBase` intermediate never touches.
  - Each Issue class has exactly one saveable field, `[SaveableField(1)] _defId` (`CombatLotrIssue.cs:29`, `DeliverGoodsLotrIssue.cs:30`, `DeliverPersonnelLotrIssue.cs:28`). If `_defId` moves into `LotrIssueBase` (the natural move, since the shared `EnsureDef` reads it), its declaring type sits at the depth the concrete classes had before, so its `MemberTypeId` is unchanged and old saves load. The break happens only if `_defId` stays declared in the subclasses (level +1, then silently null on load). The concrete type ids (`LotrIssueSaveableTypeDefiner.cs:30-37`) do not change. Whether the engine also wants a definer entry for the new intermediate type is UNVERIFIED (I found no base-type check in `DefinitionContext.cs`). The engine also offers `IConflictResolver` member remapping (`DefinitionContext.cs:142-160`). Verdict: the static helper is still the lower-risk fold. A base class is a viable alternative with a named condition, not save-breaking by construction.
  - **Elephant/Mumakil reject:** the quoted ratios (0.68, 0.33, 0.36, 0.42, 0.25) and line counts (277/210, 68/65) reproduce only on non-blank lines **with comments counted**. On code lines alone (comment lines dropped, which the finding says it did) they are BT 0.85, standing point 0.51, mission behavior 0.42, crew spawner 0.53, config 0.54. So "below 0.5 on every large pair" is false for code. The reject may still hold on simplicity grounds (a parameterised base for two creatures, with shared attack logic already in `ElephantLike`), but it needs that argument, not this measurement.
  - JSON config providers (a "known, not re-reported" row): `grep -c -E "TaomSettings\.Instance\?\.\w+ \?\? _defaults"` over `*SettingsProvider.cs` gives 10 files and 37 fallbacks, not 11 and 38 (a pattern difference, not load-bearing).

## What I did not cover

- No build, no test (brief). Parity claims for the proposed deletions and folds rest on reference greps, not a compile.
- ARCH-04: the impact-doc rows (`v1.4.7-impact.md:24`, `v1.4.8-impact.md:67,70`, `v1.5.3-impact.md:65`) and the `GameModelOverrideBindingTests` handling of the unregistered `TaomPartyNavigationModel` were not re-read. Whether uncommenting the install would crash or merely go inert on v1.5.3 is native behaviour I did not test.
- ARCH-03: I did not rebuild the finding's exact 113/3/77 split, only my own coarse one (116/4/73).
- ARCH-07: Gauntlet binding by name is verified on the `ViewModel` side only, not in the movie/widget binder. I did not check whether the save system needs a definer entry for a new intermediate `LotrIssueBase`. The Spider/ElephantLike and Warg similarity rows and the per-culture table row were not re-measured.
