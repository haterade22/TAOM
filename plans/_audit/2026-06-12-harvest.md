# /improve audit harvest — 2026-06-12

Recovered from stalled Workflow `wf_a32fca04-d55` journal (session hit usage limit before aggregation).
**99 unique findings** across 10 categories (1 re-run duplicates removed; 17 audit-results + 68 verdict-results in journal; 67 findings auto-matched to a verifier verdict — UNMATCHED ≠ refuted, just not auto-joined).

> These are RAW subagent findings. Per the /improve skill, the advisor (Claude) must personally re-read each cited location before it enters the presented table — treat every row as a hypothesis. Verdicts shown are the in-workflow verifier's view (supporting signal), not final.

## CORRECTNESS (6)
_Not audited:_ Not covered or considered-and-dropped: (1) No builds/tests run — the session verification baseline (3143 passed, validator PASS) was treated as authoritative, including BindingVerification for Harmony signatures. (2) In-flight work excluded per brief: Spider Hooks/, HeroRace CharacterSpawnerService.cs, SubModule.cs unstaged edits, spider docs, SpiderDiag battery. (3) CrashReport's pervasive `catch

### [CORRECTNESS-01] CareerDataService is never reset on new campaign — prior campaign's career data contaminates the next campaign and its saves
- **effort** S · **risk** LOW (Reset on OnNewGameCreatedEvent fires before CC stages (vanilla CharacterCreationCampaignBehavior itself initializes CC from that event; NamedCompanionBehavior.cs:18-27 documents the dispatch order), so a reset there cannot wipe the CC-selected career. Save-compat: none — only clears in-memory state at campaign boundaries.) · **confidence** HIGH · · unmatched
- **impact**: Start campaign B after playing campaign A in the same Bannerlord process: B's player inherits A's career choices, tier unlocks, and quest flags; the new career's root choice can be silently rejected at CC; AI/lord entries leak wholesale. On B's first save the leaked data is serialized into B's save file — permanent contamination. CareerCampaignBehavior.cs:46-49 fixed exactly this class for the ability cache (Phase 9b #128 P2 R1) but left the data service uncovered.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/CareerSystem/CareerPersistenceBehavior.cs:20-22` — RegisterEvents() is empty — no OnNewGameCreated/OnSessionLaunched reset, unlike every sibling persistence feature; the only bulk replace of career data is the SyncData load branch (line 118).
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/CareerSystem/CareerDataService.cs:8, 21-25, 89-96` — Singleton-held _heroData keyed by hero StringId (stable across campaigns: 'main_hero', 'lord_1_1'); SetCareer overwrites only CareerStringId — ChoiceIds/TierUnlocks/Flags are cleared only by ClearCareer, which is called solely on career switch (CareerSwitchService.cs:64) and hero death (CareerCampaignBehavior.cs:118).
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/CareerSystem/CareerSystemIoC.cs:16` — ICareerDataService registered Reuse.Singleton — process lifetime, survives campaign teardown.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/CareerSystem/CareerCreationHandler.cs:39-46` — CC finalize calls SetCareer then TryAddChoice(root, maxChoices); with leaked choices from the prior campaign, GetChoiceCount() >= maxChoicesAllowed (CareerDataService.cs:30) silently rejects the new campaign's root choice.
  - `E:/Decompiled_Bannerlord/Campaign/TaleWorlds.CampaignSystem/TaleWorlds.CampaignSystem/CampaignBehaviorDataStore.cs:26-39, 86-106` — v1.4.5 decompile: SyncData leaves the ref untouched on a missing key, and LoadBehaviorData never calls SyncData for behaviors absent from the save; new games never call it at all — so nothing ever replaces the singleton's stale dict.
- **fix sketch**: Register OnNewGameCreatedEvent in CareerPersistenceBehavior calling _dataService.RestoreData(new Dictionary...) — plus the _lastSessionStarter/_justLoadedFromSave starter-change reset from CultureConversionBehavior.cs:93-107 to also cover loads where SyncData never fired. Optionally ClearCareer before SetCareer in CareerCreationHandler as belt-and-braces.

### [CORRECTNESS-02] SpecialResourceStorageService balances survive campaign boundaries — ResetSessionState misses the storage dict
- **effort** S · **risk** LOW (Clearing storage in OnNewGameCreated (before InitializeHero, which already runs there) only removes stale state; nulling a local in the SyncData load path matches the established Messenger pattern. Legacy-save seeding (Contains==false) is exactly what the fix restores.) · **confidence** HIGH · · unmatched
- **impact**: Cross-campaign resource-balance leakage: new campaign B starts with campaign A's balances for every (hero, resource) pair except the single key InitializeHero overwrites; kingdom-switching players inherit old balances instead of StartingAmount; other heroes' entries leak wholesale and are written into B's save at the next SyncData. Wrong currency amounts, desertion/upgrade gating computed from them.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/SpecialResources/SpecialResourceStorageService.cs:8, 34-37` — Singleton _data dict (heroId:resourceId composite keys); RestoreData is the only bulk replace and no caller ever passes an empty dict at a campaign boundary.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/SpecialResources/SpecialResourceService.cs:278-293, 304` — ResetSessionState (the Phase 9b #133 P2 R1 new-campaign reset) clears only _pendingSpend/_inSession/_loggedResolveKeys — never the storage; InitializeHero overwrites just one (main_hero, resolved-resource) key.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/SpecialResources/SpecialResourcesBehavior.cs:79-91` — SyncData passes the LIVE storage dict as the ref on load — if the save lacks the key, the prior campaign's dict survives and RestoreData re-installs it (contrast MessengerCampaignBehavior.cs:77-79, which nulls a local first).
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/SpecialResources/SpecialResourcesBehavior.cs:134-146` — Seeding gate is _storage.Contains(hero, resource) — a stale entry from campaign A suppresses StartingAmount seeding in campaign B, so the player inherits A's balance for any resource pair A ever touched.
- **fix sketch**: In OnNewGameCreated call _storage.RestoreData(new Dictionary...) before InitializeHero; in SyncData's load branch use a null-initialized local (Dictionary<string,float> data = null; SyncData; _storage.RestoreData(data)) so an absent key yields empty storage instead of the prior campaign's dict.

### [CORRECTNESS-04] TroopWeight hot-path hooks cache on object identity hash with no eviction and swallow all exceptions
- **effort** S · **risk** LOW (Keying on PartyBase.Index is a strict improvement (engine-unique int); adding a session-launch cache clear and a log-once in the catch changes no happy-path behavior. If any caller reaches the getter off-thread (engine _MT precedent), the unlocked Dictionary writes are also a corruption risk — the fix should note but need not solve that beyond the clear.) · **confidence** HIGH · · unmatched
- **impact**: Patch17 postfixes PartyBase.NumberOfAllMembers/NumberOfRegularMembers — gameplay-feeding getters (party-size budget). An identity-hash collision between two live parties whose roster VersionNo values also match serves one party the other's weighted count (rare but real, and likelier as dead parties' entries accumulate forever). The unbounded dict also retains entries for every party ever created across all campaigns in the process, and the empty catch means any TroopWeightService fault disables the feature silently with zero diagnostics.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/TroopWeight/Hooks/PartyBaseNumberOfAllMembersHook.cs:11, 25-45` — Dictionary<int,(Version,Weighted)> keyed on partyBase.GetHashCode(); never cleared; whole body wrapped in catch { } (empty).
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/TroopWeight/Hooks/PartyBaseNumberOfRegularMembersHook.cs:11, 25-56` — Same pattern duplicated; also the injected IModLogger ctor parameter is accepted but never stored, so the swallow can never even log.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/TroopWeight/TroopWeightIoC.cs:13-14` — Both hooks registered Reuse.Singleton — the caches live for the whole Bannerlord process, across save-loads and campaigns.
  - `E:/Decompiled_Bannerlord/Campaign/TaleWorlds.CampaignSystem/TaleWorlds.CampaignSystem.Party/PartyBase.cs:222, 911` — PartyBase does not override GetHashCode (default identity hash — stable per object but NOT unique), while an engine-unique PartyBase.Index (Campaign.Current.GeneratePartyId) exists and is the correct key.
- **fix sketch**: Key the cache on partyBase.Index instead of GetHashCode(); clear both hooks' caches on session launch/new game; replace the empty catch with a log-once guard. Consider ConcurrentDictionary if profiling shows off-main-thread access.

### [CORRECTNESS-05] TroopWeightXmlLoader admits NaN/Infinity weights — the `<= 0` guard passes both (4th shipping of the house NaN bug class)
- **effort** S · **risk** LOW (Adding a finite-check that logs and skips the entry (same as the existing `<= 0` branch) only tightens validation; shipped XML contains finite values so behavior for valid configs is unchanged.) · **confidence** HIGH · · unmatched
- **impact**: One 'NaN' or 'Infinity' weight in user-editable troop_weights.xml silently breaks weighted member accounting for every party containing that troop — the troop-weight feature and its display patches go quietly wrong with a warning-free load log ('Loaded N troop weights'). Violates the mandatory FiniteFloatValidator pattern in a loader written before (or despite) the rule.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/TroopWeight/TroopWeightXmlLoader.cs:75-90` — float.TryParse(NumberStyles.Float) accepts the strings 'NaN' and 'Infinity'; the only semantic guard is `if (weight <= 0)` — NaN <= 0 and Infinity <= 0 both evaluate false, so non-finite weights are stored.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/TroopWeight/TroopWeightService.cs:38-44, 87-88, 98-105` — GetTroopWeight returns the stored value uninspected and multiplies it into every weighted roster count; the hooks then (int)Math.Ceiling() the NaN/∞ into garbage (int.MinValue on .NET Framework x64), which the `> __result` comparison silently discards.
  - `c:/Users/mikew/source/repos/TAOM/.claude/rules/csharp-architecture.md:Config Providers MUST Validate, rule 4` — House rule mandates rejecting NaN/±Infinity BEFORE range checks via TAOM.Core.Validation.FiniteFloatValidator and records that this exact bug shipped three times (Career #31, EditorCacheRebuild #38, CS_Road 2026-05-13).
- **fix sketch**: Insert FiniteFloatValidator.IsFinite (or float.IsNaN/IsInfinity) rejection between TryParse and the `<= 0` check, log-warn and skip; add the per-rule semantic-invalid test (weight='NaN', weight='Infinity') required by the house rule.

### [CORRECTNESS-06] CareerSystem mutation params bypass the hardened ParseFloat — NaN/Infinity flow unguarded into ability templates
- **effort** S · **risk** LOW (Rejecting non-finite values in GetFloat (return defaultValue, matching ParseFloat's hardened semantics) plus a finite guard before prop.SetValue only affects malformed configs; shipped career XML uses finite literals.) · **confidence** HIGH · · unmatched
- **impact**: A career-XML typo like value="NaN" or an overflow-to-Infinity factor silently poisons mutated ability templates, reproducing the exact Career review #31 failure class (NaN cooldown → ability permanently ready / never ready; NaN radius → all distance comparisons false) that the template-attribute path was already hardened against. User-editable XML, zero diagnostics.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/CareerSystem/Mutations/MutationParams.cs:15-20` — GetFloat returns float.TryParse(NumberStyles.Float) output with no NaN/Infinity rejection — unlike the same file's sibling CareerConfigProvider.ParseFloat, which was explicitly hardened (Phase 9b #128 P2, CareerConfigProvider.cs:457-470).
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/CareerSystem/CareerConfigProvider.cs:250-263` — Mutation attributes are captured as raw strings into the Params dictionary — they never pass through the validated ParseFloat path, so the #128 fix does not cover them.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/CareerSystem/Mutations/BuiltInCalculators.cs:8-20` — Calculators add/multiply GetFloat results into baseValue (e.g. baseValue * p.GetFloat("factor", 1f)) — NaN/∞ propagates through every operation.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/CareerSystem/Mutations/MutationService.cs:89-97` — ApplyMutation writes the calculated value into the AbilityTemplateData float property via prop.SetValue with no finite guard — the poisoned template is then consumed by ability Duration/Radius/Cooldown logic.
- **fix sketch**: Mirror CareerConfigProvider.ParseFloat's NaN/Infinity rejection inside MutationParams.GetFloat (fall back to defaultValue), and add a defensive float.IsNaN/IsInfinity skip-with-warning in MutationService.ApplyMutation before SetValue; add semantic-invalid tests per the config-validation rule.

### [CORRECTNESS-03] Loading a save without a behavior's save entry leaks prior-campaign singleton state (SiegeDefense, RacePersistence)
- **effort** M · **risk** MED (The starter-change reset must preserve the SyncData-before-OnSessionLaunched ordering (the _justLoadedFromSave flag exists precisely for this); a wrong ordering would wipe freshly-loaded data. Copying the proven CultureConversion pattern plus per-behavior tests keeps this contained.) · **confidence** HIGH · · unmatched
- **impact**: Loading any save that predates a feature (or lacks its behavior entry) after playing a current campaign in the same process silently injects the previous campaign's siege-defense events (possible unearned relation+influence rewards) and hero race assignments into the loaded game. Trigger is niche (warm process + older save) but the contaminated state then persists into that save on the next save-write.
  - `E:/Decompiled_Bannerlord/Campaign/TaleWorlds.CampaignSystem/TaleWorlds.CampaignSystem/CampaignBehaviorDataStore.cs:86-106` — LoadBehaviorData calls a behavior's SyncData only when its StringId exists in the save's behavior dict — for saves predating a feature, SyncData (and thus RestoreFromSave/Deserialize) never runs on load.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/Siege/SiegeDefenseBehavior.cs:26-48` — Reset paths are OnNewGameCreated (new game only) + RestoreFromSave (requires SyncData to fire); no OnSessionLaunched starter-change reset, so the singleton SiegeDefenseService._activeEvents (SiegeDefenseIoC.cs:13) keeps the previous campaign's events when an older save is loaded.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/Siege/SiegeDefenseService.cs:154-186` — OnHourlyTick processes the stale events: expiry, visual trackers, and GrantReward when the player sits in a same-StringId settlement that is under siege (settlement StringIds are stable authored XML ids across campaigns).
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/HeroRace/RacePersistenceService.cs:48-57, 59-91` — ResetForNewCampaign's own comment documents the absent-key carryover, but it is wired only to OnNewGameCreatedEvent (RacePersistenceBehavior.cs:18); on a load where SyncData never ran, OnSessionLaunched→RestoreHeroRaces applies the previous campaign's race map to same-StringId heroes.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/CultureConversion/Hooks/CultureConversionBehavior.cs:93-107` — The house pattern that closes this path (_lastSessionStarter change + _justLoadedFromSave) exists here and in MessengerCampaignBehavior.cs:147-170 — these two features are immune; the others never adopted it.
- **fix sketch**: Port the _lastSessionStarter/_justLoadedFromSave reset from CultureConversionBehavior into SiegeDefenseBehavior and RacePersistenceBehavior (set the flag in SyncData's load branch, clear-store on new starter when not set). Audit the remaining SyncData behaviors against the same three-path lifecycle matrix (new game / load-with-data / load-without-data).

## SEC (9)
_Not audited:_ Considered and dropped: (1) CI build.yml uses first-party actions/checkout@v4 + actions/setup-dotnet@v4 pinned at major tags only (build.yml:30,85,88) — playbook targets unpinned THIRD-party actions; no secrets used, no pull_request_target, logs clean; not worth SHA-pinning churn. (2) cecil.snk under Dependencies/.vendor-source — Mono.Cecil publishes its strong-name key deliberately, and the dir i

### [SEC-01] Pin all three stdio MCP servers — every session start executes unpinned upstream code with broad filesystem access
- **effort** S · **risk** LOW (Pinning versions can only break MCP startup if the pinned version is wrong — immediately visible, trivially reverted; no game or save impact.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: A compromised or malicious release of any of these three upstream packages executes arbitrary code on the dev machine at every Claude session start, with read/write access to the repo, the game install, and asset drives. The /security-scan MED covered only the filesystem server; serena (git HEAD — no release gating at all) and mcp-server-git are the same class and were not flagged.
  - `c:/Users/mikew/source/repos/TAOM/.mcp.json:22-31` — filesystem MCP launched via `npx -y @modelcontextprotocol/server-filesystem` with no version pin — this is the pre-existing MED already reported by /security-scan THIS session (cited, not re-derived). Its args grant access to the repo, the entire Bannerlord Modules dir, and E:\LOTRAOMAssets.
  - `c:/Users/mikew/source/repos/TAOM/.mcp.json:3-16` — serena MCP launched via `uvx --from git+https://github.com/oraios/serena` with no tag/rev pin — pulls and executes upstream git HEAD, with `--project` pointed at the TAOM repo.
  - `c:/Users/mikew/source/repos/TAOM/.mcp.json:34-38` — git MCP launched via `uvx mcp-server-git` with no version pin — latest PyPI release executed each session, with full git access to the repo.
- **fix sketch**: Pin filesystem to `@modelcontextprotocol/server-filesystem@<version>`, serena to a specific git rev/tag (`git+https://github.com/oraios/serena@<sha>`), and mcp-server-git to `mcp-server-git==<version>`. Add a bump cadence note to docs/migration/dr3-maintenance.md or the security-scan checklist so pins don't rot silently.

### [SEC-02] Harden the native signature scanner before authoring v1.4.5 patterns: no ambiguity detection, and a literal CC pattern byte silently becomes a wildcard
- **effort** S · **risk** LOW (Fail-closed-on-ambiguity can only turn a would-be wrong-site hook into a logged no-op (hooks already degrade gracefully to inert); separating the wildcard mask from byte values is a contained parser change. No save impact.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: When v1.4.5 patterns are authored, a pattern that is non-unique (or that contains a literal CC byte and thus matches far more sites than intended) hooks the wrong address in TaleWorlds.Native.dll — arbitrary memory corruption in the game process that manifests as untraceable crashes far from the hook. The manual uniqueness test in the authoring workflow is exactly the kind of behavioral-preservation step the CLAUDE.md C++ port discipline says gets skipped.
  - `c:/Users/mikew/source/repos/TAOM/Dependencies/NativeSkinFixes.NativeHooks/SignatureScanner.cpp:91-101` — FindPattern returns the FIRST match across the entire module image (code + data sections, per SizeOfImage from GetModuleBase) with no second-match scan; an ambiguous pattern silently resolves to the wrong address.
  - `c:/Users/mikew/source/repos/TAOM/Dependencies/NativeSkinFixes.NativeHooks/SignatureScanner.cpp:41-62` — ParsePattern stores wildcards as kWildcardSentinel = 0xCC in the same byte buffer as literal bytes — an authored pattern containing a literal `CC` byte (INT3 padding is common around function prologues) is indistinguishable from a wildcard and matches ANY byte (skip at line 97: `if (expected == kWildcardSentinel) continue;`).
  - `c:/Users/mikew/source/repos/TAOM/Dependencies/NativeSkinFixes.NativeHooks/SignatureScanner.h:29-35` — Header explicitly delegates ambiguity resolution to manual pattern authoring ("ambiguity should be resolved by the pattern itself") and acknowledges scanning the whole module range — the safety property is a comment, not code.
  - `c:/Users/mikew/source/repos/TAOM/Dependencies/NativeSkinFixes.NativeHooks/Signatures.h:61, 73, 85, 96, 107, 119, 131` — All 7 target signatures are currently `<PATTERN_TBD>` stubs (intentional per the file's placeholder note), so the risk is latent today — but pattern authoring for v1.4.5 is the documented next step, and step 4 of the authoring workflow (lines 21-24) makes uniqueness a manual test the runtime never enforces. Consumers (HairClothHook.cpp:298-302, CoversHeadHook.cpp:128-130) feed the unverified first match straight into MH_CreateHook.
- **fix sketch**: In FindPattern, continue scanning after the first match and return 0 (log 'ambiguous pattern') if a second match exists. Replace the in-band 0xCC sentinel with a parallel bool mask array (or reject literal CC in ParsePattern with a clear error). Optionally constrain the scan to executable sections via the PE section table. Unit-test the parser host-side.

### [SEC-03] process_faction_map.py templates file paths into Python source executed by subprocess — quote-in-path breaks into code execution
- **effort** S · **risk** LOW (Passing the path via argv instead of string templating is behavior-preserving for all valid inputs; the embedded helper script reads sys.argv instead of a literal.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Defensive-maintenance issue in a local dev tool: paths derive from the --input CLI dir joined with hardcoded region ids, so today only the developer can trigger it — but any future reuse of these helpers against externally-named files (downloaded asset packs, user-submitted maps) inherits a code-execution-via-filename hazard, and even benign paths containing an apostrophe (common in Windows user dirs) break the tool today.
  - `c:/Users/mikew/source/repos/TAOM/tools/process_faction_map.py:160-263` — find_alpha_bbox builds a Python program as an f-string and runs it via `subprocess.run([sys.executable, "-c", f"""...read_png_rgba(r'{filepath}')..."""])` (interpolation at line 260) — a path containing a single quote terminates the raw-string literal and the remainder is executed as Python.
  - `c:/Users/mikew/source/repos/TAOM/tools/process_faction_map.py:282-284` — crop_png_to_bbox uses the same f-string-into-`python -c` subprocess pattern for the crop helper, interpolating input/output paths the same way.
- **fix sketch**: Make the embedded helper read paths from sys.argv: `subprocess.run([sys.executable, "-c", SCRIPT, filepath])` with `path = sys.argv[1]` inside the script (same for crop's input/output paths and numeric bbox args). Two call sites, one shared constant script string.

### [SEC-01] Pin MCP server packages in .codex/config.toml (unpinned npx -y / uvx fetch-and-execute with full inherited env)
- **effort** S · **risk** LOW (Pinning versions only risks running a stale server until someone bumps the pin; no behavior change otherwise.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Every Codex session resolves and executes the latest published npm/PyPI package version with the user's full environment and filesystem-MCP write access to the repo and game ModuleData. A compromised or typosquat-replaced release runs arbitrary code on the dev machine. /security-scan's audited scope (.claude/, .mcp.json, settings*.json, CLAUDE.md) does not include .codex/config.toml, so this instance is currently invisible to the existing gate.
  - `c:/Users/mikew/source/repos/TAOM/.codex/config.toml:12-21` — filesystem MCP server launched via `npx -y @modelcontextprotocol/server-filesystem` with no version pin, granted the TAOM repo, two game ModuleData dirs, and E:/Decompiled_Bannerlord as roots
  - `c:/Users/mikew/source/repos/TAOM/.codex/config.toml:23-25` — git MCP server launched via `uvx mcp-server-git` — same unpinned fetch-latest-from-PyPI-and-execute pattern
  - `c:/Users/mikew/source/repos/TAOM/.codex/config.toml:9-10` — `[shell_environment_policy] inherit = "all"` — child MCP processes receive the user's full environment (incl. any API keys)
  - `c:/Users/mikew/source/repos/TAOM/.mcp.json:24-26` — the same npx -y unpinned filesystem-MCP pattern — already flagged as the pre-existing MED by /security-scan this session (cited, not re-derived)
- **fix sketch**: Pin exact versions: `@modelcontextprotocol/server-filesystem@<ver>` in both .codex/config.toml and .mcp.json, and `mcp-server-git==<ver>` for uvx. Optionally extend tools/audit_claude_config.py's scan scope to include .codex/config.toml so the existing /security-scan gate covers it.

### [SEC-02] Strip upstream GitHub Packages credential pair from local vendored UIExtenderEx nuget.config before it can ride a port into the public repo
- **effort** S · **risk** LOW (Deleting the credentials block from a gitignored reference copy breaks nothing — TAOM.sln does not build the vendor-source projects (grepped: no vendor-source project rows in the solution).) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Currently zero exposure (untracked, repo public per gh repo view). The latent risk: .vendor-source is the /adopt-external staging ground for porting upstream code; copying this subtree (or un-ignoring it) would publish a third-party access token in a PUBLIC repo, and the HTML-entity encoding is precisely the form that evades GitHub push protection and secret scanning. Defensive-maintenance fix, not an active leak.
  - `c:/Users/mikew/source/repos/TAOM/Dependencies/.vendor-source/Bannerlord.UIExtenderEx-2.13.2/src/nuget.config:8-13` — <packageSourceCredentials> block for the nuget.pkg.github.com/BUTR feed containing a username plus an HTML-entity-encoded ClearTextPassword (GitHub personal access token shape). Value not reproduced per audit rules.
  - `c:/Users/mikew/source/repos/TAOM/.gitignore:112` — `Dependencies/.vendor-source/` is gitignored — confirmed via git check-ignore; the file is local-only, NOT committed (rg -uu sweep of the vendor tree found exactly this one instance)
- **fix sketch**: Delete the <packageSourceCredentials> block (or the whole nuget.config) from the local vendor copy. Add a `packageSourceCredentials|ClearTextPassword` grep to the /adopt-external security-vet checklist so future vendored drops are swept before any file is ported into the tracked tree. Rotation is upstream's (BUTR's) call — it is their token, shipped in their release source.

### [SEC-03] Enforce pattern-uniqueness and section-bounded scanning in SignatureScanner before the 1.4.5 patterns are authored
- **effort** S · **risk** LOW (The check runs once at install time; worst case a genuinely ambiguous pattern refuses to install and the hook stays inert with a clear log line — which is already the designed degraded mode.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: When the 7 patterns are authored for v1.4.5 (or the next engine bump silently shifts code), an under-discriminating pattern that matches a wrong site first makes MinHook write a detour into arbitrary code or data — memory corruption / undefined crashes in the game process instead of the designed clean refusal-to-install. This is the playbook's 'byte-pattern scan matching the wrong site' class, and the only enforcement today is a comment.
  - `c:/Users/mikew/source/repos/TAOM/Dependencies/NativeSkinFixes.NativeHooks/SignatureScanner.cpp:88-101` — FindPattern scans the entire module image (base..SizeOfImage, headers + data + code) and returns the FIRST match with no uniqueness verification
  - `c:/Users/mikew/source/repos/TAOM/Dependencies/NativeSkinFixes.NativeHooks/Signatures.h:21-24` — the authoring procedure requires 'Test by checking the scan returns a single match' — but only as a manual step; the runtime never re-checks
  - `c:/Users/mikew/source/repos/TAOM/Dependencies/NativeSkinFixes.NativeHooks/Signatures.h:59-135` — all 7 signatures are currently <PATTERN_TBD> stubs (documented-inert state per docs/features/native-skin-fixes.md:249-253), so the gate can land before any live pattern exists
- **fix sketch**: After the first match, continue scanning for a second; if found, log both RVAs and return 0 (refuse install). Optionally walk the PE section headers and restrict the scan to executable sections. ~20 lines in SignatureScanner.cpp; no managed-side change.

### [SEC-05] Stop interpolating file paths into python -c child source in process_faction_map.py (quote-in-path code injection / breakage)
- **effort** S · **risk** LOW (Passing the path as argv to the child script (read via sys.argv) is behavior-identical for all legal paths; only the injection channel closes.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: A region PNG filename or --input directory containing a quote executes attacker-chosen Python in the child process (threat model: asset drops from third parties fed to the tool); benign case: the tool simply breaks for paths with apostrophes (e.g. C:/Users/O'Brien). Operator-run local tool, no network exposure — low severity, but it is the playbook's template-into-shell pattern verbatim.
  - `c:/Users/mikew/source/repos/TAOM/tools/process_faction_map.py:160-161, 260-261` — find_alpha_bbox builds a child interpreter script via f-string and embeds the path as `read_png_rgba(r'{filepath}')` — a single quote in the path terminates the literal and the remainder is executed as Python in the child
  - `c:/Users/mikew/source/repos/TAOM/tools/process_faction_map.py:282-284` — crop_png_to_bbox repeats the same f-string-into-`python -c` pattern for input/output paths
- **fix sketch**: Append the path(s) to the subprocess argv list and have the embedded script read sys.argv[1..n] instead of f-string interpolation; alternatively move the two embedded scripts to small helper .py files invoked with arguments.

### [SEC-04] Guard the CI build job against fork-PR execution before any self-hosted runner is attached
- **effort** S · **risk** LOW (Adding `github.event.pull_request.head.repo.fork == false` (or restricting the build job to push events) plus declaring `workflow_dispatch` in `on:` cannot break existing runs — same-repo PRs and master pushes keep building.) · **confidence** MED · ✓ verifier:REAL
- **impact**: Today the job targets GitHub-hosted windows-latest, so the exposure is nil — but the workflow's own comment documents the intended endgame (self-hosted runner with the game install). The moment that runner is attached and BANNERLORD_GAME_DIR is set as a repo variable, any fork PR executes attacker-controlled MSBuild targets/csproj on the machine that holds the Steam install and dev environment. Cheaper to add the guard now than to remember it during runner setup.
  - `c:/Users/mikew/source/repos/TAOM/.github/workflows/build.yml:77-83` — The Build & Test job comment says 'Requires Bannerlord game DLLs — runs on self-hosted runner or when BANNERLORD_GAME_DIR is set', is gated on `vars.BANNERLORD_GAME_DIR != ''`, and triggers on `pull_request` (line 6). Repository variables (unlike secrets) ARE available to fork-PR workflow runs.
  - `c:/Users/mikew/source/repos/TAOM/.github/workflows/build.yml:81` — `github.event_name == 'workflow_dispatch'` in the job condition is dead — `workflow_dispatch` is not declared in the `on:` block (lines 3-7), so that escape hatch can never fire.
- **fix sketch**: Add a fork guard to the build job condition (run only for push, same-repo PRs, or dispatch), and declare `workflow_dispatch` in the `on:` block so the existing condition branch stops being dead. One-file change, verifiable by a PR dry-run.

### [SEC-04] Gate DllMain DLL_PROCESS_DETACH hook teardown on lpReserved == nullptr (loader-lock / process-termination safety)
- **effort** S · **risk** LOW (Adding `if (lpReserved == nullptr)` around the detach cleanup (or deleting it) cannot affect the normal uninstall path, which never reaches this code with hooks still installed.) · **confidence** MED · ✓ verifier:REAL
- **impact**: MinHook's thread-freeze inside DllMain runs under the loader lock — a suspended thread holding the loader lock (or heap lock) deadlocks game shutdown; on process-termination detach (lpReserved != null) other threads are already terminated and CRT/heap state is mid-teardown, so the work is unsafe and pointless. The normal path is unaffected (managed wrapper uninstalls from OnSubModuleUnloaded, making detach a no-op via the g_installed guards) — meaning this code only ever executes on abnormal teardown, exactly where it is least safe. Hang-on-exit class, not exploitability.
  - `c:/Users/mikew/source/repos/TAOM/Dependencies/NativeSkinFixes.NativeHooks/dllmain.cpp:15-19` — DLL_PROCESS_DETACH unconditionally runs three hook uninstalls (each calls MH_DisableHook/MH_RemoveHook, which suspend-and-resume all process threads) plus Logging::Close(); lpReserved is received (line 7) but never consulted
- **fix sketch**: Wrap the three Uninstall calls in `if (lpReserved == nullptr) { ... }` (dynamic-unload only), keeping Logging::Close() or dropping it too; document that OnSubModuleUnloaded is the sole supported teardown path.

## PERF (7)
_Not audited:_ Read both required docs (operating manual + playbook section 3, Finding format, Prioritization rubric). Verified-clean (no finding): NativeSkinFixes C++ hot-path logging is properly sample-gated (HairClothHook.cpp:75-93, kHotPathLogSampleLimit + InterlockedIncrement64); CareerPerkMissionBehavior per-frame HUD path already string-cached (AbilityHudController.cs:34-38, Codex #31); BehaviorTreeMissio

### [PERF-01] SpatialGrid is rebuilt at 2s cadence — the issue #219 0.1s fix lives in a permanently-dead branch
- **effort** S · **risk** LOW (Cadence change to 0.1f restores intended #219 behavior; add a creature-presence gate so plain battles don't pay the 10Hz rebuild. No save impact.) · **confidence** HIGH · · unmatched
- **impact**: Every warg/spider/elephant behavior-tree target query (10-60m radii) reads agent positions up to 2 seconds (~20m at combat speed) stale — the exact dysfunction issue #219 documented as fixed. The fix shipped into a branch that can never run. Inverse perf cost when corrected: a 10Hz full-agent grid rebuild would run in every mission including creature-free ones unless gated.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/AdvancedCombat/AdvancedCombatBehavior.cs:10, 26-33` — GridUpdateInterval = 2f; UpdateGrid(Mission.Current.AllAgents) only every 2 seconds — this is the instance that actually owns the grid.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/Warg/WargMissionBehavior.cs:27-36, 58-62, 85-97` — The #219 fix comment ('restored to ~100ms cadence... at 2s intervals positions drift up to 20m, making the grid useless as a target filter') and the 0.1f update path — but it only runs when GetMissionBehavior<AdvancedCombatBehavior>() == null (_managesCombatInfrastructure).
  - `c:/Users/mikew/source/repos/TAOM/Main/SubModule.cs:656-661 (working tree; block is committed content — unstaged diff touches only the Patch47 hunk at ~540)` — OnMissionBehaviorInitialize unconditionally adds AdvancedCombatBehavior BEFORE WargMissionBehavior in every mission, so the warg's 0.1f branch never executes.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/Spider/SpiderMissionBehavior.cs:19-20` — Doc comment confirms the ownership: 'SpatialGrid/bone-collision ticking is owned by AdvancedCombatBehavior (always co-registered in SubModule)' — i.e., the 2f-interval instance.
- **fix sketch**: Set AdvancedCombatBehavior.GridUpdateInterval to 0.1f (single shared constant), delete the dead _managesCombatInfrastructure branch in WargMissionBehavior, and skip UpdateGrid when no BT-tracked creature (warg/spider/elephant) is present in the mission.

### [PERF-02] Patch30 (Formation.GetOrderPositionOfUnit) leads with an uncached MCM container walk and allocates a FormationAdapter per call
- **effort** S · **risk** LOW (Caching the TaomSettings reference (NameplateFadeSettingsProvider pattern, lazy ??= to handle early-null) keeps live MCM edits visible; moving cheap primitive gates before the adapter allocation is behavior-neutral.) · **confidence** HIGH · · unmatched
- **impact**: On the highest-frequency TAOM-authored code path (per-unit formation positioning), every call pays a heap allocation + a full MCM provider walk + a GetMovementState engine call — even when the feature is disabled or no layout is assigned (the common case). The patch already cites the harmony-patches.md hot-path rule for the service but missed the settings provider and the adapter allocation.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/MixedFormations/Hooks/Patch30_FormationGetOrderPositionOfUnit.cs:13-16, 27` — Patch's own comment: 'fires per-unit-per-formation-position-recalculation (up to 40,000x per frame in worst-case 200-unit formations)'; it caches the service but then does `new FormationAdapter(__instance)` on every call before any gate.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/MixedFormations/FormationLayoutService.cs:68` — ComputeUnitPlanePosition's FIRST gate is `_settings.IsEnabled` — evaluated per call.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/MixedFormations/MixedFormationsSettingsProvider.cs:7` — IsEnabled => TaomSettings.Instance?.EnableMixedFormations — uncached static-singleton access per read.
  - `c:/Users/mikew/.nuget/packages/bannerlord.mcm/5.11.4/lib/netstandard2.0/MCMv5.dll (decompiled GlobalSettings`1 + DefaultSettingsProvider.GetSettings):n/a` — TaomSettings.Instance = 2 ConcurrentDictionary ops + BaseSettingsProvider.GetSettings looping all settings containers per access — not a cached field read.
- **fix sketch**: Cache the TaomSettings instance in MixedFormationsSettingsProvider (lazy ??=); in the Prefix, check enabled + layout-assigned (formation key lookup) before constructing FormationAdapter, or pass the raw Formation through and adapt only on the assigned-layout path.

### [PERF-03] TroopWeight Postfixes re-resolve TaomSettings.Instance on every PartyBase member-count read (8 patch sites)
- **effort** S · **risk** LOW (Cached TaomSettings reference reads live values through the reference; must lazy-init (??=) because Instance can be null very early (documented in CastleRecruitmentSettingsProvider:7).) · **confidence** HIGH · · unmatched
- **impact**: Thousands of MCM container walks per second on a populated campaign map (every party-count read engine-wide goes through the Postfix), paying ConcurrentDictionary ops + a settings-container loop each time, for a boolean that almost never changes.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/TroopWeight/Hooks/PartyBase_NumberOfAllMembers_Patch.cs:17` — `TaomSettings.Instance?.EnableTroopWeight ?? true` per Postfix invocation on the PartyBase.NumberOfAllMembers getter.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/TroopWeight/Hooks/PartyBase_NumberOfRegularMembers_Patch.cs:17` — Same per-call Instance lookup; 6 more sites in the display patches (RecruitmentVM:17, PartyVM:21, PartyBaseHelper:21, GameMenuPartyItemVM:17, CampaignUIHelper x2:19-20).
  - `E:/Decompiled_Bannerlord/Campaign (grep NumberOfAllMembers):38 caller files / 71 refs` — Engine callers include DefaultPartyMoraleModel, DefaultMobilePartyFoodConsumptionModel, DefaultPartyDesertionModel, MapTracksCampaignBehavior, PartyBaseHelper (nameplate size text) — per-party per-tick and per-UI-refresh frequency.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/SettlementNameplateFade/NameplateFadeSettingsProvider.cs:10-23` — The repo's own documented exemplar: 'TaomSettings.Instance singleton accessor is cached in _settings at construction time... cached reference still picks up live MCM edits'.
- **fix sketch**: Add a lazily-cached static TaomSettings field (or a tiny ITroopWeightSettingsProvider with ctor/lazy caching) and read EnableTroopWeight through it in all 8 Patch17 entry points.

### [PERF-04] TaomMilitaryPowerModel reads up to 7 MCM properties per GetDefaultTroopPower call in per-troop simulation paths
- **effort** S · **risk** LOW (Same cached-reference pattern as PERF-03; values still read live through the reference. GetConfig() is already cached so no config staleness risk.) · **confidence** HIGH · · unmatched
- **impact**: During sieges and large map-event simulations the model is invoked for every troop on both sides each simulation round — hundreds to thousands of calls, each performing up to 7 MCM container walks (the JSON config side is properly cached; only the MCM side leaks).
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/BattleBalance/Models/TaomMilitaryPowerModel.cs:21-35` — EnableCustomTroopPower, OverrideVanillaTierPower, Tier7-10Power, HeroMultiplier/MountedMultiplier — each a provider property read per call.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/BattleBalance/BattleBalanceSettingsProvider.cs:5-17` — Every property is `TaomSettings.Instance?... ?? default` — a full MCM container walk per read, x7 per model call.
  - `E:/Decompiled_Bannerlord/Campaign/TaleWorlds.CampaignSystem/TaleWorlds.CampaignSystem.GameComponents/DefaultMilitaryPowerModel.cs:87, 105` — GetTroopPower → GetDefaultTroopPower invoked per roster element; additional per-troop call sites in MapEventSide.cs:665-688 (simulation rounds), DefaultCombatXpModel.cs:41-42, DefaultSettlementSecurityModel.cs:253, GarrisonTroopsCampaignBehavior.cs:621.
- **fix sketch**: Cache the TaomSettings reference in BattleBalanceSettingsProvider (lazy ??=), or snapshot the 7 values into locals once at the top of GetDefaultTroopPower via a single provider struct read.

### [PERF-05] TroopWeight count-hook caches are GetHashCode-keyed dictionaries with no eviction — unbounded growth plus collision risk; the fixed pattern already exists next door
- **effort** S · **risk** LOW (ConditionalWeakTable swap is behavior-preserving (same version-check logic); the pattern is already proven in the same feature.) · **confidence** HIGH · · unmatched
- **impact**: Singleton hooks accumulate one entry per party ever seen across a whole play session (bandit/caravan churn = thousands of dead entries); identity-hash collisions between a dead and a live party can return a wrong cached weighted count when roster VersionNo coincides. Memory cost is small; the asymmetry with the already-fixed _healthCache means the known-correct fix was applied to one of three caches.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/TroopWeight/Hooks/PartyBaseNumberOfAllMembersHook.cs:11, 25, 38` — Dictionary<int,(Version,WeightedResult)> keyed on partyBase.GetHashCode(); entries are added forever, never removed.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/TroopWeight/Hooks/PartyBaseNumberOfRegularMembersHook.cs:11, 25, 49` — Identical pattern in the second count hook.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/TroopWeight/TroopWeightService.cs:17-21` — The health-cache comment explicitly indicts them: 'Reference-keyed + auto-evicting on party GC (no GetHashCode collisions, no unbounded growth — unlike the hashcode-dict caches in the count hooks).'
- **fix sketch**: Replace both Dictionary<int,...> caches with ConditionalWeakTable<PartyBase, Box> mirroring TroopWeightService._healthCache (reference-keyed, auto-evicting, collision-free).

### [PERF-07] Warg BT nodes and WargRiderHandManager resolve IoC per evaluation, violating the documented lazy-cache rule the Spider/Elephant nodes already follow
- **effort** S · **risk** LOW (Mechanical conversion to the ??= pattern already proven by Spider/Elephant nodes; factory is a process singleton so caching is safe. TryGetValue-before-GetOrAdd in the factory removes the closure alloc without semantic change.) · **confidence** HIGH · · unmatched
- **impact**: Hundreds of container resolves plus closure/delegate allocations per second in warg battles (N wargs x 5 evals/sec x ~2 resolves per nearby agent), in direct conflict with the CLAUDE.md 'IoC.Resolve in hot path → lazy cache' rule; also a consistency debt — three creature features, two patterns.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/Warg/BehaviorTreeElements/PeriodicallyCheckIfCanAttackAnyone.cs:15, 29-30, 45` — `static IMissionAdapterFactory AdapterFactory => IoC.Resolve<...>()` — property getter resolves per access, hit twice per nearby agent per evaluation in both decorator classes.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/Warg/BehaviorTreeElements/WargAiControlledIsNotFacingEnemy.cs:16` — Same resolve-per-access static property.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/Warg/WargRiderHandManager.cs:14` — IoC.Resolve<IMissionAdapterFactory>() + GetAgentAdapter every mission tick whenever the player is on ANY mount (called from WargMissionBehavior.OnMissionTick:125).
  - `c:/Users/mikew/source/repos/TAOM/Main/Adapters/MissionAdapterFactory.cs:24` — GetOrAdd with a capturing lambda allocates a closure + delegate on every call, even on cache hits — compounding the per-evaluation cost.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/Spider/BehaviorTreeElements/SpiderAttackTask.cs:27-28` — The converged pattern already in-repo: `_adapterFactory ??= IoC.Resolve<...>` (also ElephantAttackTaskBase.cs:49, AttackOffCooldownDecorator.cs:41).
- **fix sketch**: Convert the three Warg BT classes and WargRiderHandManager to lazily-cached static fields (`??=`), hoist GetAgentAdapter(warg) out of the per-agent loops, and make MissionAdapterFactory.GetAgentAdapter do TryGetValue first to avoid the per-call closure.

### [PERF-06] SpatialGrid.GetAgentsInRadius scans every occupied cell per query and allocates a List per call — defeating the grid's purpose on per-frame BT paths
- **effort** M · **risk** MED (Loop-over-cell-range rewrite must replicate current inclusion semantics exactly (warg targeting was RCA'd in #219 — behavior here is load-bearing); UpdateGrid rebuild also re-allocates per-cell Lists each pass, so pooling touches the same structure.) · **confidence** HIGH · · unmatched
- **impact**: With dozens of creatures in a battle, per-frame queries each iterate all occupied cells (~100-300 on a 1000-agent field) plus a list allocation — O(occupied-cells) per query instead of the O(cells-in-radius) a spatial grid exists to provide, plus steady GC churn in missions.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/AdvancedCombat/SpatialGrid.cs:41-72` — GetAgentsInRadius does `foreach (var kvp in Grid)` over ALL occupied cells, filtering keys by min/max range, and allocates `new List<Agent>()` per query — instead of TryGetValue on only the cells inside the radius.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/Warg/BehaviorTreeElements/NoEnemyCloseDecorator.cs:17` — Radius-60 query per BT evaluation (BT components tick per-frame per creature via Agent.Tick).
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/Spider/BehaviorTreeElements/SpiderCanBiteDecorator.cs:36` — Same query pattern on the spider bite gate.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/Warg/BehaviorTreeElements/PeriodicallyCheckIfCanAttackAnyone.cs:23, 52` — Radius-10 queries (0.2s-throttled and unthrottled CheckOnce variant) — small radii where direct cell lookup would touch ~8-27 cells vs scanning all occupied cells.
- **fix sketch**: Rewrite GetAgentsInRadius to iterate x/y/z in [min..max] with Grid.TryGetValue per cell; optionally accept a caller-provided buffer to kill the per-query allocation, and reuse per-cell lists across UpdateGrid rebuilds.

## TEST (11)
_Not audited:_ EXCLUDED per spawn prompt: all in-flight work (Main/Features/Spider/Hooks/, CharacterSpawnerService.cs, SubModule.cs unstaged edits, SpiderDiag battery). Verified-and-dropped (not findings): VolunteerRecruitmentService hotspot — 2,518-line test file with boundary-roll (input x branch) cells AND an end-to-end pool-id-vs-troops_*.xml existence test (VolunteerRecruitmentServiceTests.cs:2299-2402), ex

### [TEST-01] ConfigIdValidationTests mirror tables have no source-parse consistency check
- **effort** S · **risk** LOW (Test-only change; worst case is the new source-parse assertion exposing an existing drift that must be triaged.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: This is the house mirror-table failure mode (memory: feedback_mirror_table_drifts_from_production): a culture removed/renamed in taom_spcultures.xml stays 'valid' in the mirror so the settlement-guards config validation silently rots; a newly added culture produces false failures until someone hand-edits the test (goblin + mistymountainorcs already required exactly that hand-edit per the comments). The test exists to gate game data and currently only gates itself.
  - `C:/Users/mikew/source/repos/TAOM/TAOM.Tests/Core/ConfigIdValidationTests.cs:12-32` — Hand-coded ValidCultureIds (18) and ValidKingdomIds (22) HashSets used to validate settlement_guards_config.xml culture ids.
  - `C:/Users/mikew/source/repos/TAOM/TAOM.Tests/Core/ConfigIdValidationTests.cs:193-205` — Only consistency assertions are self-referential counts (Assert 18 cultures / 22 kingdoms) — the mirror is never compared against taom_spcultures.xml / taom_spkingdoms.xml.
  - `C:/Users/mikew/source/repos/TAOM/TAOM.Tests/Features/CulturalFeats/TaomCulturalFeatsDefinitionTests.cs:324-356` — In-repo exemplar of the required pattern: source-parses TaomCulturalFeats.cs and asserts mirror == production (added after the 2026-06-07 mirror-drift RCA).
  - `C:/Users/mikew/source/repos/TAOM/Main/_Module/ModuleData/taom_spcultures.xml:2-8` — Source of truth (<Culture> elements, multi-line attrs) exists in-repo and is trivially parseable with XDocument from the test, same walk-up pattern the file already uses (FindModuleDataPath, line 34).
- **fix sketch**: In ConfigIdValidationTests, parse <Culture id> from taom_spcultures.xml and <Kingdom id> from taom_spkingdoms.xml (plus the 6 reused vanilla engine ids as an explicit allowlist) and assert the parsed sets equal ValidCultureIds/ValidKingdomIds, replacing the count-only assertions. Mirrors the TaomCulturalFeatsDefinitionTests source-parse pattern.

### [TEST-03] Shipped SpecialResources XML never read by any test or validator schema (troop ids + kingdom mappings unguarded)
- **effort** S · **risk** LOW (Additive test; worst case it surfaces existing orphaned rows that need a data fix.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: A troop rename/delete (the #212-style revamps did 40+ of these) silently orphans rows in troop_resource_costs.xml — the resource upgrade-gating for that troop just disappears with no error anywhere; same for a kingdom/culture id typo in special_resources_config.xml (resource never attaches to the kingdom). This is the exact failure class of feedback_end_to_end_xml_to_guard_smoke_required: every per-layer unit passes while the shipped composition is dead. Today only a one-off cleanup script (tools/cleanup_deleted_troops_212.py) ever swept this file.
  - `C:/Users/mikew/source/repos/TAOM/TAOM.Tests/Features/SpecialResources/SpecialResourceConfigProviderTierTests.cs:12` — Class doc: 'Uses temp files to avoid coupling to real module data path' — all SpecialResources tests run against synthetic XML; no test loads the shipped special_resources_config.xml or troop_resource_costs.xml.
  - `C:/Users/mikew/source/repos/TAOM/Main/_Module/ModuleData/special_resources/troop_resource_costs.xml:7` — <Troop id="mordor_uruk_vanguard" resource_id="war_spoils" .../> — bare troop-id refs (no NPCCharacter. prefix); tools/schemas/ contains only 3 schemas (equipmentsets, npccharacter, spcultures), so validate_moduledata never parses this file.
  - `C:/Users/mikew/source/repos/TAOM/TAOM.Tests/Features/TroopProgression/VolunteerRecruitmentServiceTests.cs:2299-2402` — Exemplar already in-repo: walks up to Main/_Module/ModuleData, parses every troops_*.xml, and cross-checks recruitment-pool troop ids — the exact pattern this pipeline lacks.
- **fix sketch**: Add one smoke test (repo-root walk-up like ConfigIdValidationTests.FindModuleDataPath) that loads the two shipped SpecialResources XMLs through SpecialResourceConfigProvider, asserts every Troop id resolves against troops_*.xml and every Kingdom/Culture id against the known-id sets. Optionally also add a tools/schemas entry so validate_moduledata covers it at commit time.

### [TEST-05] CastleNotableMaintainer vanilla-mirror volunteer math is untested in the boundary layer
- **effort** S · **risk** LOW (Mechanical extraction of a pure formula into the already-tested CastleRecruitmentService; behavior preserved by construction plus new unit tests.) · **confidence** HIGH · · unmatched
- **impact**: A sign or base error in the log-based upgrade chance silently makes castle volunteer pools never (or always) upgrade — the feature 'works' but drifts from the vanilla parity it explicitly promises (the 2026-05-31 castle-recruitment RCA family). Daily-tick behavior, so drift compounds across a campaign and is invisible in any log.
  - `C:/Users/mikew/source/repos/TAOM/Main/Features/CastleRecruitment/Hooks/CastleNotableMaintainer.cs:87-101` — FillCastleVolunteers slot loop: upgrade-chance formula MathF.Log(notable.Power / current.Tier, 2f) * 0.01f, tier gate (current.Tier < maxTier), UpgradeTargets random pick — hand-mirrored from vanilla RecruitmentCampaignBehavior, living in the Hooks boundary; grep for CastleNotableMaintainer/FillCastleVolunteers in TAOM.Tests returns nothing.
  - `C:/Users/mikew/source/repos/TAOM/Main/Features/CastleRecruitment/Hooks/CastleNotableMaintainer.cs:44-57` — EnsureCastleNotables deficit math (count existing, spawn to target) also untested; the class doc itself says all decisions should come from ICastleRecruitmentService, but the upgrade decision didn't.
  - `C:/Users/mikew/source/repos/TAOM/TAOM.Tests/Features/CastleRecruitment/CastleRecruitmentServiceTests.cs:1` — The pure service (slot probability, occupation round-robin) IS tested — confirming the asymmetry: extracted decisions tested, in-boundary decisions not.
- **fix sketch**: Move the upgrade decision into CastleRecruitmentService as e.g. ShouldUpgradeVolunteer(power, tier, maxTier, roll) + GetUpgradeChance(power, tier), unit-test known values and boundaries (tier==maxTier, power<tier), and have the maintainer call it — matching the pattern the class doc already claims.

### [TEST-02] Shipped feature-config XML cross-refs (SpecialResources troop/resource ids, SettlementGuards troop ids) are validated by nothing
- **effort** S · **risk** LOW (Additive test following two in-repo precedents (ConfigIdValidationTests + the recruitment pool e2e test); may immediately flag real stale refs needing data fixes.) · **confidence** HIGH · · unmatched
- **impact**: A troop rename/delete (routine during culture revamps — the #212 precedent) silently orphans rows: SpecialResources upgrade gating stops charging for that troop and SettlementGuards pools silently drop guards, with no test, validator, or in-game error. Contrast: VolunteerRecruitmentService already has exactly this gate (VolunteerRecruitmentServiceTests.cs:2299-2402 parses troops_*.xml and validates every pool id).
  - `Main/_Module/ModuleData/special_resources/troop_resource_costs.xml:7-10` — Bare Troop id="mordor_uruk_vanguard" + resource_id="war_spoils" refs — no NPCCharacter. prefix, so the validator's prefix-based reference scan cannot see them.
  - `tools/schemas/:n/a` — Only 3 schemas exist (equipmentsets, npccharacter, spcultures); grep for special_resources|settlement_guards across tools/schemas/*.json, taom_schema.py, validate_moduledata.py returned zero matches this session.
  - `TAOM.Tests/Core/ConfigIdValidationTests.cs:117-171` — The existing real-XML gate validates settlement_guards_config.xml CULTURE attributes only — the 39 Guard troop= refs (e.g. line 9-12: gondor_mt_fountain_guard) are unchecked.
  - `TAOM.Tests/Features/SpecialResources/SpecialResourceServiceTests.cs:231-243` — All troop-cost tests mock the config (_config.GetTroopCost(...).Returns(cost)) — no test parses the shipped XML.
  - `tools/cleanup_deleted_troops_212.py:n/a` — This one-off sweeper exists precisely because deleted-troop refs went stale in troop_resource_costs.xml during issue #212 — the failure mode has already occurred once.
- **fix sketch**: Add ConfigIdValidationTests-style tests that load troop_resource_costs.xml + settlement_guards_config.xml and assert every troop id exists in troops_*.xml/characters and every resource_id exists in special_resources_config.xml. Alternative: add the two files to a new tools/schemas entry so validate_moduledata.py + the pre-commit hook own it.

### [TEST-04] Config-provider validation logic untested (BattleBalance, WarOfTheRing) and absent+untested (ArmyTargeting float multipliers)
- **effort** S · **risk** LOW (Provider tests are pure (IPathService + IModLogger mocks, temp JSON); adding FiniteFloatValidator to ArmyTargeting touches one load path with compiled-default fallback semantics already standard in 5 sibling providers.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: The NaN-through-config bug class has shipped three times in this repo (per csharp-architecture.md). The two validated providers can regress invisibly (an inverted IsFiniteInRange check passes the suite); ArmyTargeting is still in the pre-fix state — a NaN aggression multiplier flows into TaomTargetScoreModel scoring and silently breaks besieger AI targeting campaign-wide.
  - `Main/Features/BattleBalance/BattleBalanceConfigProvider.cs:57-89` — ValidateConfig (NaN/range revert for TierPower + CulturalSurvivalBonuses, added as review fix Phase 9b #140 P2) — grep shows no provider test exists; the BattleBalance tests cover only the config sections' getters, never the revert logic.
  - `Main/Features/Diplomacy/WarOfTheRingConfigProvider.cs:49-67` — ValidateConfig with ordering invariant (Phase2.TriggerDay >= Phase1.TriggerDay, review fix #129 P2) — no WarOfTheRingConfigProviderTests exists (only WarOfTheRingServiceTests).
  - `Main/Features/ArmyTargeting/ArmyTargetingConfig.cs:8-9` — Two user-editable float dictionaries (FactionAggressionMultipliers, FactionDistanceRangeMultipliers).
  - `Main/Features/ArmyTargeting/ArmyTargetingConfigProvider.cs:35-47` — Deserializes those floats with NO semantic validation at all (no FiniteFloatValidator, no range check) and has no test file — double violation of the csharp-architecture.md 'Config Providers MUST Validate' rule and its 'one test per validation rule' test requirement.
- **fix sketch**: Add provider tests covering semantically-invalid-but-parseable values: NaN/Infinity/out-of-range TierPower and survival bonuses revert to defaults (BattleBalance); Phase2<Phase1 reorders (WarOfTheRing). For ArmyTargeting, add ValidateConfig with FiniteFloatValidator.IsFiniteInRange per multiplier plus the same tests.

### [TEST-05] TaomPartyWageModel carries untested inline wage math (ComputeMountedWageShare) in violation of the thin-model rule
- **effort** S · **risk** MED (Extraction must preserve exact float semantics (the model's own comment at TroopWeightService notes ceiling-behavior sensitivity in sibling math); wage changes touch AI budget and player economy, so a subtle rounding change is player-visible.) · **confidence** HIGH · · unmatched
- **impact**: The Rohan mounted-wage feat's share math (drives ApplyWageModifiers' discount weighting for every Rohan party, every daily wage tick) has no test: a wrong-sibling-getter or ratio inversion here would only surface as subtly wrong wages in-game. This is the one pocket of GameModel math the otherwise-solid service-layer coverage doesn't reach.
  - `Main/Features/TroopProgression/Models/TaomPartyWageModel.cs:109-121` — ComputeMountedWageShare iterates the troop roster summing GetCharacterWage(character) * count and divides by baseWageTotal — multi-line computation living in the GameModel, which gamemodels.md rule 4 bans regardless of size.
  - `Main/Features/TroopProgression/Models/TaomPartyWageModel.cs:1-175` — File is 175 lines (largest of the 40 GameModel overrides) and has zero references from TAOM.Tests (model-coverage sweep this session); the wage services it delegates to (WageModifierServiceTests.cs 546 lines, TroopCostServiceTests.cs) are tested, but this share computation is not behind either.
- **fix sketch**: Move ComputeMountedWageShare into IWageModifierService (it already receives the result), passing per-element (wage, count, isMounted) tuples from the model boundary; add tests for zero-base, no-mounted, all-mounted, and mixed-roster cells. Model drops back under the rule.

### [TEST-02] Economy math lives untested in coverage-exempt GameModel entry points (TaomPartyWageModel, TaomPartyHealingModel)
- **effort** M · **risk** MED (Extraction touches economy-wide wage/recruit-cost behavior; a subtle ordering change in ExplainedNumber composition could shift values. Pure mechanical extraction into the already-tested services keeps risk bounded.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: GetTotalWage/GetTroopRecruitmentCost run for every party every day; the Rohan mounted-wage share and the perk-discount restoration are exactly the kind of sign-flip/wrong-overload math the repo's RCAs keep catching, and the entry-point coverage exemption (ADR-008) means no test can fail when they regress. TDD is mandatory here, so this is also a named process gap.
  - `C:/Users/mikew/source/repos/TAOM/Main/Features/TroopProgression/Models/TaomPartyWageModel.cs:109-121` — ComputeMountedWageShare — roster loop computing mounted wage share (division, zero-guards) inline in the model; grep for ComputeMountedWageShare/ResolveBuyerRecruitmentPerks/TaomPartyWageModel across TAOM.Tests returns zero files.
  - `C:/Users/mikew/source/repos/TAOM/Main/Features/TroopProgression/Models/TaomPartyWageModel.cs:137-158` — ResolveBuyerRecruitmentPerks — 14-perk vanilla-parity restoration mapping (Tier>=2 gate, infantry/ranged/mercenary flags, primary vs secondary bonus selection) with no test; a wrong PrimaryPerkBonus/SecondaryPerkBonus pairing would ship silently. File is 175 lines, 8 inline branches — over the gamemodels.md rule-4 'no inline branching' line.
  - `C:/Users/mikew/source/repos/TAOM/Main/Features/BattleBalance/Models/TaomPartyHealingModel.cs:29-63` — GetSurvivalChance override carries the double settings gate, culture resolution, bonus!=0 gate, and the career-passive IoC.Resolve path inline; TaomPartyHealingModelTests (lines 10-100) only exercises the extracted static ApplyCulturalSurvivalBonus, never the gating composition.
  - `C:/Users/mikew/source/repos/TAOM/TAOM.Tests/Features/TroopProgression/WageModifierServiceTests.cs:1` — The extracted services (WageModifierService 38 tests, TroopCostService 7 tests) ARE tested — the gap is precisely the logic that never got extracted.
- **fix sketch**: Extract ComputeMountedWageShare + ResolveBuyerRecruitmentPerks (and the healing model's gating composition) into the existing tested services (WageModifierService / a small BattleBalance survival service) taking primitive/adapter inputs, then add unit tests per branch. The models shrink back to thin delegation per gamemodels.md rule 4.

### [TEST-04] Three service-registered classes have zero tests despite ADR-008's 100% service rule (CultureSettingService, CharacterTableauService, CrashReportService)
- **effort** M · **risk** MED (Splitting decision logic from reflection glue changes code paths verified only in-game; needs a manual CC smoke check after refactor.) · **confidence** HIGH · · unmatched
- **impact**: These are services in name but boundary classes in structure — the playbook's 'boundary classes growing logic is the finding'. The decidable parts (config lookup, the vlandia name special-case predicate, parameter-count dispatch) regress invisibly: a TaleWorlds private-field rename or a culture special-case bug only shows up in-game. CultureSettingService is the highest value: it gates every new campaign's player clan name.
  - `C:/Users/mikew/source/repos/TAOM/Main/Features/FactionMap/CultureSettingService.cs:44-70` — 103-line service: player-facing CC logic (Hero.MainHero.Culture assignment ordering fix + the vlandia 'dey Corvand' clan-name replacement special case with 4-condition guard) implemented via raw AccessTools reflection against sealed statics; grep across TAOM.Tests returns zero references. Touched 2026-06-01 for #264.
  - `C:/Users/mikew/source/repos/TAOM/Main/Features/HeroRace/CharacterTableauService.cs:17-60` — 268-line service: race-position config lookup + tableau refresh driven by ~10 ReflectionHelper.GetFieldValue private-field reads; zero test references. BuildConfigLookup and the per-race position decisions are pure and trivially testable but aren't.
  - `C:/Users/mikew/source/repos/TAOM/Main/Features/CrashReport/CrashReportService.cs:1` — 254-line service with zero test references (lower value — diagnostic-only path).
- **fix sketch**: Per class, split the pure decision core (e.g. 'should replace clan name?(cultureId, clanNameCount)', race->position lookup) from the reflection glue, unit-test the core, and leave the glue as a named Not-tested: boundary. CharacterTableauService's BuildConfigLookup can be tested as-is today.

### [TEST-01] CulturalFeats production-metadata pin covers only 24 of 129 feats — mirror-table drift unguarded for the rest
- **effort** M · **risk** LOW (Test-only change; the main risk is the extension surfacing real existing drift between the service-test mirror and production, which would then need triage (that is the point).) · **confidence** HIGH · · unmatched
- **impact**: A sign-flip or magnitude edit in InitializeAll for any of the ~105 pre-Wave-1 feats (wage, loyalty, party-size, raid-damage modifiers across 16 cultures) passes the entire 3,143-test suite — the exact bug class the 2026-06-07 RCA (feedback_mirror_table_drifts_from_production) documented. These values are gameplay-balance critical and edited during every culture pass.
  - `TAOM.Tests/Features/CulturalFeats/TaomCulturalFeatsDefinitionTests.cs:296-322` — Wave1Feats_ProductionMetadata_MatchesSpec source-parses TaomCulturalFeats.cs but its spec table lists exactly 24 (field, id, bonus, sign, AdditionType) rows — the only production-metadata pin in the file.
  - `TAOM.Tests/Features/CulturalFeats/CulturalFeatsServiceTests.cs:1286-1329` — EnsureFeatsInitialised injects FAKE FeatObjects with hand-mirrored EffectBonus values into the TaomCulturalFeats singleton; all dispatch tests assert against this mirror, not production InitializeAll values.
  - `Main/Features/CulturalFeats/TaomCulturalFeats.cs:1-1211` — 129 Register("taom_...") calls counted this session (grep -c), so ~105 feats' EffectBonus/sign/AdditionType are unpinned.
  - `TAOM.Tests/Features/CulturalFeats/TaomCulturalFeatsDefinitionTests.cs:282-290` — The test's own doc comment states the exact risk: 'a production sign-flip or wrong magnitude ... would pass every test' — the Codex 2026-06-07 MEDIUM finding was fixed only for the Wave 1 increment.
- **fix sketch**: Generalize the existing regex source-parser in Wave1Feats_ProductionMetadata_MatchesSpec to parse ALL <field>.Initialize(...) calls in TaomCulturalFeats.cs, then (a) assert the CulturalFeatsServiceTests mirror table values equal the parsed production values, and (b) pin count == 129. Mechanical extension of code already written.

### [TEST-03] Transpiler IL-match logic untested across 3 features; binding gate stops at target-method resolution
- **effort** M · **risk** LOW (Test-only; the real-IL variant is machine-bound (Inconclusive without a game install) exactly like the existing binding suite, so CI value is limited to dev machines — same accepted tradeoff as HarmonyPatchBindingTests.) · **confidence** HIGH · · unmatched
- **impact**: Every transpiler is deliberately fail-safe (logs + returns original IL), so an engine update that reorders IL or renames the anchor silently reverts the feature to vanilla — AI castle recruitment, character-selection action sets, and banner colors degrade with no failing test and no crash. The 2026-05-31 Codex review mandated the ordinal+anchor pattern (feedback_transpiler_ordinal_plus_anchor_failsafe) but the matcher itself shipped untested.
  - `Main/Features/CastleRecruitment/Hooks/CastleAiTranspiler.cs:27-81` — SwapIsCastleGate is a pure internal static List<CodeInstruction> -> List<CodeInstruction> function with 4 bail/success branches (reflection-miss, no get_IsCastle, anchor-missing, swap-first-only) — zero test references repo-wide.
  - `TAOM.Tests/Migration/HarmonyPatchBindingTests.cs:12-23, 37-76` — The binding gate verifies each patch's TARGET METHOD resolves against the installed engine; it never runs transpilers against the target's IL, so anchor/ordinal drift is invisible to it.
  - `Main/Features/CharacterSelection/Patches/RefreshCharacterEntityAuxPatch.cs:27-78` — Second transpiler family with 3 documented silent-degradation paths (LogTranspilerDegradation); the CharacterSelection feature has zero test files.
  - `Main/Features/BannerColorPersistence/Hooks/Banner_TryGetBannerDataFromCode_Transpiler.cs:1` — Two more transpilers in BannerColorPersistence (plus CampaignSceneNotificationHelper_CreateNotificationCharacter_Transpiler.cs) with no IL-match tests.
- **fix sketch**: Unit-test SwapIsCastleGate with synthetic CodeInstruction lists (one test per branch: swap happens at first get_IsCastle, anchor-missing bails, second get_IsCastle never touched). Then add a BindingVerification-category test that pulls real IL via PatchProcessor.GetOriginalInstructions(target) using the existing GameAssemblies loader and asserts each transpiler returns a MODIFIED stream (match succeeded).

### [TEST-06] Logic-bearing engine-coupled 'services' on the character-creation path have zero tests and sit outside the binding catalogue
- **effort** M · **risk** MED (Refactoring reflection chains on the CC path risks breaking a flow that currently works and is only verifiable in-game; pure-logic extraction is safe but the seam-cutting around statics is not.) · **confidence** MED · · unmatched
- **impact**: Both run on the new-game/character-creation critical path. An engine rename of SetSelectedCulture or the _cultures VM field degrades silently (catch-and-log at line 98-101) — wrong family names or unset cultures for every new campaign, discoverable only by playing. The playbook's 'boundary classes growing logic' is exactly this shape: decision logic (vlandia special-case, param-count dispatch) is trapped behind untestable statics.
  - `Main/Features/FactionMap/CultureSettingService.cs:17-103` — 103-line reflection chain (CharacterCreationManager -> _characterCreationContent -> SetSelectedCulture 1-vs-2-param dispatch) plus the vlandia clan-name regeneration rule — every dependency is a static (GameStateManager.Current, Hero.MainHero, Clan.PlayerClan, NameGenerator.Current); zero tests.
  - `TAOM.Tests/Migration/ReflectionSiteBindingTests.cs:21-23` — The binding catalogue explicitly excludes live-instance (instance.GetType()) reflection sites as not offline-verifiable — so none of CultureSettingService's reflected member names are covered by the binding gate either.
  - `Main/Features/HeroRace/CharacterTableauService.cs:17-46` — 268-line committed service (distinct from the in-flight CharacterSpawnerService) taking raw CharacterTableau/Equipment engine types; zero test references.
- **fix sketch**: Extract the testable decisions (vlandia-and-nonempty-clan-list rule; 1-vs-2-param SetSelectedCulture selection) into pure helpers with tests; add the statically-knowable member names (CharacterCreationState.CharacterCreationManager, _characterCreationContent, SetSelectedCulture) as ordinary rows in ReflectionSiteBindingTests since the declaring types are resolvable offline even though the call sites use instance.GetType().

## TECHDEBT (14)
_Not audited:_ NOT COVERED (other categories own them): test quality/coverage depth (cat 4), Python tool internals beyond duplication counts (cat 1/3), Dependencies/ C++ and CI security (cat 2), ModuleData XML integrity (cat 9), docs link health (cat 8). Cross-category handoff for the Docs auditor: CLAUDE.md's Harmony table still lists Patch0_BattleScenes as '(DISABLED)' but committed SubModule.cs (HEAD ~line 16

### [TECHDEBT-01] Add the architecture-enforcement tests both ADRs promise but that do not exist
- **effort** S · **risk** LOW (Pure additive test code scanning Main/ source text; worst case is a noisy assertion that gets calibrated with an explicit exemption list.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: The two house rules with the most violations (see TECHDEBT-02/03) have zero automated enforcement despite both ADRs claiming it. Violations accumulate silently between /deep-review runs; the ADR text actively misleads readers into thinking a gate exists.
  - `docs/adrs/007-adapter-pattern.md:764` — "See TAOM.Tests/Architecture/AdapterPatternArchitectureTests.cs for automated enforcement" — that file does not exist (Glob TAOM.Tests/Architecture/*.cs returns nothing; the string appears nowhere else in the repo).
  - `docs/adrs/002-thin-entry-points.md:327` — Enforcement section claims "Architecture Tests: Automated tests verify entry point line counts" — no such test exists in TAOM.Tests.
  - `TAOM.Tests:n/a` — grep for Architecture/AdapterPattern test classes across TAOM.Tests finds only feature tests; no source-scan architecture suite.
- **fix sketch**: Write an MSTest source-scan suite: (a) line-count check on classes under */Hooks/ and *Behavior.cs with a committed exemption baseline so existing debt doesn't block CI but new debt does; (b) regex check that I*Service.cs interface signatures contain no sealed TaleWorlds type names. Update both ADR Enforcement sections to point at the real file.

### [TECHDEBT-05] Live Patch0_BattleScenes carries a stale 'dormant/commented-out' comment and CLAUDE.md still says DISABLED
- **effort** S · **risk** LOW (Comment + doc-table edits only; no behavior change.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Anyone triaging a map-load AV or battle-scene bug will read 'dormant'/'DISABLED' and rule out a patch that is actually live and intercepting AccessViolationExceptions — a worst-case misleading comment on exactly the kind of code (CSE handling, retry loop) where accurate state matters.
  - `Main/SubModule.cs:156-161` — `_harmony.PatchCategory("Patch0_BattleScenes")` is active, with a comment saying it was re-enabled 2026-06-01.
  - `Main/Features/BattleScenes/Hooks/MBMapScene_GetBattleSceneIndexMap_Patch.cs:18-22` — In-code comment claims "Dormant today (category Patch0_BattleScenes is commented out in SubModule.cs)" — actively false; this Prefix (which swallows+retries AccessViolationException up to 3 times) runs in production.
  - `CLAUDE.md:433` — Harmony Patch Categories table row still reads "Patch0_BattleScenes | Battle scenes (DISABLED)".
- **fix sketch**: Rewrite the patch-file comment to reflect live status (volatile guard is now load-bearing, not future-proofing), and update CLAUDE.md row 433 to 'Battle scenes (re-enabled 2026-06-01, grid validation pending)'.

### [TECHDEBT-06] tools/ at 114 scripts: three coexisting validator generations and a top-level pile of already-applied one-shots
- **effort** S · **risk** LOW (Moving files to tools/archive/ + deleting the two superseded validators; only risk is a doc or skill still referencing an old path — grep before moving.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Agents and humans pick the wrong validator (the legacy ones silently check less), and every tools/ listing costs scan time. The taom_schema.py consolidation direction (CLAUDE.md) is established but the superseded scripts were never retired behind it.
  - `tools/:n/a` — 114 .py scripts at top level (ls count this session); no archive/ separation between live tooling and finished one-offs.
  - `tools/validate_gondor_refs.py:n/a` — Still present although CLAUDE.md labels it "(Gondor-only legacy)", superseded by validate_all_troop_refs.py, itself superseded by the schema-driven validate_moduledata.py — three generations of the same gate all invocable.
  - `tools/rollback_erebor_iron_misfile.py:n/a` — CLAUDE.md describes it as a "One-off cleanup script ... used once during #211"; siblings cleanup_deleted_troops_212.py, expand_party_templates_212.py, fix_v1_4_5_item_ids.py, migrate_equipment_type_1_4_3.py, migrate_hideouts_to_lotr.py, and 6 apply_*_troop_revamp.py scripts are all completed-issue one-shots still at top level.
- **fix sketch**: Create tools/archive/ for executed one-shots (keep history, drop discoverability), delete or stub validate_gondor_refs.py to print a pointer at validate_moduledata.py, and update tools/README.md + CLAUDE.md tables accordingly.

### [DEBT-05] tools/ guidance points at superseded validators and 41 spent one-off migration scripts remain live in tools/
- **effort** S · **risk** LOW (Doc edits plus moving dead scripts to tools/archive/ (git history preserves them); only risk is deleting a one-off someone still wanted — archiving instead of deleting removes that.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Doc-driven agents run a superseded validator with narrower coverage than validate_moduledata.py; the 36% of tools/ that is spent migrations inflates every directory scan, confuses 'which script is current', and carries a small re-run-against-drifted-XML hazard (mitigated by --dry-run defaults).
  - `tools/README.md:35-36` — validate_all_troop_refs.py and audit_item_refs.py both documented as 'superseded by validate_moduledata.py … kept for now' — three validator generations coexist (validate_gondor_refs.py → validate_all_troop_refs.py → validate_moduledata.py per CLAUDE.md:805,811).
  - `CLAUDE.md:791` — The Equipment & Armory section still instructs 'Run python tools/validate_all_troop_refs.py' (gen-2); docs/ai-includes/agent-operating-manual.md:35 gives every subagent the same gen-2 instruction — agents are routed to the weaker validator.
  - `tools/:n/a (ls)` — 114 .py scripts; 41 match the one-off apply_/generate_/cleanup_/rollback_/expand_ prefixes, many keyed to closed issues (#211/#212/#224, e.g. cleanup_deleted_troops_212.py, apply_gondor_polish_224.py) — spent mechanical migrations with --apply write modes still invocable against since-drifted XML.
- **fix sketch**: Move spent issue-keyed one-offs to tools/archive/ (or delete), update CLAUDE.md:791 and agent-operating-manual.md:35 to recommend validate_moduledata.py (keeping validate_all_troop_refs only if a coverage gap is verified), and mark/remove the gen-1 Gondor validator.

### [TECHDEBT-03] ADR-002 fat entry points: campaign behaviors 3-4x the 150-line cap holding decision logic
- **effort** M · **risk** MED (Extraction must preserve subtle ordering (inquiry callbacks, _processingArrivedMessenger latch, scratch-list removal) in ported code that currently works in-game.) · **confidence** HIGH · · unmatched
- **impact**: The exact failure class ADR-002 exists to prevent: the messenger arrival state matrix and resource desertion math are untestable, and both features have already needed Codex-review fixes in these paths (in-file comments cite review #34 catching a fugitive-state hole in this very matrix).
  - `Main/Features/Messengers/MessengerCampaignBehavior.cs:250-305` — 644-line behavior; HandleArrivedMessenger contains the target-availability state matrix (IsAlive/Disabled/IsFugitive/IsActive/NotSpawned combinations) and outcome routing inline on sealed Hero — business logic the messenger service should own via an adapter.
  - `Main/Features/Messengers/MessengerCampaignBehavior.cs:179-237` — OnHourlyTick + UpdateMessengerPosition orchestrate travel/arrival/accident flow in the behavior (math is delegated to _service, but the per-messenger lifecycle decisions are not).
  - `Main/Features/SpecialResources/SpecialResourcesBehavior.cs:338-364, 422-433` — 443-line behavior; ApplyDesertion(MobileParty, ...) loops and mutates rosters inline, plus foreach over settlements/MemberRoster.GetTroopRoster() — roster mutation logic living in the entry point.
  - `.claude/rules/harmony-patches.md:12` — House rule restated: "Entry point files MUST be <150 lines (ADR-002)" — both files are 3-4x over with logic, not just wiring.
- **fix sketch**: Extract HandleArrivedMessenger's availability matrix into MessengerService behind a small IHeroAvailabilityAdapter (pure enum-returning function, trivially testable), and ApplyDesertion into SpecialResourceService behind the existing roster adapters. Behaviors keep event wiring + inquiry display only.

### [TECHDEBT-04] Consolidate ~19 copy-pasted JSON config providers and unify the two float-validation idioms
- **effort** M · **risk** LOW (Mechanical extraction of a JsonConfigProviderBase<T> (path, parse, fallback, summary-warning) with per-feature Validate kept as the override point; existing provider tests pin behavior.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Every new feature re-copies ~50 lines of loader scaffolding; the NaN-guard rule has shipped broken three times historically (per .claude/rules/csharp-architecture.md) precisely because each provider re-implements it. Divergent copies drift — some log file-missing as Info, others as Warning.
  - `Main/Features/CastleRecruitment/CastleRecruitmentConfigProvider.cs:10-53` — Doc comment literally says "Mirrors BanditScalingConfigProvider"; LoadConfig (path-combine, File.Exists fallback, try/catch JsonConvert, Validate with rejected-flag + summary warning) is near-verbatim identical to MessengerConfigProvider.cs:24-85.
  - `Main/Features (find):n/a` — 29 *ConfigProvider/*SettingsProvider files totaling 3,879 lines; 19 of them call JsonConvert.DeserializeObject with the same scaffolding (counts from this session's find/grep).
  - `Main/Features/Messengers/MessengerConfigProvider.cs:63-77` — Hand-rolled `float.IsNaN(...) || float.IsInfinity(...) || < || >` chains instead of the house FiniteFloatValidator helper.
  - `Main/Core/Validation/FiniteFloatValidator.cs:1-67` — The house validator exists and is used by 13 feature files, while ~10 other files hand-roll float.IsNaN checks (grep counts this session) — two competing idioms for the same mandatory rule.
- **fix sketch**: Add Main/Core/Configuration/JsonConfigProviderBase<T> encapsulating Lazy-load + missing-file fallback + parse-catch + rejected-flag summary; migrate providers incrementally. Sweep hand-rolled IsNaN/IsInfinity chains to FiniteFloatValidator in the same pass.

### [DEBT-01] ADR-007 promised architecture enforcement test was never built; 4 service interfaces now carry sealed TaleWorlds types
- **effort** M · **risk** LOW (The gate itself is an additive reflection-based test with a grandfather allowlist; it changes no production code. Migrating the 4 grandfathered interfaces is separate follow-up work with its own (manageable) risk.) · **confidence** HIGH · · unmatched
- **impact**: ADR-007 is marked Mandatory but its enforcement mechanism is fictional, so the boundary erodes one self-justifying inline comment at a time — each new feature relitigates the rule, and the eroded methods are admitted-untestable (TroopWeight) in a repo where TDD is mandatory. Four interfaces today; nothing stops the fifth.
  - `docs/adrs/007-adapter-pattern.md:764` — ADR says 'See TAOM.Tests/Architecture/AdapterPatternArchitectureTests.cs for automated enforcement' — `find TAOM.Tests -iname "*architecture*" -o -iname "*AdapterPattern*"` returns nothing; the directory and test do not exist. ADR-002 (docs/adrs/002-thin-entry-points.md:327) makes the same 'Architecture Tests' enforcement claim.
  - `Main/Features/TroopWeight/ITroopWeightService.cs:11-14,29-32` — Service interface takes sealed CharacterObject/PartyBase/TroopRoster/TroopRosterElement in 5 methods; doc comment at :29-30 admits 'Not unit-tested (sealed PartyBase / TroopRoster)' — the exact testability failure ADR-007 exists to prevent.
  - `Main/Features/CultureMarketplace/ICultureMarketplaceMaintenanceService.cs:24,27` — Two methods take sealed Settlement; doc comment (:11-15) rationalizes it as pass-through that 'keeps the signature ergonomic without breaking ADR-007's intent'.
  - `Main/Features/FactionMap/ICultureSettingService.cs:7` — SetCultureOnCharacterCreation(CultureObject culture, object viewInstance, object? originalDataSource) — sealed type plus two untyped object params in a service signature.
  - `Main/Features/Arena/ITournamentService.cs:11-15,30` — BuildPrizePool returns MBList<ItemObject> (sealed); header comment openly documents the deviation 'pragmatically the existing surface keeps the boundary type'.
- **fix sketch**: Add the missing TAOM.Tests/Architecture test: reflect over TAOM.dll, assert no I*Service interface method signature references TaleWorlds.* types, seeded with an explicit allowlist of the 4 current offenders (each linking its rationale). Optionally add the ADR-002 entry-point line-count assertion in the same file. Burn down the allowlist as separate small PRs.

### [DEBT-02] JSON config-provider load skeleton duplicated across ~19 providers with drifting copies
- **effort** M · **risk** LOW (Pure structural refactor behind existing interfaces; several providers already have semantic-validation unit tests that pin behavior. Migrate incrementally, one provider per commit.) · **confidence** HIGH · · unmatched
- **impact**: ~500+ lines of copy-pasted load boilerplate; every improvement to the pattern (the NaN-validation rule that has shipped 3 bug fixes, Lazy caching, summary-warning logging) must be applied N times and demonstrably isn't (SiegeDefense). New features keep paying the copy cost and inherit whichever sibling they copied.
  - `Main/Features/RevoltTuning/RevoltTuningConfigProvider.cs:25-48` — Reference copy of the skeleton: IPathService path join → File.Exists fallback-to-defaults → ReadAllText → JsonConvert.DeserializeObject ?? new → catch→defaults → Validate.
  - `Main/Features/CastleRecruitment/CastleRecruitmentConfigProvider.cs:21-49` — Near-verbatim same skeleton (Lazy ctor, Exists check, parse-catch, defaults); BanditScalingConfigProvider.cs:16-44 and MessengerConfigProvider.cs:15-43 are the same again — ~30-40 duplicated lines each, ~19 JSON-deserializing provider/loader files matched repo-wide.
  - `Main/Features/Siege/SiegeDefenseConfigProvider.cs:21-43` — Divergent copy: public LoadConfig() with NO Lazy cache and NO Validate pass — predates the 'Config Providers MUST Validate' house rule (.claude/rules/csharp-architecture.md) and never got retrofitted, illustrating how copies drift.
- **fix sketch**: Extract a JsonConfigProviderBase<T> (or composable helper) in Main/Core owning path-resolve + exists-fallback + parse-catch + reverted-summary logging, with an abstract/virtual Validate(T) hook. Migrate providers one at a time; the migration sweep is also the audit that brings stragglers like SiegeDefense under the mandatory validate-on-load rule.

### [DEBT-04] Fat CampaignBehavior/MissionLogic entry points hold extractable decision logic with no documented exemption (ADR-002)
- **effort** M · **risk** MED (Behavior-preserving extraction of campaign-event logic; desertion/grace semantics are save-affecting player-facing behavior — characterization tests on the extracted decisions must come first (refactoring-specialist agent territory).) · **confidence** HIGH · · unmatched
- **impact**: Decision logic (grace periods, victor/ratio math, desertion application) sits where it cannot be unit-tested, in a feature (SpecialResources) that mutates player rosters — exactly the blast radius ADR-002 exists to protect. Meanwhile the rule's authority erodes because compliance is now negotiated per-file in comments.
  - `Main/Features/SpecialResources/SpecialResourcesBehavior.cs:149-258` — 443-line behavior (limit 150). OnDailyTickHero (:177-209) contains the desertion-grace/warn decision ladder with balance projections; OnMapEventEnded (:222-236) computes victor determination and enemy/player ratio inline; private helpers ApplyDesertion (:338), CountOwnedTowns (:416), GetTroopUpkeepFromParty (:428) are roster-walking logic living in the untestable layer.
  - `Main/Features/Elephant/ElephantMissionBehavior.cs:27` — Single MissionLogic class, 263 lines; WargMissionBehavior.cs (:15) is 203 lines — both over the ADR-002 limit, neither carries an exemption note.
  - `Main/Features/Messengers/MessengerCampaignBehavior.cs:19-26` — Contrast/precedent: 644 lines but with an explicit reviewed-deviation comment (Codex #34 + deep-review standards agent confirmed engine-coupled orchestration) — the exemption mechanism exists but is ad-hoc per file, not codified in the ADR.
- **fix sketch**: Extract the OnDailyTickHero ladder and OnMapEventEnded victor/ratio decisions into ISpecialResourceService (which already exists and owns the math) as pure decision methods; behavior keeps engine reads + UI messages. Then amend ADR-002 with the boundary-class exemption criteria already applied to Messengers so future deviations are governed, not improvised.

### [TECHDEBT-02] ADR-007 adapter violations: services mutate campaign state through sealed types directly
- **effort** L · **risk** MED (Refactoring working, Codex-reviewed campaign-state mutation paths behind adapters; behavior must stay byte-identical and the reward/tracker paths only manifest in long campaign sessions.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: ADR-007's whole point is testable business logic; these paths (siege reward grant, career preview equipment, prize-pool construction) are untestable without a live game and are exactly the code /deep-review keeps finding bugs in. SiegeDefenseService's reward path mutates influence/relations with no unit-test coverage possible.
  - `Main/Features/Siege/SiegeDefenseService.cs:295-301` — GrantReward inside the service does `Hero.MainHero.Clan.Influence += ...`, `Kingdom.All.FirstOrDefault`, and `ChangeRelationAction.ApplyRelationChangeBetweenHeroes` — direct sealed-type state mutation in the service layer.
  - `Main/Features/Siege/SiegeDefenseService.cs:169, 180, 266-282` — `MobileParty.MainParty?.CurrentSettlement`, `Settlement.Find`, and `Campaign.Current.VisualTrackerManager.RegisterObject/RemoveTrackedObject` called directly, alongside an injected IPlayerContextAdapter used for other lookups — the adapter boundary exists but is bypassed.
  - `Main/Features/CharacterCreation/CareerMenuService.cs:95, 182-183, 219, 315` — Service reads/writes `Hero.MainHero` (IsFemale, StringId, equipment application) directly.
  - `Main/Features/Arena/TournamentService.cs:47-62` — BuildPrizePool iterates static `Items.All` and returns `MBList<ItemObject>` — sealed types in the service signature.
  - `Main/Features (grep):n/a` — 26 *Service.cs files under Main/Features carry `using TaleWorlds.CampaignSystem` (count grep this session); the four above are the strongest logic-bearing cases.
- **fix sketch**: Per service: route the sealed-type calls through existing adapters (ISettlementAdapter, IHeroAdapter) or extend IPlayerContextAdapter / add an IVisualTrackerAdapter + IRelationActionService, then write the unit tests ADR-007 enables. Start with SiegeDefenseService (worst offender, smallest blast radius), then CareerMenuService.

### [TECHDEBT-07] VolunteerRecruitmentService: 988-line data-in-code god service; JSON migration proven for one culture and stalled
- **effort** L · **risk** MED (Recruitment is save-visible and heavily tested; conditional pools (ConditionalSettlementMap predicates) need the existing fail-closed condition-string mechanism extended, and per-culture migration must keep PickWeighted behavior identical.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Every culture rebalance (the #228-#245 lords batch shows this work is ongoing) means C# edits, rebuild, and churn in a file where a typo'd dictionary key fails silently at runtime; Gondor meanwhile carries two divergence-prone copies of the same pools (hand-written + JSON overwrite).
  - `Main/Features/TroopProgression/VolunteerRecruitmentService.cs:23-61` — Static ctor runs 34 InitializeXxx methods hard-coding recruitment pools for ~16 cultures into 4 static mutable Dictionaries (lines 12-17) — 988 lines, the repo's 3rd-largest file (median Main/ file is 32 lines) and a top-5 churn hotspot.
  - `Main/Features/TroopProgression/VolunteerRecruitmentService.cs:421, 18-21` — EnsureGondorJsonLoaded: the JSON-driven path (recruitment_pools/gondor.json, weight conversion, fail-closed condition parsing) exists and works — for exactly one of the 16 cultures, with the hand-written Gondor pools retained as a shadow safety net.
  - `.claude/rules/troops.md:checklist step 4` — Every troop add/restructure for any culture requires editing this C# file + rebuilding the mod — data changes are coupled to compilation.
- **fix sketch**: Extend the gondor.json loader to a generic per-culture recruitment_pools/<culture>.json loader (the condition-string + weight-conversion machinery already exists), migrate cultures incrementally with the hand-written initializer deleted per culture once its JSON round-trips the existing tests, ending with a <200-line pure resolver.

### [DEBT-03] VolunteerRecruitmentService: 988-line #2 churn hotspot keeps recruitment data as C# code with a half-finished JSON migration (Gondor only, dual-source)
- **effort** L · **risk** MED (Recruitment is gameplay-critical and per-culture pools encode reachability invariants — but the existing reachability flood-fill test plus 129 unit tests and the fail-closed loader give a strong safety net for an incremental migration.) · **confidence** HIGH · · unmatched
- **impact**: Every recruitment balance pass is a C# edit + rebuild in the repo's hottest feature file instead of a data edit; Gondor maintains two copies of the same pools where only the JSON one is live in-game, so a fix landed in the wrong copy looks done but isn't (in tests) or vice versa.
  - `Main/Features/TroopProgression/VolunteerRecruitmentService.cs:23,68-912` — Static ctor drives 37 hand-written InitializeXxx methods (Rohan:68 … Rhun:912) — roughly 850 of the 988 lines are per-settlement/clan/culture weighted-pool tables as code. 27 commits since 2026-01-01 (highest feature-file churn measured this audit; git log).
  - `Main/Features/TroopProgression/VolunteerRecruitmentService.cs:19,421` — Comment ':19' documents the converged direction: JSON entries OVERWRITE hand-written settlement keys; EnsureGondorJsonLoaded (:421) implements it — but Main/_Module/ModuleData/recruitment_pools/ contains only gondor.json, leaving ~15 cultures code-only.
  - `TAOM.Tests/Features/TroopProgression/VolunteerRecruitmentServiceTests.cs:1448-1689,2325-2326` — Loader has thorough tests incl. production-JSON integration (ParsesAndApplies23Groups) and the reachability flood-fill seeds from production JSON — but nothing asserts the hand-written Gondor safety-net tables stay consistent with gondor.json, so the two Gondor sources can silently drift (the mirror-table failure class from RCA 2026-06-07).
- **fix sketch**: Generalize GondorRecruitmentJsonLoader to recruitment_pools/<culture>.json, migrating 2-3 cultures per pass with the reachability test as the gate; delete each hand-written table once its JSON is proven (or add a source-parse consistency test if the safety-net pattern must stay). End state: service shrinks to resolution-cascade logic + loader.

### [TECHDEBT-08] SubModule.cs is a 749-line single-owner registration hub — 5x the entry-point cap and the documented parallel-session contention point
- **effort** M · **risk** MED (Module load order is deliberately sequenced (CrashReport-first comment at lines 96-102, deferred MovementOrder category at 650-654, View-assembly ordering note at 162); any registrar abstraction must preserve exact ordering or break startup.) · **confidence** MED · ✓ verifier:REAL
- **impact**: Every feature addition touches the same file (merge conflicts, parallel-agent lockouts, worktree overhead mandated by harness-facts.md), and the file is 5x the ADR-002 cap. The cost is process friction more than correctness — but it recurs on every feature.
  - `Main/SubModule.cs:90-245, 293-511` — Committed file is 749 lines (git show HEAD this session; the 2-line unstaged spider diff is excluded); OnSubModuleLoad ~155 lines and OnGameStart ~218 lines of per-feature wiring, plus inline shader-ticker logic in OnApplicationTick (706-726).
  - `.claude/rules/harness-facts.md:Parallel-port build watcher section` — Documents ~2 hours lost (rca-companiontactics-2026-05-06) to parallel agents contending on this exact file; SubModule.cs/IoC.cs single-ownership is a standing constraint precisely because every feature must edit them.
  - `Main/SubModule.cs:642-704` — OnMissionBehaviorInitialize hand-adds 14+ mission behaviors with per-feature resolve-and-guard blocks — each new mission feature grows this method.
- **fix sketch**: Introduce an ordered IFeatureBootstrapper (OnGameStart/OnMissionInit hooks) discovered from an explicit ordered list in SubModule, moving per-feature wiring blocks into each feature's existing *IoC.cs file; SubModule shrinks to lifecycle scaffolding + the order list. Do it incrementally, one lifecycle method at a time.

### [DEBT-06] SubModule.cs is the repo's highest-contention god file: 751 lines, 114 commits in 2026, all feature wiring inline
- **effort** L · **risk** MED (Submodule lifecycle ordering is load-bearing (deferred Patch_MissionTime category, View-assembly-init dependency, 'added LAST' constraints) — a naive split that loses ordering crashes at startup; the fix must encode order explicitly and be verified in-game, not just by tests.) · **confidence** MED · · unmatched
- **impact**: Every feature's wiring funnels through one file: merge conflicts, worktree-isolation overhead for any parallel work, and lifecycle-ordering knowledge encoded as comments rather than structure. Growth is monotonic (~40 features in, more coming: elephant polish, spider polish, careers ×15 cultures).
  - `Main/SubModule.cs:90,293,512` — OnSubModuleLoad spans ~156 lines (:90-246), OnGameStart ~219 lines (:293-512), OnGameInitializationFinished ~130 lines (:512-642) — each phase method alone exceeds the entire ADR-002 budget; 227 registration-ish calls (PatchCategory/AddModel/AddBehavior/IoC.Resolve) measured by grep. 751 lines in working tree, of which 2 are the excluded in-flight spider hunk.
  - `Main/SubModule.cs:n/a (git log)` — 114 commits touching this file since 2026-01-01 — by far the highest churn in Main/ — and CLAUDE.md designates it single-owner precisely because parallel agents collide on it (.claude/rules/harness-facts.md documents a ~2-hour session lost to the SubModule/csproj contention cascade, RCA 2026-05-06).
  - `Main/SubModule.cs:671-680` — Example of per-feature wiring inline: MissionDiagnostic resolve + AddMissionBehavior block with ordering comment ('added LAST so it sees all behaviors') — ordering knowledge lives only as comments scattered through the file.
- **fix sketch**: Introduce per-feature installer objects (mirroring the existing per-feature *IoC.cs precedent) exposing phase methods (OnGameStart/OnInitFinished/OnMissionInit), invoked from SubModule via one explicit ordered manifest list per phase. SubModule shrinks to phase orchestration + the ordering manifest; feature wiring moves next to the feature. Stage it one lifecycle phase at a time.

## DEPS (12)
_Not audited:_ Considered and dropped: (1) DryIoc.dll 4.8.8 (old 4.x major; 5.x/6.x exist) — deployed and working in the installed module bin; a major upgrade is churn with zero player value, rejected per simplicity criterion. (2) Test-stack versions (MSTest 3.1.1, Test.Sdk 17.8.0, NSubstitute 5.1.0, coverlet 6.0.0) — behind latest but functional, no security driver; not worth doing. (3) Dependencies/.vendor-sou

### [DEPS-01] Stale .claude skill docs still claim engine is v1.3.15 and tell agents to distrust the v1.4.5 decompile dump
- **effort** S · **risk** LOW (Pure doc/wording edits in 3 SKILL.md files; no code or build behavior changes. Only care needed: keep taom-src description ≤30 words per harness-facts.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Both the installed game and the decompile dump are v1.4.5 (CLAUDE.md, agent-operating-manual.md:41, re-decompiled 2026-05-22), and taom-src.ps1 auto-detects the version. These skills tell every debugging/build-fix session the opposite: that the installed engine is 1.3.15 and the dump is version-mismatched. The taom-src description misleads every conversation eagerly; /investigate step 6 can cause a session to discard correct decompile evidence mid-debug. lint_docs covers docs/, not .claude/skills, so this drift is invisible to the existing stale-version linter.
  - `.claude/skills/taom-src/SKILL.md:3, 9` — Frontmatter description (eagerly loaded into every conversation) says 'Decompile and cache TaleWorlds v1.3.15 types'; body says 'Wraps ilspycmd against the installed v1.3.15 DLLs and caches at ~/.taom-src/v1.3.15/ ... never guess signatures from the v1.4 dump'
  - `.claude/skills/investigate/SKILL.md:63, 96` — Step 6 instructs: 'E:\Decompiled_Bannerlord\ is v1.4 but the installed game is v1.3.15 ... If decompiled and installed disagree, trust installed'; symptom table cites '1.3.15 vs 1.4 decompile drift'
  - `.claude/skills/build-fix/SKILL.md:62` — Routing table says 'verify v1.3.15 signatures via ilspycmd, NOT the v1.4 decompile'
- **fix sketch**: Update taom-src description/body to 'installed engine version (currently v1.4.5, auto-detected)', rewrite /investigate step 6 and the build-fix table row to say decompile dump and installed DLLs are both v1.4.5 with ilspycmd/taom-src authoritative for signatures. Grep .claude/ for remaining load-bearing '1.3.15' refs while there (context-save example and lint-docs guidance are historical/intentional).

### [DEPS-02] Committed TaleWorlds API snapshot is stale — 3 committed patches missing; -Check reproducibility gate wired nowhere
- **effort** S · **risk** LOW (Regeneration is a read-the-DLLs script plus a doc commit; the only moving part is making sure it runs against a clean tree (the 2026-05-30 fabrication RCA hit exactly this — don't snapshot a tree carrying others' WIP, and the Spider in-flight work is uncommitted right now).) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: The snapshot exists so signature questions are answerable in-repo without the external decompile dump; three patch entries and the 'Patches: 110' count are now wrong, and nothing structurally prevents the drift from growing (the documented maintenance rule has been missed 3 times in 10 days). Functional binding verification is unaffected.
  - `docs/reference/taleworlds-api-snapshot/patch-targets.md:7` — 'Patches: 110' — snapshot last regenerated in commits c5b0438/4d288b5 (2026-05-30); grep finds no entry for Patch41_McmLayoutFix, CharacterCreationReviewStageVM_AutoFillName_Patch (Patch44), or Patch46_TournamentDwarfDismount
  - `Main/Features/Arena/Hooks/Patch46_TournamentDwarfDismount.cs:1` — [HarmonyPatch]-bearing class committed 2026-06-09 (0310617); AutoFillName patch committed 2026-06-01 (a69d630); Patch41 committed 2026-05-30 (f23434b) — all absent from the snapshot
  - `docs/reference/taleworlds-api-snapshot/README.md:53-55` — Maintenance rule: refresh 'after any patch/GameModel change'; 'pwsh tools/snapshot_api_surface.ps1 -Check' exists for CI/local reproducibility but .github/workflows/build.yml contains no snapshot step and no hook runs it
  - `TAOM.Tests/Migration/HarmonyPatchBindingTests.cs:35, 83` — Binding gate reflects over TAOM.dll (TaomAssembly.GetTypes()), so the live test gate DOES cover the new patches — only the committed snapshot doc is stale
- **fix sketch**: Run /verify-bindings (or pwsh tools/snapshot_api_surface.ps1) after the in-flight spider work lands, commit the regenerated files. To stop recurrence cheaply: add a snapshot_api_surface.ps1 -Check step next to the existing pre-commit hooks (or CI) gated on changes under Main/**/Hooks/** or Main/Features/**/*Model.cs.

### [DEPS-03] dr3-maintenance.md (authoritative dependency-bump procedure) drifted from shipped reality in 3+ places
- **effort** S · **risk** LOW (Doc + comment edits only. config-protection hook doesn't cover this file; no behavior change.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: This doc is the step-by-step procedure for every future BUTR/engine bump. Following line 200 when authoring a new stub (empty SubModules) would reintroduce the exact BLSE breakage the current stubs were fixed to avoid; the wrong file inventory makes 'verify ~28 DLLs' checks fail or pass for the wrong reasons. Versions themselves are NOT drifted — all four stub v99 minors match the shipped pins (verified incl. ButterLib 2.10.4 / MCM 5.11.4 via DLL FileVersionInfo) — only the doc text is wrong.
  - `docs/migration/dr3-maintenance.md:191-194 vs 206` — 'Stub modules' list shows exact versions (v2.4.2 / v2.13.1 / v2.10.4 / v5.11.4) while the actual stubs use the v99 strategy (v2.4.99.0 etc.) — contradicted by the doc's own v99 rule 12 lines later
  - `docs/migration/dr3-maintenance.md:200` — Claims each stub 'Has <SubModules /> empty — no DLLs load'; all four stubs ship a real AliasStubSubModule entry (Stubs/Bannerlord.Harmony/_Module/SubModule.xml:65-72), and the stub's own comment documents that an EMPTY SubModules block breaks BLSE drag-to-reorder
  - `docs/migration/dr3-maintenance.md:320-353` — 'Reference: file inventory' for Dependencies/_Module/bin/ omits the 6 BUTR.CrashReport*.dll files actually tracked in git (git ls-files shows BUTR.CrashReport.dll + Models + 4 renderers, including Renderer.ImGui which the Linux-compat section at line 293-297 implies isn't bundled)
  - `Dependencies/TAOM.Dependencies.csproj:46-47` — Comment says 'MCMv5 5.11.3' while the actual pin (line 59) and the vendored Bannerlord.MBOptionScreen.v1.4.1.dll FileVersion are 5.11.4
- **fix sketch**: Update the stub-list versions to the v99 values, rewrite the 'empty SubModules' bullet to describe the AliasStubSubModule entry (citing the BLSE rationale already in the stub comment), add the 6 BUTR.CrashReport DLLs to the file inventory + Category 3 table, and fix the 5.11.3 comment in the csproj.

### [DEPS-06] Abandoned 89MB Dependencies/.vendor-source/ tree with UIExtenderEx 2.13.2 source vs the shipped 2.13.1 pin
- **effort** S · **risk** LOW (Deleting an untracked, gitignored directory; dr3-maintenance.md documents NuGet/Workshop/NexusMods as the recovery sources, and nothing in the build references the tree.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Working-tree-wide Glob/Grep (including this audit's own csproj glob) lands in these trees; a future session verifying UIExtenderEx behavior could read 2.13.2 source as authoritative for the 2.13.1 binary that ships. Plus 89MB of dead weight in every backup/copy of the working tree. No repo-content impact (untracked).
  - `Dependencies/.vendor-source/:(directory)` — 89MB on disk, 0 files tracked (gitignored via .gitignore:112): Harmony-2.4.2.0, Bannerlord.UIExtenderEx-2.13.2, MonoMod-master, cecil-0.11.5 + 5 tarballs — leftovers of the DR1 source-merge approach the csproj comments describe as replaced by PackageReferences
  - `Dependencies/TAOM.Dependencies.csproj:57-58, 102-109 (Main/TAOM.csproj)` — Shipped pin is Bannerlord.UIExtenderEx 2.13.1; the vendored source tree is 2.13.2 — one patch version AHEAD of what actually ships
- **fix sketch**: Delete Dependencies/.vendor-source/ (or move it outside the repo) — user action since it's their local disk; if reference source is genuinely wanted, keep only versions matching the shipped pins (2.13.1, not 2.13.2). Keep the .gitignore line as a guard.

### [DEPS-02] Committed API snapshot missing patches added since 2026-05-31 — refresh-on-patch-change rule not followed
- **effort** S · **risk** LOW (Regenerating two auto-generated markdown files; the -Check mode exists to verify reproducibility.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: The snapshot's stated purpose is answering 'what does TAOM patch and what is the v1.4.x signature' without the external decompile dump; reviews and sessions relying on it cannot see the newest patch targets. No runtime risk — the dynamic HarmonyPatchBindingTests enumerate from TAOM.dll and stay green — this is drift in the committed doc-of-record.
  - `c:\Users\mikew\source\repos\TAOM\docs\reference\taleworlds-api-snapshot\patch-targets.md:whole file` — 110 data rows; grep for Patch46/TournamentFight/PrepareForMatch/CharacterCreationReviewStage returns nothing across all snapshot files.
  - `c:\Users\mikew\source\repos\TAOM\Main\Features\Arena\Hooks\Patch46_TournamentDwarfDismount.cs:1` — Added in commit ef0c326 (2026-06-09) — after the snapshot's last regeneration (c5b0438/4d288b5, 2026-05-30; backlinks-only commit 2026-05-31).
  - `c:\Users\mikew\source\repos\TAOM\docs\reference\taleworlds-api-snapshot\README.md:53` — Maintenance rule: 'After a Bannerlord version bump (or any patch/GameModel change): pwsh tools/snapshot_api_surface.ps1 to refresh' — not executed for the June patches.
- **fix sketch**: Run pwsh tools/snapshot_api_surface.ps1 and commit — but do it as part of DEPS-01 so the snapshot regenerates against the now-installed 1.4.6 engine, not stale 1.4.5 assumptions. Consider wiring a reminder (hook or /skill-stocktake check) when files under Main/**/Hooks/ change without a snapshot commit.

### [DEPS-03] taom-src SKILL.md still describes v1.3.15 as the installed engine and calls the current dump 'the v1.4 dump' to distrust
- **effort** S · **risk** LOW (Pure text edit to one SKILL.md (plus mandatory CHANGELOG entry per the pre-commit hook); no behavior change.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: The stale description taxes and misleads every session and Task spawn; the body actively teaches agents the wrong trust ordering between the dump and the installed DLLs. The script itself auto-detects correctly, so this is documentation-only — but it is the documentation agents route signature research by.
  - `c:\Users\mikew\source\repos\TAOM\.claude\skills\taom-src\SKILL.md:3` — Frontmatter description: 'Decompile and cache TaleWorlds v1.3.15 types via taom-src path <Type>' — loaded eagerly into every conversation.
  - `c:\Users\mikew\source\repos\TAOM\.claude\skills\taom-src\SKILL.md:8` — Body: 'Wraps ilspycmd against the installed v1.3.15 DLLs and caches at ~/.taom-src/v1.3.15/ ... never guess signatures from the v1.4 dump at E:\Decompiled_Bannerlord\' — inverted from reality (installed = 1.4.6, dump = 1.4.5).
  - `c:\Users\mikew\source\repos\TAOM\docs\ai-includes\agent-operating-manual.md:41` — Project policy: 'Anything in an agent prompt or doc that still says v1.3.15 is stale' — the skill file violates the policy; TRACKING.md:38 records the script was made version-auto-detecting on 2026-05-22.
- **fix sketch**: Rewrite the description and body to say the script auto-detects the installed version from Version.xml (currently caching under ~/.taom-src/v1.4.6/) and reframe the dump-vs-installed guidance to match CLAUDE.md's lookup order. One-line CHANGELOG entry.

### [DEPS-05] dr3-maintenance.md stub-version rows contradict its own v99 strategy; csproj comment cites MCM 5.11.3 against the 5.11.4 pin
- **effort** S · **risk** LOW (Doc and comment text edits only; no deployed artifact changes.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: dr3-maintenance.md is the procedure followed during the next BUTR/engine bump; a maintainer following lines 191-194 verbatim would write exact stub versions and silently regress the v99 minimum-version strategy (breaking third-party mods with vX.Y.x lower-bound pins under BLSE). The stubs themselves are currently correct — this is pure doc/comment rot in the authoritative procedure.
  - `c:\Users\mikew\source\repos\TAOM\docs\migration\dr3-maintenance.md:191-194` — Lists the four stubs as v2.4.2 / v2.13.1 / v2.10.4 / v5.11.4 (exact versions), while line 206 in the same doc mandates the vX.Y.99.0 strategy.
  - `c:\Users\mikew\source\repos\TAOM\Stubs\Bannerlord.Harmony\_Module\SubModule.xml:44` — Actual deployed values are v2.4.99.0 (and v2.13.99.0 / v2.10.99.0 / v5.11.99.0 in the sibling stubs at line 12 each) — correct per policy, contradicting the doc's rows.
  - `c:\Users\mikew\source\repos\TAOM\Dependencies\TAOM.Dependencies.csproj:47` — Comment block says 'MCMv5 5.11.3' while line 59 pins Bannerlord.MCM 5.11.4 — minor comment drift in the file the doc directs maintainers to edit.
- **fix sketch**: Update dr3-maintenance.md:191-194 to show the v99 values (or replace exact versions with 'vX.Y.99.0 per the strategy below'), and fix the 5.11.3 comment in TAOM.Dependencies.csproj to 5.11.4.

### [DEPS-06] No NuGet lockfile or NuGet.config, and the CI build job can never run on the active branch (dead workflow_dispatch condition)
- **effort** S · **risk** LOW (RestorePackagesWithLockFile adds one step to the documented bump procedure (regenerate lockfiles on version changes) and locked-mode restore can fail CI on intentional bumps until refreshed.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: For a mod whose DLLs ship to players, restore today trusts nuget.org content without hash verification and leaves transitive versions floating across machines; and no CI run ever exercises restore/build to catch drift. Practical exposure is modest given exact pins and a single-developer Windows build machine — this is cheap insurance, not an active defect.
  - `c:\Users\mikew\source\repos\TAOM\.github\workflows\build.yml:3-7, 81` — on: declares only push/pull_request to master, yet the build job's if references github.event_name == 'workflow_dispatch' — an event the workflow never subscribes to; active development is on bannerlord-1.4.5, so Build & Test currently triggers nowhere.
  - `c:\Users\mikew\source\repos\TAOM\.github\workflows\build.yml:92-93` — dotnet restore TAOM.sln with no packages.lock.json and no NuGet.config anywhere in the repo (Glob verified) — restore depends on machine-default feeds with no transitive pinning or content-hash verification.
  - `c:\Users\mikew\source\repos\TAOM\Dependencies\TAOM.Dependencies.csproj:57-59` — Mitigating context: every PackageReference across all three csproj files is exactly pinned and mutually consistent (Lib.Harmony 2.4.2 ×3, UIExtenderEx 2.13.1 ×2, MCM 5.11.4 ×2), so top-level version drift is already controlled.
- **fix sketch**: Add <RestorePackagesWithLockFile>true</RestorePackagesWithLockFile> to Directory.Build.props, commit the generated packages.lock.json files, use dotnet restore --locked-mode in CI; add a workflow_dispatch: trigger (and/or bannerlord-1.4.5 branch trigger) so the build job is actually reachable; note the lockfile-refresh step in dr3-maintenance.md's update procedure.

### [DEPS-05] Lockfile-less NuGet restore: no packages.lock.json, no nuget.config, no locked-mode CI restore
- **effort** S · **risk** LOW (Adding RestorePackagesWithLockFile + committing lock files is additive; only failure mode is CI restore breaking loudly in --locked-mode when someone bumps a pin without regenerating the lock (which is the point).) · **confidence** MED · ✓ verifier:REAL
- **impact**: Without a lockfile there is no transitive-graph pinning, no package-hash verification, and no feed pinning — a machine-level nuget.config with an extra feed could silently introduce dependency confusion, and transitive versions can float between restores. Practical exposure is modest because every direct pin is exact and nuget.org versions are immutable, but BuildResources runs arbitrary build-time code, which is where the supply-chain cost would land.
  - `.github/workflows/build.yml:97-99 (Restore step)` — 'dotnet restore TAOM.sln' with no --locked-mode; repo has zero packages.lock.json and no nuget.config (git ls-files | grep -i nuget returns nothing; no RestorePackagesWithLockFile in any csproj/props)
  - `Main/TAOM.csproj:95-114` — All PackageReferences are exact-pinned (Lib.Harmony 2.4.2, UIExtenderEx 2.13.1, MCM 5.11.4, DryIoc 4.8.8, Newtonsoft.Json 13.0.3), which mitigates version drift; Bannerlord.BuildResources 1.1.0.129 executes build-time MSBuild tasks from the feed
- **fix sketch**: Add <RestorePackagesWithLockFile>true</RestorePackagesWithLockFile> to Directory.Build.props, run dotnet restore to generate and commit lock files for Main/TAOM.Tests/Dependencies, switch the CI restore to dotnet restore --locked-mode, and add a minimal repo nuget.config pinning nuget.org as the sole source. Borderline 'not worth doing' under the simplicity criterion if the user considers the solo-dev/single-machine exposure acceptable — flagging for an explicit decision rather than asserting it must ship.

### [DEPS-04] Migration #210 closeout: all 7 runtime punch-list items unchecked and TRACKING.md self-references are stale
- **effort** M · **risk** LOW (Mostly a keyboard verification session plus doc/issue updates; the only code changes are conditional (swap VerticalBottomToTop, gate hideout award on Victory, possibly override CanMakeAlliance) and each is small and isolated.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Issue #210 cannot close, and the genuinely risky residue (alliance-formation veto under War of the Ring; CC list visual order; ruler equipment fallback; over-permissive hideout resource award) stays unverified while looking like mere paperwork. Each future session re-derives 'is the migration actually done?' from scratch because the de-facto validation (spider battles, CC work, careers) was never recorded against the checklist.
  - `docs/migration/s6-runtime-punchlist.md:20-51` — Every checkbox in all 7 sections (launch smoke, 6 VerticalBottomToTop sites, ruler-roster fallback, CanMakeAlliance veto, naval, hideout end-state gating, Category C reflection) is still '- [ ]' — none recorded as done despite ~3 weeks of in-game feature work since 2026-05-22 that de-facto exercises items 1 and 7
  - `docs/migration/TRACKING.md:49, 219` — S0 row 'Open GitHub tracking issue — ⏳ Pending' although issue #210 exists (cited in the audit brief as the open migration issue)
  - `docs/migration/TRACKING.md:13, 182` — 'Plan file: C:\Users\mikew\.claude\plans\we-did-this-on-crystalline-piglet.md' and 'S6–S12 — see plan file' point at a machine-local, non-repo path as the authority for the skipped stages
  - `docs/migration/TRACKING.md:214, 217` — Open-issues table still carries the two runtime-risk rows the punch-list owns: CanMakeAlliance may veto alliances despite MaxNumberOfAlliances=int.MaxValue (feature-break risk), and the 6-site VerticalBottomToTop inversion
- **fix sketch**: One deliberate punch-list session: launch + check rgl/diag logs (item 1 likely already evidenced — record it), eyeball the 6 ListPanel sites and ruler equipment, observe alliance formation, clear one hideout. Check boxes with dates, fix what fails, note completion in TRACKING.md (also fix the 'issue pending' row to cite #210 and drop the local plan-file path), then close #210.

### [DEPS-01] Installed engine bumped to v1.4.6 (2026-06-11); repo still targets v1.4.5 everywhere — post-bump checklist not run
- **effort** M · **risk** LOW (Fix is metadata, docs, regenerated artifacts, and read-only audits; the binding gate is already green against 1.4.6, so no code changes are forced unless the scene audit or smoke test surfaces drift.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Every signature-research workflow now mixes engine versions: CLAUDE.md and the agent-operating-manual tell every session the decompile dump AND installed DLLs are both v1.4.5 — the installed side is now 1.4.6, so 'browse dump, verify on installed DLLs' silently compares across versions. The vanilla-data-comparison post-bump checklist (scene-rename/XML-re-schema audits — the documented battle-crash class) has not been run against 1.4.6 vanilla data. SubModule.xml ships wrong dependency metadata. Mitigating: this session's baseline build + 3143 tests (incl. the BindingVerification gate, which resolves against the installed DLLs) are green — so the TAOM patch/GameModel/reflection surface does bind on 1.4.6.
  - `E:\Steam\steamapps\common\Mount & Blade II Bannerlord\bin\Win64_Shipping_Client\Version.xml:2` — <Singleplayer Value="v1.4.6"/> — file mtime 2026-06-11 05:43; Modules/Native/SubModule.xml also declares <Version value="v1.4.6"/>. BANNERLORD_GAME_DIR resolves to this install.
  - `c:\Users\mikew\source\repos\TAOM\Main\_Module\SubModule.xml:17` — <DependedModuleMetadata id="Native" order="LoadBeforeThis" version="v1.4.5.*" /> — wildcard is on the 4th component, so v1.4.6 does not match under BLSE-enforced metadata checks.
  - `C:\Users\mikew\.taom-src:n/a` — Cache contains both v1.4.5/ and v1.4.6/ directories — the auto-detecting taom-src script has already switched to decompiling 1.4.6, while E:\Decompiled_Bannerlord\ remains the 2026-05-22 v1.4.5 dump.
  - `c:\Users\mikew\source\repos\TAOM\docs\migration\dr3-maintenance.md:143-145` — Scenario A ("Bannerlord ships 1.4.6") prescribes the exact procedure: bump SubModule.xml Native constraint + smoke test. git grep/log show zero committed acknowledgment of 1.4.6 anywhere.
- **fix sketch**: Execute dr3-maintenance Scenario A: bump Main/_Module/SubModule.xml Native pin to v1.4.6.*, regenerate E:\Decompiled_Bannerlord\ via tools/decompile_bannerlord.ps1, refresh the API snapshot (recommend /verify-bindings), run the scene-ref audit tools per .claude/rules/vanilla-data-comparison.md, update the v1.4.5 claims in CLAUDE.md + docs/ai-includes/agent-operating-manual.md, then in-game smoke test.

### [DEPS-04] Migration issue #210 remains open with a 7-item human runtime punchlist as the only residue; TRACKING.md is internally stale about it
- **effort** M · **risk** LOW (Mostly human verification at the keyboard; code changes only if a check fails (each pre-scoped in the punchlist with its fix, e.g. swap to VerticalTopToBottom).) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: The 1.3.15→1.4.5 migration cannot be declared closed, and several punchlist items are real latent-defect classes (reversed UI lists at 6 sites, naked faction rulers, alliances never forming, over-permissive resource awards). The v1.4.6 bump (DEPS-01) independently requires a smoke session — one keyboard session can close both. Cited per category scope; #210 itself is already tracked, this finding inventories what closing it actually takes.
  - `c:\Users\mikew\source\repos\TAOM\docs\migration\s6-runtime-punchlist.md:17-51` — The concrete remaining work: launch smoke test (patch application), 6 VerticalBottomToTop prefab sites (possible reversed lists), ruler-equipment fallback (naked rulers), CanMakeAlliance veto check, naval-event crash check, OnHideoutCompleted permissive-gating check, Category C runtime-dynamic reflection (CC flow + CrashReport collectors).
  - `c:\Users\mikew\source\repos\TAOM\docs\migration\TRACKING.md:49` — 'Open GitHub tracking issue | ⏳ Pending' — stale: gh shows #210 '[Migration] Bannerlord 1.3.15 → 1.4.5' OPEN, created/last-updated 2026-05-22, never updated to reference the punchlist created 2026-05-28.
  - `c:\Users\mikew\source\repos\TAOM\docs\migration\s6-runtime-punchlist.md:54-56` — Closing instructions: check all boxes, note completion in TRACKING.md, close the migration tracking issue — and 'this human list only needs re-running after a Bannerlord version bump that the offline gate flags'.
- **fix sketch**: Run the punchlist in one game session (combined with the DEPS-01 1.4.6 smoke test), fix whatever fails, update the #210 issue body with the punchlist link and results, mark TRACKING.md:49 done, close #210.

## DX (10)
_Not audited:_ Considered and dropped: (1) Claude-hooks-don't-fire-for-terminal-commits — explicitly documented as a known fact in .claude/rules/harness-facts.md (standing calibration: decided/acknowledged); its consequence is folded into DX-02 impact instead. (2) FileLogger unbounded log accumulation (Main/Core/Logging/FileLogger.cs:21-24 creates a new timestamped Logs/taom_debug_*.log per session, no rotation/

### [DX-01] Pre-commit build gate never blocks (pipeline-status bug) yet costs a full solution build on every commit
- **effort** S · **risk** LOW (Hook-only change; fail-open semantics preserved. Worst case after the fix is a true-positive block on a genuinely broken build, which is the intended behavior.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: The gate is inverted theater: broken builds CAN be committed (the hook allows regardless of build result), while every commit — even docs-only — pays a full TAOM.sln build (tens of seconds). All cost, no protection, plus a false sense of safety from the CLAUDE.md table. Sibling hook check-moduledata-validation.sh shows the house pattern done right.
  - `c:/Users/mikew/source/repos/TAOM/.claude/hooks/check-build-before-commit.sh:19` — `if ! dotnet build --no-restore 2>&1 | tail -20; then` — the if tests the pipeline's exit status, which is tail's (always 0); no `set -o pipefail` anywhere in the script, so the BUILD FAILED branch (lines 20-22) is unreachable. Verified empirically this session: `bash -c 'if ! false | tail -20; then echo WOULD-BLOCK; else echo WOULD-ALLOW; fi'` prints WOULD-ALLOW.
  - `c:/Users/mikew/source/repos/TAOM/CLAUDE.md:697` — Hooks table documents this hook as 'Blocks `git commit` if build fails' — the documented protection does not exist.
  - `c:/Users/mikew/source/repos/TAOM/.claude/hooks/check-build-before-commit.sh:12` — Matcher `[[ ! "$COMMAND" =~ git\ commit ]]` violates the mandatory two-stage pattern in .claude/rules/harness-facts.md: it false-positives on `git commit-tree`/`commit-graph` (runs a full build for plumbing commands) and misses `git -C <path> commit` / `git -c k=v commit` forms entirely.
  - `c:/Users/mikew/source/repos/TAOM/.claude/hooks/check-build-before-commit.sh:16-19` — Runs `dotnet build` unconditionally for every matched commit — including docs-only, XML-only, and .claude-only commits where no C# changed — and hardcodes the machine path `c:/Users/mikew/source/repos/TAOM` instead of CLAUDE_PROJECT_DIR (line 18).
- **fix sketch**: Capture the build's own exit code (`OUT=$(dotnet build ... 2>&1); RC=$?` then tail OUT for display) or add `set -o pipefail`; adopt the two-stage git-commit matcher from check-moduledata-validation.sh; skip the build when no .cs/.csproj/.props files are staged; use ${CLAUDE_PROJECT_DIR}.

### [DX-02] CI completely dark on the active branch — 503 commits since the last run; Build & Test job is permanently un-runnable
- **effort** S · **risk** LOW (Adding `bannerlord-1.4.5` to the trigger branches and `workflow_dispatch:` to `on:` only re-enables existing jobs; the Build & Test job stays skipped until the variable is set, so no new red builds beyond genuine data failures.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Zero remote safety net for the entire 1.4.5 migration and everything since. Other contributors exist (origin/TheB0yys-Branch, origin/Theboyys-Music-Integration-1.4.5) whose commits never pass through the local Claude hooks, so nothing validates their pushes at all. The dead `workflow_dispatch` arm also means there is no manual escape hatch.
  - `c:/Users/mikew/source/repos/TAOM/.github/workflows/build.yml:4-7` — Triggers are `push: branches: [master]` + `pull_request: branches: [master]` only; `gh repo view` confirms the GitHub default branch is `bannerlord-1.4.5`, where all development happens.
  - `c:/Users/mikew/source/repos/TAOM/.github/workflows/build.yml:81` — Build & Test job gated on `vars.BANNERLORD_GAME_DIR != '' || github.event_name == 'workflow_dispatch'` — `gh api repos/haterade22/TAOM/actions/variables` returns total_count 0, and `workflow_dispatch` is absent from the `on:` block, so both arms are permanently false.
  - `c:/Users/mikew/source/repos/TAOM/.github/workflows/build.yml:1` — `gh run list` shows the last run was 2026-04-16 on master; `git log --oneline --since=2026-04-16 | wc -l` = 503 commits with zero CI — not even the XML/XSLT/JSON well-formedness job has run in ~2 months.
- **fix sketch**: Add `bannerlord-1.4.5` (or `branches: ['**']`) to push/PR triggers and add `workflow_dispatch:` to `on:`. Decide explicitly whether Build & Test needs a self-hosted runner with game DLLs (the job's own comment says so but `runs-on: windows-latest` is GitHub-hosted) or should be removed/documented as local-only.

### [DX-05] build.ps1 rejects process-scope BANNERLORD_GAME_DIR — only the User-registry value counts
- **effort** S · **risk** LOW (Changing to `$env:BANNERLORD_GAME_DIR` with a User-scope fallback is strictly more permissive; existing setup-dev-env.ps1 users are unaffected.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Confusing onboarding failure (script says the var is unset while dotnet build works), and build.ps1 is unusable in any CI or runner that injects env vars normally instead of writing the Windows user registry.
  - `c:/Users/mikew/source/repos/TAOM/build.ps1:11` — `[System.Environment]::GetEnvironmentVariable("BANNERLORD_GAME_DIR", "User")` reads only the per-user registry scope, ignoring a process/session env var.
  - `c:/Users/mikew/source/repos/TAOM/Directory.Build.props:20` — MSBuild reads `$(BANNERLORD_GAME_DIR)` normally (process scope), so plain `dotnet build` succeeds in exactly the shells where build.ps1 reports 'BANNERLORD_GAME_DIR is not set'.
  - `c:/Users/mikew/source/repos/TAOM/README.md:25-37` — Quick Start tells contributors to set the env var (via setup-dev-env.ps1) then run `.\build.ps1` — a `$env:BANNERLORD_GAME_DIR=...` user hits a false failure that contradicts the docs.
- **fix sketch**: Read `$env:BANNERLORD_GAME_DIR` first, fall back to the User-scope registry value; keep the setup-dev-env.ps1 hint in the error message.

### [DX-06] README 'By the numbers' has drifted across the board
- **effort** S · **risk** LOW (Docs-only change; no behavior affected.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Every exact count in the public-facing README is stale and rots monotonically; minor individually, but it undermines the 'README vs reality' trust for onboarding and showcases the project as less than it is (3,143 tests > 2,200).
  - `c:/Users/mikew/source/repos/TAOM/README.md:13-15` — '2,200+ unit tests' (session baseline: 3,143 passing), '50 feature modules' (55 dirs under Main/Features/), '74 feature/architecture docs' (85 files in docs/features/).
  - `c:/Users/mikew/source/repos/TAOM/README.md:125-126` — '33 custom slash-command skills, 5 specialized agents, 18 automated hooks, 15 path-scoped rule files' — on disk: 37 skill dirs, 22 hook scripts, 18 rule files.
- **fix sketch**: Either refresh the numbers once and round them ('3,100+ tests', '~55 feature modules'), or drop exact counts in favor of qualitative phrasing so the section stops needing maintenance.

### [DX-07] Seven hooks hardcode the absolute repo path; the portable CLAUDE_PROJECT_DIR pattern already exists in-repo
- **effort** S · **risk** LOW (Mechanical substitution to the pattern already proven in check-moduledata-validation.sh; fail-open semantics preserved by the `|| exit 0` guards already present.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Hooks silently no-op (fail-open) or operate on the wrong tree for any other contributor or any clone at a different path — and contributors exist (origin/TheB0yys-Branch). Logging hooks would write nothing; the build gate would build the wrong directory.
  - `c:/Users/mikew/source/repos/TAOM/.claude/hooks/check-build-before-commit.sh:18` — `cd "c:/Users/mikew/source/repos/TAOM"` — same literal path also at session-start.sh:19, session-stop.sh:5, pre-compact.sh:5, post-compact.sh:6, detect-docs-gaps.sh:16, log-agent.sh:18.
  - `c:/Users/mikew/source/repos/TAOM/.claude/hooks/check-moduledata-validation.sh:49` — The newest hook uses `cd "${CLAUDE_PROJECT_DIR:-$(pwd)}"` — the portable convention exists but was never back-ported to the older hooks.
- **fix sketch**: Replace the literal path with `${CLAUDE_PROJECT_DIR:-$(pwd)}` in the seven older hooks, matching check-moduledata-validation.sh:49. Bundle with the DX-01 fix since check-build-before-commit.sh is touched by both.

### [DX-03] Commit-before-review failure mode (recorded twice in RCAs) has no functioning guard: the Stop reminder's mute is structurally dead and it fires post-commit anyway
- **effort** S · **risk** MED (The reminder fix (marker file + message text) is inert. The stronger option — a PreToolUse(git commit) gate requiring a fresh deep-review marker when ≥2 .cs files are staged — risks over-blocking legitimate small fixes; it must mirror check-moduledata-validation.sh's fail-open structure and CLAUDE.md's own ≥2-file C# trigger to stay calibrated.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: The most-relitigated process failure in the repo's RCA corpus (2026-05-25, recurrence 2026-06-07 despite the hook existing) is guarded only by an always-on nag that (a) cannot be silenced by actually doing the review, and (b) arrives too late to prevent the commit. Constant false-positive reminders train the operator to ignore them — the exact alarm-fatigue path that let the failure recur.
  - `c:/Users/mikew/source/repos/TAOM/.claude/hooks/check-deep-review.sh:6-9` — Mute condition greps `.claude/logs/agent-audit.log` for 'deep-review|Standards Compliance|Bannerlord.*Compat|Efficiency.*Performance|Completeness Check' — but log-agent.sh:24 writes only `agent_type=` and `agent_id=` fields. Verified this session: 0 matches for the full pattern across the entire append-only log (2,293 entries since 2026-03-26; agent_type values are Explore/workflow-subagent/general-purpose/taleworlds-researcher/etc., none containing the mute strings). The mute can never fire, so the reminder nags on every Stop with any dirty .cs/.xml/.json file — i.e., near-every session — even immediately after a deep-review completed.
  - `c:/Users/mikew/source/repos/TAOM/.claude/hooks/check-deep-review.sh:17` — Reminder text says 'Bannerlord 1.3 compatibility' — stale (target is 1.4.5 per CLAUDE.md), eroding the reminder's credibility further.
  - `c:/Users/mikew/source/repos/TAOM/docs/reviews/rca-cultural-feats-wave1-2026-06-07.md:18, 60, 70` — RCA records the headline finding: 'the reviews were run AFTER commit + push, not before' and calls it 'the recurring failure the rca-crash-report-2026-05-25.md meta-finding warns about'. Line 61 explicitly notes 'The pre-commit hook enforces CHANGELOG, not issue creation; discipline is on the author' — the hook-coverage gap is acknowledged in the RCA but no gate was added. A Stop-time reminder is structurally incapable of preventing this failure mode because it fires after the commit has already happened.
- **fix sketch**: Have /deep-review write a timestamped marker (e.g. .claude/logs/.deep-review-ran, same pattern as mark-verification-run.sh writes .verification-ran); mute the Stop reminder on a fresh marker and fix the 1.3 text. Optionally add the PreToolUse commit gate keyed on staged-.cs-count ≥2, deny-with-message pointing at /deep-review.

### [DX-03] CI gates are a strict subset of local gates — no moduledata validation, doc lint, or tests run remotely
- **effort** M · **risk** LOW (Adding a python step running validate_moduledata.py in reduced mode is additive; the main risk is flagging pre-existing WARNINGs as red — scope the CI step to the same ERROR codes the pre-commit hook uses (check-moduledata-validation.sh:76-79).) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Every validation layer is local, fail-open, and Claude-session-scoped. Data regressions (duplicate ids, missing civilian types, malformed configs) from human commits or hook-bypassed sessions only surface when someone later runs the tool manually — or in-game.
  - `c:/Users/mikew/source/repos/TAOM/.github/workflows/build.yml:26-75` — The validate-xml job checks only XML/XSLT/JSON well-formedness via PowerShell parse loops; it never invokes tools/validate_moduledata.py, validate_all_troop_refs.py, or lint_docs.py.
  - `c:/Users/mikew/source/repos/TAOM/tools/validate_moduledata.py:72-86` — The validator degrades gracefully without a game install — prints a WARNING and still runs civilian-type, enum, and duplicate-id checks (`build_registries(moduledata, None)`), so a reduced CI mode is feasible on GitHub-hosted runners today.
  - `c:/Users/mikew/source/repos/TAOM/.claude/hooks/check-moduledata-validation.sh:18-21` — The local equivalent is deliberately fail-open (no python / validator crash / missing game install all ALLOW) and, per harness-facts.md, PreToolUse hooks fire only for Claude-driven commits — user-typed `git commit` in a terminal bypasses it entirely.
- **fix sketch**: Add a CI step: `python tools/validate_moduledata.py --code BROKEN_TROOP_REF --code UNKNOWN_CULTURE --code DUPLICATE_NPC_ID ...` (mirroring the pre-commit hook's ERROR set; item-ref checks need the Armory so stay local), plus optionally `python tools/lint_docs.py --summary` as a non-blocking report. Depends on DX-02 landing first so the workflow actually runs.

### [DX-04] tools/README.md indexes ~33 of 113 Python scripts while being cited as the 'full tool list'
- **effort** M · **risk** LOW (Pure documentation; worst case is a stale entry, which is the current state anyway. Marking one-shot scripts as archived/one-shot avoids misleading entries.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Discoverability tax with a known failure mode: agents and future sessions that can't find an existing tool re-derive or re-write it (the tools/ dir already shows near-duplicate generator/apply scripts; playbook category 5 names this sprawl). The scene-audit and skeleton tools that prevent crash classes are among the undocumented ones — CLAUDE.md references them, tools/README.md doesn't.
  - `c:/Users/mikew/source/repos/TAOM/tools/README.md:29-104` — Tables document ~33 Python scripts + 5 of 14 PowerShell scripts; `ls tools/*.py` returns 113 files. Absent: lint_docs.py, graph_query.py, build_backlinks.py, audit_scene_names.py / audit_battle_scenes.py / remap_stale_scene_names.py (the post-version-bump crash-prevention battery), taom-src.ps1, snapshot_api_surface.ps1, decompile_bannerlord.ps1, pe_inspect.py, audit_claude_config.py, tpac_skeleton_*.py, build_weapon_xml.py, and ~60 more.
  - `c:/Users/mikew/source/repos/TAOM/docs/ai-includes/agent-operating-manual.md:39` — The subagent briefing doc says 'Full tool list | see tools/README.md' — every spawned agent is pointed at an index missing ~70% of the tooling.
- **fix sketch**: One indexing pass: add sections for doc tooling, scene/tpac audit, decompile/snapshot, and one-shot migration scripts (mark the dead ones as one-shot/archive candidates). Optionally add a lint check (lint_docs.py already exists) asserting every tools/*.py appears in tools/README.md or an explicit exclusion list.

### [DX-02] CI is entirely dormant on the active branch and the Build & Test job is unrunnable by construction; game-independent gates (pytest, lint_docs, validator) never wired in
- **effort** M · **risk** LOW (Workflow YAML only; no runtime code changes. Initial risk is red-CI noise on the active branch, but the verification baseline (build green, 3143 tests pass, validator PASS) says current state would pass the game-independent jobs.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Zero CI signal for 552 commits / ~2 months. Every gate in the repo is Claude-dispatch-only (.git/hooks contains only samples — verified; harness-facts.md documents that Claude hooks don't fire for terminal commits), so a direct terminal commit or an external contributor PR currently passes through with NO automated check anywhere — not even XML well-formedness, since that job's trigger also excludes the active branch.
  - `c:/Users/mikew/source/repos/TAOM/.github/workflows/build.yml:3-7` — Only workflow in .github/workflows/; triggers are `push: branches: [master]` + `pull_request: branches: [master]`. README.md:17 states the active development branch AND GitHub default is `bannerlord-1.4.5`. Verified: 552 commits on bannerlord-1.4.5 not on master; master's last commit is 77aaff8 dated 2026-04-16 — no CI event in ~2 months.
  - `c:/Users/mikew/source/repos/TAOM/.github/workflows/build.yml:81` — Build job gate `if: ${{ vars.BANNERLORD_GAME_DIR != '' || github.event_name == 'workflow_dispatch' }}` — `workflow_dispatch` is not in the `on:` block (lines 3-7), so that half of the condition is dead code; the job can never be manually dispatched.
  - `c:/Users/mikew/source/repos/TAOM/.github/workflows/build.yml:79` — `runs-on: windows-latest` — a GitHub-hosted runner has no Bannerlord install, so even with the BANNERLORD_GAME_DIR repo variable set, Directory.Build.props' game-DLL references (line 20: GameFolder falls through to $(BANNERLORD_GAME_DIR)) cannot resolve. The comment on line 80 says 'self-hosted runner' but the YAML pins a hosted one.
  - `c:/Users/mikew/source/repos/TAOM/.github/workflows/build.yml:26-75` — The only other job is XML/XSLT/JSON well-formedness. CI never runs: the 3,143-test suite, `tools/validate_moduledata.py` (which supports a degraded no-game mode — validate_moduledata.py:72-86 warns and builds registries with None), `python tools/lint_docs.py`, or the game-independent Python test suite at tools/tests/ (6 test modules + fixtures, no game-path references — verified by grep this session).
- **fix sketch**: Re-point triggers to `[master, bannerlord-1.4.5]` and add `workflow_dispatch` to `on:` (or delete the dead clause). Add game-independent jobs: `pytest tools/tests`, `python tools/lint_docs.py --summary`, and validate_moduledata.py in its degraded mode (or against a committed registry snapshot). Either label the build job self-hosted-only or document it as intentionally manual; true CI build+test needs a ref-assembly strategy (separate L-effort decision).

### [DX-04] tools/README.md documents 39 of 130 scripts while being billed as the 'full tool list' — the durable audit suite is invisible at its documented discovery point
- **effort** M · **risk** LOW (Docs-only change. Main risk is wasted effort documenting spent one-shots instead of classifying them as archive — the fix should separate durable vs one-shot first.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Agents and human contributors sent to the documented index can't find the durable audit/infra tools; the realistic failure is re-deriving or re-writing an existing tool (the repo's own history shows one-off validators being superseded for exactly this reason — README lines 35-36 mark two as 'superseded'). The ~50 spent one-shot apply_*/insert_*/fix_* scripts also sit undifferentiated next to durable tools, making the directory read as noise.
  - `c:/Users/mikew/source/repos/TAOM/tools/README.md:1-190` — Counted this session: 130 scripts (*.py/*.ps1/*.sh) in tools/; 91 have no mention anywhere in the 190-line README. Undocumented durable tools include the post-version-bump scene-ref audit suite (audit_scene_names.py, audit_battle_scenes.py, remap_stale_scene_names.py — the suite CLAUDE.md's Doc Lookup table routes to for battle-crash triage), lint_docs.py, graph_query.py, build_backlinks.py, taom-src.ps1, snapshot_api_surface.ps1, audit_claude_config.py, pe_inspect.py, the tpac_skeleton_* spider pipeline, migrate_hideouts_to_lotr.py, and audit_cc_bonuses-adjacent appliers.
  - `c:/Users/mikew/source/repos/TAOM/docs/ai-includes/agent-operating-manual.md:39` — The subagent operating manual's tool catalog row says 'Full tool list | see tools/README.md' — directing every spawned agent to an index missing ~70% of the toolbox.
- **fix sketch**: Add a 'Durable pipeline tools' section covering the ~25-30 missing long-lived scripts (scene audits, doc tooling, decompile/snapshot, tpac suite, audit_claude_config) and a one-line 'One-shot/archive' table for spent migration scripts (or move them to tools/archive/). Reconcile the agent-operating-manual row with reality.

## DOCS (13)
_Not audited:_ Considered and dropped: ADR-006 numbering gap (documented 'Revoked — never filed' in docs/adrs/README.md — by design); stale 1.3.15 refs in docs/reviews/REVIEW-LOG.md, rca-*/codex-*/issue-body-* files (historical records per scope); native-skin-fixes.md 1.3.15 mentions (explicitly labeled historical RVA context, informational only); bannerlord-together-compat.md:11 (accurately states 1.4.5 co-op i

### [DOCS-01] CLAUDE.md Harmony Patch Categories table is missing 11 shipped patch categories
- **effort** S · **risk** LOW (Doc-only edit; note config-protection.sh blocks CLAUDE.md edits unless explicitly requested, so the fix must be an explicit-request edit.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: The table reads as the exhaustive patch registry; every future session checking 'is method X already patched / which category owns this' gets a false negative for ~25% of shipped categories. The Patch_MissionTime row even references Patch35_CompanionTactics' postfix while no Patch35 row exists. This is exactly the high-leverage CLAUDE.md drift the playbook calls out — it misleads every session.
  - `C:/Users/mikew/source/repos/TAOM/CLAUDE.md:Harmony Patch Categories table` — Table has 36 rows; grep of HarmonyPatchCategory("...") across Main/ yields 46 distinct categories in code (Patch47 excluded as in-flight Spider work).
  - `Main/Features/LocalizationOverride/Hooks/MBTextManager_GetLocalizedText_Patch.cs:20` — Patch25_LocalizationOverride exists in tracked code but has no table row.
  - `Main/Features/CrashReport/Hooks/Patch37_CrashReport.cs:31` — Patch37_CrashReport — whole feature's patch category absent from the table; same for Patch30_MixedFormations, Patch33_EquipPresets, Patch35_CompanionTactics (9 patch classes), Patch36_FiefManagement, Patch39_BanditPartySize, Patch41_McmLayoutFix, Patch43_BattleLoadDiagnostics.
  - `Main/Features/HeroRace/Hooks/ActionSetCode_GenerateActionSetNameWithSuffix_Patch.cs:1` — Late_ActionSetOverride / Late_Transpiler categories (HeroRace + CharacterSelection, tracked) also undocumented.
- **fix sketch**: Add the 11 missing rows (category, feature, target) to the Harmony Patch Categories table, sourcing targets from each patch class's [HarmonyPatch] attributes. Optionally add a one-line note that the table is regenerable by grepping HarmonyPatchCategory strings.

### [DOCS-02] CLAUDE.md GameModel Overrides table is missing 7 shipped Taom*Model classes
- **effort** S · **risk** LOW (Doc-only; same config-protection.sh explicit-request caveat as DOCS-01.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: The table is the canonical 'which Default*Model is already overridden' reference. A session adding e.g. an inventory-capacity or clan-tier override would not see the existing one and could register a duplicate model or re-plan done work. (Verified: class existence + git-tracked status; not AddModel registration — SubModule.cs has in-flight edits and was not used as evidence.)
  - `C:/Users/mikew/source/repos/TAOM/CLAUDE.md:GameModel Overrides table` — Table lists 33 models; grep 'class Taom\w+Model' across Main/ finds 40 tracked classes.
  - `Main/Features/CareerSystem/Models/TaomAgentStatCalculateModel.cs:1` — TaomAgentStatCalculateModel — referenced repeatedly in CLAUDE.md prose (spider/elephant mount-lock) yet absent from the table; same for TaomAgentApplyDamageModel, TaomClanTierModel, TaomInventoryCapacityModel, TaomMapVisibilityModel (all CareerSystem/Models/).
  - `Main/Features/BanditManagement/Models/TaomBanditDensityModel.cs:1` — TaomBanditDensityModel documented only in Key Paths prose, not in the GameModel table.
  - `Main/Features/CulturalFeats/Models/TaomNotableSpawnModel.cs:1` — TaomNotableSpawnModel — no table row, no prose mention found.
- **fix sketch**: Add 7 rows (model, base Default model, purpose) to the GameModel table. The committed API snapshot (docs/reference/taleworlds-api-snapshot/gamemodel-bases.md) already auto-generates the model→base mapping — crib from it.
- **verifier evidence note**: Class declarations are at Main/Features/CareerSystem/Models/TaomAgentStatCalculateModel.cs:19, TaomAgentApplyDamageModel.cs:13, Main/Features/BanditManagement/Models/TaomBanditDensityModel.cs:16, Main/Features/CulturalFeats/Models/TaomNotableSpawnModel.cs:21 (not line 1). Prose-mention claim correct

### [DOCS-03] ADR-001 (XML-only config) is contradicted by 49 shipped JSON configs — needs amendment or superseding ADR
- **effort** S · **risk** LOW (Doc-only; writing the ADR wrong could codify the wrong rule, but the existing practice is clear from 49 examples plus CLAUDE.md prose endorsing JSON configs per feature.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: This is the 'missing ADR for a repeatedly-relitigated decision' case: the codebase converged on a de facto split (XML for engine-parsed game data, JSON for TAOM-internal feature config) that no decision record captures. Any agent or reviewer that checks ADR-001 — as instructed — gets guidance the repo abandoned years of features ago, and could wrongly flag or 'fix' JSON configs.
  - `docs/adrs/001-xml-config.md:9-11 and Rules section` — States 'All configuration files for the mod must use the XML format' and 'Required: All game configuration in /ModuleData/ uses XML'; status Accepted.
  - `Main/_Module/ModuleData:(find *.json)` — 49 JSON config files ship in ModuleData: clan_heraldry/*.json (20+), charactercreation/*.json (7), recruitment_pools/gondor.json, configs/revolt_tuning_config.json, messengers/messenger_config.json, bandit_management/bandit_scaling_config.json, castle_recruitment/castle_recruitment_config.json, named_companions/named_companion_config.json, etc.
  - `docs/adrs/README.md:9` — ADR index still presents 001 as the live standard; agent-operating-manual and CLAUDE.md both direct agents to docs/adrs/ as the rules to check before flagging designs.
- **fix sketch**: Recommend /new-adr to write a superseding ADR (e.g. 011: 'XML for engine-parsed ModuleData, JSON for TAOM-internal config') and mark ADR-001 Superseded in both the file and docs/adrs/README.md.

### [DOCS-04] CLAUDE.md career starter-equipment claims 'Gondor only' — 13 cultures are now authored
- **effort** S · **risk** LOW (Doc-only; same config-protection.sh explicit-request caveat.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Future career-system sessions either re-plan authoring work already done or assume fall-through behavior for cultures that actually have rosters — wrong in both directions, on a churn-hotspot feature (career_system XML is in the last-150-commits hotspot list).
  - `Main/_Module/ModuleData/equipmentsets/taom_career_starting_equipment.xml:(grep player_career_* ids)` — Rosters exist for 13 cultures: aserai, dolguldur, empire, erebor, gondor, gundabad, isengard, khuzait, mirkwood, mordor, rivendell, sturgia, vlandia. Last touched 2026-05-29 (d7b3cbc).
  - `C:/Users/mikew/source/repos/TAOM/CLAUDE.md:Key Paths row 'CareerSystem starter equipment'` — Says 'Gondor only as of 2026-05-19' (line introduced 2026-05-19 per git log -S).
  - `C:/Users/mikew/source/repos/TAOM/CLAUDE.md:Key Paths row 'CareerSystem'` — Says 'Gondor authored end-to-end as proof of life, other 15 cultures fall through gracefully to culture default until authored'.
- **fix sketch**: Update both CLAUDE.md rows to list the 13 authored cultures (or just say '13 of 16 authored; X/Y/Z fall through') and drop the 2026-05-19 date.

### [DOCS-05] Harness skill docs pin stale engine versions: taom-src says v1.3.15, migration-status says v1.2→v1.3
- **effort** S · **risk** LOW (Text-only skill edits; CHANGELOG must be updated in the same commit (check-changelog-changed.sh hard-blocks otherwise).) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Skill descriptions load eagerly into every session and Task spawn — the agent-operating-manual warns 'anything that still says v1.3.15 is stale,' but the eager-loaded description is itself the thing asserting v1.3.15. Actively-wrong how-to for the project's primary signature-verification tool. lint_docs.py does not scan .claude/, so only a manual audit catches this.
  - `.claude/skills/taom-src/SKILL.md:3` — Eagerly-loaded description: 'Decompile and cache TaleWorlds v1.3.15 types' — engine is v1.4.5.
  - `.claude/skills/taom-src/SKILL.md:9` — Body says 'installed v1.3.15 DLLs and caches at ~/.taom-src/v1.3.15/' and 'never guess signatures from the v1.4 dump' — CLAUDE.md says the script auto-detects v1.4.5, caches under ~/.taom-src/v1.4.5/, and the v1.4.5 dump is fine for browsing. The skill's guidance is inverted against the current setup.
  - `.claude/skills/migration-status/SKILL.md:3` — Description: 'Check and summarize the v1.2 to v1.3 Bannerlord migration progress' — two migrations stale; the CLAUDE.md Skills-table row mirrors the same text.
- **fix sketch**: Reword taom-src SKILL.md to version-neutral ('installed engine DLLs; cache keyed by detected version') and fix migration-status + the matching CLAUDE.md Skills-table row to reference the current 1.4.5 tracking doc.

### [DOCS-01] CLAUDE.md registry tables drift: 7 registered GameModels and 11 live Harmony patch categories have no row
- **effort** S · **risk** LOW (Doc-only edit to CLAUDE.md; the only risk is transcribing a wrong summary into a new row.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: These two tables are the per-session registry: sessions pick the next patch number and check 'does an override for model X already exist' from them. A session adding an agent-stat or notable-spawn override would not see TaomAgentStatCalculateModel/TaomNotableSpawnModel exist and could double-register (last AddModel silently wins); a new feature picking 'next free' patch number from the table would collide with 30/33/35/36/37/39/41/43. Misleads every future session — the highest-leverage doc drift per the playbook.
  - `c:/Users/mikew/source/repos/TAOM/Main/SubModule.cs:388,393,443,444,447,448,449` — Committed HEAD registers TaomBanditDensityModel, TaomNotableSpawnModel, TaomMapVisibilityModel, TaomInventoryCapacityModel, TaomAgentStatCalculateModel, TaomAgentApplyDamageModel, TaomClanTierModel via campaignStarter.AddModel — none appear in CLAUDE.md's GameModel Overrides table (CLAUDE.md:391-427 lists 33 of 40 registered models).
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/Mcm/Hooks/Patch41_McmLayoutFix.cs:28` — HarmonyPatchCategory("Patch41_McmLayoutFix") — no row in CLAUDE.md's Harmony Patch Categories table (CLAUDE.md:429-467).
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/BattleLoadDiagnostics/Hooks/Mission_Initialize_BattleLoad_Patch.cs:10` — Patch43_BattleLoadDiagnostics — also absent from the table; full deduped code inventory shows 11 categories with no row: Patch25_LocalizationOverride, Patch30_MixedFormations, Patch33_EquipPresets, Patch35_CompanionTactics, Patch36_FiefManagement, Patch37_CrashReport, Patch39_BanditPartySize, Patch41, Patch43, Late_ActionSetOverride, Late_Transpiler (Patch47 excluded as in-flight).
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/CrashReport/Hooks/Patch37_CrashReport.cs:30` — Patch37_CrashReport category (10 patch sites in this file alone) — invisible to any session consulting the table.
- **fix sketch**: Regenerate both tables from the code inventories (rg HarmonyPatchCategory + rg 'class Taom.*Model' / git show HEAD:Main/SubModule.cs AddModel lines); add the 7 model rows and 11 category rows with one-line summaries pulled from each feature's docs/features/*.md. Also fix the stale 'all 70 feature docs' count at CLAUDE.md:259 (actual: 86) while in the file.

### [DOCS-02] taom-src SKILL.md pins the primary research skill to v1.3.15 and inverts current decompile guidance
- **effort** S · **risk** LOW (Two-line frontmatter + one-paragraph body edit; keep description ≤30 words per external-skill-ports.md.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: The skill that owns signature verification — the project's most safety-critical research step — tells every session it targets a superseded engine version and warns against the dump that is now the same version as installed. An agent reconciling the contradiction wastes a research cycle or, worse, trusts the SKILL.md over CLAUDE.md.
  - `c:/Users/mikew/source/repos/TAOM/.claude/skills/taom-src/SKILL.md:3` — description: 'Decompile and cache TaleWorlds v1.3.15 types' — skill descriptions load eagerly into every conversation and Task spawn (harness-facts.md), so this stale version claim ships to every session (visible verbatim in this session's available-skills list).
  - `c:/Users/mikew/source/repos/TAOM/.claude/skills/taom-src/SKILL.md:9` — Body: 'Wraps ilspycmd against the **installed v1.3.15 DLLs** and caches at ~/.taom-src/v1.3.15/ ... never guess signatures from the v1.4 dump at E:\Decompiled_Bannerlord' — pre-migration framing that now actively contradicts CLAUDE.md ('the script auto-detects the version from Version.xml'; the v1.4.5 dump is fine for browsing).
  - `c:/Users/mikew/source/repos/TAOM/tools/taom-src.ps1:8-9,72-79` — The script itself is version-agnostic: auto-detects from Version.xml in the resolved bin dir and throws if unparseable — the SKILL.md, not the tool, is the stale side.
- **fix sketch**: Rewrite description version-neutrally ('installed-version TaleWorlds types — auto-detected, currently v1.4.5') and update the body paragraph + cache-path example; reword the 'never guess from the v1.4 dump' sentence to the current rule (dump for browsing, taom-src for signatures).

### [DOCS-03] Doc-linter backlog at 56 findings; top cluster is 5 dead links to a doc that exists (relative-path depth bug)
- **effort** S · **risk** LOW (Link fixes + INDEX rows + one new feature doc from TEMPLATE; no code touched.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Engine-reference readers chasing the howdah crew mechanism (the deferred-disabled elephant subsystem, deliberately documented for re-enable) hit a 404 from three of the most-load-bearing engine docs; two live features (one save-impacting) are invisible to the INDEX-first doc lookup; the Mcm feature (Patch41) has zero documentation anywhere — no doc, no CLAUDE.md row (overlaps DOCS-01).
  - `c:/Users/mikew/source/repos/TAOM/docs/reference/engine/usable-machines.md:5,67,88` — Links '../features/elephant/howdah-crew-mechanism.md' — from docs/reference/engine/ that resolves to docs/reference/features/ (one '../' short). Same bug at mount-and-rider-runtime.md:86 and scene-gameentity-scriptcomponent.md:86. The target EXISTS at docs/features/elephant/howdah-crew-mechanism.md (committed b8251f7; INDEX.md:41 links it correctly).
  - `c:/Users/mikew/source/repos/TAOM/tools/lint_docs.py:summary output` — Run this session: 14 dead links, 37 stale version refs (most historical in reviews/issue-bodies — exempt per scope), 4 orphan feature docs (battle-load-diagnostics, career-quest-system, clan-heraldry, new-factions-misty-mountains-lindon), 1 missing feature doc (Mcm → docs/features/mcm.md). Total 56.
  - `c:/Users/mikew/source/repos/TAOM/docs/features/battle-load-diagnostics.md:1` — Orphan doc for a LIVE committed feature (Patch43_BattleLoadDiagnostics in code; issue #262 tracked) — no inbound link from INDEX.md or anywhere, so doc-lookup cannot find it. career-quest-system.md is the same shape (live save-impacting CareerSystem/Quests feature).
- **fix sketch**: Fix the 5 howdah links to '../../features/...', link the 4 orphans from INDEX.md (or delete clan-heraldry/new-factions if superseded — check first), author docs/features/mcm.md. The /lint-cleanup-loop skill exists for exactly this backlog — recommend the orchestrator run it.

### [DOCS-04] migration-status skill and its CLAUDE.md rows still describe the completed v1.2→v1.3 migration
- **effort** S · **risk** LOW (Either deletion (one skill file + 3 CLAUDE.md rows + CHANGELOG) or a small retarget; nothing depends on it programmatically.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Invoking the skill today funnels the v1.4.5 TRACKING.md through a v1.2→v1.3 report template with a checklist of items that finished a year ago — it produces a confidently wrong status report. Eagerly-loaded stale description taxes every session for a skill whose purpose has expired.
  - `c:/Users/mikew/source/repos/TAOM/.claude/skills/migration-status/SKILL.md:3` — description: 'Check and summarize the v1.2 to v1.3 Bannerlord migration progress' — two migrations out of date (current target 1.4.5, migration landed 2026-05-22 per CLAUDE.md header).
  - `c:/Users/mikew/source/repos/TAOM/.claude/skills/migration-status/SKILL.md:14-25` — Report format hardcodes the OLD migration's remaining-items checklist (Settlement XML / Troop XML (2 files) / Item XML / Equipment XML / 'Code Changes (TBD scope)') and points at v1.3-api-changes.md — but TRACKING.md, which it summarizes, is now the v1.4.5 audit trail.
  - `c:/Users/mikew/source/repos/TAOM/CLAUDE.md:102,181` — Skills table row and Soft-suggest routing row both repeat 'v1.2 -> v1.3' framing.
- **fix sketch**: Per simplicity-criterion, prefer retiring the skill (both migrations complete; TRACKING.md is readable directly) and removing its CLAUDE.md rows; alternatively retarget description/body/checklist at the v1.4.5 TRACKING.md structure.

### [DOCS-07] Scene-script spec asserts v1.3.15 engine behavior from never-refreshed signature snapshots
- **effort** S · **risk** LOW (Refresh-and-compare task; if signatures did drift, the doc fix may reveal a real code issue (which would be a separate, larger task).) · **confidence** MED · ✓ verifier:REAL
- **impact**: CS_Road is shipped code (Main/SceneScripts/) and this spec is its authoring how-to; anyone extending scene scripts verifies against a superseded engine surface. The claims may still hold on 1.4.5 — but nobody has checked, and the doc presents them as current.
  - `docs/scene-scripts/specs/cs-road.md:7, 9, 103` — Asserts 'Bannerlord v1.3.15 scans fields, not properties' and documents the 'Bannerlord v1.3.15 Mesh API', citing docs/scene-scripts/sigs/*-v1.3.15.txt (flagged by lint_docs.py this session).
  - `docs/scene-scripts/sigs/:(ls)` — All 8 signature snapshots are v1.3.15-named (scriptcomponentbehavior, mesh, metamesh, gameentity, scene, path, script-component-params, editable-script-component-variable); none refreshed since the 2026-05-22 v1.4.5 migration.
- **fix sketch**: Investigate-first: re-run taom-src for the 8 types against the installed v1.4.5 DLLs, diff vs the v1.3.15 snapshots, rename/refresh the sigs files, and update cs-road.md's version-pinned claims (or annotate 'verified unchanged on 1.4.5').

### [DOCS-05] Scene-script specs direct verification at v1.3.15 signature dumps — the only sigs that exist are one engine version behind target
- **effort** S · **risk** LOW (Re-dump via taom-src (auto-targets installed 1.4.5), commit alongside the old files or replace, update the two docs' references; code untouched.) · **confidence** MED · · unmatched
- **impact**: Anyone authoring or auditing a scene script (CS_Road is shipped code in Main/SceneScripts/) verifies field/API assumptions against the superseded engine; if 1.4.5 changed the editor's reflection-discovered field surface or Mesh API, the documented verification silently no longer holds.
  - `c:/Users/mikew/source/repos/TAOM/docs/scene-scripts/sigs:directory listing` — All 9 pinned ilspycmd outputs are *-v1.3.15.txt (scriptcomponentbehavior, mesh, metamesh, gameentity, scene, path, etc.); no v1.4.5 dumps exist.
  - `c:/Users/mikew/source/repos/TAOM/docs/features/scene-scripts.md:44,97` — States 'Field reflection — verified against docs/scene-scripts/sigs/scriptcomponentbehavior-v1.3.15.txt lines 339-398' and tables the sigs as the pinned verification artifacts.
  - `c:/Users/mikew/source/repos/TAOM/docs/scene-scripts/specs/cs-road.md:7,103` — Spec instructs readers that ScriptComponentBehavior/Mesh API conformance is per the v1.3.15 sig files — these are clean-room porting instructions, so the stale anchor is actively-wrong how-to, not historical prose (linter flags the strings; the instructional framing is the cost).
- **fix sketch**: Regenerate the 9 sig dumps against installed v1.4.5 (pwsh tools/taom-src.ps1 path <Type> per type), diff against the v1.3.15 versions, update scene-scripts.md:44/97 + cs-road.md:7/103 to point at the new files; note any actual API drift found.

### [DOCS-06] No ADR for the creature-as-mount architecture despite a costly in-repo reversal
- **effort** S · **risk** LOW (One new ADR document; no code changes. Mild risk of duplicating feature-doc content — keep it to decision + constraints + pointers.) · **confidence** MED · ✓ verifier:REAL
- **impact**: The recipe now governs three creatures (warg/elephant/spider) and is the template for any future creature, but the decision + its non-obvious constraints live scattered across CHANGELOG entries, docs/features/elephant.md, engine reference docs, and session memory. The next creature port (or a session that only finds the older 'troop not mount' artifacts in git history) risks re-deriving — or re-reversing — a decision that already cost a deleted architecture.
  - `c:/Users/mikew/source/repos/TAOM/CHANGELOG.md:52-55` — 'feat(spider): giant spider rideable mount WORKING in battle... detached-combatant architecture was deleted 2026-06-10 in favor of the warg/elephant Mountable recipe' — the troop-vs-mount question was answered both ways within days, with a whole architecture (Patch45 spawn-swap + wield guards) built and then deleted.
  - `c:/Users/mikew/source/repos/TAOM/CHANGELOG.md:555-559` — Earlier entry 'redesign taom_spider_creature as goblin Cavalry Soldier with horse-slot mount' — the relitigation trail is in CHANGELOG, exactly the playbook's missing-ADR signal.
  - `c:/Users/mikew/source/repos/TAOM/docs/adrs/README.md:ADR index table` — ADRs 001-010 cover code style, config format, adapters, testability, knowledge-base — no ADR records the creature-integration decision (mount via Horse-slot item + Mountable Monster, never detached combatants) or its constraints (quad_movement clip tags, rider_sit_bone, per-mesh bone palette).
- **fix sketch**: Recommend /new-adr 'creature-integration-as-ridden-mount': decision (Horse-slot item + Mountable Monster + per-agent BT, never detached spawn-swap), the reversal history as context, and the hard constraints (quad_movement tags, mount-lock in TaomAgentStatCalculateModel, bone-palette split), pointing at elephant.md/spider.md for detail. Wait until the in-flight spider.md rewrite lands so the ADR links the final doc.

### [DOCS-06] Mcm feature has no feature doc; 4 feature docs are orphans unreachable from docs/INDEX.md (linter-confirmed)
- **effort** M · **risk** LOW (Doc-only: write mcm.md, add INDEX entries, regenerate backlinks. No code touched.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: ADR-010's INDEX-first lookup fails for these systems: clan-heraldry alone backs 20+ JSON configs in ModuleData with no discoverable doc path, and the Mcm layout-fix rationale (why TAOM patches MCM's UI at all) is unreconstructable without reading the patch. Orphan docs were written and then silently dropped from the knowledge graph.
  - `Main/Features/Mcm/Hooks/Patch41_McmLayoutFix.cs:29` — Shipped, tracked patch class with no docs/features/mcm.md (lint_docs.py this session flagged the missing doc; CLAUDE.md mandates one doc per feature).
  - `docs/INDEX.md:(grep)` — Zero mentions of battle-load-diagnostics, career-quest-system, clan-heraldry, new-factions-misty-mountains-lindon — the 4 orphan docs lint_docs.py flagged this session; all 4 exist in docs/features/.
  - `C:/Users/mikew/source/repos/TAOM/CLAUDE.md:259` — Claims INDEX.md is a 'curated topical map across all 70 feature docs' — docs/features/ actually holds 84 docs (85 minus TEMPLATE.md), so the curated map has drifted ~14 docs behind.
- **fix sketch**: Write docs/features/mcm.md from the Patch41 source + git log; add the 4 orphans (and any other unindexed docs) to the relevant docs/INDEX.md sections; update the '70 feature docs' count in CLAUDE.md. /lint-docs or /lint-cleanup-loop is the natural executor.

## GAMEDATA (5)
_Not audited:_ CLEAN (checked this session, no finding): sp_battle_scenes.xml via tools/audit_battle_scenes.py — 0 crash suspects, all 256 map indices covered (the dropped-vanilla-terrain list is deliberate curation); troop_weights.xml (86 refs) and troop_resource_costs.xml (8 refs) cross-checked by XML parse against all 3,002 TAOM NPCCharacter ids — 0 broken refs despite being outside validate_moduledata.py's s

### [GAMEDATA-01] All 19 Custom Battle scene entries reference scene folders that no longer exist — every TAOM custom battle map CTDs
- **effort** S · **risk** LOW (Data-only XML edit confined to Custom Battle mode; no save-compat impact (custom battle has no saves). Risk is repointing to a wrong-but-existing scene, which is visible immediately on launch.) · **confidence** HIGH · · unmatched
- **impact**: Selecting any of the 19 TAOM scenes in Custom Battle crashes to desktop on mission load. The CustomBattles feature (Patch19 factions/commanders/troops) is effectively unusable on TAOM maps — only vanilla scenes work. Also the standard testing surface for creature/battle features (spider, elephant) silently lost its TAOM map list.
  - `c:/Users/mikew/source/repos/TAOM/Main/_Module/ModuleData/custom_battle_scenes.xml:4-103` — 19 <Scene> entries (lotrtaom_minas_tirith_forceatmo, HART_ISENGARD, Helms_Deep_Town_forceatmo, lotrtaom_hat_gondor_town_calembel, lotrtaom_kys_* battle scenes, Camloth/Dinadab/Mereham villages, Barad_Faen/Bergraw/Melgobas castles, etc.); checked every id against all Modules/*/SceneObj folders case-insensitively this session — zero exist (recursive find for 4 samples also empty). Repo copy verified byte-identical to the deployed Modules/TAOM copy.
  - `c:/Users/mikew/source/repos/TAOM/Main/_Module/ModuleData/custom_battle_scenes.xml:80-82` — In-file comment documents the failure mode for this exact class: lotrtaom_iron_hills_01_forceatmo was removed because selecting it 'CTDs on load, 8/8 attempts' — proving these entries are reachable in the Custom Battle UI and a missing/broken scene hard-crashes.
  - `c:/Users/mikew/source/repos/TAOM/Main/_Module/SubModule.xml:842` — <XmlName id="CustomBattleScenes" path="custom_battle_scenes" /> — the file is engine-registered, so all 19 entries populate the Custom Battle scene list.
  - `E:/Steam/steamapps/common/Mount & Blade II Bannerlord/Modules/TAOM_Map/SceneObj:(directory listing)` — The install now holds renamed taom_* scenes (taom_gondor_town_minas_tirith_forceatmo, taom_isengard_town_orthanc_forceatmo, taom_mordor_battle_black_gates_forceatmo, taom_rohan_castle_helms_deep_forceatmo, taom_gondor_town_lossarnach_forceatmo...) — a mass rename happened after the 2026-05-28 audit snapshot (docs/reviews/scene-name-audit-2026-05-28.txt showed these old ids OK), and custom_battle_scenes.xml was never migrated (last touched at f828d89/e0d0701, pre-rename).
- **fix sketch**: Repoint each entry to its renamed taom_* counterpart where one exists (minas_tirith, orthanc, osgiliath, black gates, dead marshes, fords of isen, helms deep, lossarnach all have obvious matches on disk); delete entries with no surviving scene (the Gondor village/castle one-offs). Then add custom_battle_scenes.xml to the audit_scene_names.py / audit_battle_scenes.py input set so the next rename wave can't miss it — neither tool reads this file today.

### [GAMEDATA-02] Live TAOM_Map settlements.xml: 3 town center scenes (Calembel, Helm's Deep, Isengard) reference deleted scene folders — town visit/siege crash
- **effort** S · **risk** MED (Edit lands in the external live module (not git-tracked), so there is no revert history — tools/remap_stale_scene_names.py writes backups, use it. Scene refs are not serialized in saves, so no save-compat impact; Calembel has no renamed counterpart on disk and needs a vanilla-fallback choice (judgment call).) · **confidence** HIGH · · unmatched
- **impact**: Entering the town scene ('Take a walk', sneak-in, lord's-hall transitions routed through center, and siege missions keyed off the center scene) at Calembel, Helm's Deep, or Isengard crashes the campaign session. These are three major, frequently-contested settlements; the crash class is exactly the 'battles/visits near specific places crash' bug 4c68256 fixed elsewhere.
  - `E:/Steam/steamapps/common/Mount & Blade II Bannerlord/Modules/TAOM_Map/ModuleData/settlements.xml:3074` — town_EW9 (Calembel): <Location id="center" scene_name="lotrtaom_hat_gondor_town_calembel" ...> across all 4 level slots — no such SceneObj folder exists in any module (audit_scene_names.py run this session: 'NO SCENE ANYWHERE').
  - `E:/Steam/steamapps/common/Mount & Blade II Bannerlord/Modules/TAOM_Map/ModuleData/settlements.xml:6100` — town_V2 (Helm's Deep): center location scene_name="Helms_Deep_Town_forceatmo" x4 — folder gone (taom_rohan_castle_helms_deep_forceatmo is the surviving renamed scene).
  - `E:/Steam/steamapps/common/Mount & Blade II Bannerlord/Modules/TAOM_Map/ModuleData/settlements.xml:9867` — town_isengard: center location scene_name="HART_isengard" x4 — no HART folder in any case (taom_isengard_town_orthanc_forceatmo is the surviving renamed scene).
  - `c:/Users/mikew/source/repos/TAOM/docs/reviews/scene-name-audit-2026-05-28.txt:114-115, 279` — All 3 ids were [OK] in the 2026-05-28 audit snapshot — this is a regression introduced by the post-05-28 scene-folder rename wave, after commit 4c68256 had just repointed 61 other stale refs in this same file. The post-bump/post-rename re-audit step in .claude/rules/vanilla-data-comparison.md was skipped for the rename.
- **fix sketch**: Run tools/remap_stale_scene_names.py (built for exactly this in 4c68256): HART_isengard -> taom_isengard_town_orthanc_forceatmo, Helms_Deep_Town_forceatmo -> taom_rohan_castle_helms_deep_forceatmo, lotrtaom_hat_gondor_town_calembel -> a vanilla empire town scene until a replacement is exported. Re-run audit_scene_names.py to confirm zero TAOM_Map crash suspects, and add 'after any SceneObj rename/delete' as an explicit trigger to the scene-reference-audit checklist.

### [GAMEDATA-04] CLAUDE.md Armory prefix→canonical-folder table contradicts live Armory for sk_dwarf_dain_* and urukscout_* (42 items)
- **effort** S · **risk** LOW (Doc-only fix (update the 2 table rows + drop or annotate the nonexistent clo_urukscout_* prefix). Moving the 42 shipped items instead would be higher-risk for zero player-visible gain — simplicity criterion says fix the doc.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: A future authoring session or generator run that trusts the table files new Dain/urukscout items into erebor//isengard/ while the existing family sits elsewhere — recreating the #211 misfile pattern (engine silently shadows one entry when ids collide; split families when they don't). Zero runtime impact today since all 42 ids are unique and the engine loads them regardless of folder.
  - `E:/Steam/steamapps/common/Mount & Blade II Bannerlord/Modules/LOTRLOME_Armory/ModuleData/LOTRLOME_items/iron_hills/body_armors.xml:(plus sibling arm/head/leg/shoulder files)` — All 38 sk_dwarf_dain_* items live under iron_hills/ — verified zero sk_dwarf_dain_* ids anywhere in erebor/*.xml — while the CLAUDE.md table row says sk_dwarf_dain_* -> erebor/ ('Dain's set').
  - `E:/Steam/steamapps/common/Mount & Blade II Bannerlord/Modules/LOTRLOME_Armory/ModuleData/LOTRLOME_items/mordor/body_armors.xml:(plus arm/head/leg files)` — All 4 urukscout_* items (body/boots/gloves/helmet) live in mordor/; isengard/ has zero — table says urukscout_*, clo_urukscout_* -> isengard/. clo_urukscout_* items do not exist anywhere in the Armory at all (only the generator tools/generate_isengard_armor.py and validate_all_troop_refs.py mention them).
  - `c:/Users/mikew/source/repos/TAOM/CLAUDE.md:Armory folder canonical home table` — The table is the MANDATORY pre-authoring reference ('before authoring a new item, grep ALL subfolders for the prefix... first folder with that prefix is the canonical home') — it is currently wrong on 2 of 10 rows. validate_moduledata.py only catches actual id COLLISIONS (DUPLICATE_ITEM_DEF), not placement, so this never trips the validator.
- **fix sketch**: Update the CLAUDE.md table: sk_dwarf_dain_* -> iron_hills/, urukscout_* -> mordor/, and remove or footnote clo_urukscout_* (never generated). Optionally extend validate_moduledata.py with a WARNING-severity prefix-placement check driven by the same table so the doc and disk can't silently diverge again.

### [GAMEDATA-05] heroes.xslt carries 3 dead rules matching TAOM-only hero ids the transform can never see; intended spouse links apply only one-directionally
- **effort** S · **risk** LOW (Deleting provably-dead xslt templates holds parity by definition; adding spouse= to the TAOM heroes.xml rows is additive. Worst case is a duplicate-marriage declaration the engine already tolerates throughout vanilla heroes.xml.) · **confidence** MED · · unmatched
- **impact**: Three dead rules misleading future lord-skills/heroes authoring (the rules look load-bearing but are no-ops), and three marriages (Hirluin/Nauriel pattern x3) depending on one-directional spouse declarations. If the engine requires both sides, the spouses start unmarried — a lore/consistency bug, not a crash.
  - `c:/Users/mikew/source/repos/TAOM/Main/_Module/ModuleData/heroes.xslt:(templates matching Hero[@id='lord_1_52_4'], 'lord_1_71_1', 'lord_1_9_5')` — Checked all 399 id-predicates in heroes.xslt against installed vanilla SandBox heroes.xml this session — these 3 ids don't exist there (0 hits in vanilla heroes.xml AND lords.xml). They are TAOM-authored heroes defined in Main/_Module/ModuleData/characters/heroes.xml, which a module's own XSLT does not transform (XSLT applies to the previously-merged document), so the rules never fire.
  - `c:/Users/mikew/source/repos/TAOM/Main/_Module/ModuleData/characters/heroes.xml:lord_1_52_4 entry` — The TAOM hero row carries a newer, richer bio and NO spouse attribute (same for lord_1_71_1 and lord_1_9_5) — the spouse="Hero.lord_1_52" the dead xslt rule would add is absent; the marriage link exists only on the vanilla-side row via the live xslt rule that sets spouse="Hero.lord_1_52_4". Whether the engine backfills the reverse link from a one-directional spouse attribute is unverified.
- **fix sketch**: Verify in-game (or decompile the Heroes XML loader) whether one-directional spouse links marry both heroes; if not, add spouse= to the 3 TAOM-side rows in characters/heroes.xml. Either way delete the 3 dead templates from heroes.xslt and note the 'own-module XSLT never sees own-module XML' rule in the xslt rule doc.

### [GAMEDATA-03] ~320 player-facing C# localization keys never harvested into taom_*_strings.xml — untranslatable in all 12 languages
- **effort** M · **risk** LOW (Additive XML harvest + translation-pipeline run; engine behavior unchanged for English (inline defaults already render). Only risk is key-id typos during harvest, caught by the existing LanguageDataXmlTests + diff review.) · **confidence** HIGH · · unmatched
- **impact**: These strings render English-only in all 12 supported languages (~10K-string pipeline; PL/SP translators already complained about exactly this class per issue #96). The cultural-feat names/descriptions are high-visibility (culture selection, encyclopedia). Open issue #96 covers the theme but its completed scope (CC narratives, career XMLs, main menu, career screen) does not include any of these families — cite it, don't close it with this.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/CulturalFeats/TaomCulturalFeats.cs:(throughout)` — 258 taom_feat_* keys (feat name + _desc pairs for every culture) exist only as inline {=key}default TextObjects — grep of all Main/_Module/ModuleData (source strings + Languages/) returns zero hits for them.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/EquipPresets/UI/PresetsOverlayVM.cs:(throughout)` — 34 taom_ep_* keys (entire EquipPresets UI) unregistered; same pattern for 11 taom_fief_* (FiefManagement hub), 8 taom_qa_* (QuickActions menu), 4 taom_cc_career_* (CC career stage), taom_extra_fast_forward_hint, taom_res_desertion.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/CrashReport/UI/CrashNotifier.cs:(throughout)` — 3 TAOM_CrashReport_* keys unregistered. Also Main/Features/TroopWeight/Hooks/PartyVMPopulatePartyListLabelHook.cs:74 coins str_party_list_label_without_max — verified absent from vanilla Native/ModuleData/module_strings.xml (only the other 3 variants exist) and from TAOM strings.
  - `c:/Users/mikew/source/repos/TAOM/Main/_Module/ModuleData/taom_module_strings.xml:(file set)` — Comparison method: 405 distinct {=KEY} ids extracted from Main/**.cs vs 7,672 ids across the 6 taom_*_strings.xml files -> 334 raw misses; after removing doc-comment false positives ({=key}/{=ID}) and 7 reused vanilla random ids (e.g. DOmb81Mu verified present in vanilla SandBox language files), ~320 real TAOM-coined gaps remain.
- **fix sketch**: Harvest the ~320 keys with their inline defaults into taom_module_strings.xml (or a new taom_feat_strings.xml given 258 feat keys; if new file: register GameText in SubModule.xml, add LanguageFile rows x12, bump the LanguageDataXmlTests count per the CLAUDE.md procedure), then run tools/translate_with_claude.py to propagate. A small CI-able checker (the grep diff above) would prevent recurrence.

## DIRECTION (12)
_Not audited:_ Considered and dropped: (1) sequencing the lords-skills batch — #228-#245 (18 open issues, ~900 NPCs by their own titles) is the largest open content theme but is fully tracked per-culture with the /lord-skills pipeline ready; a direction finding would only restate the issues. (2) Stale-looking old issues (#44 warg port, #67 siege defense, #13 race reset, #21 child generation) — the corresponding 

### [DIRECTION-06] Resolve the SiegeMountBehavior mode 1 'RESERVED' dead value: implement the promised map-spawn or remove it
- **effort** S · **risk** LOW (Removal: value 1 already behaves as vanilla, so clamping saved value 1→0 changes nothing observable. Implementation would be M effort with MED risk (map-entity spawn lifecycle) — scope the decision first.) · **confidence** HIGH · · unmatched
- **impact**: Player-facing settings surface carries a dead option; the RESERVED relabel was the stopgap after Codex review #34 caught the original silent no-op (memory: feedback_user_facing_promise_must_match_code). Direction decision: either ship the horse-on-map mode (completes the feature's original 4-mode design) or narrow the range to {0,2,3} and stop paying the hint-text/confusion tax.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/TaomSettings.cs:219-222` — MCM integer option with range 0-3 where value 1 = 'RESERVED (currently equivalent to Vanilla — full implementation deferred; would spawn the horse on the map separately)' — a selectable mode that does nothing.
- **fix sketch**: Decide in a 10-minute design pass: if the map-spawn mode is still wanted, open an issue and implement against the SiegeDismount service; otherwise change the slider max to exclude 1 (or remap), update hint text, migrate persisted value 1→0.

### [DIR-01] Close the last 3 cultures in the career starting-equipment matrix (Lothlorien, Umbar, Khand) by authoring their missing Armory folders
- **effort** M · **risk** LOW (Pure additive data (new Armory items + new rosters); the service already falls through gracefully, so a partial landing degrades to today's behavior. Underwear-bug risk is gated by validate_moduledata.py.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Players who pick a Lothlorien/Umbar/Khand career get a fully-authored career identity (names, ranks, abilities) but anonymous starting gear — the one visible payoff of archetype selection silently no-ops for 3 of 16 cultures. The upstream cause (no dedicated LOTRLOME_Armory folder for these cultures) also blocks general troop-equipment identity for them, so closing it pays twice. The generator script + 12-culture precedent make the roster half mechanical.
  - `C:/Users/mikew/source/repos/TAOM/Main/_Module/ModuleData/equipmentsets/taom_career_starting_equipment.xml:19-23` — Header documents the exact gap: 13 cultures have rosters (Gondor hand-tuned, 12 mechanically generated by tools/generate_career_starter_rosters.py); 'Lothlorien, Umbar, and Khand (battania) have no dedicated Armory folder and fall through to culture-default'.
  - `C:/Users/mikew/source/repos/TAOM/Main/_Module/ModuleData/equipmentsets/taom_career_starting_equipment.xml:77-893` — 13 distinct player_career_{culture}_infantry_m rosters confirmed (gondor, mordor, isengard, gundabad, dolguldur, erebor, rivendell, mirkwood, vlandia, empire, aserai, khuzait, sturgia) — none for battania/lothlorien/umbar anywhere in the file.
  - `C:/Users/mikew/source/repos/TAOM/Main/_Module/ModuleData/career_system/taom_careers.xml:288-339, 694-745, 1014-1057` — Careers DO exist for all three missing cultures (Khand steppe_bowmaster/chariot_warlord, Lothlorien warden/galadhrim_archer/sentinel, Umbar corsair_boarder/corsair_captain) — players can pick them but get generic culture-default gear.
  - `C:/Users/mikew/source/repos/TAOM/CLAUDE.md:CareerSystem feature entry` — Says 'Gondor only as of 2026-05-19 ... other 15 cultures fall through' — stale vs the actual 13/16 coverage; misleads planning (secondary doc-drift evidence).
- **fix sketch**: Author starter armor sets for the 3 cultures in LOTRLOME_Armory (the /author-armor + /new-culture pipelines and per-culture starter_armors.xml precedent exist), then re-run tools/generate_career_starter_rosters.py to emit the 18 missing rosters (3 cultures x 3 archetypes x 2 genders). Also refresh the stale CLAUDE.md CareerSystem line. Note: I could not read the external Armory module to confirm folder absence directly — the file header's claim is the evidence.

### [DIR-02] Refresh docs/roadmap.md and implement its Tier 1 race-stat goal via the already-existing TaomAgentStatCalculateModel slot
- **effort** M · **risk** MED (Per-race AgentDrivenProperties changes are a combat-balance lever touching every battle; needs the engine-scale research discipline (memory: feedback_engine_scale_research) and per-(race, property) tests. The roadmap refresh itself is risk-free.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: The roadmap is the stated 'where next' doc and it is stale in the direction that matters: it prices its own top-priority items as new-override work when both slots already exist with service seams, and its 'Implemented' table undercounts (CLAUDE.md lists ~35 overrides incl. SiegeEvent/TargetScore). Race-based stat bonuses (Uruk strength, Elf agility) are the roadmap's own highest-visual-impact remaining item, and the HeroRace system + race ids already exist to key them.
  - `C:/Users/mikew/source/repos/TAOM/docs/roadmap.md:4, 28, 46` — Roadmap says '31 of 121 GameModels overridden' and lists AgentStatCalculateModel (Tier 1, 'Race-based stat bonuses') and MapVisibilityModel (Tier 4, 'Ranger scouting range, Orc night vision') as remaining opportunities.
  - `C:/Users/mikew/source/repos/TAOM/Main/Features/CareerSystem/Models/TaomAgentStatCalculateModel.cs:19-64` — TaomAgentStatCalculateModel already exists (career passives + elephant/spider mount-lock) with an ICareerAgentStatService delegation seam — the roadmap's Tier 1 item is now a service extension, not a new override.
  - `C:/Users/mikew/source/repos/TAOM/Main/Features/CareerSystem/Models/TaomMapVisibilityModel.cs:8-22` — TaomMapVisibilityModel also already exists (career PartySpottingRange passive) — the Tier 4 ranger-scouting/orc-night-vision goal likewise has its override slot occupied and extensible.
- **fix sketch**: S-effort first step: rewrite roadmap.md against the real override list. M-effort feature: add a race-bonus path to ICareerAgentStatService (or a sibling IRaceStatService consumed by the same model) keyed on IRaceManager, JSON-config per race, MCM master toggle — following the exact pattern the model already uses for career passives.

### [DIR-05] Finish or formally retire SiegeDismount mode 1 (DismountKeepOnMap), the one RESERVED no-op MCM option
- **effort** M · **risk** MED (Implementing requires spawning/cleaning a map-side horse entity tied to siege lifecycle (new state-machine clear paths: siege end, save/load mid-siege, mount death — the lifecycle-state-matrix bug class). Retiring it instead is LOW risk but the enum value must remain for save-compat.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: The only selectable-but-inert MCM option I found in TaomSettings (a full RESERVED/no-op grep matched just this one). The honest hint already mitigates the user-facing harm, so the value is closure: either deliver the advertised map-side horse spawn or stop offering the slot. Low player value, but it is the repo's single remaining stated-but-undelivered MCM promise.
  - `C:/Users/mikew/source/repos/TAOM/Main/Features/TaomSettings.cs:219-222` — MCM exposes SiegeMountBehavior 0-3 with '1 = RESERVED (currently equivalent to Vanilla — full implementation deferred; would spawn the horse on the map separately)'.
  - `C:/Users/mikew/source/repos/TAOM/Main/Features/SiegeDismount/SiegeDismountService.cs:68-78` — DismountKeepOnMap case is a documented no-op ('keeping the enum value for save-compat but treating it as a full no-op until somebody implements an actual map-side horse-spawn') that logs a warning and clears state.
- **fix sketch**: Decision-first: apply the simplicity criterion. Option A (retire): clamp the MCM range to exclude 1, keep the enum member and the no-op case for save-compat, update the hint — S effort. Option B (implement): map-side horse spawn with a full lifecycle clear-path matrix — M effort. A is the better default unless the user wants the feature.

### [DIRECTION-01] Implement roadmap Tier 1 race stat bonuses through the already-built TaomAgentStatCalculateModel boundary; refresh the stale roadmap
- **effort** M · **risk** MED (UpdateAgentStats runs per-agent and the slot is shared (careers + 2 creature mount-locks); per-race AgentDrivenProperties values need range research before shipping (engine-scale lesson in memory), and a bad multiplier touches every agent in every mission.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Player value: the roadmap's own highest-visual-impact item (per-race combat identity for Uruks/Elves/Dwarves across every battle) at a fraction of its original cost because the model slot, IoC wiring, and service seam already exist. Maintainer value: the roadmap currently points future sessions at building slots that are already built.
  - `c:/Users/mikew/source/repos/TAOM/docs/roadmap.md:28,51` — AgentStatCalculateModel listed as the #1 recommended remaining override ('Race-based stat bonuses — Uruk strength, Elf agility'), Tier 1 'Highest Visual Impact'.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/CareerSystem/Models/TaomAgentStatCalculateModel.cs:19-64` — The override already exists (career passives + elephant/spider mount-lock) with a thin-boundary ICareerAgentStatService delegation seam in UpdateAgentStats — exactly where race modifiers would plug in.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/CareerSystem/Models/TaomMapVisibilityModel.cs:8-23` — Roadmap line 46 also lists MapVisibilityModel as remaining, but it is already overridden (career spotting range) — second stale row.
  - `c:/Users/mikew/source/repos/TAOM/docs/roadmap.md:4` — '31 of 121 GameModels overridden' undercounts: CLAUDE.md's table lists 33, and CareerSystem code adds at least 4 more (TaomAgentStatCalculateModel, TaomMapVisibilityModel, TaomClanTierModel.cs:21, TaomAgentApplyDamageModel.cs:12 — all seen this session).
- **fix sketch**: Add an IRaceStatService mirroring the ICareerAgentStatService pattern, consumed in the same UpdateAgentStats boundary; JSON per-race modifier config with FiniteFloatValidator; one test per (race, property) cell. Refresh docs/roadmap.md implemented-table and count in the same PR (S).

### [DIRECTION-04] Close the last 3 career starting-equipment cells (Lothlorien, Umbar, Khand) and fix the stale CLAUDE.md row
- **effort** M · **risk** LOW (Additive XML + generator rerun; main risks are Armory canonical-folder discipline and whether Khand/battania is actually CC-reachable for players (worth confirming before authoring its items — if not, the cell count drops to 2).) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Player value: the 3 remaining playable cultures get the same career-equipment experience as the other 13. Maintainer value: correcting the CLAUDE.md row redirects future effort from 'author 15 cultures' (done) to 'author 3 Armory starter folders' (the real remaining work).
  - `c:/Users/mikew/source/repos/TAOM/Main/_Module/ModuleData/equipmentsets/taom_career_starting_equipment.xml:19-23` — Header documents the current state: 13 cultures covered (Gondor hand-tuned, 12 generated by tools/generate_career_starter_rosters.py); 'Lothlorien, Umbar, and Khand (battania) have no dedicated Armory folder and fall through to culture-default'.
  - `c:/Users/mikew/source/repos/TAOM/Main/_Module/ModuleData/equipmentsets/taom_career_starting_equipment.xml:29-903` — Grep of culture= rows confirms exactly 13 distinct cultures (gondor, mordor, isengard, gundabad, dolguldur, erebor, rivendell, mirkwood, vlandia, empire, aserai, khuzait, sturgia) × 3 archetypes × 2 genders.
  - `c:/Users/mikew/source/repos/TAOM/Main/_Module/ModuleData/career_system/taom_careers.xml:(grep this session)` — lothlorien, umbar, and battania each carry 3 eligible careers — players of those cultures select a career but receive no archetype gear.
  - `c:/Users/mikew/source/repos/TAOM/CLAUDE.md:368` — CLAUDE.md still says 'Gondor only as of 2026-05-19' — stale by 12 cultures; misleads every future session about where the gap actually is.
- **fix sketch**: Author starter_armors.xml for lothlorien/umbar (and khand if CC-reachable) via the /author-armor pipeline (15-item starter convention per CLAUDE.md:368), re-run generate_career_starter_rosters.py for the new cultures, update the CLAUDE.md row in the same commit.

### [DIR-03] Re-enable the diagnosed-but-deferred elephant howdah crew + spine bone-tracking
- **effort** L · **risk** MED (Physics/collision changes can reintroduce the slide or new clipping; verification is in-game only (no unit-test story), and the 2026-06-10 RCA showed this area produces multiple independent causes (memory: feedback_cumulative_isolation_needs_single_variable_control).) · **confidence** HIGH · ✗ verifier:NOT-REAL
- **impact**: The Harad war elephant — a marquee faction visual — currently ships with an empty howdah riding at a fixed offset. Unlike most deferred work, the investigation is already paid for: both slide sources are isolated, the fix direction is named in the doc, and the re-enable checklist exists. This is the highest-value item where remaining cost is mostly implementation, not diagnosis.
  - `C:/Users/mikew/source/repos/TAOM/Main/Features/Elephant/ElephantMissionBehavior.cs:105` — 'DEFERRED (2026-06-10): crew spawn is a CONFIRMED slide source and is disabled for now.' — TrySpawnHowdahCrew call commented out.
  - `C:/Users/mikew/source/repos/TAOM/Main/Features/Elephant/TaomHowdahMachine.cs:143-151` — Spine bone-tracking likewise DEFERRED-disabled (fixed-offset positioning only) with pointer back to the feature doc.
  - `C:/Users/mikew/source/repos/TAOM/docs/features/elephant.md:38-47, 163-166, 633-643` — Root cause DIAGNOSED (physics bodies inside the elephant collision capsule; fix = shared-FaceGroupId crew/floor collision change); fix deferred by explicit project-owner decision with a re-enable checklist. Open issues #278/#272 track the elephant.
- **fix sketch**: Implement the shared-FaceGroupId crew/floor collision fix named at elephant.md:47, re-enable TrySpawnHowdahCrew and the TryRepositionToBone branch one at a time (single-variable control per the RCA discipline), and run the doc's re-enable checklist in Custom Battle. Cite/close against #278/#272 rather than a new issue.

### [DIR-06] Generalize the Gondor-only JSON recruitment-pool loader to per-culture files, migrating incrementally as cultures churn
- **effort** L · **risk** MED (Recruitment pools are gameplay-critical (wrong pool = wrong faction troops everywhere); mitigated by the overwrite-with-fallback design and per-culture integration tests, but each migrated culture needs a hand-written-vs-JSON equivalence test (the mirror-table-drift lesson).) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Moves recurring per-culture recruitment balance work from C# (rebuild + recompile + service tests per tweak) to validated data files, using a pipeline that already exists, is tested, and keeps the hand-written pools as a fail-safe. Maintainer value concentrates exactly where the repo's churn already is. Not a big-bang rewrite: the Gondor precedent shows per-culture migration is independently shippable.
  - `C:/Users/mikew/source/repos/TAOM/Main/_Module/ModuleData/recruitment_pools:(directory)` — Contains exactly one file: gondor.json. The loader is literally named GondorRecruitmentJsonLoader (per docs/features/volunteer-recruitment.md:130).
  - `C:/Users/mikew/source/repos/TAOM/Main/Features/TroopProgression/VolunteerRecruitmentService.cs:55-912` — ~38 hand-written static Initialize*() methods cover every other culture/clan pool (Rohan, Harad+clans, Dunland+clans, Isengard, Dale, Mordor, Mirkwood, Umbar, DolGuldur, Erebor, Lothlorien, Rhun, Shaghana, Abanissa, Goblin, MistyMountainOrcs, Gundabad, Rivendell...) — every balance change is a C# edit + rebuild + test churn.
  - `C:/Users/mikew/source/repos/TAOM/docs/features/volunteer-recruitment.md:42, 94, 189-198` — The JSON path was designed as the extensible layer: JSON entries OVERWRITE hand-written keys at runtime, hand-written stays as safety net, loader has full NaN/Infinity/fail-closed-condition validation and integration tests against the real file — the architecture anticipates more cultures.
  - `C:/Users/mikew/source/repos/TAOM:git log (recon)` — VolunteerRecruitmentService.cs is a top churn hotspot over the last 150 commits; most recent trunk commit 5cf687c is itself a recruitment-reachability fix for Mirkwood and Umbar — the churn is real and ongoing.
- **fix sketch**: Rename/generalize GondorRecruitmentJsonLoader to enumerate recruitment_pools/*.json; migrate one culture per session, starting with whichever culture's pools next need a balance change (Mirkwood/Umbar are the most recently touched), asserting JSON output == hand-written output before deleting nothing (hand-written stays as the documented safety net, per the Gondor design).

### [DIRECTION-02] Generalize the gondor.json recruitment-pool loader: migrate the 17-culture hand-written pools to per-culture JSON
- **effort** L · **risk** MED (Pool weights are gameplay-critical (the shadowing-priority cascade is subtle — settlement/clan pools replace, not merge); mitigated by doing it per-culture, keeping hand-written as fallback for one release, and the existing guard test.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Maintainer value: per-culture recruitment retunes become data edits instead of C# recompiles, shrink the largest hand-written block in a 988-line hotspot service, and open the door to a tools/schemas validation entry for pools (the taom_schema.py consolidation direction). The reachability guard makes this unusually safe for its size.
  - `c:/Users/mikew/source/repos/TAOM/Main/_Module/ModuleData/recruitment_pools/gondor.json:1` — The recruitment_pools/ directory contains exactly one file (354 lines) — Gondor is the only data-driven culture.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/TroopProgression/VolunteerRecruitmentService.cs:412-433` — EnsureGondorJsonLoaded: the JSON-overwrites-hand-written-with-fallback loading seam is built, idempotent, fail-safe — and used exactly once.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/TroopProgression/VolunteerRecruitmentService.cs:25-60` — Static initializer calls ~30 InitializeXxx methods covering 17+ cultures of pool data hard-coded in a 988-line service (a churn hotspot in the last 150 commits).
  - `c:/Users/mikew/source/repos/TAOM/CHANGELOG.md:151-154` — AllNonMilitiaNonBossTroops_AreReachableFromARecruitmentPoolRoot guard test floods the real upgrade graph from AllPooledTroopIds() including the JSON-loaded Gondor pool — the regression net for a migration already exists.
- **fix sketch**: Rename/generalize GondorRecruitmentJsonLoader to a recruitment_pools/<culture>.json convention; generate the JSONs mechanically from the current static data (the *10000 weight conversion is already specified); migrate 2-3 cultures per batch with weight-equivalence tests, then delete the static methods.

### [DIRECTION-03] Field the cave troll — the third creature through the proven warg→elephant→spider mount pipeline
- **effort** L · **risk** MED (The deferral reason is real: spawning it via party templates without engine-side support risks a spawn crash, and the quad_movement-tag AV class cost two RCA days on the spider. A troll is bipedal, so the quadrupedal action-set recipe needs adaptation — approach (ridden 'troll-goad' driver vs unridden) is the open design question.) · **confidence** HIGH · ✓ verifier:REAL
- **impact**: Player value: a marquee LOTR creature that is ~80% authored in data becomes fieldable, closing a CHANGELOG-documented gap and paying off the cave_troll_master career's promise. The creature-mount recipe (Monster + Mountable + rider_sit_bone, movement-tagged clips, BT auto-attack, mount-lock in TaomAgentStatCalculateModel) is now proven three times, making each next creature disproportionately cheaper.
  - `c:/Users/mikew/source/repos/TAOM/CHANGELOG.md:147-149` — cave_troll (Mordor, L51) explicitly deferred from recruitment as a documented known gap — 'a non-humanoid Monster never spawned in any party template'; the reachability guard test whitelists it alongside militia/bosses.
  - `c:/Users/mikew/source/repos/TAOM/Main/_Module/ModuleData/troops/troops_mordor.xml:1968-1982` — The troop is fully authored: race="cave_troll", is_basic_troop="true", BodyProperty.fighter_cave_troll, dedicated wm_cave_troll_* mace items — assets exist, reachability doesn't.
  - `c:/Users/mikew/source/repos/TAOM/Main/_Module/ModuleData/career_system/taom_careers.xml:889-895` — Shipped Gundabad career 'cave_troll_master' (Pit-Driver / Troll-Goad) whose entire lore is driving cave trolls in battle — the player-facing promise already exists with no troll on the field.
  - `c:/Users/mikew/source/repos/TAOM/docs/tools/spider-skeleton-tpac-tools.md:3` — The tpac inspection/patching toolchain built for the spider 'generalizes to any non-humanoid Bannerlord rig (snakes, dragons, trolls, ...)' — trolls are a named generalization target of the tooling.
  - `c:/Users/mikew/source/repos/TAOM/Main/_Module/ModuleData/raceage/race_age_config.json:13` — cave_troll race lifecycle config already exists (maxAge 500 — matching docs/testing/tester-checklist-discord.md:64).
- **fix sketch**: Spike plan, not build plan: (1) byte-check cave_troll's animation tpac with the spider tools for movement-tag presence; (2) decide ridden (goblin/orc driver, straight spider recipe reuse) vs unridden; (3) clone the elephant feature shape (BT swing/grab, config, mount-lock or spawn guard) and gate recruitment behind the same pools used for elites.

### [DIRECTION-05] Finish the mumakil set-piece: re-enable howdah crew spawn + spine bone-tracking behind the named FaceGroupId fix
- **effort** L · **risk** MED (Both subsystems were disabled because they demonstrably broke mount movement; re-enabling risks regressing a shipped, in-game-proven feature. Must follow the single-variable-control isolation discipline (the slide RCA refuted two prior root-cause conclusions) — re-enable crew and bone-tracking separately, worst case re-defer cleanly.) · **confidence** HIGH · ✗ verifier:NOT-REAL
- **impact**: Player value: the crewed howdah is the signature Harad battlefield set-piece the whole elephant feature builds toward; today the machine ships visibly incomplete (static howdah, no archers). This is the highest-visibility deferred item named in CLAUDE.md with a concrete fix hypothesis already recorded.
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/Elephant/ElephantMissionBehavior.cs:105` — 'DEFERRED (2026-06-10): crew spawn is a CONFIRMED slide source and is disabled for now.'
  - `c:/Users/mikew/source/repos/TAOM/Main/Features/Elephant/TaomHowdahMachine.cs:143-151` — Spine bone-tracking deferred for the same confirmed slide cause — the howdah currently neither carries crew nor tracks the elephant's spine.
  - `c:/Users/mikew/source/repos/TAOM/CLAUDE.md:War Elephant entry (Key Paths)` — Names the re-enable recipe explicitly: 'both confirmed slide sources: physics bodies inside the elephant collision capsule; re-enable with a shared-FaceGroupId crew/floor collision fix' — tracked under open issues #278/#272 (cited, not re-reported).
- **fix sketch**: Spike: spawn one crew agent with crew + howdah floor sharing a FaceGroupId so the engine skips inter-body collision inside the elephant capsule; verify no slide with a single-variable control battle; then re-enable bone-tracking as a second isolated step.

### [DIR-04] Revive the disabled Mordor cave troll using the now-proven creature pipeline
- **effort** L · **risk** MED (The original 'WIP; not ready' cause is undocumented — could be animation/skeleton work (cheap to diagnose now) or missing assets (external dependency). Recruitment wiring also interacts with vanilla GetBasicVolunteer per the disable comment. Spawn-path bugs in this class historically crash battles.) · **confidence** MED · ✓ verifier:REAL
- **impact**: A distinctive Mordor battlefield creature with all items, body properties, and troop definition already authored — currently dead data. The creature pipeline knowledge (warg/elephant/spider, per-agent BehaviorTree pattern, clip-tag RCA) makes the 'not ready' blockers categorically cheaper to diagnose now than when it was shelved. Reuses the existing per-agent BT architecture rather than inventing one.
  - `C:/Users/mikew/source/repos/TAOM/Main/_Module/ModuleData/troops/troops_mordor.xml:1967-2045` — Fully-authored cave_troll NPCCharacter (race=cave_troll, level 51, named items wm_cave_troll_1h_mace_a/b/c, lotr_troll_helmet/armor/bracers) commented out: 'DISABLED 2026-05-14: Cave troll WIP; not ready for live game yet. Re-enable by uncommenting.'
  - `C:/Users/mikew/source/repos/TAOM/Main/_Module/ModuleData/TAOM_bodyproperties.xml:262` — fighter_cave_troll BodyProperty template exists — the supporting data chain was built, not just sketched.
  - `C:/Users/mikew/source/repos/TAOM/Main/_Module/ModuleData/career_system/taom_careers.xml:138, 416` — olog_hai_warchief ('War-Troll' / 'Olog-hai') and far_harad_halftroll careers already sell troll fantasy in player-facing text — content demand exists in shipped data.
  - `C:/Users/mikew/source/repos/TAOM/CLAUDE.md:Giant Spider feature entry` — The 2026-06-10/11 spider RCA codified the creature-pipeline failure modes (quad_movement clip tags, action-set surface, per-mesh bone limits, monster usage sets) that were unknown when the troll was shelved on 2026-05-14 — the debugging cost that blocked it has since been paid down by warg→elephant→spider.
- **fix sketch**: Spike first: uncomment in a scratch branch, run the probe-battery method from the spider RCA to find what actually breaks (skeleton/action set/clips), and document the blocker. If clip tags or action-set surface, the fixes are the known byte-patch/alias recipes. Decide troop-vs-other delivery against the reversed creature-as-mount verdict (memory: feedback_nonhumanoid_creature_troop_not_mount) — a biped troll is the one case the mount recipe does not cover, so this needs an explicit design call.
