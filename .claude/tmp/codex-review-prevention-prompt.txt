You are doing an adversarial review of a "prevention infrastructure" commit in TAOM (Bannerlord 1.3.15 mod). The commit's purpose: build mechanical gates that catch the recurring failure modes from three prior Codex review passes on a Tier 1 productivity-skills adoption.

Three prior passes (all in docs/reviews/codex-*-tier1-*-2026-04-26.md) found 19 issues across 5 categories:
1. Wrong API/lifecycle assumption (6 bugs)
2. Process violation (3 bugs — CHANGELOG missed twice, counter math off)
3. Gitignore blast (1 HIGH)
4. Port drift from external skill suites (2 bugs)
5. Bloat creep (3 bugs — descriptions creeping back over 30 words)

This commit (b7e7188) builds prevention for each. Your job: verify the prevention actually works, find any bug it would have introduced, and report process compliance.

CHANGESET (one commit):
- b7e7188 feat(.claude): prevention infrastructure for recurring harness bugs

Files added/modified:
- .claude/rules/harness-facts.md (NEW, always-load) — pinned Claude Code load semantics with doc URLs
- .claude/rules/external-skill-ports.md (NEW, scoped to .claude/skills/**/SKILL.md) — port-drift checklist
- .claude/hooks/check-changelog-changed.sh (NEW) — PreToolUse(Bash) blocks git commit if .claude/CLAUDE.md/AGENTS.md staged but CHANGELOG.md not
- .claude/hooks/check-claude-files-tracked.sh (NEW) — PreToolUse(Bash) blocks git commit if files exist on disk under .claude/{skills,agents,rules,hooks}/ but are gitignored or untracked
- tools/audit-review-counter.sh (NEW) — recompute REVIEW-LOG totals, verify AGENTS.md matches, --fix to update in place
- .claude/skills/context-budget/scan.sh — added skill description bloat lint (was agents-only)
- .claude/settings.json — wired both new hooks into PreToolUse(Bash) chain
- AGENTS.md — counter updated to 27 reviews / 71 bugs (auto via the new validator)
- CLAUDE.md — Hooks table + Scoped Rules table updated
- CHANGELOG.md — entry under 2026-04-26 (later)

VERIFICATION BASELINE (the 5 facts harness-facts.md asserts — verify they match current Claude Code docs):
- Skill descriptions load eagerly; bodies lazily on invocation. https://code.claude.com/docs/en/skills
- Hooks declared in skill frontmatter only fire while that skill is invoked. https://code.claude.com/docs/en/hooks
- Rules WITHOUT `paths:` load at conversation start; ANY `paths:` field (including `**/*`) is conditional. https://code.claude.com/docs/en/memory
- MEMORY.md cap-loads first ~200 lines / ~25KB.
- `triggers:` field is undocumented in current Claude Code skill schema.

KNOWN SUSPECTS (verify each — CONFIRM/DISPUTE/UNVERIFIED with file:line evidence):

1. **check-changelog-changed.sh JSON output safety.** The hook embeds a fixed message — no path interpolation — so the JSON injection class doesn't apply. But verify: does the heredoc emission produce well-formed JSON in all cases? Could the `$()` substitution from python3 fail in a way that makes the script emit malformed JSON?

2. **check-changelog-changed.sh decision logic edge cases.**
   - What if the user runs `git commit -F message.txt` or pipes a heredoc? Does the hook still trigger correctly (the COMMAND grep would still match "git commit")?
   - What if the commit message itself contains the literal string "git commit"? (Unlikely but possible: `git commit -m 'reverting the bad git commit from yesterday'`.) Does this cause double-firing or any other issue?
   - What if the user runs `git -C path commit`? The grep `*"git commit"*` should match. Verify.
   - What about `git stash` or `git revert` operations that internally create commits? Should they be blocked?
   - What if the user has staged hunks (git add -p) that touch CHANGELOG.md but the file is ALSO partially modified-not-staged? Does `git diff --cached --name-only` correctly include it?

3. **check-claude-files-tracked.sh false positives.**
   - The script finds files under .claude/{skills,agents,rules,hooks}/. Does it correctly skip the `.claude/tmp/freeze/.gitignore` file (intentionally there to gitignore the state file, but `.gitignore` files matching `*.md|*.sh|*.json|*.yaml|*.yml` won't match — verify)?
   - Does it correctly handle subdirectories (e.g., `.claude/skills/freeze/check-freeze.sh` is two levels deep)?
   - What if a hook was just removed via `git rm` but the script still exists on disk in a stash or worktree? False positive risk.
   - The script blocks if a file is "untracked AND unstaged". What if it's tracked but DELETED on disk (e.g., the `bin/` directory was renamed away)? The find won't see it but git ls-files would. Is that a real risk worth handling?

4. **audit-review-counter.sh regex robustness.**
   - The script extracts `[0-9]+ Codex reviews total, [0-9]+ bugs found` from REVIEW-LOG.md. What if a future entry adds the word "approximately" or punctuation drift breaks the regex?
   - Same for AGENTS.md header `### Lessons From Prior Reviews \([0-9]+ reviews, [0-9]+ bugs found\)`. What if the format slightly drifts (e.g., "Reviews" capitalized differently, or "(N reviews so far, M bugs found)")?
   - The `--fix` path uses Python with sys.argv. Cygpath conversion is applied. But the `OLD_HEADER` and `NEW_HEADER` are interpolated into a shell here-doc that becomes Python-string args. What if the header contains a single-quote or backtick? (Currently it doesn't, but a future edit could break this silently.)

5. **harness-facts.md correctness.**
   - Verify the 5 facts above against current Claude Code docs. If any have drifted (Anthropic releases between 2026-04-26 and now), flag.
   - The doc URLs cited (https://code.claude.com/docs/en/skills, /hooks, /memory) — do they currently exist and contain the cited claims?
   - The slug derivation rule: "drive letter lowercased + `--` + path with `/` and `\` replaced by `-`". Verified against this machine's `c--Users-mikew-source-repos-TAOM`. But: is this convention stable across Claude Code versions, or could it change?

6. **scan.sh bloat lint behavior.**
   - The lint flags skill descriptions over 30 words. extract_description() only reads the FIRST line of the `description:` value. What if a skill uses YAML multiline (`description: |\n  long description\n  spanning multiple lines\n`)? The word count would be wrong — possibly understated.
   - Verify: count current word counts on /freeze and /investigate to confirm they're 21w / 23w as the CHANGELOG claims.

7. **scan_skills/scan_agents body-counting bug REGRESSION risk.**
   - The Tier 1 fix changed scan_skills and scan_agents to count frontmatter only (eager) vs full body (lazy). Verify the eager column still uses `estimate_frontmatter_tokens` and not `estimate_prose_tokens` after this commit's bloat-lint addition. Defensive check — bloat lint diff might have inadvertently restored the bug.

8. **PreToolUse hook activation scope (the meta-question).**
   - These hooks fire when CLAUDE invokes Bash for git commit. Do they fire when the USER runs git commit directly via the host shell (not through Claude)? This is critical: if the user is doing manual commits, these hooks are irrelevant — and the prevention infrastructure is only "prevention while Claude is the one committing." Verify against Claude Code hook docs and state explicitly in the verdict.

NEW BUGS FOUND BY YOU (not in the prior known-suspects list):
- Anything else.

REQUIRED OUTPUT SECTIONS:

A. KNOWN SUSPECTS RESPONSE — one CONFIRMED/DISPUTED/UNVERIFIED per suspect with file:line citations.

B. PROCESS COMPLIANCE
- CHANGELOG.md updated for b7e7188? Verify with `git diff 4964299..b7e7188 -- CHANGELOG.md`.
- GitHub issue created for the prevention bundle? Per CLAUDE.md "Every feature... must have a GitHub issue."
- AGENTS.md counter consistent with REVIEW-LOG.md? Run `bash tools/audit-review-counter.sh` to check.
- Was `/deep-review` run on this commit prior to shipping? (Per CLAUDE.md "Run before EVERY commit touching C#" — the prevention bundle is .claude/ only, not C#. State whether the spirit of the rule applies and whether it was followed.)

C. NEW FINDINGS — anything you discover, severity HIGH/MEDIUM/LOW with file:line and concrete fix.

D. RECURSION RISK — could ANY bug we shipped in this commit defeat the prevention infrastructure it's supposed to enable? E.g., does check-changelog-changed.sh have a bug that would cause it to false-allow when it should block? If yes, that's a HIGH because it makes the prevention theater.

E. RECOMMENDED FOLLOW-UP — should anything go in a fourth fix commit?

QUALITY GATES:
- File:line citations on every finding.
- Verify each Claude Code semantic claim against official docs (URLs in harness-facts.md). Mark UNVERIFIED if you can't.
- Don't invent issues. If a section has nothing to flag, write "No findings."
- Test edge cases by reading the code, not by inference. The hooks parse JSON from stdin — trace what happens with malformed input.

WRITE YOUR FINAL REPORT TO: docs/reviews/codex-adversarial-prevention-2026-04-26.md (overwrite if it exists). One-line top summary: PASS / NEEDS FIXES + count of HIGH/MEDIUM/LOW findings.
