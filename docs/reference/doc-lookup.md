# Doc Lookup (task -> doc)

> Task-oriented index of TAOM docs. Extracted from CLAUDE.md 2026-07-18 (Tier 2 restructure); since 2026-08-05 (diet round 2) CLAUDE.md "Doc Lookup" is a pointer only — this file is the complete task table. `docs/INDEX.md` is the fuller curated topical map.


**Start here for any doc question:** [docs/INDEX.md](../../docs/INDEX.md) — curated topical map across the feature docs, ADRs, reviews, ai-includes, and migration docs. Knowledge-base architecture: [ADR-010](../../docs/adrs/010-knowledge-base-architecture.md).

| Need to... | Read |
|------------|------|
| Write tests / TDD workflow | [tdd-enforcement.md](../../docs/ai-includes/tdd-enforcement.md) |
| Research TaleWorlds mechanics | [taleworlds-research-guide.md](../../docs/ai-includes/taleworlds-research-guide.md) |
| Debug / iterate on problem | [iterative-problem-solving.md](../../docs/ai-includes/iterative-problem-solving.md) |
| Compare multiple approaches | [multi-approach-validation.md](../../docs/ai-includes/multi-approach-validation.md) |
| Understand architecture | [architecture.md](../../docs/ai-includes/architecture.md) |
| Check design patterns | [patterns.md](../../docs/ai-includes/patterns.md) |
| Work with GUI/sprites/UI | [gui-sprite-system.md](../../docs/features/gui-sprite-system.md) |
| Check ADR rules | [docs/adrs/](../../docs/adrs/README.md) |
| Ensure code quality | [code-quality.md](../../docs/ai-includes/code-quality.md) |
| Consult / append the master lessons-learned record | [LESSONS-LEARNED.md](../../docs/reviews/LESSONS-LEARNED.md) (index) → `docs/reviews/lessons/<category>.md` — **read the category file before touching a subsystem; append the `### rule / Why missed / Prevent / Source` entry there after every RCA** |
| Review or rebaseline troop skill balance per culture | [troop-skill-balance.md](../../docs/features/troop-skill-balance.md) — run the read-only overview (`analyze_troop_balance.py`) BEFORE `rebalance_troops.py`; downward deltas can signal a too-weak modifier, not over-tuned troops |
| Review LORD stats + the perks each lord's skills unlock, per culture (read-only) | [lord-perk-review.md](../../docs/features/lord-perk-review.md) — `extract_perks.py` (perk catalog) + `analyze_lord_balance.py` (per-culture HTML); authoritative skills resolve via `skill_template`→`taom_lord_skill_sets.xml`, NOT the inline `<skills>`; no single-formula parity (two lord-skill systems) |
| Check migration status | [migration/TRACKING.md](../../docs/migration/TRACKING.md) |
| Respond to a Bannerlord engine update (Steam force-bump or deliberate migration) | `/engine-bump` → [.claude/skills/engine-bump/SKILL.md](../../.claude/skills/engine-bump/SKILL.md); worked example + the changelog-item→surface→verdict table: [v1.4.8-impact.md](../../docs/migration/v1.4.8-impact.md) (previous bump: [v1.4.7-impact.md](../../docs/migration/v1.4.7-impact.md)). **Archive every baseline BEFORE regenerating** — Steam overwrites the install in place, so an assembly missing from the decompile stack when an update lands has no recoverable baseline afterwards |
| Audit/fix scene refs after a version bump (battle-near-place crashes) | [scene-reference-audit.md](../../docs/reference/scene-reference-audit.md) — `audit_scene_names.py` + `audit_battle_scenes.py` + `remap_stale_scene_names.py`; vanilla renames/removes scenes between versions |
| Compare TAOM data against current vanilla before editing mirrored XML | [.claude/rules/vanilla-data-comparison.md](../../.claude/rules/vanilla-data-comparison.md) — settlements/sp_battle_scenes/spcultures/xslt; auto-loads when those files are edited |
| Convert a purchased UE/FBX asset kit into a Bannerlord kit (Rivendell/Tents precedent) | [ue-to-bannerlord-asset-pipeline.md](../../docs/reference/ue-to-bannerlord-asset-pipeline.md) — UE headless export, Blender normalize (Store-app launcher, staleness gotchas), spec-gloss conversion, `_mtl.tpac` generation (checksum unvalidated), t_-material naming exception |
| Validate ModuleData cross-refs/ids before committing (broken item/troop refs, unknown culture, dup ids, civilian type), or ask what references a given XML id | [moduledata-validation.md](../../docs/features/moduledata-validation.md): run `python tools/validate_moduledata.py`; schema-driven, supersedes the per-culture ref validators; auto-loaded rule + pre-commit hook wire it in. **This is TAOM's XML cross-reference graph, not merely a linter.** `tools/taom_schema.py` builds the id registries and resolves the `Item.` / `NPCCharacter.` / `Culture.` / `PartyTemplate.` prefixed refs across the repo's 259 `Main/_Module/ModuleData` XML. **Know the uneven three-module reach before trusting a PASS:** schema contracts are repo-only by design (the validator's own comment: "TAOM's schema contracts are not applied to a foreign module"), the CLI extends the *cross-reference* sweep to `LOTRLOME_Armory`'s 382 for 641 files a run (405 of them localization XML with no prefixed refs at all), and `TAOM_Map` is in **no** root list except the settlement walk, where exactly two of its files are opened: `settlements.xslt` (one boolean, does it strip) and `settlements.xml` (culture attribute plus town/village economy). So its 1,012 `Culture.` refs, 988 of them the per-settlement `culture=`, are never checked for `UNKNOWN_CULTURE`, and because they feed `settled_cultures` a typo there would MASK a `LANDLESS_CULTURE` error rather than raise one. Note the inversion on settlements: the repo's `Main/_Module/ModuleData/settlements.xml` (863 elements) has its refs checked but contributes to no registry, while the live `TAOM_Map` one (988) is the sole registry source and goes unchecked; `tools/taom_query.py` is the importable query API over them (`find_references` plus the `*_exists` checks, no CLI entry point); `tools/taom_mcp_server.py` IS the `taom-moduledata` MCP server that `.mcp.json` registers. Consult these before concluding TAOM has no XML graph capability |
| Check a culture actually owns settlements before authoring lords, clans or a kingdom for it | [lord-spawn-guard.md](../../docs/features/lord-spawn-guard.md) — `python tools/validate_moduledata.py`'s `LANDLESS_CULTURE` check (severity ERROR) flags any culture carried by a `Lord`-occupation `NPCCharacter`, a `<Faction>` or a `<Kingdom>` that owns no settlement; allow a deliberate one via `_LANDLESS_BY_DESIGN` with a stated reason. The settlement registry honours `TAOM_Map/settlements.xslt`'s unconditional `<xsl:template match="Settlement"/>` strip — without that it counts vanilla's 494 deleted settlements and every culture looks landed while the game crashes |
| Retune how big lord parties are, or work out what `max_value` in `taom_partyTemplates.xml` does | [party-template-sizing.md](../../docs/reference/party-template-sizing.md): the max sum is the **spawn** ceiling, not the steady-state size. `FindAppropriateInitialRosterForMobileParty` fills each stack to `min + (max - min) * r` with one ratio per party, and for a lord party that ratio is `party.RandomFloat()`, independent of the template, so the expected roster is the midpoint of the min and max sums. `GetUpperTroopLimit()` has ZERO callers in the Campaign assembly. Steady state is `PartySizeLimit` (TAOM overrides it with `TaomPartySizeModel`, which also carries the culture feats and the TroopWeight elite tax) and it was deliberately NOT raised to match. `HeroSpawnCampaignBehavior`'s new-campaign top-up now fires for only the few lords whose ratio draw landed under their own limit (about 4% for Mordor), so the template alone sets starting size for the rest, and because only the spread was scaled the troop MIX shifts too. Retune with `python tools/rebalance_party_template_maxes.py` (dry-run default, idempotent, 193 templates in scope) |
| Bind a new culture's party templates, or work out why a TAOM culture is fielding Calradian troops | [culture-playability-wiring.md](../../docs/features/culture-playability-wiring.md) "Party-template binding contract": eight engine-read attributes decide which troops spawn (`default`/`villager`/`militia`/`rebels`/`vassal_reward_party_template` + `settlement_patrol_template_level_1/2/3`). A `spcultures.xslt` block inherits vanilla's value for anything it never names, so the culture fields Calradians with nothing wrong visible in the file; that has shipped four times. Caravans are read ONLY from the plural `<caravan_party_templates>` CHILD elements and the deserializer APPENDS rather than replaces, so you must emit yours AND add the wrapper to that block's `not(self::...)` filter or the culture carries both. `caravan_party_template` as an ATTRIBUTE is never read. Two bindings are crash surfaces: `SpawnPatrolParty` dereferences `partyTemplate.ShipHulls` and `SpawnCaravan` calls `GetRandomElementWithPredicate` on the list, neither guarded. Gate: `dotnet test TAOM.Tests --filter FullyQualifiedName~CulturePartyTemplate` |
| Check an XSLT stylesheet transforms correctly, or find out which stylesheets nothing checks | `/xslt-check` (`.claude/skills/xslt-check/SKILL.md`) plus the auto-loaded [.claude/rules/xslt.md](../../.claude/rules/xslt.md). **Know the coverage before reading a clean report as clean.** The three modules ship **16** stylesheets: 8 in the repo (1,063 `<xsl:template>` elements between them), 7 in `LOTRLOME_Armory` (`action_sets`, `action_types`, `crafting_templates`, `weapon_descriptions`, `monster_usage_sets`, `Animations/action_sets`, `MonsterUsage/LOTR/lotr_monster_usage_spider`), 1 in `TAOM_Map` (`settlements.xslt`). `/xslt-check` resolves its target under `Main/_Module/ModuleData/$ARGUMENTS` and hardcodes that same prefix in its transform command, so **the 8 external stylesheets are unreachable**, and its file-mapping table names 6 of the repo's 8 (`action_strings.xslt` and `comment_strings.xslt` are absent from it). No validator models XSLT either: `tools/taom_schema.py` opens exactly one stylesheet, `TAOM_Map/ModuleData/settlements.xslt`, and only to test `_SETTLEMENT_STRIP_RE`. Every registry is built from authored source XML, never transform output, so a block that rewrites a vanilla culture in place is invisible to all of them. For `spcultures.xslt` the executable gate is the row above |
| Find art an asset cleanup deleted, and every item / troop still wearing it | [armoury-mesh-cleanup.md](../features/armoury-mesh-cleanup.md): `python tools/audit_deleted_mesh_impact.py`; diffs the COOKED packs against the authoring tree, because `validate_mesh_refs.py` reads only the packs and returns PASS on art deleted since the last re-cook |
| Check every `mesh=` / `body_name=` ref resolves (after ANY weapon/armor authoring) | [mesh-ref-validation.md](../../docs/features/mesh-ref-validation.md) — `python tools/validate_mesh_refs.py --scan-bodies`; a missing `bo_` body is a CONFIRMED infinite mission-load hang (#352), not cosmetic; a clean PASS means only "clean within `--items` scope" |
| Confirm a troop will actually FIGHT with the weapon it spawns holding (after authoring any crafted weapon, by hand or by generator) | Run `python tools/audit_polearm_shield_parity.py`; since 2026-08-20 the PostToolUse hook `.claude/hooks/check-polearm-shield-parity.sh` also runs it after any edit that could introduce the defect, so a regression speaks without being asked. A crafted weapon with no inline `<Weapon>` takes its usages from `WeaponDescription` membership, and the FIRST matching description in the crafting template's order becomes the primary (`Crafting.cs:566-608`), so a polearm absent from `OneHandedPolearm` resolves to a `requires_no_shield` primary. Pair that with a shield in the roster and the troop holds the weapon through the pre-battle phase, because spawn wield is plain slot order, then draws its sidearm when combat starts and never touches it again. **Nothing errors and nothing logs**, and the flag's only managed consumer is a UI tooltip, so the behaviour is native and unreachable by decompile search. It has shipped three times (#445 rosters, #449 Dale spears, the Black Numenorean lance) and a player found each one. Check which descriptions a new weapon is ABSENT from, never which it is present in: the lance was registered in 3 of 4 and looked done. Registration belongs in `tools/register_one_handed_polearms.py` (`ONE_HANDED_ITEMS`), never a hand edit, because the Armory is not in this repo and a refresh reverts it silently. Mechanism: [item-usage-features.md](../../docs/reference/item-usage-features.md); applied-edit record: [lotrlome-armory-snapshot/README.md](../../docs/reference/lotrlome-armory-snapshot/README.md) |
| Author or audit a shield (`item_usage`, grip, collision capsule) | [armory-shield-audit.md](../../docs/reference/armory-shield-audit.md) — `hand_shield` (centre-grip) vs `shield` (forearm) and the offhand-bone flag each REQUIRES; `hand_shield` blocks a narrower ±0.55 cross-body arc; two `body_name`s look mistyped and must NOT be corrected |
| Update BUTR/MCM/ButterLib dependencies | [migration/dr3-maintenance.md](../../docs/migration/dr3-maintenance.md) — version pinning, Steam Workshop fallback, smoke test, risk scenarios |
| Cut a release / bump the module version / resolve a crash report's `TaomVersion` to a commit | [release-process.md](../../docs/reference/release-process.md) — skill `/release`. `<Version>` in `Main/_Module/SubModule.xml` changes only in a release commit, tagged `vX.Y.Z` (annotated) and pushed; `git push` does NOT push tags. Bumping it also means the #371 Dependencies pairing (`Dependencies/_Module/SubModule.xml` `<Version>` **must equal** Main's `DependedModuleMetadata version=`, or players get bind-posed characters). **Five versions are unresolvable** — `v2.0.11/.12/.14/.16/.17` appear in no commit on any branch, yet two crash reports cite `v2.0.12`; on meeting one, fall back to `TaomDllSha1` + engine version instead of inventing a commit range. The 11 backfilled `v2.0.x` tags and their commits are tabulated there |
| Know what TAOM gates under co-op, and which "is this multiplayer?" question to ask | [coop-interop.md](../../docs/features/coop-interop.md) — three DIFFERENT questions, never interchangeable: `ICoopPresenceProvider.IsCoopActive` (a co-op module is loaded — process-constant, safe near patch application), `ICoopSessionProvider.IsAuthority`/`ShouldDeferToHost` (may THIS peer mutate shared world state — session-varying, read live, never cache), `IDedicatedServerProvider.IsDedicatedServer` (which binaries folder we loaded from). Picking presence where authority was meant silently disabled TAOM rules for solo players who merely had the module enabled |
| Work out why a joiner arrives as the wrong race / with no career / with native starting gold | [player-possession.md](../../docs/features/player-possession.md) — every co-op base discards the CC hero at the join hand-off, so the grants ran against a hero about to be thrown away; re-applied from `Hero.MainHero.StringId` alone (no co-op assembly referenced). The other half of the race loss is [hero-race.md](../../docs/features/hero-race.md) — capture refuses below 2 registered races, because a TAOM-less host reads every hero as race 0 and that map rides the save transfer |
| Deploy TAOM to a Bannerlord dedicated server | `MirrorWin64ShippingClientToServer` in `Main/TAOM.csproj` + `Dependencies/TAOM.Dependencies.csproj` — `./build.ps1` already mirrors the ASSEMBLED client folder (not build output) into `bin/Win64_Shipping_Server/`; there is no separate step. Without that folder the server logs `Cannot find: …\Win64_Shipping_Server\TAOM.dll` and runs a VANILLA simulation over TAOM's map — no race capture, no War of the Ring. Behaviour gating: [coop-interop.md](../../docs/features/coop-interop.md) |
| Audit knowledge-base health — dead links, orphan/missing feature docs, stale version refs, doc-vs-config drift | [doc-health-linter.md](../features/doc-health-linter.md) — `python tools/lint_docs.py --summary` (skill: `/lint-docs`). Seven checks; three of them (config-example drift, pin-vs-doc version mismatch, CLAUDE.md/AGENTS.md eager budget) block a commit via `.claude/hooks/check-doc-config-drift.sh`. **Do not read a clean run as "no rot"** — since #399 the stale-version check fires only on marker-word phrasing (`current` / `target` / `now` / `builds against` / …) and its patterns never match v1.4.5 or v1.4.6, so prose that says "built for" or "requires" an old version is invisible ([#405](https://github.com/haterade22/TAOM/issues/405)) |
| Audit the SHAPE of the knowledge base (orphan clusters, god nodes, bridges), not just whether its links resolve | [doc-graph.md](../features/doc-graph.md): `python tools/graph_query.py metrics --summary` (skill: `/doc-graph`). `lint_docs.py` asks whether each link resolves; this asks whether the graph those links form still hangs together, and the two answers diverge (an orphan here is a doc with no inbound **or** outbound `.md` link, across every eligible doc, not `lint_docs`'s feature-doc-only inbound check). **A ratchet now gates the same metrics:** `tools/check_doc_graph_ratchet.py` gates orphans and components against `tools/doc_graph_baseline.json` in the `validate-xml` CI job. It went unrun from 2026-06-08 to 2026-08-18, during which isolated docs grew 64 to 153 and components 70 to 156 with every unit test green, because those tests check the algorithm against synthetic fixtures and never read the live tree. Lowering the baseline is the goal: link isolates in, run `--update`, commit the drop. Treat any metric you did not produce yourself as stale. Scope limit: `.md` nodes only, so it models no `.xml`, `.xslt` or `.cs` file |
| Find the blast radius of a C# change, or the architectural hubs, across the whole codebase | **graphify** (external, installed, wired into nothing): `graphify affected "X" --depth 2`, `god-nodes`, `explain`. Full guidance, including when NOT to reach for it: [adopt-graphify-v8-2026-08-18.md](../reviews/adopt-graphify-v8-2026-08-18.md) "How to actually use it". **Treat it as a lead generator, never a citation:** 40 of its nodes cite a `source_file` it never opened and **every one of those citations is wrong** (27 name a `.cs` path that exists nowhere; the class is usually real, the path invented), so confirm against the real file before acting. It parses no `.xml` or `.xslt` at all, so never use it for troop/item/culture questions (use the `taom-moduledata` MCP or `python tools/validate_moduledata.py`; `tools/taom_query.py` is the library behind them and has no CLI), and Serena beats it for "who calls this" because Serena is always current |
| Diagnose a dedicated server that dies on boot with `KeyNotFoundException` at `/action_sets/action` | `python tools/audit_action_set_parity.py` — an `<action>` parented by `<action_sets>` instead of an `<action_set>` (usually a self-closing `<action_set>` orphaning its nested overrides). The CLIENT build tolerates the malformed file, so a clean single-player session proves nothing; the audit exits non-zero on any root-level `<action>` |
| Find settlements whose entrance AI parties cannot path to (wedged parties, repeating "Path finding target is not valid") | `taom.audit_settlement_entrances` — see [dev-console.md](../../docs/features/dev-console.md). `PathFaceRecord.IsValid()` is TRUE for the offending faces, so nothing cheaper than a navmesh-island comparison detects them. **The command ships; the corrected coordinates do not exist yet** — they need one in-game campaign run, then apply to the LIVE `TAOM_Map/ModuleData/settlements.xml`, never the repo shadow |
| Debug a campaign CTD on the daily clan tick (`Sequence contains no matching element` out of `Campaign.Tick`) | [lord-spawn-guard.md](../../docs/features/lord-spawn-guard.md) — vanilla `HeroSpawnCampaignBehavior.SpawnLordParty` ends at an unguarded `Settlement.All.First(x => x.Culture == hero.Culture)`. It takes TWO faults at once: a faction whose `InitialHomeSettlement` is null (a mod creating a clan without `SetInitialHomeSettlement`) AND a culture owning no settlement. `Patch65_LandlessCultureSpawnGuard` repairs the anchor instead of skipping the original. The crash report carries NO TAOM frame — absence of a TAOM patch on the stack does not exonerate TAOM's data |
| Work on the enlisted-service loop (serving under a lord), or debug a player stuck at the service wait menu | [enlistment.md](../../docs/features/enlistment.md) — the persisted state machine is the whole design: party presence (`IsActive`/`IsVisible`) is an OUTPUT asserted from state, never read back as state (the donor mod's root defect), the hourly reconciler is the ONLY terminal-decision authority (menus just render), and `DischargeService.Execute` is the ONLY path out of service — it restores presence BEFORE clearing the record, which is what makes a "record cleared, party still hidden" softlock unrepresentable. Menu redirection is `Patch66` |
| Understand why an enlisted player isn't being credited for something (renown, momentum, a career quest) | [enlistment.md](../../docs/features/enlistment.md) "Interactions" — fighting inside a lord's army breaks any system that attributes by party LEADER. `SpecialResources` hit this first and moved to `MapEvent.PlayerSide == WinningSide` participation; the #375 audit then fixed the WotR siege gate and two `CareerQuest` objectives the same way. Before adding a new leader-keyed reward, ask what it does during someone else's siege |
| Add or retune enlisted service content (duties, wages, ranks, merit bands) | `Main/_Module/ModuleData/enlistment/enlistment_config.json` + `enlistment_duties.json` — all data rows; the provider SKIPS a malformed row with a warning rather than silently defaulting (the M1 trap). 13 field duties run on 5 mechanics; flavour is data, mechanics are code. `Reuse.Singleton` provider ⇒ **full application restart**, not a save-load |
| Author or regenerate the per-rank service armour | `python tools/generate_enlistment_rosters.py` (dry-run default, `--apply`, `--seed-missing` is append-only so hand edits survive) then `python tools/audit_enlistment_roster_coverage.py`. Donors come from each culture's OWN troop tree, which is what makes the kit race-correct; ids use RUNTIME culture ids (`vlandia` = Rohan). A new equipment XML needs `SubModule.xml` registration + a **full game restart** before it loads |
| Promote a troop into a companion / debug battlefield promotions | [field-commission.md](../../docs/features/field-commission.md) — merit pools per troop TYPE not per soldier (the reason the bar sits at 32), is spent only on a COMPLETED promotion (declining, quitting, or a full retinue never burns it), the race allow-list fails closed, and the whole feature suppresses itself while enlisted because the fairness ratio inverts inside a lord's army. **To debug:** MCM → Battlefield Promotions → Promotion Diagnostics writes a `[FieldCommission]` trace of eligibility, merit accrual and every offer; `taom.fc_status` dumps live config + per-troop banked merit |
| Use agent teams | [agent-teams.md](../../docs/ai-includes/agent-teams.md) |
| Brief/spawn a subagent correctly | [agent-operating-manual.md](../../docs/ai-includes/agent-operating-manual.md) — execution model (can't invoke skills), tool catalog, what to recommend |
| Author a new culture's armor + troop tree (end-to-end) | [new-culture-authoring.md](../../docs/ai-includes/new-culture-authoring.md) — phases, helpers, color convention, iteration loops |
| Fix or add a town's tavern mercenaries (`<basic_mercenary_troops>`) | [tavern-mercenaries.md](../../docs/features/tavern-mercenaries.md) — the engine's 70/30 roll + upgrade walk, why hires are leaf `*_merc` copies (occupation drives ×2 recruit / ×1.5 wage), `tools/oneoff/generate_tavern_mercenaries.py` |
| Add or fix lord skills + traits (any culture, any canonical character) | [lord-skills-authoring.md](../../docs/ai-includes/lord-skills-authoring.md) — TAOM SkillSet system, archetype catalog, per-NPC override recipes, gotchas |
| Add a rideable creature mount end-to-end (assets → Monster/action/usage XML → C# BT → validation) | [creature-mount-authoring.md](../../docs/ai-includes/creature-mount-authoring.md): elephant+spider-distilled; warg = reference implementation; 1.4.6 lookup-hardening (total key coverage, no `CanAttack`, `actt_dash` jump start, 45-row jump tables); 18-gotcha index; `tools/audit_mount_parity.py`. **Only needed when the creature gets a rig of its own**; reskinning a vanilla skeleton is the cheap path, see the row below |
| Make a rideable mount out of an EXISTING vanilla skeleton (the cheap path) | [war-ram.md](../features/war-ram.md): the worked example, TAOM's first horse-skeleton reskin. Skin the meshes and every barding to the donor rig bone for bone and the Monster collapses to `base_monster` + `action_set` + tuning attributes: **Phases 1 to 5 of [creature-mount-authoring.md](../ai-includes/creature-mount-authoring.md) are skipped and no animation data is authored at all**, and inheriting the donor's Monster brings `Mountable`, `family_type=1`, its `monster_usage` and all twelve rein attributes (gotcha #18 sidestepped, not deepened). The cost is coupling: a reskin SHARES its action vocabulary with the engine, so grep the INHERITED usage set for every action the feature binds and check each candidate's `ActionCodeType` band before binding it, because "our code never fires this" no longer implies "nothing fires this". External-module data plane: [lotrlome-war-ram-changes.md](./lotrlome-war-ram-changes.md); the two clips this got wrong: [rca-war-ram-2026-08-28.md](../reviews/rca-war-ram-2026-08-28.md) |
| Replace/refine a mount's FBX/tpac assets | [creature-mount-authoring.md](../../docs/ai-includes/creature-mount-authoring.md) → "REPLACING FBX / TPAC FILES" — back up, 4 silent-break failure modes, **MANDATORY post-deploy gate `python tools/verify_mount_assets.py <creature>`**; skeleton-drop fix = re-BUNDLE via `tpac_skeleton_inject.py` (a standalone skeleton tpac CRASHES the engine) |
| Refine/author creature locomotion or rider clips in Blender (not data/XML) | [creature-animation-blender-mcp-workflow.md](../../docs/ai-includes/creature-animation-blender-mcp-workflow.md) — Blender 5.1.2 slotted-action API, `harness.py` toolkit, gait biomechanics, the `quad_movement` Kit-compile boundary |
| Create a craftable weapon (FBX → tpac → 4 XML files, by hand) | [weapon-creation-workflow.md](../../docs/ai-includes/weapon-creation-workflow.md) — manual Step A–Z; per-piece schema, `bo_` collision convention, brace/couch/pike, **bows/shields = no decimals** rule. Automated alternative: [weapon-xml-pipeline.md](../../docs/features/weapon-xml-pipeline.md) |
| Plan future GameModel overrides | [roadmap.md](../../docs/roadmap.md) |
| Add or update translations | [TRANSLATOR_GUIDE.md](../../docs/localization/TRANSLATOR_GUIDE.md) + [tools/README.md](../../tools/README.md#localization-pipeline) |
| Understand MBSubModuleBase lifecycle or Harmony patch registration | [submodule-lifecycle-and-harmony.md](../../docs/reference/engine/submodule-lifecycle-and-harmony.md) — when callbacks fire, patch kinds (Prefix/Postfix/Transpiler), deferred-apply gotcha, managed vs native boundary |
| Understand the campaign→mission seam (encounters → battles) | [campaign-to-mission-bridge.md](../../docs/reference/engine/campaign-to-mission-bridge.md) — `EncounterManager`→`MapEvent`→`MissionState.OpenNew`, the single managed↔native handoff; AI auto-resolve without a Mission |
| Understand campaign object graph (Hero/Clan/Kingdom/MobileParty/Settlement) | [campaign-object-graph.md](../../docs/reference/engine/campaign-object-graph.md) — sealed types, nav-property gotchas, `Settlement.Culture` not engine-saved, castle `.Village==null` NRE |
| Debug agent spawn crashes / non-humanoid creature spawn / `AddSkinMeshes` | [agent-spawn-and-render-pipeline.md](../../docs/reference/engine/agent-spawn-and-render-pipeline.md) — `FromCharacterObj` vs `FromHorseObj` (skips skin meshes), `AgentBuildData`, `AgentVisuals` |
| Understand mount/rider runtime or creature seating (howdah, riderless spider) | [mount-and-rider-runtime.md](../../docs/reference/engine/mount-and-rider-runtime.md) — two-phase `EventControlFlag` mount, `RiderSitBone`, `UsableMachine` howdah `StandingPoint` seating |
| Understand formation geometry, team AI, or `AutoGenerated.dll` DivideByZero | [formations-and-team-ai.md](../../docs/reference/engine/formations-and-team-ai.md) — count-division sites (lines 756/1295/1428/1449), `_MT` threading, spider DivideByZero lead |
| Work out which formation a lord or troop fights in, or stop a race/culture from being mounted | [formations-and-team-ai.md](../../docs/reference/engine/formations-and-team-ai.md) "Which formation a spawned agent joins": `default_group` is the formation for TROOPS but is ignored for HEROES (`CharacterObject.GetFormationClass` reads live `BattleEquipment` instead), so audit the Horse slot, not the attribute; `EquipmentIndex.Horse == ArmorItemEndSlot == 10`; unused `GetAgentTroopClass_Override` hook. Data gate: `MOUNTED_DWARF` in [moduledata-validation.md](../../docs/features/moduledata-validation.md), which since #515 allowlists **exactly two ids** (`taom_war_ram_a`, `taom_war_ram_b`, pinned in `tools/taom_schema.py` rather than prefix-matched) across both halves of the check, so a dwarf on a war ram is legal and a dwarf on anything else still errors |
| Understand DailyTick / CampaignTime / party AI / staggered AI ticks | [campaign-tick-time-and-party-ai.md](../../docs/reference/engine/campaign-tick-time-and-party-ai.md) — heartbeat loop, `CampaignTime` struct, `TickPartialHourlyAi` stagger |
| Find out where a town's market gold actually goes, or why a caravan is parked | [economy-diagnostics.md](../../docs/features/economy-diagnostics.md) — `taom.print_town_ledger` (per-flow drain attribution) + `taom.print_caravans` (which engine gate holds it). No engine code logs either |
| Understand settlement food / prosperity / hearth / caravans (why garrisons starve) | [settlement-economy-food-prosperity.md](../../docs/reference/engine/settlement-economy-food-prosperity.md) — the food math, village caps, prosperity death-spiral, + the `TaomSettlementFoodModel` Troop-Weight fix |
| Understand the quest/issue system, or convert vanilla quests to LOTR | [issue-and-quest-system.md](../../docs/reference/engine/issue-and-quest-system.md) (engine A-to-Z) + [lotr-issues.md](../../docs/features/lotr-issues.md) (**IMPLEMENTED** 2026-06-20 — 43 vanilla issues suppressed + replaced via XML-config + 3 generic templates) |
| Browse all engine process docs | [docs/reference/engine/](../../docs/reference/engine/) — full arc: campaign heartbeat → object graph → encounter seam → mission lifecycle → agent spawn → formation/team AI → mount/rider → combat stats → usable machines → UI → save/object system → GameModel → scene/script → campaign behaviors → items → module integration → settlement economy |

---

| Touch supply caravans / orders / the order screen | [supply-lines.md](../../docs/features/supply-lines.md) |
| Touch field camps / ambush / lookout / camp visuals | [field-camp.md](../../docs/features/field-camp.md) |
| Touch refuges / wardens / refuge militia / defence bonus | [refuge.md](../../docs/features/refuge.md) |

<!-- backlinks-start auto-generated; edit lint_docs.py / build_backlinks.py to change -->

## Referenced by

- [docs/reference/release-process.md](./release-process.md)
- [docs/reviews/adopt-graphify-v8-2026-08-18.md](../reviews/adopt-graphify-v8-2026-08-18.md)

<!-- backlinks-end -->
