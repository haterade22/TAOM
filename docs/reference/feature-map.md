# Feature & Component Map

> Full `Main/Features/` + component path map. Extracted from CLAUDE.md 2026-07-18 (Tier 2 restructure). The crash-time traps stay inline in CLAUDE.md "Key Paths".


| Component | Path |
|-----------|------|
| Mod code | `Main/` (.NET Framework 4.7.2) |
| Mod tests | `TAOM.Tests/` (MSTest + NSubstitute) |
| Features | `Main/Features/` |
| Adapters | `Main/Adapters/` |
| BannerColorPersistence | `Main/Features/BannerColorPersistence/` (Patch23 + Patch24). Patch23 makes the party leader's CLAN colours win over the kingdom colour on the UI surfaces and, via the `Mission.SpawnAgent` prefix, on battlefield armour tint; it reads `clan.Color`/`clan.Color2` with no `MapFaction` hop. Patch24 blocks vanilla kingdom-sync for the player clan and pushes a ruling clan's colours onto its kingdom banner. 7 flags in `configs/banner_color_config.json`. See [banner-color-persistence.md](../features/banner-color-persistence.md) |
| BannerInjection | `Main/Features/BannerInjection/` re-stamps `banner_key` from XML/XSLT onto kingdoms and clans on new game and save load; skips ruling clans and player-edited banners. See [banner-injection.md](../features/banner-injection.md) |
| Clan heraldry data | `Main/_Module/ModuleData/clan_heraldry/*.json` + `tools/generate_clan_heraldry.py`, per-clan `color`/`color2` and `default_party_template`. Colours are authored in the spec; `template_id` and `roster` are synced FROM the live files by `tools/sync_clan_specs_from_live.py` (#589), so re-applying a spec is a no-op and a stale one is refused. See [clan-heraldry.md](../features/clan-heraldry.md) |
| Core | `Main/Core/` |
| CharacterCreation | `Main/Features/CharacterCreation/` |
| AtmospherePersistence | `Main/Features/AtmospherePersistence/` |
| AdvancedCombat | `Main/Features/AdvancedCombat/` (SpatialGrid, BoneCollision, CustomAttacks) |
| CombatMechanics | `Main/Features/CombatMechanics/` (combat feel pack via `TaomCombatMechanicsModel` — crush-through/cleave/unstoppable/charge-knockdown/shield-pen + race modifiers; 4 pure services + validated config `combat_mechanics/combat_mechanics_config.json`; see the GameModel table row + `docs/features/combat-mechanics.md`) |
| CompanionTactics | `Main/Features/CompanionTactics/` (Patch35): equipment-based role badges on party and Order of Battle tooltips; Order of Battle overlay with Assign Heroes (role-matched captains through `HeroAutoAssigner.PlanCaptains` and vanilla's accept-captain path) and name-only presets (MCM `EnableFormationPresets`, off by default); display-only battle action bar. See [companion-tactics.md](../features/companion-tactics.md) |
| DreadAura | `Main/Features/DreadAura/`: Nazgûl and Sauron drain nearby enemy morale until formations break. One `MissionLogic`, **no GameModel override**: it CALLS `BattleMoraleModel.CalculateMoraleChangeToCharacter` for tier/hero resistance rather than overriding it. Sources are keyed by hero StringId (the `nazgul_nine` set; the Nine are race `nazghul` since #644, but the set names exactly them) OR race (`sauron`). Config `dread_aura/dread_aura_config.json`; see [dread-aura.md](../../docs/features/dread-aura.md) |
| SignatureStrikes | `Main/Features/SignatureStrikes/`: a configured hero's melee hit (any weapon except thrown or ranged) carries effects keyed on the swing direction: an overhead Slam (guaranteed knockdown on the struck agent, a boulder-style ring of knockdown + damage, a fear burst; also on a ground hit) and a side-swing Sweep (knock-back stagger on the struck agent and the ring in front). One `MissionLogic` on `OnMeleeHit` (enqueue) + `OnMissionTick` (ring), two thin verdicts on `TaomCombatMechanicsModel`, **no Harmony patch**. Long per-kind cooldowns (20 s / 12 s) plus a live MCM multiplier. Shipped for `lord_1_17` / race `sauron`, and (#645) the Nine: `nazgul_nine` hero set / race `nazghul`, a SCREAM on the overhead and both side swings (ring around the wraith, damage + stagger + 25 morale, one 15 s timer, a module sound). One `signatures` list in config `signature_strikes/signature_strikes_config.json`; see [signature-strikes.md](../../docs/features/signature-strikes.md) |
| CulturalFeats | `Main/Features/CulturalFeats/` (TaomCulturalFeats, 16 GameModel overrides) |
| AiPartySize | `Main/Features/AiPartySize/` (#461: opt-in scaling, NEUTRAL by default (1.0x / +0 / garrison 1.0 / relief 0), that when raised through MCM lets AI lord parties HOLD their spawned roster instead of being shed back to the vanilla 50-150 cap in one tick. One service consumed by three EXISTING models: `TaomPartySizeModel` (cap + garrison override), `TaomFoodConsumptionModel` and `TaomPartyWageModel` (relief, because morale desertion ignores the cap). No new patch. **Must run before the TroopWeight penalty** or the shed trims to the unscaled limit; MCM-only knobs; see [ai-party-size.md](../features/ai-party-size.md)) |
| CustomBattles | `Main/Features/CustomBattles/` (Custom battle factions, commanders, troops) |
| Arena | `Main/Features/Arena/` (TaomTournamentModel — per-participant culture armor) |
| MainMenuCustomizer | `Main/Features/MainMenuCustomizer/` (hide Campaign, rename Sandbox → "Enter The Age Of Men") |
| MenuLinkColors | `Main/Features/MenuLinkColors/` (Patch64 — game-menu hyperlinks recoloured by the linked object's culture; 20 `Link.Taom.*` styles in `GameMenu.InfoText`; colours live only in `Main/_Module/GUI/Brushes/GameMenu.xml`; see `docs/features/menu-link-colors.md`) |
| SettlementNameplateFade | `Main/Features/SettlementNameplateFade/` (Patch38 postfix on `SettlementNameplateWidget.DetermineTargetAlphaValue`: multiplies the plate's target alpha by a distance fade, MCM near/far band; since #591 it first applies the relation floor from SettlementNameplateRelation. See [settlement-nameplate-fade.md](../features/settlement-nameplate-fade.md)) |
| SettlementNameplateRelation | `Main/Features/SettlementNameplateRelation/` (#591: `TaomSettlementPlateWidget` is the `SettlementNameplateLayout` container in the three nameplate prefabs; it colours the parchment bar, name text and diamond frame from the VM's `Relation` int and mirrors the item widget's alpha and colour factor onto the bar and frame, which `Widget.Render` never passes to children. Plate opacity by relation and the tint strength are MCM sliders in `Map UI/Settlement Nameplates` (#596, live), applied through the Patch38 postfix and a static settings provider on the widget. Colours are `#RRGGBBAA` attributes on the widget. See [settlement-nameplate-relation.md](../features/settlement-nameplate-relation.md)) |
| FiefGranting | `Main/Features/FiefGranting/` (Patch70 swaps vanilla's `SettlementClaimantDecision` for `TaomSettlementClaimantDecision`, which multiplies vanilla's merit by concentration / siege-participation / absent-from-siege / culture-fit / ruler terms and denies the King's Vote to a ruler already holding most of the kingdom (#458). Merit weights the election heavily; the 1/2/3-point ballot decides it, and the earlier "all three finalists tie" claim was refuted in review (#460). Since #565 participation is `FiefGrantingCampaignBehavior`'s per-clan record of the winning assault (share of `ContributionToBattle`, persisted, kept only for a claim-opening capture, cleared on any other transfer; the engine-derived rules are in `FiefSiegeCaptureRules`), not the sticky `Town.LastCapturedBy` stamp, and the player is scored like any clan by default. Ten MCM knobs, no ModuleData copy. Data half: `tools/apply_starting_fief_spread.py` split Lasgalen 7/7, Imladris 5/5 and Lothlorien 4/4 across their clans. See [fief-granting.md](../../docs/features/fief-granting.md)) |
| LordSpawnGuard | `Main/Features/LordSpawnGuard/` (Patch65 — vanilla `SpawnLordParty` ends with an unguarded `Settlement.All.First(x => x.Culture == hero.Culture)`; a landless culture on a lord whose faction has no `InitialHomeSettlement` CTDs the daily clan tick (#374). Prefix anchors the faction, `InvalidOperationException`-only finalizer backstops it. Data half: the Khand cluster retagged khuzait → battania so Variag owns land; `LANDLESS_CULTURE` validator check stops the regression. See [lord-spawn-guard.md](../../docs/features/lord-spawn-guard.md)) |
| StaleCharacterRepair | `Main/Features/StaleCharacterRepair/` (Patch83: a character the save restored under an id current ModuleData no longer defines is built by `GetUninitializedObject` and never reached by the XML pass that would fill it in, so several fields stay null and the engine derefs them unguarded. `CharacterObject` persists only 2 fields. Reported crash was `BasicCharacterObject.GetSkillValue` on save load (bundle 065939b6), but `UpgradeTargets` (null, an auto-property initializer) crashes the party screen at `PartyCharacterVM:1113` and `BodyPropertyRange` crashes agent spawn, so the repair fills all four rather than moving the crash. Postfix on `MBObjectManager.PreAfterLoad`, five outcomes not a bool, in-game notice because the stale ids ride into every future save. See [stale-character-repair.md](../../docs/features/stale-character-repair.md)) |
| MapEventGuard | `Main/Features/MapEventGuard/` (Patch82 restores the engine's own pairing of `MapEvent.BattleObserver` with `MapEvent.TroopUpgradeTracker` before `SimulateBattleSetup` reads it. Four unguarded dereferences in `MapEventSide` lean on that pairing; removing the MAIN party from a live map event nulls the tracker for good AND re-exposes the event to `MapEventManager.Tick`, so the CTD lands later on a pure-vanilla stack (#551). Prefix clears the dangling observer, which is what a `BattleSimulation` constructor leaves behind when it throws. See [map-event-guard.md](../../docs/features/map-event-guard.md)) |
| MapLoadDiagnostics | `Main/Features/MapLoadDiagnostics/`: `Patch89` (four categories, applied separately from `OnSubModuleLoad`) writes a `[MapLoad]` heartbeat every 5 seconds on the campaign map and a sequence-numbered lifecycle trace of state pushes, the map state and screen seams, and every raise of the loading window, and every lower that actually took it down, with its caller chain. Diagnosed the v1.5.0 map-load stall on its first run. See [map-load-diagnostics.md](../features/map-load-diagnostics.md) |
| ReturnToArmy | `Main/Features/ReturnToArmy/` (Patch87: vanilla's "Return to Army" only leaves a village and hides "Leave" for every non-leader army member, so a member who is in the army but not merged into it (`AttachedTo == null`, the state a Player Switcher takeover of a lord marching to join an army inherits) is stuck in any town or castle. The prefix runs vanilla's own Leave for that one row and vanilla for every other, #566. Pure decision in `ReturnToArmyRules`. See [return-to-army.md](../../docs/features/return-to-army.md)) |
| LordPartyTemplates | `Main/Features/LordPartyTemplates/` (Patch88: a lord named in `lord_party_templates.json` fields his own party template instead of his clan's. The engine has no per-hero template: both roster reads go through `Clan.DefaultPartyTemplate`, so a postfix on that getter swaps the result only while a lord spawn is in flight, marked by scope patches on `SpawnLordParty` and `InitializeLordPartyProperties`. Faramir fields Ithil Guard rangers, Sauron a Black Numenorean and Uruk host; their clanmates are untouched, #580. See [lord-party-templates.md](../../docs/features/lord-party-templates.md)) |
| ShaderPrecompilation | **PARKED 2026-09-25:** menu option and Patch21 commented out in `SubModule.cs`, both MCM attribute stacks in `TaomSettings.cs`; re-enable = uncomment all three; previously re-enabled 2026-09-11 (#560) after the 2026-08-20 park. `Main/Features/ShaderPrecompilation/` holds the main-menu shader pre-compile walk: every loaded character in batches of 1,000 (equipment shaders compile at preload, so coverage is roster membership), then the TAOM `_forceatmo` scenes only when the MCM opt-in `EnableShaderPrecompileScenePasses` is on (off by default; the scene loads crash some GPUs, #287); crash-skip guard for scenes. See [shader-precompilation.md](../../docs/features/shader-precompilation.md) |
| PartyIconScale | `Main/Features/PartyIconScale/`: `Patch53` transpilers swap the three `0.3f` scale literals (leader figure in `SandBoxViewHelpers+MobilePartyVisualHelper.GetHumanAgentPartyVisual` since v1.5.0; mount and human world frame in `MobilePartyVisual.AddCharacterToPartyIcon`) for `PartyIconScaleConfig.GetScale()`, so map party icons honour the MCM "Map Figure Scale" slider (default 0.15 = half vanilla). See [party-icon-scale.md](../../docs/features/party-icon-scale.md) |
| SkipCampaignIntro | `Main/Features/SkipCampaignIntro/` — `Patch58` Prefix on `SandBoxGameManager.OnLoadFinished` skips the vanilla campaign intro video on a NEW game → straight into character creation; save-loads untouched, fail-safe to vanilla; hardcoded always-skip, no MCM toggle. See [skip-campaign-intro.md](../../docs/features/skip-campaign-intro.md) |
| NativeSkinFixes | `Main/Features/NativeSkinFixes/` — managed P/Invoke wrapper for `TAOM.NativeSkinFixes.dll` (covers_head morph + hair/beard cloth). **PARKED 2026-07-08 — OFF by default, DISABLED at the wiring level**; re-enable = uncomment the install branch in `SubModule.cs` + flip the default back to `true`. See [native-skin-fixes.md](../../docs/features/native-skin-fixes.md) |
| DevConsole | `Main/Features/DevConsole/` — the `taom.*` console command contract: `TaomConsole` dispatch shell (cheat gate → help → catch-all), three `DevConsoleGuard` gates (campaign / mission-incl-custom-battle / cheat-mode-only), pure `DevConsoleArgs` parsers, and a startup `DevConsoleDiscoveryAudit` that asks the engine whether it registered our commands. Per-feature commands live in `Main/Features/<X>/Cheats/`. See [dev-console.md](../../docs/features/dev-console.md) |
| SiegeDefense | `Main/Features/Siege/` (timed defense events when watched factions are besieged; config-driven watched factions, CampaignTime deadline, relation+influence reward on arrival) |
| SpecialResources | `Main/Features/SpecialResources/` (11 resources across 18 kingdoms — War Spoils/Gems/Castar/Marks/Elven Wine/Lake Fish/War Drums/Tribal Relics/Dunlending Ale/Plunder/War Banners; XML-driven with many-to-one kingdom/culture mappings, shared balance, pending transaction upgrades, desertion at 0, notifications, Patch26, composite `heroId:resourceId` storage). Earning routes through the pure `SpecialResourceEarnPolicy`: a win is `MapEvent.PlayerSide == MapEvent.WinningSide` — participation, not command, because the old "player IS the winning side's `LeaderParty.LeaderHero`" gate paid **zero** to anyone fighting inside another lord's army, in single-player as much as co-op — and all 5 earn paths are suppressed on a dedicated server, whose `Hero.MainHero` is the idle world-gen hero nobody plays. Since 2026-09-11 (#558) one `GetDailyBreakdown` feeds the tick, the tooltip, the daily toast, the map-bar flag and the console dump; every outflow toasts the measured debit; and only a row with `daily_upkeep > 0` is an upkeep troop, so the 50 merchant-only emissary rows never desert. Since 2026-09-13 (#590) the encyclopedia troop tree badges the troops that cost a resource to upgrade, recruit or keep, through `EncyclopediaUnitBadgeMixin` on `EncyclopediaUnitVM` and the `EncyclopediaUnitTreeNodeItem.xml` clone |
| CareerSystem | `Main/Features/CareerSystem/` — career/class progression: 50 XML-driven careers across 16 cultures (passives wired into GameModels, ability system + in-battle energy bar injected into Native's AgentStatus HUD, keystone exclusivity + per-career glyphs, damage attribution, UIExtenderEx career screen, SyncData persistence, CC career selection + archetype-driven starting equipment). See [career-system.md](../../docs/features/career-system.md) |
| SettlementGuards | `Main/Features/SettlementGuards/` (per-settlement guard customization — XML-driven guard troop pools with settlement→clan→culture fallback, spawn-point filtering, weighted random selection, per-culture spear mapping; Harmony prefixes on private GuardsCampaignBehavior methods) |
| HeroRace | `Main/Features/HeroRace/` (non-human race rendering: per-race monster base, action-set resolution, save/load race persistence with a name legend, Save/Load-preview CTD guard, and per-race UI framing. Framing offsets live in `ModuleData/configs/CharacterAvatarPatch.json` (3D tableau, `Patch72`) and `CharacterImagePatch.json` (2D portraits, `Patch4`), owned by one `RacePositionStore` and tunable in-game via five `taom.*_race_offset*` commands. Dwarf eye height is an MCM slider. See [hero-race.md](../features/hero-race.md)) |
| Refuge | `Main/Features/Refuge/` (movable player base raised from a ready field/fortified camp: garrison/prisoner/stash storage via vanilla screens, warden (companion or promoted soldier, NEVER killed on dismantle), Refuge->Stronghold tiers, defender damage reduction through the combat MODEL chain (no Harmony combat patches), persisted militia bookkeeping, clan-screen listing + click-to-manage via `Patch75`, raids off by default; definer base 726901201. See [refuge.md](../features/refuge.md)) |
| PlayerSwitcher | `Main/Features/PlayerSwitcher/`: pick an existing lord at character creation and play as them (#514). The design is one number: the handover runs from an `ICharacterCreationContentHandler` at priority **1100**, after TAOM's own 1050, so every grant and every `ApplyFinalEffects` side effect has already landed on the throwaway creation hero and clan that the handover then deletes. `Patch77` only attaches the picker panel, and `Patch78` skips the six backstory menus straight to the career choice once a lord is picked, because those answers only ever applied to the hero being discarded. Two reflection sites, both probed. No `SaveableTypeDefiner`. See [player-switcher.md](../features/player-switcher.md) |
| TimeAcceleration | `Main/Features/TimeAcceleration/` three rebindable campaign-map time tiers (Space / E / Ctrl+Space, GameKey ids 500 to 502 under Options > Campaign Map), three MCM multiplier sliders, and the MapBar Extra Fast Forward button. Both MapBar fast-forward buttons route through `ITimeAccelerationService` (#574), so a click writes `Campaign.SpeedUpMultiplier` like the keys do. See [time-acceleration.md](../features/time-acceleration.md) |
| UncapturableHeroes | `Main/Features/UncapturableHeroes/`: Sauron and the Nine are never taken prisoner; they escape as fugitives. Two hooks in `Patch76` and **no escape code**: denying `Hero.CanBecomePrisoner` lets vanilla's own `MapEvent` fall-through apply `MakeHeroFugitiveAction`, and a `TakePrisonerAction.Apply` prefix covers capture inside a settlement that changes hands. Identity is `heroSets: nazgul_nine` (the race rule lists only `sauron`; the Nine are race `nazghul` since #644 but come in through the set); does NOT block death. Config `uncapturable_heroes/uncapturable_heroes_config.json`; see [uncapturable-heroes.md](../features/uncapturable-heroes.md) |
| FieldCamp | `Main/Features/FieldCamp/` (campaign-map camps: field/fortified with morale + foraging, terrain-gated ambush and lookout; timestamp-derived build progress; authored tpac visuals (`Main/_Module/AssetPackages/`, first in repo) with the source's vanilla-mesh fallback chain; map overlay Make Camp button on the Momentum MapView template; `Patch74` nameplate icon; lookout range through `TaomMapVisibilityModel`'s contributor seam, never a second AddModel; definer base 726901101. See [field-camp.md](../features/field-camp.md)) |
| SupplyLines | `Main/Features/SupplyLines/` (resupply convoys ordered from towns/castles/friendly lords: goods from real market stock (deducted), troops via the alignment-gated volunteer model, escorts, live price breakdown; a live search box finds any trade good across every orderable market and lands on its source (#587); full-screen Gauntlet order UI on the `[GameStateScreen]` attribute path (no CreateScreen patch); caravan is TAOM's first custom PartyComponent, teleport-movement by design with the Bearing binding CI-pinned; definer base 726901001. See [supply-lines.md](../features/supply-lines.md)) |
| NamedCompanions | `Main/Features/NamedCompanions/` (18 lore companions as recruitable wanderers — Aragorn/Legolas/Gimli/etc; `is_hero="true"` + `occupation="Wanderer"`, JSON config for spawn settlements, vanilla dialog integration, race persistence via existing HeroRace system) |
| BlackNumenoreans | Data-only, no `Main/Features/` module. Mordor's human line and its only horse cavalry: 13 troops T5-T9 in `Main/_Module/ModuleData/troops/troops_mordor.xml` (`mordor_num_*`, levels 26/31/36/41/46) plus 113 Armory entries under `LOTRLOME_items/mordor/` and the two global weapon/shield files. Troops carry **no** `race=` attribute (absent deserializes to index 0 = `human`; TAOM never writes `race="human"`). Standalone elite line: `taom_spcultures.xml` untouched, no volunteer pool, so `mordor_num_` is exempted in `VolunteerRecruitmentServiceTests.IsIntentionallyUnrecruited` and since #584 (2026-09-13) the troops arrive through exactly four templates: the two house clans (`clan_empire_south_1` Dolgubeth, `clan_empire_south_9` Wawrim), Sauron's per-hero template, and the vassal reward (`BlackNumenoreanPartyTemplateTests` forbids a fifth). Four generators under `tools/generate_black_numenorean_*` / `apply_` / `wire_`. See [black-numenorean.md](../../docs/features/black-numenorean.md) |
| TroopWeight | `Main/Features/TroopWeight/` (`Patch17_TroopWeight`, six patches: the shed-on-upgrade guard plus five display surfaces). A per-troop-id weight from `Main/_Module/ModuleData/TroopWeights/troop_weights.xml` (105 live rows; unlisted troops weigh 1.0, no level table in code) that `TaomPartySizeModel` turns into a party-size-limit deflation of `weighted - raw` slots (`ComputeSizePenalty`, clamped to `baseLimit - 1`); every count reads raw everywhere. Since #585 (2026-09-13) the file follows a level rule, 3.0 at level 41 and above, 2.0 below, mount packages apart, pinned by `TroopWeightLevelBandTests`. MCM toggle `EnableTroopWeight` (`RequireRestart = false` is load-bearing). See [troop-weight-system.md](../../docs/features/troop-weight-system.md) |
| KingdomVoices | Data-only, no `Main/Features/` module. Per-race combat voice sets: `Main/_Module/ModuleData/lotr_{dwarf,uruk,uruk_hai}_voice_def.xml` (56 slots each) + 249 `<module_sound>` entries + 436 clips under `Main/_Module/ModuleSounds/`. Registration is `project.mbproj`, NOT `SubModule.xml`. Voice binds to **race**, never culture, through `<voice_types>` in **LOTRLOME_Armory's** unversioned `skins.xml`. 7 of 14 races bound, 2 of those have an unbound adult female skin that falls back to a vanilla voice. FMOD loose-file path (no bank authoring). See [kingdom-voices.md](../../docs/features/kingdom-voices.md) |
| RevoltTuning | `Main/Features/RevoltTuning/` (JSON-tunable soft-nerf of vanilla revolt mechanic for LOTR's frequent settlement flips; raises loyalty thresholds + dampens different-culture penalties; semantic validation rejects out-of-range / sign-flipped values; consumed by `TaomSettlementLoyaltyModel`) |
| SettlementFood | `Main/Features/SettlementFood/`: `TaomSettlementFoodModel` + MCM/JSON food knobs (consumption divisors, base/village/flat production, storage caps) plus the prosperity-scaled `hinterlandFoodPerProsperity` production term, which must stay strictly below `1/prosperityFoodDivisor`. **Ships TUNED, not vanilla (#546)**: 70 of 72 towns previously started food-negative and starved their own garrisons at 10%/day. The original Troop-Weight garrison correction is an inert no-op since the 2026-07-11 rework. See [settlement-food.md](../../docs/features/settlement-food.md) |
| PrisonerRecruitment | `Main/Features/PrisonerRecruitment/` — prisoner morale waiver via `TaomPrisonerRecruitmentCalculationModel`: same culture, or same non-Neutral side (reuses Execution's `IAlignmentService`), returns 0 not vanilla −1/−2. No Harmony; MCM-gated; bandits never waive. See [prisoner-recruitment.md](../../docs/features/prisoner-recruitment.md) |
| AdvancedStartOptions | `Main/Features/AdvancedStartOptions/`: a `[StartOptionsProvider]` static method (engine-discovered, no Harmony) adds TAOM's fourteen kingdoms to every Advanced Starting Options faction picker, removes the United Empire scenario, and 49 string overrides re-localise the menu for the renamed vanilla kingdoms: the 26 menu-facing rows in `global_strings.xml` (the only file the main-menu `GlobalTextManager` reads), re-applied by `Main/Features/LocalizationOverride/GlobalStringsOverrides.cs` so a TAOM row replaces Native's same-id entry instead of trailing it (#604), and the 23 in-game value-name rows in `taom_module_strings.xml`. Bannerlord v1.5.x. See [advanced-start-options.md](../features/advanced-start-options.md) |
| AlignmentRecruitment | `Main/Features/AlignmentRecruitment/` — blocks volunteer recruitment at enemy-aligned settlements (Free vs Evil, kingdom StringId) via a single `TaomVolunteerModel` -1 override, no Harmony; Symmetric/GoodRejectsEvil modes + independent player/AI MCM gates. Issue #286. See [alignment-recruitment.md](../../docs/features/alignment-recruitment.md) |
| NavalTravel | `Main/Features/NavalTravel/` — sail the open sea without the Naval DLC (player-initiated via sail key, sea-only), #296. **PARKED 2026-06-26 — DISABLED at the wiring level** (no naval navmesh on TAOM_Map, #120); RE-ENABLE = uncomment the 3 `SubModule.cs` blocks + flip the `enabled` defaults back to true. See [naval-travel.md](../../docs/features/naval-travel.md) |
| MarriageAlignment | `Main/Features/MarriageAlignment/`: a Free-aligned hero cannot marry an Evil-aligned one (Boromir wed a Misty Mountain orc, #542). Blocks in `TaomMarriageModel.IsCoupleSuitableForMarriage`, the chokepoint for every path, keyed on CULTURE not kingdom; `Patch81` transpiler narrows the AI partner-clan draw so Free clans keep their marriage rate. Neutral marries anyone. Future marriages only. See [marriage-alignment.md](../../docs/features/marriage-alignment.md) |
| WandererAllegiance | `Main/Features/WandererAllegiance/`: a wanderer refuses to be hired across the Free/Evil line (#575, Aragorn serving Mordor). Two condition-gated NPC lines on vanilla's `companion_hire` token at priority 110 pre-empt the hire reply; no Harmony patch. Keyed on the wanderer's CULTURE and the player's kingdom-then-culture; Neutral serves anyone; symmetric; hire-block only. MCM `World / Wanderer Allegiance`. See [wanderer-allegiance.md](../features/wanderer-allegiance.md) |
| NazgulFamily | `Main/Features/NazgulFamily/`: the nine Ringwraiths (Witch-King `lord_1_15`, Khamûl `lord_1_48`, + 7) take no spouse/parents/children: `heroes.xslt` data strip + `TaomMarriageModel` runtime marriage block + `NazgulFamilyBehavior` legacy-save clear; no MCM toggle. `TaomMarriageModel` is shared with MarriageAlignment (single model slot). See [nazgul-family.md](../../docs/features/nazgul-family.md) |
| Messengers | `Main/Features/Messengers/` — paid messenger dispatch (encyclopedia + dialog); N-day travel, arrival conversation with settlement-vs-field routing, ambush rolls, primitive-dict SyncData, UIExtenderEx `EncyclopediaHeroPage` button; LOTRAOM port. See [messengers.md](../../docs/features/messengers.md) |
| QuickActions | `Main/Features/QuickActions/` — 4-option inventory "Sell All" menu (Sell Damaged / Sell Low Value / Unequip All / vanilla via thread-static bypass) + per-save inventory-search toggle; `IInventoryVMAdapter` shared with EquipPresets. See [quick-actions.md](../../docs/features/quick-actions.md) |
| CultureDoctrine | `Main/Features/CultureDoctrine/`: each culture's AI fights field battles with its own doctrine. At `EarlyStart` (after `MissionCombatantsLogic.EarlyStart`) one `MissionLogic` replaces every AI team's vanilla tactic list with the list `culture_doctrine/culture_doctrines.json` authors for the side's majority culture: nine vanilla `TacticComponent` subclasses that scale `GetTacticWeight` by a per-culture multiplier, plus eleven TAOM tactics on a shared `TaomTacticBase` (`ShieldWall`, `TwoLineWall`, `InfantryMass`, `Envelop`, `CavalryDominance`, `EoredScreen`, `ArcherRing`, `ArcherAdvance`, `DisciplinedLine`, `HitAndRun`, `MumakVanguard`) driven by pure plan tables and weight functions over six TAOM `BehaviorComponent`s (a braced wall holding and advancing, a foot charge at the enemy foot that squares up against horse, a cavalry cycle charge, a throwing-infantry skirmish, an enveloping wing) plus volley control; every foot behaviour targets through `TargetSelection` (nearest infantry, never horse while foot stand) and the `engagement` block of the JSON sets the cavalry, high-ground and hold distances. Below the tactics: per-culture morale (never-rout, bravery) through two battle morale models, a per-soldier aggression post-pass on the agent-stat models, and troop-to-formation routing through `GetAgentTroopClass_Override`. **No Harmony patch**; everything past `EarlyStart` runs on the async AI thread with immutable state. MCM `Battle Tactics/Culture Doctrine` (off until the A/B passes) with morale and aggression sub-toggles, `taom.tactic_status`. #608 (2026-09-16, Phase C and D 2026-09-17). See [culture-doctrine.md](../features/culture-doctrine.md) |
| MissionPerf | `Main/Features/MissionPerf/`: `[MissionPerf]` frame-time heartbeat every 5 s of wall clock in every mission (frames, fps, avg/p95/max ms, agent and formation counts, GC counts), the measurement a battle-AI change is judged against. Toggle on the Battle Load Diagnostics page. See [mission-perf-heartbeat.md](../features/mission-perf-heartbeat.md) |
| SmartCavalryAI | `Main/Features/SmartCavalryAI/` — player-commanded cavalry hit-and-run line-charge state machine (`ICavalryChargeService` + `Patch31_FormationSetMovementOrder` prefix/postfix, recursion-guarded, dwell budgets, vanilla-charge hand-off; adapters wrap TaleWorlds APIs). v2 in #586 (2026-09-13). See [smart-cavalry-ai.md](../../docs/features/smart-cavalry-ai.md) |
| BannerBearers | `Main/Features/BannerBearers/` — formations raise their standard; bearers keep their race. Drives the engine's native `BannerBearerLogic` (vanilla banners only hero-captained formations) via `TaomBattleBannerBearersModel` + one `OnTeamDeployed` `SetFormationBanner` call — **deployment-only; a later call freezes bearers**. See [banner-bearers.md](../../docs/features/banner-bearers.md) |
| CultureMarketplace | `Main/Features/CultureMarketplace/` — daily LOTRLOME item injection into town markets keyed to owner culture (K=6 weighted draws per `DailyTickSettlementEvent`, per-town roster cap, conquest shifts market identity next tick; no Harmony, no SyncData). See [culture-marketplace.md](../../docs/features/culture-marketplace.md) |
| CultureConversion | `Main/Features/CultureConversion/` — conquered fiefs + bound villages gradually adopt the new owner's culture after a hold-timer: `Settlement.Culture` flip persisted via SyncData (not engine-saved), notable replacement (#325), converted-settlement recruitment branch, MCM "Culture Conversion". See [culture-conversion.md](../../docs/features/culture-conversion.md) |
| Enlistment | `Main/Features/Enlistment/` (+ `Content/`, `Duties/`, `Equipment/`, `Hooks/`, `Presentation/`): serve as a common soldier in a lord's party (#375): persisted 8-state machine (party presence is an OUTPUT, never state), single discharge pipeline that always hands the player back somewhere usable (INV-D1), 7-day commander-grace, real-time service pump + hourly reconciler sharing one retry budget, one encounter-ownership policy, settlement following (you enter the town with the column) through the one chokepoint that also builds the `PlayerEncounter` every vanilla settlement menu dereferences (#510),  live wait-menu status board, "speak with your commander" + "ask for work", informed leave with desertion as an explicit choice, MCM master off switch that releases anyone mid-service, wages from the commander's purse with arrears, ranks/trust/4-domain reputation, 13 field duties that auto-resolve on one skill check without ever detaching the player (#428) + 11 interactive + 3 incidents (JSON data rows), 268 authored service kits (armour AND weapons, keyed on culture + assignment + rank, no mounts at any assignment) drawn once per rank (#525), placement into the assignment-matched formation (#441), and a transient battle-only join into the commander's army that closes the #443 gap by putting both on `PlayerTeam` (no rank holds a command: `TaomBattleInitializationModel` keeps the Order of Battle screen shut, the role strip makes the player a soldier under AI orders at every rank, and the after-deployment correction undoes vanilla's own captain assignment, #576; the reconciler holds presence while the battle's encounter is open and ignores the "army left" edge its own disband raises, #577). Field-test additions (2026-08-11): shore-leave pass so the settlement menu is usable while the column rests there (shipped inert and fixed 2026-08-25, #512: the pass now suspends service following, so it ends when the PLAYER leaves the settlement rather than when the lord does, and an arrival popup offers it because the wait menu's fast-forward left about two real seconds to click the option), wage reporting plus a display-only clan-finance line, per-battle renown through the `Grant` chokepoint, war mirroring at oath that unwinds on discharge without touching wars the player brought with him, and a transpiler fixing vanilla's order banner. See [enlistment.md](../../docs/features/enlistment.md) |
| FieldCommission | `Main/Features/FieldCommission/` — battlefield promotion of a proven troop into a named companion (#376, native rewrite of the RF_Promoted donor): fair-fight merit banked per troop type, merit spent only on a completed promotion, race allow-list, companion cap that defers rather than discards, level-budgeted skills, suppressed while enlisted via `IEnlistmentStateQuery`. See [field-commission.md](../../docs/features/field-commission.md) |
| CaravanTrade | `Main/Features/CaravanTrade/` — caravans range past the local town cluster, trade across the Free-vs-Evil war per `WarTradePolicy`, and carry fuller baskets; 4 `Patch59` postfixes + 2 `TaomCaravanModel` overrides → pure `ICaravanTradeService`; master-off = exact vanilla, save-clean (#329). See [caravan-trade.md](../../docs/features/caravan-trade.md) |
| EconomyDiagnostics | `Main/Features/EconomyDiagnostics/` — read-only town market-gold telemetry: one `Patch68` recorder on `SettlementComponent.ChangeGold` (the pool's sole mutator) + 4 outermost-wins flow tags → bounded 7-day `ITownGoldLedger`; plus `CaravanGateDiagnosticsService` (in `CaravanTrade/Diagnostics/`) naming which engine gate parks a caravan. Read via `taom.print_town_ledger` / `taom.print_caravans`. No gameplay change. See [economy-diagnostics.md](../../docs/features/economy-diagnostics.md) |
| WarOfTheRingMomentum | `Main/Features/WarOfTheRingMomentum/` (+ `UI/`) — Evil-vs-Good war-progress meter (#327): decaying event-fed momentum, on-map ratio bar + popup, opt-in victory, chunked-SyncData persistence (the v2.0.9 save-corruption fix). See [war-of-the-ring-momentum.md](../../docs/features/war-of-the-ring-momentum.md) |
| BattleLoadDiagnostics | `Main/Features/BattleLoadDiagnostics/`: session and mission forensics in `Logs/taom_debug_*.log`. `[BattleLoad]` attack-to-playable phase stamps (Patch43) and the loading-stall watchdog; `[ExitStall]` main-thread stacks when a mission exit stalls (#331); `[MemSample]`/`[MemStation]` memory telemetry (#386); `[MissionStall]` stacks of an agent tick or mission frame stuck 10 s or more, the frozen-battle signature (Patch91, #634). See [battle-load-diagnostics.md](../features/battle-load-diagnostics.md) |
| AutoResolveDiagnostics | `Main/Features/AutoResolveDiagnostics/` (behavior = event wiring + the `MapEvent`-keyed pending map only; census/ids/emit split into `{I,}AutoResolveLogWriter.cs` per ADR-002, the map staying behind because it is keyed by the sealed `MapEvent` per ADR-007) (+ `Main/Adapters/{MapEventBattleLogAdapter,TroopCensusAdapter}.cs`) — one raw record per completed map battle, tagged `[AutoResolve]` in `Logs/taom_debug_*.log`: fielded roster from a **start-of-battle snapshot** (nothing readable at battle end survives — the engine strips a defeated party's `MemberRoster` AND rebuilds `MapEventParty.Troops` from it), per-troop dead/wounded/routed, engine `strength`/`advantage`, siege block (`settlementAdvantage` 3.6–6.0, the term that decides sieges), terrain, morale, tactics, session. Plus a once-per-session `[AutoResolveCensus]` dump of every CharacterObject's engine-side tier/power/formation/HP — ground truth that validated the offline model 829/829 and caught a missing ×1.2 mounted multiplier. Stores NO derived values so analysis can change without a rebuild. Offline reports + replay simulator + hard cross-check gates via `tools/analyze_battle_logs.py`. No gameplay change. See [auto-resolve-diagnostics.md](../../docs/features/auto-resolve-diagnostics.md) |
| SaveLoadDiagnostics | `Main/Features/SaveLoadDiagnostics/` — always-on `[SaveLoad]` save/load lifecycle logging (Patch61, 15 hooks) stamping the exact failing type/SaveId/chunk to `Logs/taom_debug_*.log`; offline triage/repair via `tools/inspect_sav.py` + `tools/repair_sav_strings.py`. See [save-load-diagnostics.md](../../docs/features/save-load-diagnostics.md) |
| Scene scripts | `Main/SceneScripts/` (engine-discovered `ScriptComponentBehavior` subclasses for map authors; CS_Road procedural mesh generator + Roads/ pure helpers; clean-room ports from external inspiration via `docs/scene-scripts/specs/` + ATTRIBUTION.md procedure) |
| EditorCacheRebuild | `Main/Features/EditorCacheRebuild/` — parallel + incremental + resumable settlement distance-cache rebuild (~108 hr vanilla → ~7 min on 863 settlements), singleplayer MCM trigger only; runtime-only despite the "Editor" name (rename deferred); NavalDLC port tracked at #120. See [editor-cache-rebuild.md](../../docs/features/editor-cache-rebuild.md) |
| Warg Combat | `Main/Features/Warg/` (BT elements, WargAttackService, WargMissionBehavior) |
| Giant Spider | `Main/Features/Spider/` — Dol Guldur giant spider as a ridden mount (`taom_spider_creature` rider + `spider_mount_a`, vanilla cavalry spawn, no spawn patch); directional-attack `SpiderBehaviorTree` + Patch47/48 dismount guards; data in LOTRLOME_Armory (movement clips MUST carry `quad_movement`). See [spider.md](../../docs/features/spider.md) |
| War Elephant | `Main/Features/Elephant/`: Harad ridden mount that auto-attacks (warg-pattern BT on shared `Main/Features/ElephantLike/` nodes, #305; trample/tusk cooldowns); mount-lock in `TaomAgentStatCalculateModel`; howdah crew ON for the howdah harness since 2026-09-19 (#627, `HowdahCrewSpawner`), bone tracking still off (a slide source, "Slide root-cause isolation"). Issues #278, #627. See [elephant.md](../../docs/features/elephant.md) |
| MountDespawn | `Main/Features/MountDespawn/` retires killed mounts a few seconds after death (default 5s, MCM toggle + 3-30s slider under Performance/Dead Mount Cleanup). Thin MissionBehavior owns the `Agent` handles, service owns the timing and sees only indices. Allowlisted to field battle / siege / sally-out. No vanilla `AgentRemovalLogic` exists; corpse retirement is native. See [mount-despawn.md](../features/mount-despawn.md) |
| Mumakil | `Main/Features/Mumakil/` — giant Oliphaunt: 3×-scale War Elephant clone minus the howdah (shared `ElephantLike` BT via `MumakilCombat.Profile`, elephant rig/clips reused, size = Horse-item `body_length="300"`); Monster/item/mesh live in external LOTRLOME_Armory. See [mumakil.md](../../docs/features/mumakil.md) |
| War Ram | `Main/Features/WarRam/`: Dwarven war ram, TAOM's first HORSE-SKELETON RESKIN. Monster is the vanilla `horse_2` shape (`base_monster="horse"`), inheriting `Mountable`, `family_type=1`, `monster_usage="horse"` and all twelve rein attributes, and authoring one clip only. Attack is the bespoke head-butt `act_war_ram_butt` (2026-09-18, authored on the engine `horse_skeleton`, bound in the Armory's `as_war_ram` over `as_horse`); the horse rig's only attack clip is the kick, so `act_horse_kick` stood in before. The rig brings no saddle either, since `sk_eb_goat_*` are bare pelts and the seat is on the bardings; `MOUNT_WITHOUT_HARNESS` now requires a harness beside EVERY `Horse` slot in the repo, not only a ram's. Six-rung Ironpass branch 16-41 entered by the pooled `ironpass_ram_herder`; 31+ costs Gems; 12 mounted lords. Dwarves ride below every other culture at every level. Monster/items live in external LOTRLOME_Armory. #515. See [war-ram.md](../../docs/features/war-ram.md) + [ledger](../../docs/reference/lotrlome-war-ram-changes.md) |
| Animalia Elk and Moose | `Main/Features/Animalia/`: each animal's antler attack on the shared elephant-like engine (one config, one service and profile per animal, one tree class, one mission behavior with a tracker per animal, keyed on `taom_animalia_elk` / `taom_animalia_moose`). Procedure: [quadruped-pack-to-horse-skeleton-workflow.md](../../docs/ai-includes/quadruped-pack-to-horse-skeleton-workflow.md). Two Fab packs as HORSE-SKELETON RESKINS WITH THEIR OWN ANIMATION: `tools/blender/reskin_animalia_to_horse.py` bends each mesh onto `horse_skeleton` keeping the pack's weights (the moose keeps its own neck via `--profile moose`), `tools/blender/retarget_animalia_to_horse.py` moves the pack's clips onto the engine rig through the same fit (64 elk + 33 moose), 1K textures. In use: `animalia_elk_08` and `animalia_moose_big`, sources in `LOTRLOME_Armory/AssetSources/creature/elk/`, meshes and materials imported in the Kit. A separate second elk. Riders: the moose carries Thranduil, the Mirkwood lord battle templates and the generated lord and ruler templates (a saved hero keeps the battle equipment it was saved with; heroes equipped from these rosters after a restart, and an Advanced Start as a Mirkwood lord or ruler, get the moose); the Animalia elk carries `mirkwood_rochenlas` and the `elk_rider` career start (`AnimaliaMountWiringTests`). Sizes on the Monsters (elk 100, moose 150). #646. See [animalia-elk-moose.md](../../docs/features/animalia-elk-moose.md) |
| Elk | `Main/Features/Elk/`: the great elk, the second HORSE-SKELETON RESKIN. `Monster.taom_elk` is `base_monster="horse"` on the ram's own action set `as_war_ram`, so the ram's head-butt plays as the elk's antler charge and no animation data was authored. Size 110 on the Monster (`taom_body_length`, copied into the item at game init: [monster-size.md](../../docs/features/monster-size.md)); the reach scales with the live body (`ElkConfig.ReachScalesWithBody`). One-branch BT under any rider, the player included (#643): one 60 Blunt blow that can kill, owned by the rider and scaled by their career charge bonus, single target, 10 s cooldown. Ridden by `mirkwood_beleglas`, the top cavalry (since 2026-09-23 the moose carries Thranduil and the lords, the Animalia elk the lower cavalry and the `elk_rider` career start); every elk-family mount paired with `taom_elk_saddle_a` (`ElkMountWiringTests`). Monster/items live in external LOTRLOME_Armory. #636. See [elk.md](../../docs/features/elk.md) + [ledger](../../docs/reference/lotrlome-elk-changes.md) |
| Troll Brute Force | `Main/Features/TrollBruteForce/` (#649): per-troll behaviour tree, pure `TrollBruteForceService`, `BruteForceRing`; the action and bindings in the live Armory. See [troll-brute-force.md](../../docs/features/troll-brute-force.md) |
| Troll races | No race code (`RaceManager` reads the race list from the engine); the C# is the Brute Force tree (`Main/Features/TrollBruteForce/`) and the trolls' 200 health (`TaomCharacterStatsModel` over `combat_mechanics_config.json` `baseHitPoints`). `cave_troll`: `human_skeleton`, `as_cave_troll_warrior` over `as_human_warrior` with 213 Fab clip overrides (`tools/bind_troll_action_set.py`), troop `cave_troll` in `troops_mordor.xml`. `hill_troll` (2026-09-24): its own `troll_skeleton_a`, re-framed to the human's bone axes on export (human clips then bend about the right axes but keep the human's rest relations, so they are retargeted onto the hunched rest as well), human physics copied (`tools/tpac_skeleton_copy_physics.py`), all ten skins and a standalone filled `as_hill_troll_warrior` (`tools/wire_hill_troll_race.py`, `patch_dwarf_action_parity.py`), the 52 Fab clips retargeted onto it (`tools/blender/retarget_mannequin_to_human.py --engine-skeleton`, leg IK, root height kept; `gen_troll_anim_clips.ps1 -TravelScale <pelvis_scale>`). The `hill_troll` troop (Mordor, 2026-09-25) mirrors `cave_troll` with the two-handed mace only and no armour items; both trolls run the Brute Force tree (`Main/Features/TrollBruteForce/`, #649, `ActionSetsByMonster`). Race, Monster, skins, action sets and clips live in external LOTRLOME_Armory. See [troll-race.md](../../docs/features/troll-race.md) + [ledger](../../docs/reference/lotrlome-hill-troll-changes.md) |
| Monster Size | `Main/Features/MonsterSize/` + `Main/Adapters/MonsterSizeCatalogAdapter.cs`: a mount's size is the TAOM attribute `taom_body_length` on its `<Monster>` (the engine has none: it sizes a mount only from the ridden item's `body_length`). `IMonsterSizeService.ApplyMonsterSizes`, called from `SubModule.OnGameInitializationFinished` on every game init, reads the merged Monsters XML and writes the value into every Horse item naming that Monster through `HorseComponent.BodyLength`'s private setter (the Monster wins over the item's placeholder silently, the summary line showing "(was N)"; a size no Horse item's Monster matches is warned), then recomputes the item's cached `Effectiveness`. Whole numbers 10 to 1000 only. Profiles with `reachScalesWithBody` multiply their 1.0x reach by the live `Agent.AgentScale` (`ElephantLikeReach.Scale`: a finite positive scale up to 10x, else 1). In use: `taom_elk` 110, `taom_animalia_elk` 100, `taom_animalia_moose` 150. The engine logs one "not declared" line per sized Monster; `validate_xml_schemas.py` allowlists the attribute. #646. See [monster-size.md](../../docs/features/monster-size.md) |
| BanditManagement | `Main/Features/BanditManagement/`: LOTR bandit culture replacement (8 cultures) + PlayerProgress-scaled hideout density/party sizes (`TaomBanditDensityModel` + `Patch39`/`Patch40`); MCM only (the shadowed JSON was deleted in #559), vanilla floor enforced for the scaled values. The hideout boss fight is NOT scaled: `Patch86` (two prefixes, assault and sneak-in) plus `IHideoutBossFightService` hold it at boss + the MCM `Hideout Boss Bodyguards` count (default 4), and the boss party templates are one `1/1` boss plus 3-4 soldiers (#564). See [bandit-management.md](../../docs/features/bandit-management.md) |
| PreloadBodyGuard | `Main/Features/PreloadBodyGuard/` (Patch90: a prefix on `PreloadHelper.WaitForMeshesToBeLoaded` that drains collision-body names no loaded tpac ships, with a bounded 5 s wait, logs them as `[PreloadGuard]`, and lets vanilla's loop run; the runtime half of #599, the only protection for a player whose Armory copy already carries a bad pair. Six callers: campaign battles, tournaments, custom battle, multiplayer, naval, the education screen. See [preload-body-guard.md](../features/preload-body-guard.md), #601) |
| ArmoryRefAudit | `tools/audit_armory_refs.py` + `/armory-audit` + `validate_moduledata.py` `MISSING_COLLISION_BODY`: the one command after any Armory sync. Every mesh and collision-body ref over the whole Armory ModuleData plus the repo's, catalogue drift flagged from the LIVE refs, every broken item joined to the troops carrying it, dead item ids, generator drift; committed report `docs/audits/armory-ref-audit.md`; `session-start.sh` prints `ARMORY ART DRIFT` when a `*_geo.tpac` is newer than the catalogue. A missing body is the #352 infinite load; the elf start hung two days on the 2026-09-11 art drop (#599). See [armory-ref-audit.md](../features/armory-ref-audit.md) |
| CaravanBanditParity | `tools/rebalance_template_power.py` + `AiPartySizeService.ApplyCaravanScaling` : bandit warbands and caravans sized against each other by troop POWER rather than headcount, because the flee decision is a step function at `localAdvantage == 1` and partial increases do nothing. Raiders 200 -> 56-80 bodies (76-79 power), caravans 20-36 -> 60-88 (93+ power, L = 1.18); bandit floor at 12.5% of the ceiling keeps the early game varied (12-32). The caravan member cap (+70 flat, ungated) is the mandatory other half: over-cap caravans are shed daily and lose half their speed. SupplyLines was split onto its own `supply_caravan_template_*` crew templates (#549) so a caravan balance pass can no longer move the player's logistics escort. See [caravan-bandit-parity.md](../features/caravan-bandit-parity.md) |
| CastleRecruitment | `Main/Features/CastleRecruitment/` — Patch42: player + AI recruit volunteer troops from castles (spawned castle-safe notables drawing `castle_*` pools; MCM "Castle Recruitment" + `castle_recruitment/castle_recruitment_config.json`; disable = inert). See [castle-recruitment.md](../../docs/features/castle-recruitment.md) |
| LotrIssues | `Main/Features/LotrIssues/` — replaces all 43 vanilla procedural issues with 43 XML-configured LOTR issues via 3 generic templates (DeliverGoods/DeliverPersonnel/Combat); no Harmony patch or GameModel override; new-campaign feature. Issue #291. See [lotr-issues.md](../../docs/features/lotr-issues.md) |
| CoopInterop | `Main/Features/CoopInterop/` — the co-op seam every other feature gates on, split by what each question can actually answer: `ICoopPresenceProvider` (process-constant — "a co-op module is loaded in this launcher session"), `ICoopSessionProvider` + `CoopSessionPolicy` (session-varying — `IsAuthority` / `ShouldDeferToHost`, read live, never cached), `IDedicatedServerProvider` (which binaries folder this assembly loaded from — `Win64_Shipping_Server` in `Assembly.GetExecutingAssembly().Location`, failing closed to "not a server"). The last is deliberately NOT co-op role: a client-hosted session's host also reports IsServer but is a real player who must keep earning. Plus `SaveDefinerCollisionGuard` and a `Diagnostics/` Harmony census. See [coop-interop.md](../../docs/features/coop-interop.md) |
| PlayerPossession | `Main/Features/PlayerPossession/` — re-applies TAOM's character-creation grants (race, culture startup gold, career, special-resource seed) to the hero a multiplayer join hands the player, since every co-op base discards the locally created one. Detection is pure engine state — `Hero.MainHero.StringId` recorded at session launch, compared later — so no co-op assembly is referenced and it works on a base that does not exist yet. Both registrations are `Reuse.Singleton` **because** the choices are recorded in one campaign and consumed in the campaign that replaces it, not as a style default. `Hero.MainHero` also changes on heir succession, so three independent guards keep single-player inert. See [player-possession.md](../../docs/features/player-possession.md) |
| Vendored Main-module DLLs | `Main/_Module/bin/Win64_Shipping_Client/` — allowlisted vendored binaries `MinHook.x64.dll` + `TAOM.NativeSkinFixes.dll` (`TAOM.dll` + `TAOM.pdb` stay ignored; do NOT vendor `MCMv5.dll` here — MCMv5 comes from TAOM.Dependencies + the `Bannerlord.MCM` NuGet). The sibling `bin/Win64_Shipping_Server/` that a Bannerlord dedicated server loads is NOT vendored and never committed: `MirrorWin64ShippingClientToServer` in `Main/TAOM.csproj` + `Dependencies/TAOM.Dependencies.csproj` copies the ASSEMBLED client folder (not build output — the vendored natives and NuGet companions only exist after `PostBuildCopyToModules`) into the game folder at deploy time. See [dr3-maintenance.md](../../docs/migration/dr3-maintenance.md) |
| TAOM.Dependencies stub modules | `Stubs/Bannerlord.{Harmony,UIExtenderEx,ButterLib,MBOptionScreen}/_Module/SubModule.xml` — four alias stubs at the standard BUTR module IDs (v99 version strategy; deployed via `DeployTAOMDependenciesStubs`) so third-party mods stay toggleable in the vanilla launcher. See [dr3-maintenance.md](../../docs/migration/dr3-maintenance.md) |
| TAOM.Dependencies defensive infrastructure | `Dependencies/Foundation/`: 19-class runtime error-tolerance layer (PatchShield/SaveShield/crash-loop detection; BetaDeps v0.7.5.1 clean-room port; opt-out flags in the module dir). See [dr3-maintenance.md](../../docs/migration/dr3-maintenance.md) |
| NativeSkinFixes C++ source | `Dependencies/NativeSkinFixes.NativeHooks/` — standalone `.vcxproj` (not in `TAOM.sln`) building `TAOM.NativeSkinFixes.dll` into `Main/_Module/bin/Win64_Shipping_Client/`; byte-pattern scan targets in `Signatures.h`; rebuild `pwsh Dependencies/NativeSkinFixes.NativeHooks/Build.ps1`. See [native-skin-fixes.md](../../docs/features/native-skin-fixes.md) |
| BehaviorTrees library (inlined) | `Main/BehaviorTrees/` — generic BT engine (Selector, Sequence, RandomSelector, Decorators, Tasks, blackboard). No TaleWorlds dependencies. Compiles into `TAOM.dll`. |
| BehaviorTreeWrapper library (inlined) | `Main/BehaviorTreeWrapper/` — Bannerlord/Agent bindings over `BehaviorTrees` (`BehaviorTreeMissionLogic`, `BehaviorTreeAgentComponent`, listeners). `BehaviorTreeMissionLogic : MissionLogic` (NOT just `MissionBehavior` — regression rule, see `docs/reviews/rca-looter-battle-nre-2026-05-24.md`). Compiles into `TAOM.dll`. |
| Alliance.Wargs | External module: Monster id="warg", animations, items |
| CC narrative data | `Main/_Module/ModuleData/charactercreation/` (JSON) |
| XML config (per-feature) | `Main/_Module/ModuleData/<feature>/` — every feature's config/data paths + validation rules live in its `docs/features/<name>.md` Configuration section (sprites: `Main/_Module/GUI/SpriteParts/` + [gui-sprite-system.md](../../docs/features/gui-sprite-system.md)) |
| XSLT files | `Main/_Module/ModuleData/*.xslt` |
| Culture definitions + party-template bindings | `Main/_Module/ModuleData/taom_spcultures.xml` (24 TAOM-native cultures), `spcultures.xslt` (the 6 retagged vanilla ids), `taom_partyTemplates.xml` (every template TAOM authors). Eight engine-read `<Culture>` attributes decide which troops spawn for a culture; a retagged block inherits vanilla's binding for any attribute it never names, which is how nine town-owning cultures shipped fielding Calradians (fixed 2026-08-12). Caravans come only from the plural CHILD elements, which the deserializer appends rather than replaces. Contract, the two crash surfaces and the `CulturePartyTemplateTests` gate: [culture-playability-wiring.md](../../docs/features/culture-playability-wiring.md). What a stack's `max_value` actually controls (a spawn ceiling, not a party size) and the per-culture max-sum targets: [party-template-sizing.md](../../docs/reference/party-template-sizing.md) |
| Custom lords XML | `Main/_Module/ModuleData/characters/lords.xml` |
| **TAOM_Map settlements (LIVE)** | `<game>/Modules/TAOM_Map/ModuleData/settlements.xml` — **external module, NOT in repo**; the repo's `Main/_Module/ModuleData/settlements.xml` is a **STALE SHADOW** (edits don't affect the game). Live renames: `tools/Apply-MapVillageNames.py`. See [taom-map-settlement-naming.md](../../docs/reference/taom-map-settlement-naming.md) |
| Player starting equipment (starter kit) | `Main/_Module/ModuleData/equipmentsets/taom_char_creation_equipment.xml` (256 culture-default rosters), `taom_career_starting_equipment.xml` (78 career rosters: each culture's lowest troop gear, #629), `taom_player_start_vanilla_override.xml` (70 rosters for the six vanilla-mapped cultures, generated from the career kit, `_replaceWhileMerging` on the roster) + LOTRLOME_Armory `LOTRLOME_items/<folder>/starter_kit.xml` (127 `starter_<donor>` twins for the culture-default rosters, generated) and the older `starter_armors.xml`. Tools `tools/generate_starter_kit.py`, `tools/wire_starter_kit_rosters.py`; both invariants pinned by `StarterKitCoverageTests`. See [starting-equipment-tuning.md](../../docs/features/starting-equipment-tuning.md) |
| VolunteerRecruitment service | `Main/Features/TroopProgression/VolunteerRecruitmentService.cs` (+ per-culture partials under `RecruitmentPools/VolunteerRecruitmentService.<Culture>.cs`, #308) — settlement/clan/culture volunteer pools + conditional pools driving `TaomVolunteerModel.GetBasicVolunteer`. See [volunteer-recruitment.md](../../docs/features/volunteer-recruitment.md) |
| TaleWorlds DLLs | `%BANNERLORD_GAME_DIR%\bin\Win64_Shipping_Client`; every project takes them through `GameReferences.targets` (`-p:TaomGameRefs=RefAsm` switches to BUTR's reference assemblies; `GameReferencesTargetsTests`) |
| Decompiled source | `E:\Decompiled_Bannerlord\` (pre-decompiled, organized by category) |
| CI/CD | `.github/workflows/csharp.yml` (C# build, unit tests and binding gate on hosted Windows, no game; test categories in `.claude/rules/tests.md`), `.github/workflows/build.yml` (hook harness, Python tests, XML validation) |
| Ranged ladders | `tools/ranged_ladders.json` (per-tier curves for speed, damage, accuracy and skill; 19 kingdom lines ranked overall, damage and accuracy; the tiers each fields; donors; the Armory donor and ammo tables; `hero_ceiling`), `tools/ranged_ladder.py` (library), `tools/generate_ranged_ladder_items.py` (123 `ladder_*_t<tier>` items + English loc rows into the live Armory and the mirror), `tools/rebalance_ranged_ladders.py` (report + roster and skill rewrite), `tools/restat_ranged_donors.py` (the Armory's own bows and ammo), `tools/sync_ranged_ladder_translations.py` (translated names and the translator cache follow renamed ids), `RANGED_LADDER_INVERSION` and `RANGED_DAMAGE_CEILING` gates in `tools/taom_schema.py`. `docs/features/ranged-ladders.md` (#582, #617) |
| graphify code graph (C# blast radius) | `tools/graphify_taom.py` (`refresh`, `status`, `affected`, `explain`, `god-nodes`) + `.claude/hooks/check-graphify-usage.sh`; the graph lives outside the repo at `E:\graphify\TAOM` ([ADR-012](../adrs/012-graphify-code-graph-in-the-workflow.md)). See [graphify-code-graph.md](../features/graphify-code-graph.md) |
| One-off scripts (finished) | `tools/oneoff/` — one-off migration/authoring scripts move here when done; `tools/` keeps only living tools (see `tools/README.md` § One-offs) |
| Shared build props | `Directory.Build.props` |
| Skills | `.claude/skills/` |
| Rules | `.claude/rules/` |
| Agents | `.claude/agents/` |
| Codex config | `.codex/config.toml` |
| Codex instructions | `AGENTS.md` (project root) |

---

<!-- backlinks-start auto-generated; edit lint_docs.py / build_backlinks.py to change -->

## Referenced by

- [docs/modding/file-catalogue.md](../modding/file-catalogue.md)
- [docs/modding/README.md](../modding/README.md)

<!-- backlinks-end -->
