[
 {
  "refuted": true,
  "reason": "The quote is verbatim (Main/SubModule.cs:142) and the prefix-mismatch fact is real — \"com.taom.mod\" matches none of PatchShieldPolicy.CompiledProtectedOwnerPrefixes under IsProtectedOwner's OrdinalIgnoreCase StartsWith, and the shipped coop-modules.txt [harmony-owner-prefixes] section is empty, so nothing adds it at runtime. But the claim fails on scope and on its load-bearing coop argument. (a) SCOPE: PatchShield.TryUnpatchOffendingPatches calls harmony.Unpatch(originalMethod, HarmonyPatchType.Prefix/Postfix/Transpiler, owner) — bound to the ONE method whose finalizer caught a MissingMethod/MissingField/TypeLoad, deduped once per target. It cannot unpatch \"TAOM's OWN ~62 patch categories\"; UnpatchAll appears only in TAOM's own shutdown (SubModule.cs:1223), never in PatchShield. (b) CONTRADICTED: PatchShieldPolicy.ShouldUnpatchForeignOwners(coopActive) => !coopActive, and PatchShield applies mayUnpatch to EVERY owner (\"co-op active — would unpatch owner '{owner}' on {targetKey} (withheld)\"), including com.taom.mod. Under a detected co-op session the strip path is entirely dead, so it does not \"prove the unpatch path is live\" for co-op — it is live against Coop only because CoopPresence.CompiledModuleDefaults is { \"BannerlordTogether\", \"BattleLinkMPClient\" } and coop-modules.txt [modules] lists the same two. (c) IMPACT OVERREACH: the co-op-relevant fix is adding \"Coop\" to the module-id list (one line in coop-modules.txt, no rebuild), which the claim omits; adding \"com.taom.mod\" to the owner prefixes is real pre-existing hardening but not a co-op compatibility requirement.",
  "corrected_claim": "TAOM's Main-assembly Harmony owner id is \"com.taom.mod\" (Main/SubModule.cs:142, the only `new Harmony(` in Main), and it matches no entry in PatchShieldPolicy.CompiledProtectedOwnerPrefixes (\"TAOM\", \"Bannerlord.ButterLib\", \"butterlib.\", \"Bannerlord.UIExtenderEx\", \"bannerlord.uiextender.ex\", \"Bannerlord.MBOptionScreen\", \"Bannerlord.ModuleLoader\", \"Bannerlord.MCM\", \"bannerlord.mcm.\", \"MCM\", \"MCMv5\", \"MCM.UI.Adapter\", \"BUTR.\", \"HarmonyLib.\", \"0Harmony\") under IsProtectedOwner's case-insensitive StartsWith; the shipped Dependencies/_Module/coop-modules.txt adds no owner prefixes. So when a SHIELDED method throws MissingMethodException/MissingFieldException/TypeLoadException, PatchShield.TryUnpatchOffendingPatches may strip com.taom.mod's prefix/postfix/transpiler FROM THAT ONE METHOD (harmony.Unpatch(originalMethod, ...), deduped per target) — not from TAOM's ~62 patch categories wholesale. This exposure is real because PatchShield.Install only skips methods whose declaring assembly starts with \"TAOM\", and TAOM patches TaleWorlds types. It is a pre-existing hardening gap, NOT proof that the unpatch path is live under Coop: PatchShieldPolicy.ShouldUnpatchForeignOwners(coopActive) => !coopActive and PatchShield's mayUnpatch check is owner-agnostic, so a detected co-op session disables the strip for every owner including com.taom.mod. The path is live against BannerlordCoop only because CoopPresence.CompiledModuleDefaults / coop-modules.txt [modules] list only \"BannerlordTogether\" and \"BattleLinkMPClient\". TAOM impact: primary fix is adding \"Coop\" to coop-modules.txt [modules] (config-only, no rebuild), which disables the strip entirely; secondary defensive fix is adding \"com.taom.mod\" to CompiledProtectedOwnerPrefixes plus a PatchShieldPolicyTests case pinning it, which also protects non-co-op sessions.",
  "corrected_severity": "MEDIUM"
 },
 {
  "refuted": true,
  "reason": "The cited quote is verbatim and correct, but the claim built on it is false in two places and the remedy is incomplete.\n\n(1) \"PRIMARY... single instance owns every explicit patch in GameInterface\" is contradicted by the source. A whole-tree grep for `new Harmony(` in coopsrc returns FOUR owner ids, not one:\n- GameInterface/GameInterface/GameInterfaceModule.cs:41 — `private static readonly Harmony harmony = new Harmony(\"Bannerlord.Coop\");`\n- Coop/Coop/CoopMod.cs:97 — `new Harmony(\"Coop.UILoading\").PatchCategory(typeof(IGameInterface).Assembly, \"UILoadingPatches\");`\n- Coop/Coop/BootPatches.cs:58 — `Harmony harmony = new Harmony(\"Coop.BootFix\");`\n- GameInterface/GameInterface.Registry.Auto/AutoRegistryFactory.cs:18 — `private readonly Harmony harmony = new Harmony(\"CoopAutoRegistryFactory\");`\n\n\"Coop.UILoading\" owns patch classes that live INSIDE the GameInterface assembly: GameInterface.Services.UI.Patches/LoadingWindowPatches.cs (`[HarmonyPatchCategory(\"UILoadingPatches\")]`, prefix on `LoadingWindow.DisableGlobalLoadingWindow`, postfix on `EnableGlobalLoadingWindow`) and GameInterface.Services.UI.Patches/GauntletDefaultLoadingWindowManagerPatches.cs (postfix on `GauntletDefaultLoadingWindowManager.TaleWorlds.Engine.ILoadingWindowManager.EnableLoadingWindow`). These are explicit GameInterface patches NOT owned by \"Bannerlord.Coop\".\n\n\"Coop.BootFix\" (reached from CoopMod.cs:63 `BootPatches.Apply();`) patches `GauntletLayer.LoadMovieAux`, `FontFactory.DefaultFont` getter and `Font.TryLoadFontFromPath` — Gauntlet UI methods adjacent to TAOM's own GauntletUI patch surface.\n\n(2) The parts of the claim that DO hold: the AutoSync transpiler engine and the Missions categories genuinely run under \"Bannerlord.Coop\", because both take the DI-registered singleton. GameInterfaceModule.cs:45 `builder.RegisterInstance(harmony).As<Harmony>().SingleInstance();`; AutoSyncPatchCollector.cs:24 `public AutoSyncPatchCollector(Harmony harmony)` with the transpiler applied at line 82 `harmony.Patch(item, null, null, new HarmonyMethod(item2));`; AutoSyncPatcher.cs:26 `public AutoSyncPatcher(Harmony harmony, ...)` with line 64 `harmony.PatchAllUncategorized(Assembly);`; MissionModule.cs:30-31 registers `new HarmonyPatchCategoryRegistration(...)` which GameInterface.cs:53 applies as `patchCategory.Apply(harmony)` on the injected instance.\n\n(3) The claimed TAOM impact is an overreach. PatchShieldPolicy.cs:78 matches by StartsWith: `if (owner!.StartsWith(prefix, StringComparison.OrdinalIgnoreCase)) return true;`. An entry of exactly \"Bannerlord.Coop\" therefore protects one of the four ids and leaves \"Coop.UILoading\", \"Coop.BootFix\" and \"CoopAutoRegistryFactory\" strippable. A correct data-only edit needs BOTH \"Bannerlord.Coop\" and \"Coop\".\n\n(4) Severity is inflated. TAOM's own design makes the owner-prefix list secondary: PatchShieldPolicy.cs:96 `public static bool ShouldUnpatchForeignOwners(bool coopActive) => !coopActive;`, consumed at PatchShield.cs:317 `var mayUnpatch = PatchShieldPolicy.ShouldUnpatchForeignOwners(coopActive);` and gated at line 358 `if (!mayUnpatch) { ...(withheld)...; continue; }`. Adding the module id \"Coop\" to the `[modules]` section of Dependencies/_Module/coop-modules.txt (currently only `BannerlordTogether` / `BattleLinkMPClient`, lines 14-15) disables the strip for EVERY owner id at once. coop-modules.txt:25-26 states this explicitly: \"This is normally unnecessary, because the unpatch path is already disabled outright whenever a module above is active.\" So the BLOCKER is the missing `[modules]` entry — a co-op-detection finding already listed as a known fact — not the harmony-owner-prefix line, which is belt-and-braces.\n\nNOT FOUND: any code path that gives \"Bannerlord.Coop\" ownership of the UILoadingPatches category, and any usage of the `CoopAutoRegistryFactory` Harmony field inside AutoRegistryFactory.cs (it is declared at line 18 and never referenced again in that file — patches route through `PatchCollector.AddPrefix/AddPostfix`, i.e. back onto the injected \"Bannerlord.Coop\" instance).",
  "corrected_claim": "BannerlordCoop creates FOUR Harmony owner ids, not one. \"Bannerlord.Coop\" (GameInterface/GameInterface/GameInterfaceModule.cs:41) is the DI-registered singleton and does own the AutoSync transpiler engine (AutoSyncPatchCollector.cs:82, AutoSyncPatcher.cs:64), the uncategorized + \"HarmonyStaticFixes\" + \"GameStartedPatches\" GameInterface patches (GameInterface.cs:49-50, 62-65), and both Missions categories \"CoopMissilePatches\"/\"CoopAgentVoicePatches\" (MissionModule.cs:30-31 applied via GameInterface.cs:53). But it does NOT own every GameInterface patch: the \"UILoadingPatches\" category — LoadingWindowPatches and GauntletDefaultLoadingWindowManagerPatches, both in the GameInterface assembly — is applied by `new Harmony(\"Coop.UILoading\")` at Coop/Coop/CoopMod.cs:97. A third id \"Coop.BootFix\" (Coop/Coop/BootPatches.cs:58, invoked from CoopMod.cs:63) patches GauntletLayer.LoadMovieAux, FontFactory.DefaultFont and Font.TryLoadFontFromPath. A fourth, \"CoopAutoRegistryFactory\" (AutoRegistryFactory.cs:18), is declared but unused in that file.\n\nTAOM impact: because PatchShieldPolicy.IsProtectedOwner uses StartsWith (PatchShieldPolicy.cs:78), a belt-and-braces allowlist edit must add BOTH \"Bannerlord.Coop\" AND \"Coop\" to `[harmony-owner-prefixes]` in Dependencies/_Module/coop-modules.txt — \"Bannerlord.Coop\" alone leaves the three Coop.* ids strippable. This edit is secondary, not the blocking fix: the blocking fix is adding module id \"Coop\" to the `[modules]` section, which flips CoopPresence.IsActive and makes PatchShieldPolicy.ShouldUnpatchForeignOwners return false (PatchShieldPolicy.cs:96), withholding the strip for all owners (PatchShield.cs:317, 358). Both are data-only, no rebuild.",
  "corrected_severity": "MEDIUM"
 },
 {
  "refuted": true,
  "reason": "The quote is verbatim and the id literals exist, but two load-bearing parts of the claim fail.\n\nVERIFIED PARTS. The cited line is exact — `coopsrc/Coop/Coop/CoopMod.cs:97`: `new Harmony(\"Coop.UILoading\").PatchCategory(typeof(IGameInterface).Assembly, \"UILoadingPatches\");`. A tree-wide grep for `new Harmony` / `CreateAndPatchAll` / `Harmony.PatchAll` returns exactly four id literals and no others: `\"Coop.UILoading\"` (CoopMod.cs:97), `\"Coop.BootFix\"` (Coop/Coop/BootPatches.cs:58 `Harmony harmony = new Harmony(\"Coop.BootFix\");`), `\"Bannerlord.Coop\"` (GameInterface/GameInterface/GameInterfaceModule.cs:41), `\"CoopAutoRegistryFactory\"` (GameInterface/GameInterface.Registry.Auto/AutoRegistryFactory.cs:18). The two category classes carrying `[HarmonyPatchCategory(\"UILoadingPatches\")]` are `GameInterface.Services.UI.Patches/LoadingWindowPatches.cs:8` and `.../GauntletDefaultLoadingWindowManagerPatches.cs:9`, so \"loading-window patches\" is accurate. BootPatches.cs:64/72/80/95 really do patch `Font.TryLoadFontFromPath`, `FontFactory.DefaultFont` getter and `GauntletLayer.LoadMovieAux` (ctor fallback), so \"FontFactory/GauntletLayer boot guards\" is accurate. And TAOM protects none of them: `Dependencies/Foundation/PatchShieldPolicy.cs:23-49` `CompiledProtectedOwnerPrefixes` contains no Coop entry, and `Dependencies/_Module/coop-modules.txt:17` `[harmony-owner-prefixes]` is empty (\"Empty by default: we do not know any co-op mod's Harmony id\").\n\nREFUTATION 1 — \"CoopAutoRegistryFactory (object-registry patching)\" is false. That Harmony instance never patches anything. A grep for `harmony` across the whole of AutoRegistryFactory.cs returns exactly ONE hit, the declaration at line 18; the field is never read again in the 137-line file. All registry lifetime patching is routed through the injected collector instead — `AddRegistry<T>` calls `PatchCollector.AddPrefix(constructor, patch)` (line 87) and `PatchCollector.AddPostfix(destroyMethod, patch2)` (line 92), and `AutoSyncPatchCollector` receives its Harmony by DI (`AutoSyncPatchCollector.cs:24 public AutoSyncPatchCollector(Harmony harmony)`), which `GameInterfaceModule.cs:45 builder.RegisterInstance(harmony).As<Harmony>().SingleInstance();` binds to the `\"Bannerlord.Coop\"` instance. Constructing a `Harmony` object registers no patches, so no `Patch.owner` in the process will ever equal `\"CoopAutoRegistryFactory\"` — adding it to the allowlist protects nothing. Only THREE ids can ever appear as a patch owner: `Bannerlord.Coop`, `Coop.UILoading`, `Coop.BootFix`. (Same check clears the other candidates: `Missions.Tournaments.Patches/TournamentCombatPatchInstaller.cs:12 Install(Harmony harmony)` and `GameInterface.Utils/FragileDetourGuard.cs:32 Apply(Harmony harmony)` both take an injected instance, minting no new id.)\n\nREFUTATION 2 — BLOCKER is overstated. The owner allowlist is not the primary gate. `Dependencies/Foundation/PatchShield.cs:317` reads `var mayUnpatch = PatchShieldPolicy.ShouldUnpatchForeignOwners(coopActive);` and `PatchShieldPolicy.cs:96` defines `public static bool ShouldUnpatchForeignOwners(bool coopActive) => !coopActive;`. At PatchShield.cs:358-362, `if (!mayUnpatch) { DiagLog.Log(Tag, $\"co-op active — would unpatch owner '{owner}' on {targetKey} (withheld)\"); continue; }` — once a co-op module is detected, NOTHING is unpatched regardless of owner id. TAOM's own config documents this at coop-modules.txt:25-26: \"This is normally unnecessary, because the unpatch path is already disabled outright whenever a module above is active.\" The real single-point fix is adding module id `Coop` to `[modules]`; the prefix entries are belt-and-braces. The unpatch path is also not routine — PatchShield.cs:267/281 shows it only fires when a `MissingMethodException`/`MissingFieldException`/`TypeLoadException` escapes a shielded method. Two conditions must coincide (module-id miss AND missing-API trinity) for any harm, so this is a defense-in-depth gap, not a blocker.\n\nThe prefix-hygiene advice in the impact (\"Coop.\" covers both dotted ids; do not use bare \"Coop\") is correct — `IsProtectedOwner` at PatchShieldPolicy.cs:78 is `owner!.StartsWith(prefix, StringComparison.OrdinalIgnoreCase)`.",
  "corrected_claim": "BannerlordCoop constructs four Harmony id literals, but only THREE can ever own a patch: \"Bannerlord.Coop\" (GameInterfaceModule.cs:41 — the DI-shared instance used by AutoSyncPatchCollector, TournamentCombatPatchInstaller and FragileDetourGuard), \"Coop.UILoading\" (CoopMod.cs:97, PatchCategory \"UILoadingPatches\" → LoadingWindowPatches + GauntletDefaultLoadingWindowManagerPatches) and \"Coop.BootFix\" (BootPatches.cs:58 → Font.TryLoadFontFromPath, FontFactory.DefaultFont getter, GauntletLayer.LoadMovieAux/ctor). The fourth, \"CoopAutoRegistryFactory\" (AutoRegistryFactory.cs:18), is a dead field never used to patch — it delegates to PatchCollector, which holds the \"Bannerlord.Coop\" instance — so allowlisting it is inert. TAOM protects none of the three today (PatchShieldPolicy.CompiledProtectedOwnerPrefixes has no Coop entry; coop-modules.txt [harmony-owner-prefixes] is empty). TAOM impact: the primary fix is adding module id \"Coop\" to coop-modules.txt [modules], which sets ShouldUnpatchForeignOwners=false and disables stripping for every owner (PatchShield.cs:358-362). As belt-and-braces, add \"Coop.\" and \"Bannerlord.Coop\" (two entries, not four) to [harmony-owner-prefixes] — never bare \"Coop\", since IsProtectedOwner is a case-insensitive StartsWith.",
  "corrected_severity": "MEDIUM"
 },
 {
  "refuted": true,
  "reason": "The QUOTE is genuine (verbatim, PartyBaseHelperPatch.cs:11-17) and the Harmony MECHANISM is real, but the claim's consequence and its prescribed fix are both wrong.\n\n1) QUOTE VERIFIED. `coopsrc/GameInterface/GameInterface.Services.PartyBases.Patches/PartyBaseHelperPatch.cs` is 18 lines total: `[HarmonyPatch(typeof(PartyBaseHelper))] internal class PartyBaseHelperPatch` with `[HarmonyPatch(\"HasFeat\")] [HarmonyPrefix] public static bool HasFeat(PartyBase party, FeatObject feat, ref bool __result)` setting `__result = (party != null && party.LeaderHero?.Culture?.HasFeat(feat) == true) || (party != null && party.MapFaction?.Culture?.HasFeat(feat) == true) || (party != null && party.Owner?.Culture?.HasFeat(feat) == true) || (party != null && party.Settlement?.Culture?.HasFeat(feat) == true);` then `return false;`. TAOM's `Main/Features/CulturalFeats/Hooks/PartyBaseHelper_HasFeat_Patch.cs:34-39` is the same shape. Vanilla has exactly ONE overload (`E:/Decompiled_Bannerlord/.../Helpers/PartyBaseHelper.cs:373 public static bool HasFeat(PartyBase party, FeatObject feat)`), so both patches bind the same MethodInfo. Neither declares `[HarmonyPriority]`.\n\n2) THE OUTCOME IS NOT INDETERMINATE — TAOM WINS, DETERMINISTICALLY. The claim says \"whichever loses\". Apply order decides it, and it is fixed. TAOM applies at module load: `Main/SubModule.cs:193 _harmony.PatchCategory(\"Patch18_CulturalFeats\");` sits inside `protected override void OnSubModuleLoad()` (declared at line 104; next method `OnBeforeInitialModuleScreenSetAsRoot` at 396). Coop's patch is uncategorized (`GameInterface/GameInterface/GameInterface.cs:50 harmony.PatchAllUncategorized(assembly);`) and `PatchAll()` is only reached from `Coop.Core.Client.States/MainMenuState.cs:47 gameInterface.PatchAll();` inside `Handle_NetworkConnected` (and `CoopartiveMultiplayerExperience.cs:385/425`) — i.e. after the player connects to a coop session, long after TAOM's OnSubModuleLoad. With equal priority Harmony orders prefixes by add-index ascending, so TAOM's prefix runs first, returns false, and COOP's prefix is the one skipped. Coop's patch is also guarded by `if (Harmony.HasAnyPatches(harmony.Id)) { ...; return; }` (GameInterface.cs:42) so it never re-applies to jump the queue.\n\n3) THE LOSING PATCH CARRIES NO FEATURE. The claim asserts \"a feature breaks with no exception and no log\". Coop's HasFeat prefix is an 18-line file that calls no message broker, no ObjectManager, no AutoSync intercept, and stores nothing — it is a pure null-safe rewrite of the vanilla body (crash hardening), not a sync patch. Losing it costs Coop nothing. Worse for the claim: TAOM's replacement is STRICTLY SAFER than Coop's. Coop evaluates `party.Owner?.Culture`, and `PartyBase.Owner` (PartyBase.cs:189-204) returns `Settlement.Owner` for a non-mobile party, which is `public Hero Owner => OwnerClan.Leader;` (Settlement.cs:107) — an unguarded getter that NREs when OwnerClan is null. TAOM's `CultureFeatAdapter.ResolvePartyCulture` (line 73-76) deliberately routes around it via `party.MobileParty?.Owner?.Culture`. Coop's version also diverges from vanilla semantics: vanilla is FIRST-MATCH (early `return` per branch, PartyBaseHelper.cs:373-396) while Coop ORs across all four cultures, over-granting feats. So the only scenario where anything degrades is the one that does NOT occur (Coop winning).\n\n4) THE PRESCRIBED FIX IS ACTIVELY HARMFUL. \"Convert from a skip-original Prefix to a Postfix\" would let the vanilla body run — and the vanilla body is exactly what NREs (`party.Culture` = `MapFaction.Culture` unguarded; `party.Owner` → `Settlement.OwnerClan.Leader`). That is the crash TAOM's prefix was written to fix (`docs/reviews/rca-culturefeat-partyculture-nre-2026-06-15.md`, referenced in the patch's own doc comment lines 24-25). It would reintroduce the NRE in single-player, where Coop's prefix is never applied at all. The offered alternative is also wrong: `Patch18_CulturalFeats` is a two-patch category — gating it off would also disable `Main/Features/CulturalFeats/Hooks/Campaign_InitializeDefaultCampaignObjects_Patch.cs:8`, which is what registers TAOM's custom feats in the first place, deleting the whole feature.\n\n5) NOTHING ELSE CONTRADICTS THE MECHANISM. `FragileDetourGuard.Apply(harmony)` (GameInterface.cs:48) prefixes `HarmonyLib.PatchTools.DetourMethod` and skips detours only when `isFragile(method) && !DoesFieldStore(method) && !hasLoadBearingPatch(method)` (FragileDetourGuard.cs:64); once TAOM has patched HasFeat, `HasLoadBearingPatch` returns true, so it does not interfere either way. NOT FOUND: any config flag, category gate, or second Coop patch on HasFeat.\n\nNet: real overlap, benign resolution under the actual apply order, and a remedy that would cause the regression it claims to prevent.",
  "corrected_claim": "PREFIX-VS-PREFIX OVERLAP on PartyBaseHelper.HasFeat — resolves in TAOM's favour; no TAOM change required, do NOT convert to a Postfix. Coop installs an uncategorized skip-original prefix (GameInterface.Services.PartyBases.Patches/PartyBaseHelperPatch.cs:11-17) applied only when a coop session connects (GameInterface.cs:50 via MainMenuState.cs:47 Handle_NetworkConnected). TAOM installs an identically-shaped prefix at module load (SubModule.cs:193, inside OnSubModuleLoad). Neither sets a priority, so Harmony orders by add-index and TAOM's prefix runs first, returns false, and Coop's is skipped. That is the benign direction: Coop's prefix does no sync work at all (no broker, no AutoSync, no ObjectManager) — it is only a null-safe rewrite of the vanilla body, and TAOM's is strictly safer (Coop's `party.Owner?.Culture` still evaluates PartyBase.Owner → Settlement.Owner => OwnerClan.Leader, which NREs on a null OwnerClan; TAOM routes via MobileParty.Owner) and matches vanilla's first-match precedence, whereas Coop ORs across all four cultures and over-grants feats. Action for TAOM: none functionally. Optional hardening — add an explicit `[HarmonyPriority(Priority.High)]` (or `[HarmonyBefore(\"Bannerlord.Coop\")]`) to PartyBaseHelper_HasFeat_Patch so the win does not depend on module load order, and add a regression test pinning it. Explicitly reject both remedies in the original finding: a Postfix cannot stop the throwing vanilla body (reintroduces the rca-culturefeat-partyculture-nre-2026-06-15 crash in single-player), and gating Patch18_CulturalFeats off would also disable Campaign_InitializeDefaultCampaignObjects_Patch, which registers TAOM's feats.",
  "corrected_severity": "LOW"
 },
 {
  "refuted": false,
  "reason": "Every load-bearing element verified against source. (1) The cited quote is verbatim in e:/repos/TAOM/Dependencies/_Module/coop-modules.txt — the [modules] section contains exactly the three comment lines plus 'BannerlordTogether' and 'BattleLinkMPClient', with no third entry. (2) The workshop module's id really is 'Coop': E:/Steam/steamapps/workshop/content/261550/3770450698/SubModule.xml line 4 '<Id value=\"Coop\"/>'. (3) Detection is exact-set membership, not prefix/substring: CoopPresence.cs:131-132 'var known = new HashSet<string>(config.ModuleIds, StringComparer.OrdinalIgnoreCase); _activeCoopModuleIds = active.Where(known.Contains)...' over CompiledModuleDefaults (CoopPresence.cs:35-39) = the same two ids. Grep of Dependencies/ and Main/ for a 'Coop' module id returns nothing. So IsActive is false. (4) PatchShieldPolicy.cs:96 'public static bool ShouldUnpatchForeignOwners(bool coopActive) => !coopActive;' → true. (5) Coop's methods ARE in the shield's scope: PatchShield.Install enumerates 'Harmony.GetAllPatchedMethods()' (PatchShield.cs:148) at OnSubModuleLoad and OnGameInitializationFinished (SubModule.cs:191, 245), while Coop applies its patches earlier at MainMenuState.Handle_NetworkConnected → 'gameInterface.PatchAll()' (Coop.Core/Coop.Core.Client.States/MainMenuState.cs:47). (6) ShouldSwallow (PatchShield.cs:267-281) calls TryUnpatchOffendingPatches on MissingMethodException/MissingFieldException/TypeLoadException, which strips every non-protected owner via 'harmony.Unpatch(originalMethod, HarmonyPatchType.Prefix/Postfix/Transpiler, owner)' (PatchShield.cs:366-368). (7) Coop is not protected: its Harmony ids are 'Bannerlord.Coop' (GameInterface/GameInterface/GameInterfaceModule.cs:41 'private static readonly Harmony harmony = new Harmony(\"Bannerlord.Coop\");'), 'Coop.BootFix' (Coop/Coop/BootPatches.cs:58), 'Coop.UILoading' (Coop/Coop/CoopMod.cs:97), 'CoopAutoRegistryFactory' (GameInterface.Registry.Auto/AutoRegistryFactory.cs:18) — none matches any entry in PatchShieldPolicy.CompiledProtectedOwnerPrefixes (there is no bare 'Bannerlord.' prefix; the list has 'Bannerlord.ButterLib', 'Bannerlord.MCM', 'Bannerlord.UIExtenderEx', etc.). (8) The cited TAOM comment is verbatim at PatchShield.cs:336-341 (claim said 336-340; the block runs one line longer — trivial). CONTRADICTION HUNT FOUND NOTHING that saves TAOM: no MCM gate (TaomSettings.cs has no PatchShield/Unpatch entry), only the patchshield-disabled.flag file opt-out (PatchShield.cs:105-114); coop-modules.txt is union-only so it cannot remove defaults but CAN add. I found evidence that STRENGTHENS the 'irreversible' wording: GameInterface.cs:40-44 'public void PatchAll() { if (Harmony.HasAnyPatches(harmony.Id)) { AutoSyncPatcher.RebindHandlers(); return; } ...' — once any Bannerlord.Coop patch survives elsewhere, a later PatchAll early-returns and never re-applies what PatchShield stripped. The claimed fix is also real: coop-modules.txt is parsed from RuntimeLog.ModuleDir and ADDED to CompiledModuleDefaults (CoopPresence.cs:151-152), so one line under [modules] flips ShouldUnpatchForeignOwners to false and routes targets to the _withheld set (PatchShield.cs:316-322, 358-362). Only qualifications, none refuting: the strip applies only to SHIELDED methods (TAOM-declared types, TaleWorlds.GauntletUI/TwoDimension/MountAndBlade.GauntletUI namespaces, and SaveShield targets are skipped — PatchShield.cs:59-68, 167-198), finalizers are not unpatched, and the whole path is latent — it needs a MissingMethod/MissingField/TypeLoad exception on a shared method to fire, which is not guaranteed in a matched-engine (both 1.4.7) session. That conditionality is why I grade it HIGH rather than BLOCKER, but the mechanism and the prescribed fix are exactly as claimed.",
  "corrected_claim": "TAOM's co-op detection does not recognise module id \"Coop\" (verified: the workshop SubModule.xml declares <Id value=\"Coop\"/>, and CoopPresence matches active module ids by exact case-insensitive set membership against only \"BannerlordTogether\" and \"BattleLinkMPClient\"), so CoopPresence.IsActive stays false in a BannerlordCoop session and PatchShieldPolicy.ShouldUnpatchForeignOwners(false) returns TRUE. PatchShield shields every Harmony-patched method in the AppDomain (Harmony.GetAllPatchedMethods, passes at OnSubModuleLoad and OnGameInitializationFinished — both after Coop applies its patches at MainMenuState.Handle_NetworkConnected), excluding only TAOM-declared types, the TaleWorlds.GauntletUI / TwoDimension / MountAndBlade.GauntletUI namespaces, SaveShield's own targets, and the patchshield-disabled.flag opt-out. If any shielded method then throws MissingMethodException / MissingFieldException / TypeLoadException, TryUnpatchOffendingPatches strips the prefixes, postfixes and transpilers (not finalizers) of every non-allowlisted owner on that method — and Coop's owner ids (\"Bannerlord.Coop\", \"Coop.BootFix\", \"Coop.UILoading\", \"CoopAutoRegistryFactory\") match none of PatchShieldPolicy's protected prefixes. The strip is effectively permanent for the process because GameInterface.PatchAll early-returns on Harmony.HasAnyPatches(\"Bannerlord.Coop\"), so Coop never re-applies what was removed. TAOM's own comment (PatchShield.cs:336-341) states the consequence: removing a peer's sync patch \"silently desynchronises two campaigns, which corrupts both saves and cannot be diagnosed from a log\". FIX (unchanged from the original claim, and verified reachable): add `Coop` under [modules] in Dependencies/_Module/coop-modules.txt — the file is union-merged into CompiledModuleDefaults at CoopPresence.cs:151-152 — and add it to CoopPresence.CompiledModuleDefaults for a compiled-in guarantee. CAVEAT ON SEVERITY: the strip is latent, not certain — it requires a missing-API-trinity exception on a shared shielded method, which is not expected when both mods target the same engine build. Also note the fix has a deliberate second effect documented in the same file: SaveShield will then rethrow save/load failures instead of swallowing them.",
  "corrected_severity": "HIGH"
 },
 {
  "refuted": false,
  "reason": "The quote is verbatim (CampaignTickPatch.cs lines 9-21; the claim's \"line 11\" is off by one or two - the attribute is line 9, class line 10 - but the text is exact, not paraphrased). An exhaustive grep for every patch on Campaign's tick methods returns exactly four, none of them a suppressing Prefix: CampaignTickPatch.cs:13 [HarmonyPostfix] on \"Tick\"; CampaignRobustnessPatches.cs:15 [HarmonyFinalizer] on \"RealTick\" (`if (__exception != null) Logger.Error(...); return null;`); MapEventVisibilityClientPatch.cs:17 [HarmonyPostfix] on \"RealTick\"; CampaignTimePacingPatches.cs:11 [HarmonyPostfix] on \"TickMapTime\". Grep for `typeof(Campaign), \"Tick|RealTick|TickMapTime\"` and `HarmonyPatch(\"Tick\"|\"RealTick\"|\"TickMapTime\")` across the whole tree found no other Campaign target; the remaining \"Tick\" prefixes are on other types (Army, MobilePartyAi, SiegeEventManager, BattleSimulation). I found no override, config flag, or alternate code path that freezes the client campaign - and three pieces of evidence that AFFIRM the client ticks harder than the analyst claimed. (1) CampaignTimePacingPatches.cs:14 `if (ModInformation.IsClient && ContainerProvider.TryResolve<IMapTimeTrackerInterface>(out var instance)) instance.ApplyClientSimulationTime(__instance, realDt);` and MapTimeTrackerInterface.ApplyClientSimulationTime rubber-bands time forward - `mapTimeTracker._numTicks += tickCorrection; mapTimeTracker._deltaTimeInTicks = num;` - rather than zeroing it, so client map time genuinely advances and hourly/daily campaign events fire on the client. (2) MapStatePatch.cs:11-27 is a transpiler on MapState.OnMapModeTick that REMOVES the GameStateManager.ActiveState guard (`list.RemoveRange(i - 2, 5)`) - the opposite of freezing. (3) ClientSyncPolicy.cs:23-26 `return !syncStates.Contains(clientLogic.State.GetType());` with syncStates = { typeof(CampaignState), typeof(MissionState) } confirms IsOriginalAllowed() is false during live campaign play, so the Postfix's publish branch is the live client path; CampaignTick is consumed as a client-side heartbeat by MapEventInitializationBarrier.cs:79, SiegeDefenderVictoryHandler.cs:30, TournamentSessionHandler.cs:342 and BattleHostHandler.cs:106. The claimed TAOM impact is a real consequence, not an overreach, and I found the mod's own admission of the design: client-side behavior suppression is done PER NAMED BEHAVIOR via RegisterEvents prefixes, e.g. DisableFoodConsumptionBehavior.cs:10-14 `[HarmonyPatch(\"RegisterEvents\")] private static bool Prefix() { return ModInformation.IsServer; }` and DisableRecruitmentCampaignBehavior.cs:15-25 `if (ModInformation.IsServer) { return true; } SubscribeSessionLaunchedOnly(__instance); return false;`. That pattern is repeated across ~25 files in 12 `*.Patches.Disable` namespaces (Armies, Battles, Clans, Companions, Heroes, Hideouts, MapEvents, MobileParties, PlayerCaptivityService, Settlements, Towns, Caravans). It enumerates vanilla types by name; TAOM's CampaignBehaviors are not in that list and will register and tick on both peers. TWO CORRECTIONS, neither of which overturns the claim. (a) \"Every TAOM CampaignBehavior tick handler\" is too absolute: HourlyPartyTickServerPatch.cs:11-16 `[HarmonyPrefix] [HarmonyPatch(\"HourlyTickParty\")] private static bool HourlyTickPartyPrefix(ref MobileParty mobileParty) { return ModInformation.IsServer; }` on typeof(CampaignEvents) suppresses that one dispatch entirely on the client, so TAOM handlers registered to HourlyTickPartyEvent will NOT run client-side; MobilePartyAi.Tick, Army.Tick and SiegeEventManager.Tick are likewise authority-gated. (b) Severity BLOCKER is one notch high: BannerlordCoop ships the exact remedy pattern TAOM needs (an IsServer-gated RegisterEvents prefix / early-return in the behavior), and TAOM's existing co-op detection only needs module id \"Coop\" added to the BannerlordTogether/BattleLinkMPClient list. This is mandatory work for compatibility, not an obstacle to it - HIGH, not BLOCKER.",
  "corrected_claim": "The client ticks the campaign, and campaign time genuinely advances on it. Campaign.Tick carries a Postfix only (GameInterface.Services.PlayerCaptivityService.Patches/CampaignTickPatch.cs:13); Campaign.RealTick carries only a Finalizer (CampaignRobustnessPatches.cs:15) and a Postfix (MapEventVisibilityClientPatch.cs:17); Campaign.TickMapTime carries only a Postfix (CampaignTimePacingPatches.cs:11). There is no suppressing Prefix on any of them anywhere in the tree. The client is not frozen but rubber-banded: MapTimeTrackerInterface.ApplyClientSimulationTime advances `_numTicks`/`_deltaTimeInTicks` toward server time, and MapStatePatch.cs transpiles the ActiveState guard OUT of MapState.OnMapModeTick. BannerlordCoop instead suppresses client-side simulation per named vanilla behavior, via ~25 `[HarmonyPatch(\"RegisterEvents\")] Prefix => ModInformation.IsServer` patches across 12 `*.Patches.Disable` namespaces. TAOM impact: TAOM cannot rely on the client being frozen. TAOM CampaignBehaviors are unknown to that allowlist, so their RegisterEvents runs and their tick handlers fire on BOTH peers unless TAOM gates them itself - with one carve-out: handlers subscribed to CampaignEvents.HourlyTickPartyEvent are already suppressed client-side by HourlyPartyTickServerPatch.cs:11-16, as are MobilePartyAi.Tick, Army.Tick and SiegeEventManager.Tick consumers. What TAOM must do: (1) add module id \"Coop\" to its co-op detection alongside \"BannerlordTogether\"/\"BattleLinkMPClient\"; (2) when co-op is detected, have every TAOM CampaignBehavior that mutates shared campaign state either skip RegisterEvents or early-return in its tick handlers on the non-authoritative peer, mirroring the DisableFoodConsumptionBehavior pattern.",
  "corrected_severity": "HIGH"
 },
 {
  "refuted": false,
  "reason": "The cited quote is VERBATIM — DisableMilitiasCampaignBehavior.cs is exactly 15 lines and matches character-for-character, including `return ModInformation.IsServer;`. The core mechanism claim survives every attack I made on it:\n\n(1) NO GENERIC GATE EXISTS. `rg 'CampaignBehaviorBase'` across all 3270 files returns only: NoHarmonyLoader generic constraints (Coop/Coop.Lib.NoHarmony/NoHarmonyLoader.cs:155,157,161,230), `DummyBehavior.cs:6`, and two coop-owned behaviors (PlayerPartyInteractionCampaignBehavior.cs:6, CoopTournamentCampaignBehavior.cs:13). There is no `HarmonyPatch(typeof(CampaignBehaviorBase`. Confirmed.\n\n(2) APPLICATION IS BLANKET-BY-ASSEMBLY, NOT REGISTRY-DRIVEN. GameInterface/GameInterface/GameInterface.cs:48-55: `harmony.PatchCategory(assembly, \"HarmonyStaticFixes\"); harmony.PatchAllUncategorized(assembly);` — the uncategorized hand-written Disable* prefixes are the entire behavior-suppression layer. No third-party registration hook for disable patches: NOT FOUND (HarmonyPatchCategoryRegistration is fed only by coop's own MissionModule.cs:30-31).\n\n(3) NO CONTRADICTING CODE PATH. I specifically hunted for a generic tick suppressor and it does not exist. GameInterface.Services.PlayerCaptivityService.Patches/CampaignTickPatch.cs:12-20 patches `Campaign.Tick` with a POSTFIX only (`[HarmonyPostfix] private static void Postfix_Tick()`), publishing a CampaignTick message — it does not skip the original. The only generic client suppression is GameInterface.Services.MobilePartyAIs.Patches/PartiesThinkPatch.cs:26-31, `if (ModInformation.IsClient) { return false; }`, which blocks `Campaign.PartiesThink` (party AI) only, not CampaignEvents dispatch. So client campaigns DO tick and un-gated behaviors DO fire.\n\n(4) THE IMPACT IS REAL, NOT AN OVERREACH — and the failure mode is worse than the analyst stated. GameInterface.Utils/GenericPatches.cs:727-732 (FieldIntercept) and :781-786 (PropertyIntercept): `if (ModInformation.IsClient) { Logger.Error(\"Client updated unmanaged {type}\", typeof(TItem)); fieldInfo.SetValue(instance, item); return; }` — the client write is APPLIED LOCALLY and NEVER BROADCAST. This is the live path for the shipped AutoSync output, confirmed in the mod's own AutoSyncExport: `public class Town_DynamicPatches : GenericPatches<Town_DynamicPatches, Town>` (Town/Town_DynamicPatches.cs:28). So an un-gated TAOM behavior ticking on a client silently diverges campaign state and spams the error log.\n\n(5) THE NAMED TAOM BEHAVIORS ARE ALL REAL. All 10 resolve in e:/repos/TAOM (e.g. Main/Features/AlignmentDesertion/Hooks/AlignmentDesertionBehavior.cs, Main/Features/Diplomacy/WarOfTheRingBehavior.cs, Main/Features/RaceAge/RaceAgeBehavior.cs).\n\nTWO CORRECTIONS, neither fatal to the claim:\n\n(a) THE COUNT IS WRONG BY 3x. `rg -l '\\[HarmonyPatch(\"RegisterEvents\")\\]'` = 129 files; extracting the enclosing `HarmonyPatch(typeof(X))` and de-duplicating yields ~127 distinct types, not 42. Every one of the 127 is a vanilla TaleWorlds campaign behavior (AgingCampaignBehavior … WorkshopsCampaignBehavior), so the qualitative \"all vanilla\" half is correct; the number is invented or drawn from a narrower grep.\n\n(b) THE IDIOM IS SLIGHTLY BROADER THAN \"RegisterEvents prefixes\". Some disable patches blanket-target every declared method of the behavior rather than just RegisterEvents, e.g. GameInterface.Services.Heroes.Patches.Disable/DisableHeroSpawnCampaignBehavior.cs:15 `return AccessTools.GetDeclaredMethods(typeof(HeroSpawnCampaignBehavior));`. Still hand-written and still per-vanilla-type, so this refines rather than refutes.\n\n(c) THE TAOM BEHAVIOR LIST IS PARTIAL — TAOM has 31 files matching `: CampaignBehaviorBase`, so 10 named is an undercount. This understates the work rather than overstating the impact.\n\nSEVERITY: downgrading BLOCKER to HIGH. The requirement is genuine and unavoidable, but the failure mode evidenced in the source is state divergence plus `Logger.Error(\"Client updated unmanaged ...\")` spam, not a crash or a hard load failure; it is incrementally fixable per-behavior, and only behaviors that write into the 43 AutoSync'd campaign types actually diverge (read-only or TAOM-private-state behaviors are inert). That is a HIGH-severity compatibility requirement, not a BLOCKER.",
  "corrected_claim": "BannerlordCoop suppresses double-ticking ONLY via a hand-written, per-vanilla-type allowlist of Harmony prefixes returning `ModInformation.IsServer` — the canonical form being a `[HarmonyPatch(\"RegisterEvents\")] Prefix()` (GameInterface/GameInterface.Services.Settlements.Patches.Disable/DisableMilitiasCampaignBehavior.cs:6-14), with some variants blanket-targeting every declared method of the behavior instead (DisableHeroSpawnCampaignBehavior.cs:15 `return AccessTools.GetDeclaredMethods(typeof(HeroSpawnCampaignBehavior));`). There is no generic CampaignBehaviorBase gate: `HarmonyPatch(typeof(CampaignBehaviorBase` returns nothing tree-wide, and the whole layer is applied by `harmony.PatchAllUncategorized(assembly)` (GameInterface/GameInterface/GameInterface.cs:50) with no third-party registration hook (NOT FOUND). The allowlist covers ~127 distinct types across 129 files — NOT 42 — and every one of them is vanilla (MilitiasCampaignBehavior, VillageGoodProductionCampaignBehavior, TownSecurityCampaignBehavior, AgingCampaignBehavior, … WorkshopsCampaignBehavior).\n\nClient campaigns still tick: `Campaign.Tick` carries only a Postfix (CampaignTickPatch.cs:12-20) and the sole generic client suppression is `Campaign.PartiesThink` (PartiesThinkPatch.cs:28, `if (ModInformation.IsClient) return false;`). Any behavior outside the allowlist therefore runs its hourly/daily ticks on clients, and when it writes an AutoSync'd field the intercept applies the write locally without broadcasting it — GenericPatches.cs:727-731 `if (ModInformation.IsClient) { Logger.Error(\"Client updated unmanaged {type}\", typeof(TItem)); fieldInfo.SetValue(instance, item); return; }` — producing silent divergence plus error-log spam. This path is live for shipped AutoSync output (`Town_DynamicPatches : GenericPatches<Town_DynamicPatches, Town>`).\n\nTAOM IMPACT (real, scope corrected): TAOM must author its own equivalent gate, since coop exposes no extension point. The faithful copy of coop's idiom is a single early return in each TAOM behavior's RegisterEvents — or one TAOM-owned Harmony prefix over TAOM's own behavior types — returning false when a coop client session is active. Scope is TAOM's ~31 `: CampaignBehaviorBase` subclasses, triaged by whether they mutate the 43 AutoSync'd campaign types; the 10 previously named (AlignmentDesertionBehavior, CareerQuestCampaignBehavior, CastleRecruitmentBehavior, CultureConversionBehavior, CultureMarketplaceBehavior, WarOfTheRingBehavior, MessengerCampaignBehavior, RaceAgeBehavior, SiegeDefenseBehavior, WarOfTheRingMomentumBehavior) all exist and are a subset, not the full set. Prerequisite: TAOM's co-op detection must additionally recognise module id \"Coop\".",
  "corrected_severity": "HIGH"
 },
 {
  "refuted": true,
  "reason": "The quote is verbatim, but three of the claim's load-bearing specifics are wrong and the severity is a large overreach.\n\n(1) COUNT WRONG. `grep -c \"HarmonyTranspiler\" HeroFieldPatches.cs` = **10**, not eleven. The ten intercepted fields (lines 43/79/115/151/187/226/265/301/337/373) are `LastTimeStampForActivity`, `_characterObject`, `_firstName`, `_name`, `_heroState`, `_defaultAge`, `_birthDay`, `_power`, `_homeSettlement`, `IsPregnant`. The class has no Prefix/Postfix/Prepare/priority attributes (grep returned NONE).\n\n(2) SOURCE INCOMPLETE — the analyst missed a SECOND Coop class on the same method. `AutoSyncExport/Hero/Hero_DynamicPatches.cs:33-49`: `private static IEnumerable<MethodBase> TargetMethods() { ... yield return AccessTools.Method(typeof(TaleWorlds.CampaignSystem.HeroCreator), \"DeliverOffSpring\"); }` in `[HarmonyPatch] public class Hero_DynamicPatches : GenericPatches<Hero_DynamicPatches, Hero>` (namespace `AutoSync`), carrying 9 more `[HarmonyTranspiler]`. Real total on `DeliverOffSpring` is 19 transpilers from two classes in two assemblies, not 11 from one.\n\n(3) IMPACT OVERSTATED — DECISIVE. `E:/Decompiled_Bannerlord/Campaign/TaleWorlds.CampaignSystem/TaleWorlds.CampaignSystem/HeroCreator.cs:258-281` is the entire v1.4.7 `DeliverOffSpring` body and it writes NO Hero field or property. It reads `mother.CharacterObject.Race`, calls `HeroCreationModel` methods, calls `CreateHero`, then builds a `HeroInitializationArgs` (`heroInitializationArgs.SetMother(...).SetFather(...)` etc. — setters on the args struct, not on Hero) and calls `InitializeHeroFromSettings(...)`. The actual Hero writes (`hero.Mother = ...`, `hero.Culture = ...`) live in `InitializeHeroFromSettings` at line 297+, which is NOT a TargetMethod of either Coop class. Therefore every one of Coop's 19 transpilers matches zero instructions and passes the IL through unchanged on this method. TAOM's anchor (`call Debug.SilentAssert` at line 260, the FIRST statement) cannot be perturbed. There is no functional collision — only nominal co-patching.\n\n(4) THE CLAIM'S STATED SAFETY REASONING IS ALSO FACTUALLY WRONG. \"Coop's transpilers only substitute `stfld` with a `call` ... they preserve instruction count\" is false as a general statement: `GenericPatches.cs:697-713` `FieldTranspiler` yields `new CodeInstruction(OpCodes.Ldstr, fieldName)` THEN `new CodeInstruction(OpCodes.Call, fieldIntercept)` for one `stfld` (2-for-1); `PropertyTranspiler` (756-767) does the same; `InterceptMemberMutationCalls` (350-378) injects `OpCodes.Dup`; `ArrayFieldChangeTranspiler` (550-570) injects `OpCodes.Ldarg_0`. The conclusion (TAOM survives) is right, but not for the stated reason — TAOM's matcher scans by opcode/operand over the final list and is count-agnostic anyway.\n\n(5) SCOPE. These patches apply only inside a coop session: `GameInterface.cs:40-56` `PatchAll()` → `harmony.PatchAllUncategorized(assembly)` (HeroFieldPatches is uncategorized), invoked from `MainMenuState.cs:47` `gameInterface.PatchAll();` inside `Handle_NetworkConnected`. Not a single-player startup hazard.\n\n(6) THE CLAIMED TAOM ACTION IS ALREADY DONE. `Main/Features/RaceAge/Hooks/DeliverOffSpring_RaceAssert_Patch.cs:45-52` and `:94-98` both `LogTranspilerDegradation(...)` then `return newInstructions.AsEnumerable();` — they degrade, never throw — and `LogTranspilerDegradation` (110-119) is `try { ... } catch { }`-wrapped. No audit work remains.\n\nNOT FOUND: any Coop transpiler on TAOM's other six transpiler targets. `Banner.TryGetBannerDataFromCode`, `CampaignSceneNotificationHelper`, `RefreshCharacterEntityAux` return zero hits in coopsrc; `AiVisitSettlementBehavior` appears only at `AiEngagePartyBehaviorPatches.cs:19` as a `RegisterEvents` target with `private static bool Prefix() => ModInformation.IsServer;` — a prefix on a different method, not a transpiler. So \"only transpiler-vs-transpiler *method*\" survives; \"eleven transpilers from HeroFieldPatches\" does not.",
  "corrected_claim": "NOMINAL TRANSPILER CO-PATCHING (no functional conflict) on HeroCreator.DeliverOffSpring. Two Coop classes target the method — `GameInterface.Services.Heroes.Patches.HeroFieldPatches` (HeroFieldPatches.cs:37, 10 stfld-rewriting transpilers) and the AutoSync-generated `AutoSync.Hero_DynamicPatches` (AutoSyncExport/Hero/Hero_DynamicPatches.cs:49, 9 more) — for 19 total, alongside TAOM's Patch13_RaceAge transpiler. However, the v1.4.7 `DeliverOffSpring` body (HeroCreator.cs:258-281) performs no writes to any Hero field or property; all hero state is written in the untargeted `InitializeHeroFromSettings` (line 297+). All 19 Coop transpilers therefore match nothing and pass the IL through unchanged, leaving TAOM's `Debug.SilentAssert` anchor (line 260, first statement) intact. Coop's patches apply only on coop-session start (GameInterface.cs:40-56 via MainMenuState.cs:47), not at module load. TAOM's patch already degrades to a no-op rather than throwing when its anchor is absent (DeliverOffSpring_RaceAssert_Patch.cs:45-52, 94-98) with a try/catch-wrapped logger (110-119). No TAOM change required; this is a documentation/watch item only — re-check if a future Coop release adds `InitializeHeroFromSettings` to either TargetMethods list, or if the engine inlines it into DeliverOffSpring.",
  "corrected_severity": "LOW"
 },
 {
  "refuted": true,
  "reason": "Partial refutation: the quote is verbatim and the MECHANISM is fully correct, but the IMPACT is overstated and the analyst missed a countervailing gate.\n\nUPHELD (verified):\n- The quote at CampaignTimePacingPatches.cs:8 is exact, not paraphrased. It is the ONLY patch on Campaign.TickMapTime (grep confirms one hit); postfix-only, so vanilla runs.\n- Vanilla MapTimeTracker.Tick does \"_deltaTimeInTicks = (long)(seconds * (float)CampaignTime.TimeTicksPerSecond); _numTicks += _deltaTimeInTicks;\" — the clock advances locally.\n- MapTimeTrackerInterface.ApplyClientSimulationTime applies a bounded correction: \"mapTimeTracker._numTicks += tickCorrection\" where GetTickCorrection clamps via \"val = Math.Max(0.0 - num, Math.Min(localDeltaTicks, val))\" with \"num = (float)localDeltaTicks * 0.25f\". Authoritative assignment (\"mapTimeTracker._numTicks = serverTicks\") occurs only on TryConsumeHardSync or ApplyCampaignJoinBaseline. Slew, not server-driven. Claim's mechanism is right.\n\nREFUTED (impact overstated — \"Any TAOM DailyTick\" is false):\nCoop hard-blocks an entire tick family on the client. GameInterface/GameInterface.Services.MobileParties.Patches/PartyTickPatch.cs:\n  [HarmonyPatch(typeof(CampaignPeriodicEventManager), \"TickPeriodicEvents\")]\n  [HarmonyPrefix]\n  private static bool Prefix_TickPeriodicEvents() { return ModInformation.IsServer; }\nVanilla TickPeriodicEvents() is \"PeriodicHourlyTick(); PeriodicDailyTick(); PeriodicQuarterDailyTick();\" covering DailyTickParty/Town/Settlement/Hero/Clan, HourlyTickParty/Settlement/Clan, MobileParty.DailyTick and QuarterDailyPartyTick. All suppressed client-side. The analyst did not account for this.\n\nBUT THE CORE HAZARD SURVIVES (so severity holds):\nThe GLOBAL ticks travel a different path. Campaign.CreateCampaignEvents does \"_dailyTickEvent = CampaignPeriodicEventManager.CreatePeriodicEvent(CampaignTime.Days(1f), initialWait); _dailyTickEvent.AddHandler(DailyTick);\" and Campaign.DailyTick does \"CampaignEventDispatcher.Instance.DailyTick();\" plus WeeklyTick. These fire via CampaignPeriodicEventManager.OnTick -> SignalPeriodicEvents() -> MBCampaignEvent.CheckUpdate(), called from Campaign.Tick(). Grepping the coop tree for SignalPeriodicEvents / MBCampaignEvent / CheckUpdate / CampaignPeriodicEventManager \"OnTick\" returns NOT FOUND. Campaign.Tick carries only a postfix (CampaignTickPatch.cs, Postfix_Tick). So DailyTickEvent, HourlyTickEvent, WeeklyTickEvent and QuarterHourlyTickEvent DO fire on the client off the locally-slewed clock.\n\nAutoSync provides no backstop. AutoSyncExport/Hero/Hero_DynamicPatches.cs TargetMethods() enumerates only AccessTools.GetDeclaredConstructors(typeof(Hero)) and AccessTools.GetDeclaredMethods(typeof(Hero)) plus six hardcoded engine methods — TAOM's own IL is never transpiled, and Hero.Culture is a public field (\"public CultureObject Culture;\" Hero.cs:117), so a TAOM write emits stfld inside TAOM's assembly and is invisible to the interceptor. Even when intercepted, the client branch is \"if (ModInformation.IsClient) { Logger.Error(\\\"Client updated managed {type}\\\", \\\"Culture\\\"); instance.Culture = newValue; return; }\" — logs and applies, does not block.",
  "corrected_claim": "The client's campaign clock advances locally and is slewed toward the server, not driven by it — CONFIRMED exactly as claimed (Campaign.TickMapTime has a postfix only; ApplyClientSimulationTime applies a delta to MapTimeTracker._numTicks clamped to [-0.25 * localDeltaTicks, +localDeltaTicks], with hard assignment reserved for desync/join).\n\nCorrected impact scope: campaign day/hour boundaries ARE crossed independently on the client, but coop already suppresses the PER-OBJECT tick family. PartyTickPatch.cs prefixes CampaignPeriodicEventManager.TickPeriodicEvents and MobilePartyHourlyTick with \"return ModInformation.IsServer\", killing DailyTickParty/Town/Settlement/Hero/Clan, HourlyTickParty/Settlement/Clan, QuarterDailyPartyTick and MobileParty.DailyTick/HourlyTick on the client.\n\nThe GLOBAL tick events are NOT suppressed — they run through CampaignPeriodicEventManager.OnTick -> SignalPeriodicEvents -> MBCampaignEvent.CheckUpdate, none of which coop patches (NOT FOUND). So CampaignEvents.DailyTickEvent, HourlyTickEvent, WeeklyTickEvent and QuarterHourlyTickEvent genuinely double-run.\n\nAgainst TAOM's 12 tick registrations: 7 double-run — CultureConversionBehavior.cs:45, WarOfTheRingBehavior.cs:34, WarOfTheRingMomentumBehavior.cs:79, RaceAgeBehavior.cs:31, CareerQuestCampaignBehavior.cs:40 (DailyTickEvent); MessengerCampaignBehavior.cs:64, SiegeDefenseBehavior.cs:36 (HourlyTickEvent). 5 are already gated by coop — AlignmentDesertionBehavior.cs:34-35, CastleRecruitmentBehavior.cs:43, CultureMarketplaceBehavior.cs:65 (DailyTickSettlement/Party), SpecialResourcesBehavior.cs:51 (DailyTickHero).\n\nSeverity stays BLOCKER: CultureConversion mutating settlement culture on a client-local daily boundary is exactly the shared-state divergence the claim describes, and AutoSync will not catch it (TAOM's stfld is outside the transpiled method set; the interceptor's client branch logs and applies anyway).\n\nWhat TAOM must do: add a server-authority guard to the 7 global-tick handlers (early-return when a coop client is detected), and extend TAOM's co-op detection to recognise module id \"Coop\" alongside \"BannerlordTogether\" and \"BattleLinkMPClient\". TAOM should mirror coop's own gate shape rather than invent one.",
  "corrected_severity": "BLOCKER"
 },
 {
  "refuted": false,
  "reason": "The quote is verbatim, not paraphrased: Common/Common/ModInformation.cs lines 8-10 read exactly \"public static bool IsServer { get; set; } = false;\" / \"public static bool IsClient => !IsServer;\". A repo-wide grep for `IsServer *=` finds exactly two assignment sites, both at session start: Coop.Core/Coop.Core/CoopartiveMultiplayerExperience.cs:367 (\"ModInformation.IsServer = true;\" inside StartAsServerCore) and :501 (\"ModInformation.IsServer = false;\" inside StartAsClientCore). Nothing else writes it — CoopMod.cs's `isServer` (line 71/76, set from the /server and /client command-line switches) is a separate private field that is never propagated to ModInformation. Common.dll genuinely loads in ordinary single-player: Coop/Coop/CoopMod.cs NoHarmonyInit() calls AssemblyHellscape.CreateAssemblyBindingRedirects() and GameThread.Instance.MarkGameThread(), and GameThread.cs is \"namespace Common\", so the static initializer runs and IsServer==false / IsClient==true with no coop session. I looked for a contradicting guard and found none — instead I found corroboration that goes further than the claim: (1) DestroyContainerCore() clears the Autofac container and calls ContainerProvider.Clear() but never resets ModInformation.IsServer, so the flag is sticky and IsClient is *false* for the rest of the process after hosting once; (2) Coop itself never uses IsClient as a bare gate — every call site sits behind CallOriginalPolicy.IsOriginalAllowed() (GameInterface/GameInterface.Policies/CallOriginalPolicy.cs:11-27), which returns true whenever ContainerProvider.TryResolve<ISyncPolicy> fails, e.g. GameInterface.Registry.Auto/LifetimePatches.cs:15-17 (\"if (!CallOriginalPolicy.IsOriginalAllowed()) { if (ModInformation.IsClient)\") and GameInterface.AutoSync.Templates.Patches.FieldSetTranspilerTemplate.txt:43-51 (\"if (CallOriginalPolicy.IsOriginalAllowed()) { ... return; } if (ModInformation.IsClient)\"). The claimed TAOM impact is therefore a real consequence, not an overreach in substance. Two precision defects, neither fatal: the prescribed predicate ContainerProvider.TryGetContainer(out _) is a coarser proxy than Coop's own CallOriginalPolicy.IsOriginalAllowed() (which additionally folds in AllowedThread.IsThisThreadAllowed() and the resolved ISyncPolicy), and \"subscribed\" should read \"enabled in the launcher\" since a subscribed-but-disabled module never loads Common.dll. Severity is the one genuine overstatement: TAOM contains zero references to ModInformation today, so nothing is currently broken and nothing is blocked — this is a guardrail on code not yet written whose fix is one extra conjunct. BLOCKER implies an obstacle to compatibility; this is a design constraint that costs nothing to honour. Downgrading to MEDIUM (silent, total single-player breakage if the naive predicate is ever written, and the sticky flag makes the bug non-obvious in testing). Separately worth noting for the wider analysis, though not part of this claim: ContainerProvider.Alive (ContainerProvider.cs:30, \"public static bool Alive { get; } = _lifetimeScope != null;\") is evaluated once at static init and is permanently false — anyone reaching for it instead of TryGetContainer gets a silently dead predicate.",
  "corrected_claim": "ModInformation.IsServer (Common/Common/ModInformation.cs:8) defaults to false and IsClient is defined as !IsServer (line 10), so IsClient evaluates TRUE in ordinary single-player whenever the Coop module is merely ENABLED (not merely subscribed — a disabled module never loads Common.dll; when enabled, CoopMod.NoHarmonyInit's use of Common.GameThread forces the static initializer to run). IsServer is written at exactly two sites, both at session start: CoopartiveMultiplayerExperience.cs:367 (StartAsServerCore, =true) and :501 (StartAsClientCore, =false). It is additionally STICKY — DestroyContainerCore() clears the container via ContainerProvider.Clear() but never resets IsServer — so after hosting a session and returning to the main menu, IsClient reads FALSE for the remainder of the process. Bare IsClient is therefore wrong in both directions and is never used as a standalone gate by Coop itself: every Coop call site sits behind CallOriginalPolicy.IsOriginalAllowed() (GameInterface/GameInterface.Policies/CallOriginalPolicy.cs:11-27), which returns true (= \"not in coop, run vanilla\") whenever ContainerProvider.TryResolve<ISyncPolicy> fails. TAOM IMPACT: TAOM must never gate tick or authority behavior on IsClient alone; the predicate must be conjoined with a live-session check. Prefer mirroring Coop's own gate — treat \"no resolvable ISyncPolicy / no container\" as single-player — using ContainerProvider.TryGetContainer(out _) as the session probe. Do NOT use ContainerProvider.Alive (ContainerProvider.cs:30): it is initialized once at static-init time and is permanently false. Since TAOM cannot take a hard compile-time dependency on Coop's Common.dll, both reads must go through reflection, with \"type not found\" meaning single-player.",
  "corrected_severity": "MEDIUM"
 },
 {
  "refuted": true,
  "reason": "The quote is verbatim and the mechanism is real, but the claimed TAOM impact is factually wrong on one third of its own evidence, and its central causal assertion is false as stated.\n\n(1) QUOTE VERIFIED. coopsrc/Coop/Coop/AssemblyHellscape.cs:10-14 contains the 13-name array exactly as quoted. It is genuinely wired: CoopMod.cs:68, inside NoHarmonyInit(), calls \"AssemblyHellscape.CreateAssemblyBindingRedirects();\". No guard, no config flag, no alternate path — grep for AssemblyResolve/AssemblyHellscape across all 3260 decompiled files returns only these two sites. All 12 cited version numbers verify exactly via AssemblyName.GetAssemblyName on disk (Coop: Serilog 4.2.0.0, System.Memory 4.0.2.0, System.Buffers 4.0.4.0, System.Numerics.Vectors 4.1.5.0, S.R.CS.Unsafe 6.0.1.0, S.C.Immutable 9.0.0.2; TAOM: 2.0.0.0, 4.0.1.1, 4.0.3.0, 4.1.4.0, 4.0.4.1, 1.2.5.0).\n\n(2) THE OVERLAP SET IS WRONG. TAOM's actual list is Dependencies/SubModule.cs:39-69, \"RedirectedSimpleNames\", 22 entries: 0Harmony, MCMv5, Bannerlord.UIExtenderEx, Bannerlord.ButterLib, Bannerlord.Harmony, 5x Microsoft.Extensions.*, Serilog, Serilog.Extensions.Logging, Mono.Cecil, MonoMod.Core, MonoMod.Utils, System.Buffers, System.Memory, System.Numerics.Vectors, System.Runtime.CompilerServices.Unsafe, System.Threading.Tasks.Extensions, System.ValueTuple, Newtonsoft.Json. \"System.Collections.Immutable\" is NOT present. TAOM ships the DLL at 1.2.5.0 in Dependencies/_Module/bin/Win64_Shipping_Client/ but does not redirect the name — so TAOM's handler can never answer GameInterface's reference to it (GameInterface.csproj:237-238, HintPath to Coop's 9.0.0.2). The claim's headline example is the one name where TAOM is provably not involved. The genuine sixth overlap is System.Threading.Tasks.Extensions (SubModule.cs:65), which the claim never mentions.\n\n(3) \"TAOM's handler answers first for every one of these names\" IS FALSE. TAOM's handler is not a prebuilt map like Coop's LoadedRedirects dictionary. RedirectBundledDependencies (SubModule.cs:101-119) walks AppDomain.CurrentDomain.GetAssemblies() and returns null when no assembly with that simple name is already loaded. TAOM ships NO System.Threading.Tasks.Extensions.dll in either _Module/bin (verified: \"NOT PRESENT\"; the only copies in the repo are under TAOM.Tests/bin and tools/), so TAOM returns null for it and Coop's own 4.2.1.0 resolves. Combined with (2), TAOM's handler cannot answer for 2 of the 6 names the claim lists.\n\n(4) THE TRIGGER IS UNDEMONSTRATED. AssemblyResolve is a last-resort event that fires only after normal binding fails. Coop ships its own copies of all seven DLLs alongside its assemblies in bin/Win64_Shipping_Client/ (directory listing confirms Serilog.dll, System.Memory.dll, System.Buffers.dll, System.Numerics.Vectors.dll, System.Runtime.CompilerServices.Unsafe.dll, System.Collections.Immutable.dll, System.Threading.Tasks.Extensions.dll), which is the LoadFrom-context probe path for its own dependencies. The claim asserts the hijack happens for \"every one of these names\" without showing the binder fails first. That is the difference between BLOCKER and a defensive hardening item.\n\n(5) The ordering premise is right but for the wrong reason. Module id \"Coop\" is pinned NOWHERE in TAOM — not in Dependencies/_Module/coop-modules.txt ([modules] lists only BannerlordTogether and BattleLinkMPClient) and not in either SubModule.xml ModulesToLoadAfterThis (Dependencies/_Module/SubModule.xml:58-70 pins only those same two). TAOM's handler still registers first, but transitively: Dependencies/_Module/SubModule.xml:23 pins Native after TAOM.Dependencies, and Coop's SubModule.xml DependedModules requires Native. The claim credits an explicit pin that does not exist.\n\nThe surviving finding is real and worth fixing — for five names (Serilog, System.Memory, System.Buffers, System.Numerics.Vectors, System.Runtime.CompilerServices.Unsafe) TAOM's version-blind redirect can hand Coop a downlevel assembly it was not compiled against — but the claim as written misidentifies a third of its own overlap set and overstates the mechanism as certain.",
  "corrected_claim": "Coop installs an AssemblyResolve handler (coopsrc/Coop/Coop/AssemblyHellscape.cs:10-14, wired at CoopMod.cs:68 in NoHarmonyInit) claiming 13 assembly simple names. SIX overlap TAOM's 22-name version-blind list in Dependencies/SubModule.cs:39-69 — but the set is Serilog, System.Memory, System.Buffers, System.Numerics.Vectors, System.Runtime.CompilerServices.Unsafe, and System.Threading.Tasks.Extensions. System.Collections.Immutable is NOT one of them: TAOM ships it (1.2.5.0) but does not redirect the name, so TAOM's handler is not involved despite GameInterface referencing Coop's 9.0.0.2.\n\nTAOM's handler registers first (transitively, not by an explicit pin — \"Coop\" appears in neither coop-modules.txt nor either SubModule.xml; ordering comes from Dependencies/_Module/SubModule.xml:23 pinning Native after TAOM.Dependencies, and Coop DependedModule'ing Native). But it is not an unconditional map: RedirectBundledDependencies (SubModule.cs:101-119) scans AppDomain.CurrentDomain.GetAssemblies() and returns null if no assembly with that simple name is already loaded. TAOM ships no System.Threading.Tasks.Extensions.dll, so it returns null there too.\n\nActual hijack surface: FIVE names where TAOM both redirects and ships a mismatched version — Serilog 2.0.0.0 vs Coop's 4.2.0.0, System.Memory 4.0.1.1 vs 4.0.2.0, System.Buffers 4.0.3.0 vs 4.0.4.0, System.Numerics.Vectors 4.1.4.0 vs 4.1.5.0, System.Runtime.CompilerServices.Unsafe 4.0.4.1 vs 6.0.1.0. Coop's Common.dll/GameInterface.dll/Coop.dll are compiled against the higher versions (Common.csproj:16-17, GameInterface.csproj:149-150, Coop.csproj:15-16).\n\nThis is a conditional hazard, not a demonstrated blocker: AssemblyResolve fires only after normal binding fails, and Coop ships its own copies of all seven DLLs in its own bin/Win64_Shipping_Client/, which is on the LoadFrom-context probe path for its dependencies. Compounding factor the original claim missed: Coop's LoadedRedirects (AssemblyHellscape.cs:16) is a static field initializer running AppDomain.CurrentDomain.Load(str) for all 13 names at type-init — i.e. before the += on line 20 — so whatever those partial-name Loads bind to becomes Coop's permanent map, and TAOM's already-registered version-blind handler can seed it with TAOM's downlevel copies.\n\nTAOM SHOULD make RedirectBundledDependencies version-aware (return the loaded assembly only when its version satisfies the requested AssemblyName, else null) and add \"Coop\" to coop-modules.txt [modules] plus ModulesToLoadAfterThis in both manifests. Severity HIGH, not BLOCKER — real and worth hardening defensively, but the failure was not shown to actually fire.",
  "corrected_severity": "HIGH"
 },
 {
  "refuted": false,
  "reason": "Quote is verbatim (SubModule.xml lines 8-14) and the full 37-line file confirms the stronger reading: Coop declares NO <ModulesToLoadAfterThis>, NO <IncompatibleModules>, NO <DependedModuleMetadatas> at all — only 5 vanilla <DependedModule> entries. Grepping the decompiled tree for IncompatibleModules/ModulesToLoadAfterThis/ModuleHelper/GetActiveModules found no contradicting code path: Coop's only module awareness is runtime client/server manifest parity (TaleWorldsModuleInfoProvider.cs:10 'ModuleHelper.GetActiveModules()' -> NetworkModuleVersionsValidate.cs:23), not load order. TAOM side confirmed: Main/_Module/SubModule.xml:36-39 and Dependencies/_Module/SubModule.xml:69-70 pin only BannerlordTogether + BattleLinkMPClient; neither mentions Coop. The proposed mechanism is engine-real, not launcher folklore: ModuleInfo.cs:131 parses 'ModulesToLoadAfterThis'/'Module' and ModuleHelper.cs:264 feeds it to MBMath.TopologySort, while that file contains zero DependedModuleMetadata references — exactly the vanilla-vs-BUTR split the claim asserts. The 'do NOT add <DependedModule Id=\"Coop\"/>' caution quotes real reasoning already in Main/_Module/SubModule.xml:26. HIGH severity is if anything understated: Coop ships its own 0Harmony.dll (2,461,696 bytes, with Mono.Cecil + MonoMod.*) in bin/Win64_Shipping_Client/, which is precisely the bundled-Harmony collision that Dependencies/_Module/SubModule.xml:58-68 cites as the reason for the existing pins. Two non-fatal imprecisions: Dependencies/_Module/SubModule.xml's ModulesToLoadAfterThis has 28 entries, not 'the same two ids' (only its trailing co-op block is those two); and the impact omits the third coupled file, Dependencies/_Module/coop-modules.txt [modules], which both manifests' comments require be kept in sync and which BundledDependencyManifestTests.ModulesToLoadAfterThis_ContainsEveryCoopModuleIdFromTheShippedConfig pins.",
  "corrected_claim": "Coop's SubModule.xml (E:/Steam/steamapps/workshop/content/261550/3770450698/SubModule.xml) declares no ordering whatsoever — no <ModulesToLoadAfterThis>, no <IncompatibleModules>, no <DependedModuleMetadatas> — only five vanilla <DependedModule> entries (Native, SandBoxCore, Sandbox, CustomBattle, StoryMode @ v1.4.7). Nothing in the decompiled Coop source establishes load order either; its only module awareness is runtime client/server manifest parity via ModuleHelper.GetActiveModules() (TaleWorldsModuleInfoProvider.cs:10 -> NetworkModuleVersionsValidate.cs:23). TAOM's pins currently list only BannerlordTogether and BattleLinkMPClient, so all ordering must be asserted from TAOM's side. Required edits are THREE coupled files, not two: (1) add '<Module Id=\"Coop\" />' to <ModulesToLoadAfterThis> in Main/_Module/SubModule.xml (lines 36-39) — the engine-honoured pin, since ModuleInfo.cs:131 parses this element and ModuleHelper.cs:264 feeds it to MBMath.TopologySort while nothing parses DependedModuleMetadatas; (2) add the same '<Module Id=\"Coop\" />' to the co-op block of Dependencies/_Module/SubModule.xml's <ModulesToLoadAfterThis> (which already holds 28 entries, of which lines 69-70 are the two co-op ids); (3) add 'Coop' to the [modules] section of Dependencies/_Module/coop-modules.txt — both manifests' comments mandate this sync and BundledDependencyManifestTests.ModulesToLoadAfterThis_ContainsEveryCoopModuleIdFromTheShippedConfig enforces it (coop-modules.txt -> both manifests). Mirror with '<DependedModuleMetadata id=\"Coop\" order=\"LoadAfterThis\" optional=\"true\" />' in both files for BUTR/BLSE launchers. Do NOT add a <DependedModule Id=\"Coop\"/> — Main/_Module/SubModule.xml:26 already reasons that would make TAOM unlaunchable without the co-op mod. Severity is HIGH and arguably understated: Coop ships its own 0Harmony.dll (2,461,696 bytes) plus Mono.Cecil/MonoMod in bin/Win64_Shipping_Client/, which is exactly the bundled-Harmony AppDomain collision Dependencies/_Module/SubModule.xml:58-68 gives as the reason the existing pins exist.",
  "corrected_severity": "HIGH"
 },
 {
  "refuted": false,
  "reason": "Quote verified verbatim at E:/repos/TAOM/Dependencies/SubModule.cs:101-112, including the `Assembly?` return type and both nested loops. It supports the claim exactly: `var requested = new AssemblyName(args.Name)` feeds only `requested.Name` into the `string.Equals(requested.Name, simpleName, StringComparison.Ordinal)` test, and the inner loop `return loaded;` fires on the first `AppDomain.CurrentDomain.GetAssemblies()` entry matching by `loaded.GetName().Name` — `loaded.GetName().Version` is never read. `requested.Version` is genuinely dead.\n\nNo overriding guard exists anywhere. `RedirectedSimpleNames` (lines 39-69) is a compile-time `static readonly string[]` with no conditional. `InstallAssemblyResolveHandler` (line 80) is called from the static cctor (line 23) and from AliasStubSubModule.cs:45 — both strictly before `CoopPresence.Refresh()` (SubModule.cs:182). `grep -n \"Redirect|Assembly|Serilog|Version\"` over Dependencies/Foundation/CoopPresence.cs returns only an unrelated doc-comment. `Dependencies/_Module/coop-modules.txt` self-documents its scope as PatchShield/SaveShield only (\"TAOM stops PatchShield from unpatching third-party Harmony patches and makes SaveShield rethrow save/load failures\") and does not list \"Coop\".\n\nThe impact premise is empirically confirmed, not speculation. Measured via System.Reflection.AssemblyName.GetAssemblyName over both bin folders: Serilog TAOM Asm=2.0.0.0/Prod=3.0.1 vs COOP Asm=4.2.0.0/Prod=4.2.0; System.Runtime.CompilerServices.Unsafe 4.0.4.1 vs 6.0.1.0; System.Memory 4.0.1.1 vs 4.0.2.0; System.Buffers 4.0.3.0 vs 4.0.4.0; System.Numerics.Vectors 4.1.4.0 vs 4.1.5.0; System.Threading.Tasks.Extensions absent in TAOM vs 4.2.1.0 in Coop. TAOM's copy is older in every case where it ships one. All are strong-named with identical public key tokens (Serilog PKT 24-C2-F7-52-A8-E5-8A-10 on both sides), so the CLR would normally refuse the downgrade — only an AssemblyResolve handler can force it, and .NET Framework accepts the handler's return without identity verification. The claimed six-name collision set is exactly the intersection of TAOM's RedirectedSimpleNames with Coop's 13-name list in coopsrc/Coop/Coop/AssemblyHellscape.cs:10-14.\n\nLoad order favours TAOM: Dependencies/_Module/SubModule.xml pins `<Module Id=\"Native\" />` in `<ModulesToLoadAfterThis>`, and Coop's SubModule.xml declares `<DependedModule Id=\"Native\" DependentVersion=\"v1.4.7\"/>`, so TAOM's handler registers first and AssemblyResolve invokes in registration order. TAOM's Serilog is provably in the AppDomain early: Dependencies/_Module/SubModule.xml lists `<Assembly value=\"Serilog.dll\" />` under the ButterLib SubModule entry.\n\nCorroboration the original analyst did not cite, which strengthens rather than weakens the finding: coopsrc/Coop/Coop/AssemblyHellscape.cs:16 force-loads all 13 names in a static field initializer — `private static readonly Dictionary<string, Assembly> LoadedRedirects = RedirectedAssemblies.ToDictionary((string str) => str, (string str) => AppDomain.CurrentDomain.Load(str));` — invoked from CoopMod.NoHarmonyInit() (coopsrc/Coop/Coop/CoopMod.cs:68 `AssemblyHellscape.CreateAssemblyBindingRedirects();`). Coop's own handler then serves that dictionary for the entire session (line 24), so one bad bind is cached permanently.\n\nThe single defect in the original submission is the proposed remedy, not the finding. \"Compare loaded.GetName().Version to requested.Version and return null when the loaded version is lower, so the CLR falls through to Coop's handler or Coop's own bin\" fails on two grounds: (a) `AppDomain.CurrentDomain.Load(str)` passes a bare simple name, so `requested.Version` is null and a lower-version comparison has no operand — TAOM's copy is returned regardless; (b) \"falls through to Coop's handler\" leads to `LoadedRedirects`, which was itself populated through that same ambiguous bare-name path. Under the verdict rules a wrong remedy is not a false claim, and the claim as literally stated (\"matches on simple name only and returns the first loaded assembly with that name, discarding the requested version entirely\") is exactly correct. Severity BLOCKER stands: Serilog 3.0.1 force-bound in place of 4.2.0 also breaks Coop's own Serilog.Enrichers.Process / Serilog.Sinks.File / Serilog.Sinks.Seq DLLs, and CoopMod.SetupLogging (CoopMod.cs:153) calls `LogManager.Configuration.Enrich.WithProcessId().WriteTo.Sink(...)` during NoHarmonyInit, i.e. at module startup.",
  "corrected_claim": "TAOM's AssemblyResolve handler (Dependencies/SubModule.cs:101-119) matches on simple name only and returns the first already-loaded assembly with that name, discarding the requested version entirely. Six of its 22 RedirectedSimpleNames collide with BannerlordCoop's 13 force-loaded assemblies (AssemblyHellscape.cs:10-14): Serilog, System.Memory, System.Buffers, System.Numerics.Vectors, System.Runtime.CompilerServices.Unsafe, System.Threading.Tasks.Extensions. TAOM's copy is strictly older in every case it ships one (Serilog Asm 2.0.0.0/Prod 3.0.1 vs Coop 4.2.0.0; Unsafe 4.0.4.1 vs 6.0.1.0; System.Memory 4.0.1.1 vs 4.0.2.0; System.Buffers 4.0.3.0 vs 4.0.4.0; System.Numerics.Vectors 4.1.4.0 vs 4.1.5.0). All are strong-named with matching public key tokens, so only the handler can force the downgrade. TAOM.Dependencies pins Native in ModulesToLoadAfterThis and Coop DependedModule's Native, so TAOM's handler always registers and fires first. Coop caches the result permanently: AssemblyHellscape.cs:16 populates LoadedRedirects once via AppDomain.CurrentDomain.Load(simpleName) inside a static field initializer, and serves that dictionary from its own handler for the whole session.\n\nCORRECTION to the proposed fix: comparing requested.Version does NOT work, because Coop's force-load passes a bare simple name (requested.Version is null, so there is nothing to compare) and because \"falling through to Coop's handler\" reaches a dictionary populated by that same ambiguous bare-name path. The working fix is the analyst's stated alternative applied UNCONDITIONALLY — delete those six entries from RedirectedSimpleNames — not gated on CoopPresence, since the resolver installs in the static cctor (SubModule.cs:23) long before any co-op probe runs and coop-modules.txt does not list \"Coop\" at all.",
  "corrected_severity": "BLOCKER"
 },
 {
  "refuted": true,
  "reason": "The quote is verbatim (ModuleValidator.cs:29-33) and the version-strictness core holds, but the claim's scope and its TAOM impact are both wrong. (1) Scope: ShouldValidateModule (ModuleValidator.cs:45-56) is not \"every non-official module\" — it additionally skips any id starting \"DedicatedServer.\" (`if (module.Id != null && module.Id.StartsWith(\"DedicatedServer.\", StringComparison.OrdinalIgnoreCase)) return false;`) and it deliberately KEEPS official DLC modules (`if (module.IsOfficial && !module.IsDlc) return false;` falls through for a DLC, which is IsOfficial==true && IsDlc==true). (2) ValidateNoDlc is one-sided: Validate calls `ValidateNoDlc(clientModules, out error)` (line 15) only — a host with DLC enabled is never checked. (3) The claimed impact is an overreach and directionally backwards: the gate fires on mismatch between two installs, not on failure to bump. TAOM's stubs are pinned constants that intentionally do not track upstream — Stubs/Bannerlord.Harmony/_Module/SubModule.xml states \"Version strategy (BetaDeps parity, DR3 Phase 4 — 2026-05-25): pinned to v2.4.99.0 rather than the actual shipped v2.4.2.\" A version that never changes is the least likely component to drift, so \"TAOM's release process must lockstep-bump all of these\" is the opposite of what the code requires. (4) The claim also omits the actually-important consequence in the same method, ModuleValidator.cs:35-40: `clientModules.Where(clientModule => serverModules.All(moduleInfo2 => moduleInfo2.Id != clientModule.Id))` producing \"Server does not support module '<Id>'.\" — this makes it SET equality, so a TAOM player running any personal extra mod the host lacks is also rejected; that is the real support burden, not the stubs. (5) There is a narrow client-side bypass the claim did not surface, ValidateModuleState.cs:92: `if (obj.What.Matches || string.Equals(obj.What.Reason, \"Server does not support module 'Coop'.\", StringComparison.Ordinal))` — an exact-string escape hatch for the dedicated-server case; it does not rescue version mismatches, but it shows the path is not unconditionally fatal. Confirmed-correct parts: ApplicationVersion.IsSame(other, checkChangeSet:true) compares ApplicationVersionType/Major/Minor/Revision/ChangeSet (E:/Decompiled_Bannerlord/Core/TaleWorlds.Library/TaleWorlds.Library/ApplicationVersion.cs:71-82); IsDlc == (Type == ModuleType.OfficialOptional) (TaleWorldsModuleInfoProvider.cs:13); the list comes from ModuleHelper.GetActiveModules(); ModuleValidator is DI-registered (Coop.Core/Coop.Core.Common/CommonModule.cs:35) on the mandatory join path (ValidateModuleState.cs:61 -> ResolveCharacterState.Handle_ModuleVersionsValidate). TAOM's Main (Id \"TAOM\", v2.0.15), Dependencies (Id \"TAOM.Dependencies\", v2.0.5) and all four stubs carry no <Official> node or `<Official value=\"false\" />`, so they are all validated.",
  "corrected_claim": "Coop's ModuleValidator (GameInterface.Services.Modules.Validators/ModuleValidator.cs) enforces exact SET equality of the active module list between host and joining client: for each module surviving ShouldValidateModule, the ids must match both directions (server-side missing -> \"To join the server the module '<Id>' with version '<V>' is required.\"; client-side extra -> \"Server does not support module '<Id>'.\") and versions must satisfy ApplicationVersion.IsSame(other, checkChangeSet: true), i.e. Type/Major/Minor/Revision/ChangeSet all equal. ShouldValidateModule skips official non-DLC modules AND any id beginning \"DedicatedServer.\" (case-insensitive), but does NOT skip official DLC modules. ValidateNoDlc rejects modules whose Type == ModuleType.OfficialOptional, but is applied to the CLIENT list only (line 15) — the host's DLC state is unchecked. Module lists come from ModuleHelper.GetActiveModules(). For TAOM this means: TAOM (v2.0.15), TAOM.Dependencies (v2.0.5), TAOM_Map, and the four non-official alias stubs (Bannerlord.Harmony v2.4.99.0, Bannerlord.UIExtenderEx v2.13.99.0, Bannerlord.ButterLib v2.11.99.0, Bannerlord.MBOptionScreen v5.12.99.0) are all validated, and every participant must run a byte-identical enabled-module set. This requires NO TAOM code change — it is a packaging/documentation constraint. The stubs are the least fragile part (their versions are deliberately pinned constants and do not track upstream, per the comment in Stubs/Bannerlord.Harmony/_Module/SubModule.xml), so no \"lockstep bump\" discipline is needed; the real risks are (a) a player who installed BUTR's genuine Bannerlord.Harmony/ButterLib/MCM instead of TAOM's stub, and (b) any personal extra mod on either side, which the client-extra branch rejects outright. The only escape hatch in the flow is a narrow exact-string bypass in ValidateModuleState.cs:92 for the reason \"Server does not support module 'Coop'.\" (dedicated-server case); it does not relax version checks.",
  "corrected_severity": "MEDIUM"
 },
 {
  "refuted": false,
  "reason": "VERBATIM CHECK — PASS. `GameInterface/GameInterface.Services.Time.Patches/GameStateManagerPatches.cs` is 38 lines and matches the cited quote character-for-character, starting at line 7 (`[HarmonyPatch(typeof(GameStateManager))]`). No paraphrase, no omitted guard, no policy check: the OnTick prefix body is unconditional apart from the `ActiveState is MapState` early-return.\n\nDOES IT SUPPORT THE CLAIM — YES, ON BOTH HALVES.\n(a) Force-tick. Vanilla `GameStateManager.LastOrDefault<T>()` is real (C:\\Users\\mikew\\.taom-src\\v1.4.7\\TaleWorlds.Core.GameStateManager.cs:158 `public T LastOrDefault<T>() where T : GameState`), and `GameState.OnTick` is `protected internal virtual void OnTick(float dt)` (TaleWorlds.Core.GameState.cs:146), so the `((TaleWorlds.Core.GameState)mapState).OnTick(dt)` callvirt dispatches to `MapState.OnTick`. That override runs `OnMapModeTick(dt)` (TaleWorlds.CampaignSystem.GameState.MapState.cs:160).\n(b) Disable-request block. Vanilla `GameStateManager.OnTick` is: `if (ActiveStateDisabledByUser) { ActiveState.OnIdleTick(dt); } else { ActiveState.OnTick(dt); }` (GameStateManager.cs:186-200) with `ActiveStateDisabledByUser => _activeStateDisableRequests.Count > 0` (line 67). A prefix returning false on `RegisterActiveStateDisableRequest` pins that list empty forever, so the idle-tick branch is dead.\n\nSEARCHED FOR CONTRADICTING CODE — FOUND ONLY REINFORCEMENT.\n1. `GameInterface/GameInterface.Services.Time.Patches/MapStatePatch.cs:11` patches `[HarmonyPatch(typeof(MapState), \"OnMapModeTick\")]` with a transpiler that locates `get_ActiveState` bracketed by `Ldarg_0`/`Bne_Un_S` and does `list.RemoveRange(i - 2, 5)`. That deletes vanilla's `base.GameStateManager.ActiveState == this` guard (MapState.cs:184), which is the only thing that would have made the forced tick a no-op. What survives is `Campaign.Current.RealTick(dt); ... Campaign.Current.Tick();`. This is the exact opposite of a contradiction — it is the second half of the mechanism.\n2. `FragileDetourGuard` (GameInterface/GameInterface.Utils/FragileDetourGuard.cs) can skip detours, but only when `isFragile(method) && !DoesFieldStore(method) && !hasLoadBearingPatch(method)` — prefixes count as load-bearing, and `IsFragile` only fires for methods whose x64 unwind data sits <5 bytes from BeginAddress. Neither patch is skippable.\n3. No `ModInformation.IsClient` / `CallOriginalPolicy` gate exists on either patch class. Both are uncategorized, so `harmony.PatchAllUncategorized(assembly)` (GameInterface/GameInterface/GameInterface.cs:50) applies them wholesale.\n4. Scope is actually WIDER than claimed: `PatchAll()` runs on the client at `Coop.Core/Coop.Core.Client.States/MainMenuState.cs:47` (`gameInterface.PatchAll();` inside `Handle_NetworkConnected`) AND on the host at `Coop.Core/Coop.Core/CoopartiveMultiplayerExperience.cs:385` in the \"Hosting Coop Server\" path. Host and client both lose the pause.\n\nIMPACT — REAL, AND IF ANYTHING UNDERSTATED. The claim's \"TimeControlMode == Stop is invalid\" is proven by files the analyst did not cite: `GameInterface/GameInterface.Services.CampaignService.Patches/TimeControlModePatch.cs:13` is a setter prefix that is literally `private static bool Prefix(Campaign __instance, ref CampaignTimeControlMode value) { return false; }`, and `GameInterface/GameInterface.Services.Heroes.Patches/TimePatches.cs:21-40` returns false on every setter path while the getter Postfix does `__result = CurrentMode;` — a static only written by `OverrideTimeControlMode`, which is driven by the network (`TimeControlInterface.ClientSetTimeControl` ← `NetworkChangeTimeControlMode`). So TAOM cannot merely no longer *rely* on the value; TAOM cannot *write* it at all, and reads return a server-authoritative global. Additionally `DisableGameMenuPausePatches.cs:18-27` retargets every `Campaign.TimeControlMode` setter call inside `GameMenu.ActivateGameMenu/StartWait/EndWait/ExitToLast/SwitchToMenu` + `MapScreen.HandleLeftMouseButtonClick/HandleMouse` to the empty `SetTimeControlModeDeference(Campaign _, CampaignTimeControlMode _2) { }` — menus provably stop pausing time. The only remaining pause is global and server-side: `PlayerOccupancyPauseHandler.cs:43` pauses only `if (!ModInformation.IsClient && AllPlayersOccupied())`, and the sole unpause policies registered anywhere are `DisconnectedPlayersServerHandler.cs:29` and `OverloadedPeerManager.cs:55` — nothing tied to a local screen.\n\nCONCRETE TAOM EXPOSURE (checked, not assumed): `Main/Features/TimeAcceleration/TimeControlAdapter.cs:22` does `set { if (Campaign.Current != null) Campaign.Current.TimeControlMode = (CampaignTimeControlMode)value; }`, and `Main/Features/TimeAcceleration/TimeAccelerationService.cs:40/48/66` save-and-restore through it. Under coop that write is either a silent no-op or (via TimePatches' `MessageBroker.Instance.Publish(... new TimeSpeedChangedAttempted(...))`) a request that changes time for every player in the session. The claim's parenthetical examples (tournament flow, settlement inventory writes) I did not individually verify in TAOM, but the general mechanism is established beyond doubt.\n\nVERDICT: quote genuine, mechanism confirmed by two independent patch classes plus the engine source, no counter-evidence anywhere in the tree. HIGH severity stands — it is not BLOCKER because TAOM can gate the affected features behind coop detection rather than being structurally incompatible.",
  "corrected_claim": "On BOTH host and client (`PatchAll` runs in `MainMenuState.Handle_NetworkConnected` and in the host path of `CoopartiveMultiplayerExperience`), BannerlordCoop force-ticks the MapState whenever the active game state is not MapState (`GameStateManagerPatches` OnTick prefix), and `MapStatePatch`'s transpiler strips vanilla's `base.GameStateManager.ActiveState == this` guard from `MapState.OnMapModeTick` so that forced tick actually reaches `Campaign.Current.RealTick(dt)` and `Campaign.Current.Tick()`. The vanilla active-state disable mechanism is dead (`RegisterActiveStateDisableRequest` prefix returns false, so `ActiveStateDisabledByUser` is permanently false and the `OnIdleTick` branch never runs). Beyond the cited file, time control is fully expropriated: the `Campaign.TimeControlMode` setter is blocked outright (`TimeControlModePatch.Prefix` returns false; `TimePatches.Prefix` returns false on all paths) and the getter is overridden to a static driven only by server `NetworkChangeTimeControlMode` messages, while `DisableGameMenuPausePatches` retargets every TimeControlMode write inside `GameMenu.ActivateGameMenu/StartWait/EndWait/ExitToLast/SwitchToMenu` and `MapScreen.HandleLeftMouseButtonClick/HandleMouse` to an empty method. TAOM impact: any TAOM code that assumes the campaign is paused while a menu/inventory/mission screen is open loses that assumption, and TAOM cannot even set `Campaign.Current.TimeControlMode` — `Main/Features/TimeAcceleration/TimeControlAdapter.cs:22` becomes a no-op or a session-wide time change for all players. The only pause left is global and server-authoritative (`PlayerOccupancyPauseHandler` pauses only when ALL connected players are in a settlement or map event).",
  "corrected_severity": "HIGH"
 },
 {
  "refuted": false,
  "reason": "Every element checks out against source. (1) The quote is VERBATIM at E:/repos/TAOM/Dependencies/_Module/coop-modules.txt lines 10-15 — not paraphrased. (2) It supports the claim: CoopPresence.cs:35-39 CompiledModuleDefaults = { \"BannerlordTogether\", \"BattleLinkMPClient\" } (cited line number exact), and CoopPresence.cs:131-132 matches active ids ONLY against that set — `var known = new HashSet<string>(config.ModuleIds, StringComparer.OrdinalIgnoreCase); _activeCoopModuleIds = active.Where(known.Contains).Distinct(...)`. (3) The module id really is exactly \"Coop\": E:/Steam/steamapps/workshop/content/261550/3770450698/SubModule.xml `<Id value=\"Coop\"/>`. (4) No contradicting path exists — grep for \"Coop\"/\"Bannerlord.Coop\" across Main/ and Dependencies/ (.cs/.xml/.txt) returns ZERO hits, so there is no alternate detector, no assembly-name fallback, no override. (5) The consequences are real at both named consumers: PatchShieldPolicy.cs:96 `ShouldUnpatchForeignOwners(bool coopActive) => !coopActive` makes PatchShield.cs:317 mayUnpatch true, reaching harmony.Unpatch at PatchShield.cs:366-368; and PatchShieldPolicy.CompiledProtectedOwnerPrefixes (lines 23-49) contains no prefix matching \"Bannerlord.Coop\" or \"Coop.*\" (\"Bannerlord.ButterLib\", \"Bannerlord.UIExtenderEx\", \"Bannerlord.MBOptionScreen\", \"Bannerlord.ModuleLoader\", \"Bannerlord.MCM\" all miss), so Coop's patch owners are strippable. SaveShieldPolicy.ShouldSwallow has `if (!coopActive) return true;` so SAVE-LOAD faults keep being swallowed. (6) All four claimed Harmony ids verified at the exact cited lines: GameInterfaceModule.cs:39 `public const string HarmonyId = \"Bannerlord.Coop\";`, BootPatches.cs:58 `Harmony harmony = new Harmony(\"Coop.BootFix\");`, CoopMod.cs:97 `new Harmony(\"Coop.UILoading\").PatchCategory(...)`, AutoRegistryFactory.cs:18 `private readonly Harmony harmony = new Harmony(\"CoopAutoRegistryFactory\");` — and a tree-wide grep for `new Harmony(` returns exactly those four, so the list is complete, not cherry-picked. The claimed impact is NOT overstated. If anything it is UNDERSTATED (which the verdict rules do not treat as refutation): adding \"Coop\" to [modules] alone breaks the build, because BundledDependencyManifestTests.cs:266 (`ModulesToLoadAfterThis_ContainsEveryCoopModuleIdFromTheShippedConfig`) asserts every [modules] id is also pinned in `<ModulesToLoadAfterThis>` of BOTH Dependencies/_Module/SubModule.xml and Main/_Module/SubModule.xml. That pin is load-bearing here: Coop ships its own 0Harmony.dll (E:/Steam/.../3770450698/bin/Win64_Shipping_Client/0Harmony.dll), the exact dual-Harmony-instance hazard described at Dependencies/_Module/SubModule.xml:58-68. One minor imprecision, not a refutation: IsProtectedOwner (PatchShieldPolicy.cs:70-81) is a StartsWith prefix match, so a single prefix \"Coop\" covers Coop.BootFix, Coop.UILoading and CoopAutoRegistryFactory — only \"Bannerlord.Coop\" needs a second entry; and once \"Coop\" is in [modules] the prefixes are belt-and-braces only, since the unpatch path is disabled outright.",
  "corrected_claim": "CONFIRMED as stated, with the required TAOM change expanded. TAOM cannot detect BannerlordCoop: coop-modules.txt [modules] (lines 14-15) and CoopPresence.CompiledModuleDefaults (CoopPresence.cs:35-39) both list only BannerlordTogether and BattleLinkMPClient, and CoopPresence.cs:131-132 matches the launcher's active-module ids against nothing else. Coop's launcher id is exactly \"Coop\" (its SubModule.xml `<Id value=\"Coop\"/>`), matched case-insensitively via StringComparer.OrdinalIgnoreCase. With IsActive false: PatchShieldPolicy.ShouldUnpatchForeignOwners returns true, so PatchShield.cs:366-368 will Unpatch Coop's Prefix/Postfix/Transpiler patches on the missing-API trinity (no compiled protected prefix matches \"Bannerlord.Coop\" or \"Coop.*\"), and SaveShieldPolicy.ShouldSwallow returns true so SAVE-LOAD failures stay swallowed instead of rethrown. TAOM must (a) add `Coop` under [modules] in Dependencies/_Module/coop-modules.txt, (b) add \"Coop\" to CoopPresence.CompiledModuleDefaults so detection survives deletion of the config file, and (c) — omitted by the original claim and enforced by BundledDependencyManifestTests.cs:266 — add `<Module Id=\"Coop\"/>` to `<ModulesToLoadAfterThis>` in BOTH Dependencies/_Module/SubModule.xml and Main/_Module/SubModule.xml (plus the BUTR-launcher `<DependedModuleMetadata id=\"Coop\" order=\"LoadAfterThis\" optional=\"true\"/>` mirror in Main/_Module/SubModule.xml), or the change fails its own test. That load-order pin is functionally required, not cosmetic: Coop ships its own 0Harmony.dll. Optionally populate [harmony-owner-prefixes] with `Coop` (prefix-covers Coop.BootFix at BootPatches.cs:58, Coop.UILoading at CoopMod.cs:97, CoopAutoRegistryFactory at AutoRegistryFactory.cs:18) and `Bannerlord.Coop` (GameInterfaceModule.cs:39) — belt-and-braces only, since IsActive alone disables the unpatch path.",
  "corrected_severity": "HIGH"
 },
 {
  "refuted": true,
  "reason": "The quote is verbatim and correctly located (AutoSyncBuilder.cs:78-83), and the structural mechanism is real: lines 102-105 turn each loaded assembly's .Location into a MetadataReference, and line 116 throws InvalidOperationException on Emit failure. TAOM's handler is genuinely version-blind and unguarded (Dependencies/SubModule.cs:106 `if (!string.Equals(requested.Name, simpleName, StringComparison.Ordinal)) continue;`, with \"Serilog\" at line 54 and \"System.Numerics.Vectors\" at line 63, and no CoopPresence gate - CoopPresence is consumed only by SaveShield.cs:273 and PatchShield.cs:316). But the claimed impact is materially wrong in two of its three legs.\n\n(1) The System.Numerics.Vectors leg is false. GameInterface.dll's actual compile-time reference is `System.Numerics.Vectors, Version=4.1.3.0, PublicKeyToken=b03f5f7f11d50a3a` (read via ReflectionOnlyLoadFrom on the installed workshop DLL) - NOT 4.1.5.0. 4.1.5.0 is merely the file Coop ships in bin/Win64_Shipping_Client, so Coop already runs with a version-drifted facade. Decisively, `Assembly.Load(\"System.Numerics.Vectors\")` is a PARTIAL name, which carries no version expectation at all, so TAOM's 4.1.4.0 satisfying it is ordinary partial-name binding, not poisoning. And only 1 of the 893 shipped AutoSyncExport files (AssemblyInfo.cs) mentions System.Numerics - the reference is inert in the generated code.\n\n(2) The \"separate code path\" framing is wrong. `RedirectBundledDependencies` (SubModule.cs:109-118) iterates `AppDomain.CurrentDomain.GetAssemblies()` and returns an already-loaded match, else null. It can never load TAOM's DLL from disk or introduce a version the process lacks. Coop's Serilog binding is therefore decided at Coop's module load / first `LogManager.GetLogger` call, not at the Roslyn compile - `AutoSyncBuilder` itself holds a static `ILogger Logger = LogManager.GetLogger<AutoSyncBuilder>()` at line 18, and line 77 (`list.Add(typeof(ILogger).Assembly)`) already captures whatever Serilog GameInterface is JIT-bound to before any of the cited Load calls run. If TAOM's Serilog wins that earlier binding, Coop fails at its first log statement far upstream of AutoSync; this is the same root cause as the parent finding, not an independent one.\n\n(3) The claim also understates the gap: TAOM ships Serilog 2.0.0.0 (Dependencies/_Module/bin/Win64_Shipping_Client/Serilog.dll), against Coop's 4.2.0.0 - a two-major-version gap that would fail loudly and early rather than silently \"emit an assembly bound to the wrong Serilog.\"\n\nA residual real risk survives: because Bannerlord loads module DLLs via LoadFrom context, the full-name loop at line 82 (`Assembly.Load(\"Serilog, Version=4.2.0.0, ...\")`) can miss the default Load context, fire AssemblyResolve, and receive TAOM's Serilog 2.0.0.0. Combined with line 77's already-captured Serilog 4.2.0.0, `list` would hold two different Serilog paths, which `.Distinct()` on paths (line 104) preserves, giving Roslyn two same-simple-name metadata references and an Emit failure at line 116. That is a genuine but conditional MEDIUM symptom of one root cause (TAOM's redirect list overlaps Coop's bundled deps), fixed by the same change - not a standalone HIGH.",
  "corrected_claim": "Coop's AutoSync Roslyn compile (GameInterface/GameInterface.AutoSync.Builders/AutoSyncBuilder.cs:78-83) resolves its metadata references through `Assembly.Load`, and TAOM's unconditional, version-blind AssemblyResolve handler (Dependencies/SubModule.cs:101-119) can answer for `Serilog`, which is in TAOM's RedirectedSimpleNames list (line 54). The System.Numerics.Vectors half of the original finding does not hold: that call is a partial-name Load with no version expectation, GameInterface.dll's real reference is 4.1.3.0 (not 4.1.5.0), and only 1 of 893 generated AutoSync files touches System.Numerics.\n\nThe surviving risk is Serilog only. TAOM ships Serilog 2.0.0.0; GameInterface.dll references Serilog 4.2.0.0. TAOM's handler compares simple names only and returns whatever Serilog is already loaded in the AppDomain, so with TAOM.Dependencies loading before Coop, TAOM's 2.0.0.0 is the likely winner. Because Bannerlord loads module DLLs in the LoadFrom context, the full-name loop at line 82 can miss the default Load context and fire AssemblyResolve; the result is either (a) Coop bound to Serilog 2.0.0.0 from its first log call - which fails at Coop module load, far upstream of AutoSync - or (b) two distinct Serilog paths in `list`, preserved by `.Distinct()` at line 104, yielding duplicate same-simple-name metadata references and an Emit failure that throws InvalidOperationException at line 116 when the player connects to a co-op server (MainMenuState.Handle_NetworkConnected, MainMenuState.cs:47).\n\nThis is not an independent finding: it is one downstream symptom of the single root cause that TAOM's redirect list overlaps Coop's bundled dependencies, and it carries the same fix - gate `RedirectBundledDependencies` on co-op presence (which requires adding \"Coop\" to CoopPresence.cs's module-id list, currently only \"BannerlordTogether\" and \"BattleLinkMPClient\") or drop Coop-owned simple names from RedirectedSimpleNames. The claim's one accurate distinguishing point stands: the compile runs post-connect, so a main-menu-only smoke test will not exercise it.",
  "corrected_severity": "MEDIUM"
 },
 {
  "refuted": true,
  "reason": "The quote is genuine and verbatim (Hero_DynamicPatches.cs lines 88-99), and the described mechanic is correct FOR THE AUTOSYNC-GENERATED FIELD-INTERCEPT PATH. But the claim is stated as a general rule about \"an unauthorized client write to a synced field,\" and its TAOM-impact section makes three assertions that the source contradicts.\n\n(1) NOT ALL UNAUTHORIZED CLIENT WRITES APPLY LOCALLY. The AutoSync layer never blocks - `grep -rn \"static bool\" AutoSyncExport/` returns ZERO results across all 893 files, and all 193 generated prefixes are `private static void ...Prefix(...)`, which under Harmony cannot skip the original. So within AutoSync the claim holds. But the hand-written GameInterface layer does the opposite: 83 sites across 40 files use bool prefixes that DROP the write. GameInterface.Services.Armies.Patches/ArmyPatches.cs:31-42 - \"private static bool OnAddPartyInternalPrefix(ref Army __instance, MobileParty mobileParty) { if (CallOriginalPolicy.IsOriginalAllowed()) { return true; } if (ModInformation.IsClient) { return false; } ...\". Same shape in ArmySetKingdomPrefix, GoldSettlementComponentPatch, ChangeSkillLevelPatch, GainRawXpPatch, KingdomPatches, MapEventPatches, ItemRosterPatch and 33 other files. On the client the value is silently discarded, not applied - the opposite failure mode from the one claimed.\n\n(2) \"TAOM WILL NOT CRASH UNDER COOP - THERE IS NO EXCEPTION TO CATCH\" IS AN OVERREACH. The blocking pattern is applied to CONSTRUCTORS. GameInterface.Services.PartyComponents.Patches.Lifetime/LordPartyComponentLifetimePatches.cs: \"private static IEnumerable<MethodBase> TargetMethods() { return AccessTools.GetDeclaredConstructors(typeof(LordPartyComponent)); }\" with \"private static bool Prefix(LordPartyComponent __instance) { if (CallOriginalPolicy.IsOriginalAllowed()) { return true; } if (ModInformation.IsClient) { Logger.Error(\\\"Client created managed {name}\\\", typeof(LordPartyComponent)); return false; } ...\". A suppressed constructor yields an object with every field left at null/default; downstream access is a NullReferenceException, not a quiet desync. The same construct exists for CaravanPartyComponent, GarrisonPartyComponent, MilitiaPartyComponent, VillagerPartyComponent, CustomPartyComponent, BanditPartyComponent, Monster, EquipmentRoster, CharacterSkills and Clan lifetime patches.\n\n(3) \"THE ONLY SIGNAL IS `Client updated managed ...` ERROR LINES\" IS FALSE. GameInterface.Services.MobilePartyAIs.Patches/PartiesThinkPatch.cs:26-30 - \"private static bool PartiesThinkPrefix(Campaign __instance, ref float dt) { if (ModInformation.IsClient) { return false; } ...\". This blocks Campaign.PartiesThink wholesale on clients with NO log call and without even consulting CallOriginalPolicy. Divergence introduced this way leaves zero trace in Coop_client.log, so a grep-the-log QA strategy is necessary but provably not sufficient.\n\n(4) \"NO ENGINE-LEVEL BACKSTOP / TAOM CANNOT DETECT ITS OWN DIVERGENCE AT RUNTIME\" IS OVERSTATED. A reachable audit facility exists: GameInterface.Services.Settlements.Commands/SettlementAuditorCommand.cs - \"[CommandLineFunctionality.CommandLineArgumentFunction(\\\"audit\\\", \\\"coop.debug.settlements\\\")]\", plus HeroAuditor, MobilePartyAuditor and SettlementComponentAuditor, each client-initiated (\"Audit is only available client side\" in HeroAuditor.Audit) and comparing client state against a server response. It is genuinely limited - HeroAuditor.AuditData only checks count mismatch and `objectManager.TryGetObject<Hero>(heroAuditData.Id, out var _)`, i.e. object-id registration, never field values - so it would not catch the Hero.Culture divergence in the cited example. But \"no backstop\" is wrong; the correct statement is \"the backstop covers object registration, not field values.\"\n\nCORROBORATED SUPPORTING FACTS (these parts of the claim survive): AutoSyncExport is authoritative runtime code, not a debug artifact - GameInterface.AutoSync.Builders/AutoSyncBuilder.cs compiles the same syntax trees via \"CSharpCompilation.Create(\\\"AutoSync\\\", source, ...)\" then \"return Assembly.Load(memoryStream.GetBuffer());\", and AutoSyncConfiguration.cs dumps them (\"public static bool ExportFiles { get; set; } = true;\"). The gate semantics are as implied: CallOriginalPolicy.IsOriginalAllowed() returns true if AllowedThread.IsThisThreadAllowed() (a [ThreadStatic] counter set while applying inbound network messages) or if ISyncPolicy.AllowOriginal(); ClientSyncPolicy returns \"!syncStates.Contains(clientLogic.State.GetType())\" for {CampaignState, MissionState}. ModInformation.IsClient is simply \"=> !IsServer\". And \"grep -rn -A4 'if (ModInformation.IsClient)' | grep -c throw\" returns 0 across GameInterface - nothing throws deliberately, which is why the crash risk is an indirect NRE rather than a catchable coop exception.",
  "corrected_claim": "BannerlordCoop has TWO divergent enforcement behaviours for unauthorized client writes, not one, and the choice depends on which patch layer owns the member.\n\nLAYER 1 - AutoSync generated patches (42 `*_DynamicPatches.cs`, 361 intercept sites, covering the 43 declared campaign types). Behaves exactly as the original claim describes: log an Error and apply locally without publishing. Evidence, AutoSyncExport/Hero/Hero_DynamicPatches.cs:88-99: \"if (CallOriginalPolicy.IsOriginalAllowed()) { instance.Culture = newValue; return; } if (ModInformation.IsClient) { Logger.Error(\\\"Client updated managed {type}\\\", \\\"Culture\\\"); instance.Culture = newValue; return; }\" - the server-only tail at line 102 (\"var message = new Hero_Culture_SetLocalMessage(instance, newValue); MessageBroker.Instance.Publish(instance, message);\") is unreachable on a client. This layer NEVER blocks: zero `static bool` prefixes exist in the entire 893-file export, and all 193 generated property-setter prefixes are `void`, which under Harmony cannot skip the original. Result: silent value divergence plus log spam.\n\nLAYER 2 - hand-written GameInterface patches (83 sites in 40 files). Does the OPPOSITE: it blocks. ArmyPatches.cs:31-42 - \"if (ModInformation.IsClient) { return false; }\" in a `static bool` prefix, which skips the original entirely. The client's write is DISCARDED, so a read-after-write returns the stale value. Applied to constructors this is a crash vector, not a desync: LordPartyComponentLifetimePatches.cs targets \"AccessTools.GetDeclaredConstructors(typeof(LordPartyComponent))\" and returns false on clients, leaving a fully uninitialized object whose first field access is an NRE. The same constructor-suppression exists for Caravan/Garrison/Militia/Villager/Custom/Bandit party components, Monster, EquipmentRoster, CharacterSkills and Clan.\n\nSome blocks are also SILENT. PartiesThinkPatch.cs:26-30 - \"if (ModInformation.IsClient) { return false; }\" - suppresses Campaign.PartiesThink on clients with no log line and without consulting CallOriginalPolicy at all.\n\nNothing anywhere throws deliberately (`grep -A4 'if (ModInformation.IsClient)' | grep -c throw` = 0 across GameInterface), so neither layer surfaces a catchable coop-specific exception.\n\nA partial backstop DOES exist, contrary to \"no backstop\": the client-initiated auditors (HeroAuditor, MobilePartyAuditor, SettlementAuditor, SettlementComponentAuditor), reachable via console commands such as `coop.debug.settlements.audit` (SettlementAuditorCommand.cs). They compare client state to a server response but only verify object-id registration and object counts - HeroAuditor.AuditData checks \"objectManager.TryGetObject<Hero>(heroAuditData.Id, out var _)\" and count mismatch - so they detect object-graph divergence, never field-value divergence.\n\nCORRECTED TAOM IMPACT. TAOM can both quietly desync AND crash under coop, depending on which member it touches. Any TAOM write to an AutoSync-managed field on a client diverges silently; any TAOM code path that constructs a party component or invokes one of the 83 hand-blocked members on a client gets a no-op and may NRE downstream. TAOM's QA pass must do THREE things, not one: (a) grep Coop_client.log for `Client updated managed` and `Client created managed`; (b) run the `coop.debug.*` audit commands, understanding they cover object registration only; (c) explicitly test the silently-blocked paths (PartiesThink and the component constructors) since they emit no log evidence whatsoever. The concrete TAOM remediation is unchanged in kind but wider in scope: TAOM must detect module id \"Coop\" (its current detection only recognises \"BannerlordTogether\" and \"BattleLinkMPClient\") and gate its campaign-mutating behaviours to server-authority, because passive coexistence produces both failure modes.",
  "corrected_severity": "HIGH"
 },
 {
  "refuted": false,
  "reason": "The claim survives adversarial verification on every axis.\n\n(1) QUOTE IS VERBATIM. CommonModule.cs lines 40-44 read exactly: \"protected void RegisterAllTypesWithInterface<TModule, TInterface>(ContainerBuilder builder, bool autoInstantiate = false)\\n\\t{\\n\\t\\tforeach (Type @interface in InterfaceCollector.GetInterfaces<TInterface>(typeof(TModule).Namespace))\\n\\t\\t{\\n\\t\\t\\tIRegistrationBuilder<object, ConcreteReflectionActivatorData, SingleRegistrationStyle> registration = builder.RegisterType(@interface).AsSelf().InstancePerLifetimeScope();\" — character-for-character, not a paraphrase.\n\n(2) THE QUOTE SUPPORTS THE CLAIM. Callers confirmed by grep, only two: ClientModule.cs:46-47 \"RegisterAllTypesWithInterface<ClientModule, IHandler>(builder, autoInstantiate: true); RegisterAllTypesWithInterface<ClientModule, IPacketHandler>(builder, autoInstantiate: true);\" and ServerModule.cs:62-63 the identical pair with ServerModule. Namespaces verified at the source: ClientModule.cs:19 \"namespace Coop.Core.Client;\" and ServerModule.cs:26 \"namespace Coop.Core.Server;\". So typeof(TModule).Namespace is literally \"Coop.Core.Client\" / \"Coop.Core.Server\" as claimed.\n\n(3) THE SCAN IS GENUINELY APPDOMAIN-WIDE. Common/Common.Util/InterfaceCollector.cs:10-15: \"public static IEnumerable<Type> GetInterfaces<T>(string @namespace) { return from t in AppDomain.CurrentDomain.GetDomainTypes(@namespace) where typeof(T).IsAssignableFrom(t) && t.IsConcrete() select t; }\". And Common/Common.Extensions/ReflectionExtensions.cs:40-64: \"Assembly[] assemblies = domain.GetAssemblies(); ... foreach (Assembly assembly in array) { try { Type[] types = assembly.GetTypes(); foreach (Type type in types) { if (type.Namespace != null && type.Namespace.StartsWith(namespacePrefix)) { list.Add(type); } } } catch (ReflectionTypeLoadException) { } }\". There is NO assembly allowlist, no strong-name check, no \"is this a Coop assembly\" guard. A foreign assembly declaring types under those namespace prefixes IS collected.\n\n(4) NO CONTRADICTING CODE PATH FOUND. RegisterAllTypesWithInterface is non-virtual protected and neither ClientModule nor ServerModule overrides it. No config flag gates it (contrast GameInterface.Services/ServiceModule.cs:49 \"if (AutoSyncConfiguration.Enabled)\", which gates a DIFFERENT scan). Grep for RegisterModule<ClientModule>/<ServerModule> returns exactly two sites, both in Coop.Core/CoopartiveMultiplayerExperience.cs, and they are mutually exclusive: :366-369 \"DestroyContainer(); ModInformation.IsServer = true; ContainerBuilder containerBuilder = new ContainerBuilder(); containerBuilder.RegisterModule<ServerModule>();\" versus :500-503 \"DestroyContainer(); ModInformation.IsServer = false; ContainerBuilder containerBuilder = new ContainerBuilder(); containerBuilder.RegisterModule<ClientModule>();\". There is no host mode building both, so \"instantiated only on the server\" / \"only on clients\" is exact.\n\n(5) THE MIRRORED SPLIT IS REAL. Coop's own handlers confirm the pattern: ServerKingdomHandler.cs:20-22 \"namespace Coop.Core.Server.Services.Kingdoms.Handlers; public class ServerKingdomHandler : IHandler, IDisposable\" against ClientKingdomHandler.cs under Coop.Core.Client.Services.Kingdoms.Handlers. The registration is functional as a hook because the handler's constructor subscribes to the broker (ServerKingdomHandler.cs:53 \"messageBroker.Subscribe<DecisionAdded>(HandleLocalDecisionAdded);\") and autoInstantiate:true triggers AutoActivate at container build.\n\n(6) THE INTERFACES ARE TRIVIALLY IMPLEMENTABLE. Common/Common.Messaging/IHandler.cs:5-7 \"public interface IHandler : IDisposable { }\" — an empty marker. Common/Common.PacketHandlers/IPacketHandler.cs:6-11 exposes \"PacketType PacketType { get; }\" and \"void HandlePacket(NetPeer peer, IPacket packet);\".\n\nWHY NOT REFUTED ON IMPACT: the impact is a real consequence, not an overreach. I looked hard for a reason it would fail and found only cost/hazard qualifications to \"for free\", not contradictions — so the claim stands, with the caveats folded into corrected_claim rather than treated as refutation. Severity HIGH is correct: this is the cleanest documented extension seam in the mod, and it is side-aware, which no other seam is (ServiceModule.cs:48 scans prefix \"GameInterface\" and :53 \"AutoSync\", both registered into BOTH containers via GameInterfaceModule, hence side-agnostic).",
  "corrected_claim": "CLAIM UPHELD AS STATED, with four load-bearing caveats that \"for free\" hides.\n\nVerified mechanism: CommonModule.RegisterAllTypesWithInterface<TModule,TInterface> (CommonModule.cs:40-49) resolves typeof(TModule).Namespace and passes it to InterfaceCollector.GetInterfaces<T>, which walks AppDomain.CurrentDomain.GetAssemblies() and keeps every type whose Namespace.StartsWith(prefix), is concrete, non-generic, non-abstract, and assignable to T (InterfaceCollector.cs:10-20, ReflectionExtensions.cs:40-64). ClientModule.cs:46-47 and ServerModule.cs:62-63 each invoke it twice, for IHandler and IPacketHandler, with autoInstantiate:true. Prefixes are \"Coop.Core.Client\" and \"Coop.Core.Server\" (ClientModule.cs:19, ServerModule.cs:26). Containers are mutually exclusive (CoopartiveMultiplayerExperience.cs:366-369 server, :500-503 client), so side-specificity is exact. Therefore a TAOM type in namespace Coop.Core.Server.Taom.* is discovered and auto-activated on the authority side only, and Coop.Core.Client.Taom.* on the apply side only.\n\nCAVEATS THAT MUST BE ENGINEERED AROUND (all grounded in the same files):\n\nA. HARD ASSEMBLY DEPENDENCY. Implementing IHandler requires referencing Common.dll (Common.Messaging.IHandler); IPacketHandler additionally drags in LiteNetLib (IPacketHandler.cs:2 \"using LiteNetLib;\" and the \"void HandlePacket(NetPeer peer, IPacket packet);\" member). These types cannot live in TAOM's main assembly or TAOM breaks when Coop is absent. They need a separate optional glue DLL.\n\nB. THE GLUE ASSEMBLY MUST BE LOADED, NOT MERELY PRESENT. ReflectionExtensions.cs:44 uses \"domain.GetAssemblies()\", which returns only assemblies already loaded into the AppDomain. .NET loads assemblies lazily on first type touch. TAOM must force-load the glue assembly (touch a type from its SubModule) before the user starts a coop session, or the scan sees nothing. The container is built at session start (CoopartiveMultiplayerExperience.cs:379, :513), not at game start, so there is a wide window — but it is not automatic.\n\nC. FAILURE IS FATAL AND UNGUARDED. autoInstantiate:true maps to registration.AutoActivate() (CommonModule.cs:45-48), so every discovered type is constructed during \"container = containerBuilder.Build();\" — a line that is NOT inside a try/catch at either CoopartiveMultiplayerExperience.cs:379 (server) or :513 (client). A TAOM handler with an unresolvable constructor dependency or a throwing constructor hard-fails the entire coop session start, not just TAOM's feature.\n\nD. FAILURE CAN ALSO BE SILENT. ReflectionExtensions.cs:59-61 is \"catch (ReflectionTypeLoadException) { }\" wrapped around \"assembly.GetTypes()\" for the WHOLE assembly. If TAOM's glue assembly has any unresolvable type reference (e.g. built against a different Coop version), every one of its types is dropped with zero diagnostics and TAOM's hooks simply never run.\n\nE. REGISTERED AsSelf ONLY. CommonModule.cs:44 is \".RegisterType(@interface).AsSelf()\" — not .As<IHandler>(). TAOM cannot resolve its own handler by interface from the container; it works only via constructor-time IMessageBroker subscription, which is exactly how Coop's own handlers operate (ServerKingdomHandler.cs:53-61), so this is a shape constraint, not a blocker.\n\nSEPARATE GATE (not part of this claim, but it conditions whether the seam is ever reached): ModuleValidator.cs:21-40 rejects any join where the server's and client's non-official module sets or versions differ, so TAOM must be installed at identical version on both sides — including inside the DedicatedServer's own Modules tree.",
  "corrected_severity": "HIGH"
 },
 {
  "refuted": false,
  "reason": "VERBATIM CHECK: PASS. `SerializableTypeMapper.cs:26-45` contains exactly the quoted text (the `...` in the citation elides the `SelectMany(delegate(Assembly a) { try { return a.GetTypes(); } catch (ReflectionTypeLoadException) { return Array.Empty<Type>(); } })` body, which does not change the meaning).\n\nSUPPORTS THE CLAIM: YES, on both halves.\n(a) Auto-registration: `CollectProtoContracts()` is called from the parameterless constructor (`public SerializableTypeMapper() { CollectProtoContracts(); }`, line 21-24) and scans `AppDomain.CurrentDomain.GetAssemblies()` with no assembly allowlist — only `where !a.IsDynamic`. It feeds `AddTypes` → `Register` for every type where `type.IsDefined(typeof(ProtoContractAttribute), inherit: false)`.\n(b) Deterministic id from FullName: `Register` does `int num = StableId(fullName);` where `StableId` (line 112-121) is FNV-1a over `type.FullName` masked with `0x7FFFFFFF` — no ordering dependence, no counter, no attribute-supplied id.\n\nIT IS THE ACTUAL WIRE MAP: `ProtoBufSerializer.Serialize` writes `new ProtoMessageWrapper(id, ...)` where `id` comes from `typeMapper.TryGetId`, and `Deserialize` resolves via `typeMapper.TryGetType(protoMessageWrapper.TypeId, out var type)` (`ProtoBufSerializer.cs:42-60`). `CommonModule.cs:22-23` binds `SerializableTypeMapper`→`ISerializableTypeMapper` and `ProtoBufSerializer`→`ICommonSerializer`; this is the only implementation of either interface in the tree.\n\nSEARCHED FOR CONTRADICTIONS, FOUND NONE:\n- No second `ISerializableTypeMapper` implementation, no subclass, no config flag gating the scan (grep over the whole tree returns only `SerializableTypeMapper.cs`).\n- Late-loading is handled, not blocked: `TryGetId` calls `Register(type)` on miss (line 104-108), and `TryGetType` re-runs `CollectProtoContracts()` when `AppDomain.CurrentDomain.GetAssemblies().Length != lastScannedAssemblyCount` (line 86-89).\n- Explicit `AddTypes` calls elsewhere (`AutoSyncPatcher.cs:36`, `AutoRegistryFactory.cs:78` adding `NetworkCreateInstance<T>`/`NetworkDestroyInstance<T>`) exist only for closed generics and dynamically-emitted types the `!a.IsDynamic` scan cannot see — they supplement, not replace, the scan.\n- No message-type allowlist anywhere on the wire path: `MessagePacketHandler.HandlePacket` deserializes and reflectively `Publish<T>`es whatever type came back; `AggregateMessagePacketHandler` does the same; `MessageBroker.Publish` no-ops on unknown types (`if (message == null || !subscribers.ContainsKey(typeof(T))) return;`). The only per-type gate is `MessagePacket.Create`'s `RuntimeTypeModel.Default.IsDefined(message.GetType())`, which a `[ProtoContract]` type satisfies.\n\nIMPACT CHECK: `IMessage` and `IEvent` do live in Common.Messaging (`IEvent.cs`: `public interface IEvent : IMessage`), so a TAOM type shaped `[ProtoContract(SkipConstructor = true)] class NetworkTaomWotrMomentumChanged : IEvent` is exactly what the scan picks up. The impact is real, not an overreach — but it is INCOMPLETE in one respect worth recording (see corrected_claim): registration is free, transmission is not. Sending requires an `INetwork` instance (`Common.Network/INetwork.cs:24 void SendAll(IMessage message)`), and the only public way to obtain one is `GameInterface.ContainerProvider.TryGetContainer(out ILifetimeScope)` (`ContainerProvider.cs:40`) — so TAOM needs a reference to GameInterface.dll too, not just protobuf-net + Common.dll. That is an addition to the impact, not a refutation of the claim.",
  "corrected_claim": "Any non-dynamic loaded assembly's types carrying [ProtoContract] (declared directly, `inherit: false`) are auto-registered into the wire type map by `SerializableTypeMapper.CollectProtoContracts()`, with the id being FNV-1a of `Type.FullName` masked to 31 bits (`StableId`). Late-loaded assemblies are covered by the on-demand `Register` in `TryGetId` and the assembly-count-triggered rescan in `TryGetType`. TAOM's `[ProtoContract]` message types therefore need NO Coop-side registration change. Three caveats the original claim omits: (1) transmission still needs an `INetwork` resolved via `GameInterface.ContainerProvider.TryGetContainer`, so TAOM must reference GameInterface.dll in addition to Common.dll and protobuf-net; (2) `StableId` collisions are fatal — `Register` throws `InvalidOperationException` from the mapper's constructor, which would fail Coop's container build for every peer; (3) if TAOM.dll ever throws `ReflectionTypeLoadException` from `GetTypes()`, the scan silently drops the ENTIRE assembly (`return Array.Empty<Type>()`), and a receiving peer that then hits an unmapped id gets `Deserialize` → `null` → an unguarded `ArgumentNullException` out of `MessagePacketHandler.PublishEvent`.",
  "corrected_severity": "MEDIUM"
 },
 {
  "refuted": true,
  "reason": "The quote is verbatim and the NARROW half of the claim (IAutoSync discovery is assembly-scoped) is correct, but the headline \"AutoSync codegen CANNOT be extended by TAOM\" is contradicted by other code, and the stated TAOM impact is an overreach. Codegen is driven by AutoSyncRegistry.Registrations, not by IAutoSync. AutoSyncRegistry.cs:8 declares `public class AutoSyncRegistry`; line 10 exposes `public readonly Dictionary<Type, AutoSyncRegistryItem> Registrations`; lines 22/34/50/64 expose `public void AddField(FieldInfo field, bool debug = false, bool coalesce = false)`, `public void AddProperty(...)`, `public bool AddTargetMethod(...)`, `public void AddSerializer<TargetType>(...)`. AutoSyncModule.cs:15 registers it AsSelf (`builder.RegisterType<AutoSyncRegistry>().InstancePerLifetimeScope();`) and ContainerProvider.cs:51 exposes `public static bool TryResolve<T>(out T instance) where T : class`. The IAutoSync classes are only constructor shims over those public methods (AlleySync.cs:11: `registry.AddField(AccessTools.Field(typeof(Alley), \"_name\"));`). Codegen reads the registry lazily: AutoSyncBuilder.cs:92 `autoSyncRegistry.Registrations.SelectMany(...)`, invoked from AutoSyncPatcher.cs:62 `Assembly = autoSyncBuilder.Build();` inside `PatchAll()` which is only called on network connect (MainMenuState.cs:47 `gameInterface.PatchAll();` inside `Handle_NetworkConnected`) — long after TAOM's SubModule loads. Generated output references only the target type's assembly plus Coop infra (AutoSyncExport/Alley/Alley__tag_SetNetworkMessage.cs: `public record Alley__tag_SetNetworkMessage : GenericNetworkEvent<Alley, byte[]>`), so a TAOM registration on a vanilla campaign type would compile against the existing metadata reference set (AutoSyncBuilder.cs:79-83 + AssemblyInfo.cs listing TaleWorlds.CampaignSystem/Core/etc.). Therefore the impact sentence \"TAOM must hand-write handler + message pairs instead of declaring fields for AutoSync\" is false — TAOM can declare fields and still get generated handler+message pairs. NOT FOUND: any guard, flag, or subclass that widens the IAutoSync scan beyond GameInterface, and NOT FOUND any external (non-GameInterface) caller of AutoSyncRegistry in the shipped source — the extension path is available but unused by the mod itself.",
  "corrected_claim": "AutoSync's IAutoSync auto-discovery is restricted to the GameInterface assembly (AutoSyncModule.cs:43-50, `Assembly assembly = GetType().Assembly;`), so TAOM cannot ship a `class TaomWotrSync : IAutoSync` and have it picked up automatically. AutoSync codegen itself IS extensible by TAOM, however: the codegen source is `AutoSyncRegistry.Registrations` (AutoSyncBuilder.cs:92), and AutoSyncRegistry is a public class with public `AddField`/`AddProperty`/`AddTargetMethod`/`AddSerializer` and a public mutable `Registrations` dictionary, resolvable via `ContainerProvider.TryResolve<AutoSyncRegistry>` (ContainerProvider.cs:51). Because `Build()` is invoked lazily and once from `AutoSyncPatcher.PatchAll()` (AutoSyncPatcher.cs:60-63), which fires on network connect (MainMenuState.cs:47), TAOM has a runtime window to register vanilla-type fields and get generated message+handler pairs that compile against the existing reference set. The real constraints are: (a) no auto-discovery — TAOM must explicitly resolve the registry; (b) a strict ordering requirement, since `AutoSyncPatcher.Assembly` is a static built only on the first PatchAll; (c) a compile-time or reflection dependency on GameInterface.dll; (d) client and server must register identically.",
  "corrected_severity": "MEDIUM"
 },
 {
  "refuted": false,
  "reason": "The quote is verbatim at ModuleValidator.cs:29-40 and the code is genuinely the join gate: MainMenuState.cs:83 -> ValidateModuleState.cs:61 sends the client's full ModuleHelper.GetActiveModules() set, ResolveCharacterState.cs:68 runs Validate on it, and ValidateModuleState.cs:97-100 disconnects on failure. No config flag, guard, subclass or alternate path bypasses it (CompatibilityInfo.CompatibleWith is the only looser check and is referenced solely by CompatibilityInfoBinaryPackage.cs, not the handshake). The claimed TAOM impact is real and, on the shipped dedicated server, harsher than stated: its Modules dir is only Coop/DedicatedServer.Windows/Native/SandBox/SandBoxCore, so after ShouldValidateModule filtering the server set is {Coop} and a TAOM client is rejected outright. Corrections rather than refutations: (a) the claim says 'EXACTLY equal', but ValidateModuleState.cs:92 lets the client ignore a rejection whose reason is exactly \"Server does not support module 'Coop'.\", so a Coop-only extra is tolerated; (b) ModuleValidator.cs:51 also skips ids prefixed 'DedicatedServer.'; (c) ModuleValidator.cs:58-68 additionally rejects any client DLC before module matching; (d) the ChangeSet emphasis is inert for TAOM because ApplicationVersion.FromString assigns changeSet = defaultChangeSet for 3-part strings like 'v2.0.15'/'v2.0.5', so both sides parse identically. Severity downgraded to MEDIUM: the fix is operational (ship the identical module set to host, clients and any dedicated server), requires zero TAOM code change, and does not block compatibility.",
  "corrected_claim": "The join handshake is a hard server-side gate. On connect the client sends its full active module list (ValidateModuleState.cs:61, TaleWorldsModuleInfoProvider.cs:10) and the server runs ModuleValidator.Validate (ResolveCharacterState.cs:68); failure disconnects the client (ValidateModuleState.cs:97-100). After filtering out official non-DLC modules and ids prefixed 'DedicatedServer.' (ModuleValidator.cs:45-56), every remaining server module must be present client-side at an ApplicationVersion.IsSame(..., checkChangeSet: true) match, and any extra client module is rejected symmetrically. Two carve-outs: any client DLC is rejected first by ValidateNoDlc (ModuleValidator.cs:58-68), and the client silently ignores a rejection whose reason is exactly \"Server does not support module 'Coop'.\" (ValidateModuleState.cs:92), which only fires when Coop is the sole extra. TAOM impact: TAOM (v2.0.15), TAOM.Dependencies (v2.0.5), TAOM_Map, LOTRLOME_Armory, the four BUTR alias stubs, and every other non-official module a player has active must exist on both sides at identical declared version strings — the SubModule.xml <Version> string is compared, not DLL contents. ChangeSet strictness is inert for TAOM because ApplicationVersion.FromString defaults ChangeSet for 3-part version strings. The shipped dedicated server ships only Coop/DedicatedServer.Windows/Native/SandBox/SandBoxCore, so its validated set is {Coop} and a TAOM client is refused until the whole TAOM stack is installed server-side. Remedy is operational: document 'every participant and the server run the identical TAOM release and module set'; no TAOM code change is required and there is no partial-match mode.",
  "corrected_severity": "MEDIUM"
 },
 {
  "refuted": true,
  "reason": "The QUOTE is genuine and the DETECTION FACT holds; the PRESCRIBED FIX is wrong as stated, so the finding is refuted on impact.\n\nVERIFIED (claim's factual core is sound):\n1. Quote is verbatim at e:/repos/TAOM/Dependencies/Foundation/CoopPresence.cs:35-39 — `private static readonly string[] CompiledModuleDefaults = { \"BannerlordTogether\", \"BattleLinkMPClient\", };`. No paraphrase.\n2. The shipped list also lacks it — Dependencies/_Module/coop-modules.txt:14-15 `[modules]` section contains only `BannerlordTogether` / `BattleLinkMPClient`.\n3. The match is exact-equality against launcher module Ids: CoopPresence.cs:131-132 `var known = new HashSet<string>(config.ModuleIds, StringComparer.OrdinalIgnoreCase); _activeCoopModuleIds = active.Where(known.Contains)...`, fed by IncompatibleModDetector.cs:222-224 which reads `moduleInfo.GetType().GetProperty(\"Id\")`. The Coop mod's id IS \"Coop\": E:/Steam/steamapps/workshop/content/261550/3770450698/SubModule.xml `<Id value=\"Coop\"/>`. So \"Coop\" is genuinely unmatched today.\n4. All three named consumers do key on CoopPresence: SaveShield.cs:286 `var swallow = SaveShieldPolicy.ShouldSwallow(category, CoopPresence.IsActive, !IsSwallowEnabled());`; PatchShield.cs:316-317 `var coopActive = CoopPresence.IsActive; var mayUnpatch = PatchShieldPolicy.ShouldUnpatchForeignOwners(coopActive);`; Main/SubModule.cs:1076-1077 `var coopIds = Dependencies.Foundation.CoopPresence.ActiveCoopModuleIds; if (coopIds.Count > 0)`. And SaveShieldPolicy.cs:32-35 confirms `if (!coopActive) return true;` / `return !string.Equals(category, SaveLoadCategory, ...)`. The SAVE-LOAD target set (SaveShield.cs:79) includes `TaleWorlds.SaveSystem.SaveManager`.`Load`, the same method Coop patches (GameInterface.Services.Save.Patches/LoadPatches.cs:11-19, `[HarmonyPatch(typeof(SaveManager), \"Load\", ...)]`), so the failure mode is real.\n\nREFUTATION — the claimed fix is incomplete and, as written, RED-tests the suite:\nThe claim asserts \"Add 'Coop' to CompiledModuleDefaults AND to the shipped Dependencies/_Module/coop-modules.txt. This is the single required TAOM code change for this dimension.\" An existing test forbids exactly that two-file change in isolation. TAOM.Tests/Infrastructure/Dependencies/BundledDependencyManifestTests.cs:265-300, `ModulesToLoadAfterThis_ContainsEveryCoopModuleIdFromTheShippedConfig`, reads every id in coop-modules.txt `[modules]` and then, `foreach (var manifest in new[] { DepsSubModule, MainSubModule })`, asserts `pinned.Any(p => string.Equals(p, id, StringComparison.OrdinalIgnoreCase))` against `<ModulesToLoadAfterThis>`, failing with \"coop-modules.txt lists module '{id}' but .../_Module/SubModule.xml does not pin it in <ModulesToLoadAfterThis>\". Both manifests currently pin only the two legacy ids (Dependencies/_Module/SubModule.xml:69-70; Main/_Module/SubModule.xml:37-38).\n\nThat pin is not bookkeeping — it is load-order-critical for this specific mod. Dependencies/_Module/SubModule.xml:58-66 states the reason: \"BannerlordTogether ships its own 0Harmony.dll at the SAME version we deploy (2.4.2.0), and HarmonyLib's patch registry is per-assembly-instance static state. If its copy wins the AppDomain slot ... Harmony.GetAllPatchedMethods() called from ours cannot see its patches at all — which silently blinds both PatchShield and the [HarmonyCensus] report\". Coop reproduces that hazard: `0Harmony.dll` is present in E:/Steam/steamapps/workshop/content/261550/3770450698/bin/Win64_Shipping_Client/. So without the manifest pins, the very census writer the claim says would be \"immediately activated\" can be blinded, and the AssemblyResolve collapse the comment relies on may not get its ordering.\n\nMinor secondary gap: both manifests also carry `<DependedModuleMetadata id=\"...\" order=\"LoadAfterThis\" optional=\"true\" />` mirrors for the two existing ids (Main/_Module/SubModule.xml:27-28, Dependencies/_Module/SubModule.xml block) as the BUTR-launcher mirror; the claim omits these too (not test-pinned, but convention).\n\nAlso note the claim's own premise about safety is not fully clean: CoopPresence.cs:90-100 documents a KNOWN GAP — detection is the launcher's ACTIVE list, so if Coop's SubModule ctor throws, TAOM still runs in co-op mode and SaveShield rethrows save-load faults for a dead co-op layer. Adding \"Coop\" inherits that gap; it is not a pure no-downside flip.\n\nSeverity stays HIGH: the underlying detection miss is real and load-bearing. The finding is refuted because its prescription is wrong — following it verbatim breaks an existing pinned test and leaves the Harmony-instance ordering unestablished.",
  "corrected_claim": "TAOM's co-op detection does not recognise module id \"Coop\" (CoopPresence.cs:35-39 compiled defaults and Dependencies/_Module/coop-modules.txt:14-15 both list only BannerlordTogether and BattleLinkMPClient), while the mod's manifest declares `<Id value=\"Coop\"/>`. Detection is exact-equality on launcher module Id (CoopPresence.cs:131-132 over IncompatibleModDetector.cs:222-224), so SaveShield's SAVE-LOAD rethrow (SaveShield.cs:286 + SaveShieldPolicy.cs:32-35), PatchShield's unpatch withholding (PatchShield.cs:316-317) and the Harmony census (Main/SubModule.cs:1076-1077) all stay in solo mode during a Coop session. The fix is FOUR coupled edits, not two: (1) \"Coop\" in CompiledModuleDefaults; (2) \"Coop\" under `[modules]` in Dependencies/_Module/coop-modules.txt; (3) `<Module Id=\"Coop\" />` in `<ModulesToLoadAfterThis>` of BOTH Dependencies/_Module/SubModule.xml and Main/_Module/SubModule.xml — mandatory, since BundledDependencyManifestTests.ModulesToLoadAfterThis_ContainsEveryCoopModuleIdFromTheShippedConfig (lines 265-300) fails otherwise, and Coop ships its own bin/Win64_Shipping_Client/0Harmony.dll, the exact two-Harmony-instance hazard the pin exists to defeat; (4) the matching `<DependedModuleMetadata id=\"Coop\" order=\"LoadAfterThis\" optional=\"true\" />` BUTR mirrors. The residual KNOWN GAP at CoopPresence.cs:90-100 (a co-op module whose ctor throws still reads as active, making SaveShield rethrow for a dead co-op layer) applies to \"Coop\" as well.",
  "corrected_severity": "HIGH"
 },
 {
  "refuted": true,
  "reason": "The quote is verbatim (CoopartiveMultiplayerExperience.cs:502-504) and the \"container is built only inside CoopartiveMultiplayerExperience\" half holds — `grep -rn \"new ContainerBuilder\"` returns exactly two hits, lines 368 and 502, both in that file. But the load-bearing assertion, \"there is NO hook for a third party to contribute Autofac registrations before Build()\", is contradicted by three separate AppDomain-wide type scans that run as Autofac module Load() during Build():\n\n(1) GameInterface/GameInterface.Registry/RegistryModule.cs:19-32 — `foreach (Type registry in GetRegistries()) { builder.RegisterType(registry).AsSelf().InstancePerLifetimeScope().AutoActivate(); }` where GetRegistries is `from t in AppDomain.CurrentDomain.GetDomainTypes() where t.IsConcrete() && t.GetInterfaces().Any((Type i) => i.IsGenericType && i.GetGenericTypeDefinition() == typeof(IAutoRegistry<>)) select t;` — NO namespace filter, NO assembly filter.\n\n(2) GameInterface/GameInterface.Services/ServiceModule.cs:22-42 registers everything returned by `InterfaceCollector.GetInterfaces<IHandler>(\"GameInterface\")`, `GetInterfaces<IGameAbstraction>(\"GameInterface\")`, `GetInterfaces<IAuditor>(\"GameInterface\")`.\n\n(3) Coop.Core/Coop.Core.Common/CommonModule.cs:40-50 `RegisterAllTypesWithInterface<TModule, TInterface>` → `InterfaceCollector.GetInterfaces<TInterface>(typeof(TModule).Namespace)`, invoked from ClientModule.cs:46-47 and ServerModule.cs:62-63 for IHandler/IPacketHandler.\n\nCommon/Common.Util/InterfaceCollector.cs:12 is `from t in AppDomain.CurrentDomain.GetDomainTypes(@namespace)`, and Common/Common.Extensions/ReflectionExtensions.cs:40-63 loops `domain.GetAssemblies()` and admits any type where `type.Namespace.StartsWith(namespacePrefix)` — an assembly-agnostic, prefix-only filter. Only AutoSyncModule.cs:45 (`Assembly assembly = GetType().Assembly;`) is genuinely assembly-closed. All the extension interfaces are public: `public interface IAutoRegistry<T>` (IAutoRegistry.cs:6), `public abstract class AutoRegistryBase<T>` (AutoRegistryBase.cs:8), `public interface IHandler` (Common.Messaging/IHandler.cs:5), `public interface IGameAbstraction` (GameInterface.Services/IGameAbstraction.cs:3), `public interface IAuditor` (Common.Audit/IAuditor.cs:3).\n\nSecondary contradictions: \"only two hardcoded modules\" ignores lines 505-512 of the same method, which `RegisterInstance(configuration).As<INetworkConfig>().SingleInstance()` and `RegisterInstance(advertisementConfig).AsSelf().SingleInstance()` from the parameters of the PUBLIC `StartAsClient` — a caller-supplied registration path. The claimed impact (\"TAOM cannot call RegisterModule/RegisterType against Coop's container\") also overreaches: GameInterface/ContainerProvider.cs exposes `public static bool TryGetContainer(out ILifetimeScope)` and `public static void SetContainer(ILifetimeScope)`, so a third party can `BeginLifetimeScope(b => b.RegisterType<...>())` and re-set it — direct registration is reachable, just post-Build.\n\nSeverity is overstated at HIGH because TAOM almost certainly needs no container access at all: `Common.Messaging/MessageBroker.cs:16` declares `public static MessageBroker Instance`, and CommonModule.cs:31 registers it as `RegisterInstance(MessageBroker.Instance).As&lt;IMessageBroker&gt;().SingleInstance().ExternallyOwned()` — the message bus is a process-wide singleton TAOM can subscribe to without touching Autofac. Combined with TAOM defining no MBObjectBase subclasses (so no IAutoRegistry need), this is an informational architecture note, not a compatibility blocker.",
  "corrected_claim": "Coop's Autofac container is constructed only inside CoopartiveMultiplayerExperience — two `new ContainerBuilder()` sites (line 368 server, line 502 client), each with two top-level `RegisterModule` calls plus optional `RegisterInstance` of the caller-supplied INetworkConfig/SessionAdvertisementConfig (lines 505-512). It is NOT closed to third parties: registrations are contributed before Build() completes by AppDomain-wide type scans inside the modules. RegistryModule.cs:27-32 scans every loaded assembly with no namespace filter for concrete `IAutoRegistry<>` implementors and registers each `.AsSelf().InstancePerLifetimeScope().AutoActivate()`. ServiceModule.cs:46-64 and CommonModule.cs:40-50 scan every loaded assembly filtered only by a namespace *prefix* (\"GameInterface\", \"AutoSync\", \"Coop.Core.Client\", \"Coop.Core.Server\") for public `IHandler`, `IPacketHandler`, `IGameAbstraction`, `IAuditor`. Only AutoSyncModule.cs:43-50 is assembly-scoped. Post-build, `ContainerProvider.TryGetContainer`/`SetContainer` (both public static) allow a child lifetime scope with arbitrary registrations to be swapped in. TAOM impact: TAOM cannot pass its own Autofac Module to the root builder, but it has at least three supported registration routes if it ever needs one — and for its actual needs it needs none, since `MessageBroker.Instance` is a public static singleton (MessageBroker.cs:16) registered ExternallyOwned (CommonModule.cs:31) and usable with no container involvement.",
  "corrected_severity": "LOW"
 },
 {
  "refuted": false,
  "reason": "The quote is verbatim at ServiceModule.cs:22-26 and 46-48, and the mechanism holds when traced deeper than the analyst went. InterfaceCollector.GetInterfaces<T> (Common/Common.Util/InterfaceCollector.cs:12) delegates to AppDomain.CurrentDomain.GetDomainTypes(@namespace), whose body (Common/Common.Extensions/ReflectionExtensions.cs:40-64) loops every assembly from domain.GetAssemblies() with the sole filter \"if (type.Namespace != null && type.Namespace.StartsWith(namespacePrefix))\" plus IsConcrete (IsClass && !IsGenericType && !IsAbstract). I grepped for an assembly-name guard and found none on this path. IHandler is public (Common/Common.Messaging/IHandler.cs:5). Timing supports the claim: the container is built at coop session start (CoopartiveMultiplayerExperience.cs:368-379 server, 502-513 client), after game modules are loaded. Full DI is real: IMessageBroker (CommonModule.cs:31), INetwork (ClientModule.cs:30 / ServerModule.cs:38) and IObjectManager (ObjectManagerModule.cs:18) are all in the same container, and GameInterfaceModule.AttachToComponentRegistration additionally injects a per-type ILogger. WorkshopWarehouseHandler.cs:38-53 is the exact shape a TAOM handler would mimic. TWO CORRECTIONS, neither refuting: (1) the impact phrase \"register synced state\" overstates reach — the IAutoSync field/property sync path is assembly-locked, not namespace-filtered: AutoSyncModule.cs:43-49 reads \"Assembly assembly = GetType().Assembly; string text = GetType().Namespace; return from t in assembly.GetTypes() where t.GetInterface(\\\"IAutoSync\\\") != null\" (note text is computed and never used — dead code). So squatting yields a DI-constructed message handler only, not AutoSync participation. Message serialization needs no squat at all: SerializableTypeMapper.cs:26-52 scans all AppDomain assemblies for [ProtoContract] with no namespace filter. (2) Severity HIGH is inflated: TAOM has zero \"namespace GameInterface*\" declarations today (grepped Main/ and TAOM.Tests/), so nothing is currently at risk — this is an optional opportunity that also forces a hard compile-time reference to Coop's Common.dll/GameInterface.dll (satellite-assembly territory). Two hazards worth pinning if TAOM ever squats: ServiceModule.cs:34-41 throws InvalidOperationException for any discovered IGameAbstraction type without a derived interface, and every handler is .AutoActivate(), so a throwing or unresolvable ctor fails container Build() and kills coop startup entirely.",
  "corrected_claim": "GameInterface.Services.ServiceModule discovers IHandler implementations across the WHOLE AppDomain (all loaded assemblies, no assembly filter) filtered only by Type.Namespace.StartsWith(\"GameInterface\"), and registers each AsSelf().InstancePerLifetimeScope().AutoActivate(). A concrete, non-generic TAOM class with a public constructor in a namespace starting with \"GameInterface\" is therefore auto-instantiated at coop session start with IMessageBroker/INetwork/IObjectManager/ILogger injected. SCOPE LIMIT: this reaches the message-handler path only. The IAutoSync field/property sync path is assembly-locked (AutoSyncModule.cs:45 \"Assembly assembly = GetType().Assembly\"), so namespace squatting cannot register synced fields; conversely [ProtoContract] message serialization is already AppDomain-wide and needs no squat. RISKS: a discovered type implementing IGameAbstraction without a derived interface throws InvalidOperationException (ServiceModule.cs:41), and AutoActivate means any ctor failure aborts the whole container build and coop startup. TAOM has no GameInterface namespace today, so this is an available (undocumented, unpinned) extension point rather than a live compatibility hazard — pin it with a test and re-verify on every Coop update if adopted.",
  "corrected_severity": "MEDIUM"
 },
 {
  "refuted": true,
  "reason": "The quote is verbatim and the Coop-side mechanism is confirmed (uncategorized patch, applied via GameInterface.cs:50 harmony.PatchAllUncategorized on both the client path MainMenuState.cs:47 and host path CoopartiveMultiplayerExperience.cs:385/:425; UnpatchAll at GameInterface.cs:71-73 is an empty body). The collateral list is also confirmed — all 13 named Disable*.cs files exist and four spot-checked ones are the identical RegisterEvents-Prefix-false shape. But the CLAIMED TAOM IMPACT is materially wrong on the aging half, which is the reason the finding was rated HIGH. (1) TAOM does not depend on AgingCampaignBehavior for race lifespans: Main/Features/RaceAge/RaceAgeBehavior.cs:31 registers its own `CampaignEvents.DailyTickEvent.AddNonSerializedListener(this, OnDailyTick)`, and OnDailyTick (lines 37-58) does `if (_raceAgeService.ShouldDieOfOldAge(hero.Race, hero.Age))` then `_heroAgeAdapter.KillByOldAge(hero.HeroId)`. Coop disables only the specific vanilla types it names — it has no blanket behavior filter (NoHarmonyLoader.RemoveBehavior<RemoveType> is opt-in per type, NoHarmonyLoader.cs:237) — so TAOM's behavior, added unconditionally at Main/SubModule.cs:600, keeps ticking inside a coop session. (2) TAOM already bypasses vanilla aging deliberately: TaomAgeModel.cs:15-17 declares `MaxAge => 10000` and `BecomeOldAge => 5000`, while vanilla's gate is AgingCampaignBehavior.cs:288 `!(hero.Age >= (float)Campaign.Current.Models.AgeModel.BecomeOldAge)` — so IsItTimeOfDeath never fires under TAOM in single-player either. Disabling AgingCampaignBehavior removes nothing TAOM was consuming for elven immortality or dwarf lifespans. The claim's own prescribed fix (\"reimplement the tick in its own behaviour which Coop cannot disable\") is exactly what TAOM already shipped. The pregnancy half survives: PregnancyCampaignBehavior.cs:120 (`Campaign.Current.Models.PregnancyModel.GetDailyChanceOfPregnancyForHero(hero)`) and :194 (`CampaignTime.DaysFromNow(Campaign.Current.Models.PregnancyModel.PregnancyDurationInDays)`) are the only drivers and TAOM has no replacement, so TaomPregnancyModel does go dead — along with the downstream birth path TAOM hooks in Main/Features/RaceAge/Hooks/DeliverOffSpring_RaceAssert_Patch.cs, since HeroCreator.DeliverOffSpring is only reached via PregnancyCampaignBehavior.CheckOffspringToDeliver. Half the claimed impact is fabricated by inference, so severity drops from HIGH to MEDIUM.",
  "corrected_claim": "BannerlordCoop unconditionally disables AgingCampaignBehavior and PregnancyCampaignBehavior on both server and client via RegisterEvents Prefix-false patches (GameInterface/GameInterface.Services.Heroes.Patches.Disable/DisableAgingCampaignBehavior.cs:6-14 and DisablePregnancyCampaignBehavior.cs:6-14), applied through the uncategorized sweep at GameInterface/GameInterface/GameInterface.cs:50 and never unpatched (UnpatchAll is empty, GameInterface.cs:71-73). Same treatment hits ~148 other vanilla behaviours including BattleCampaignBehavior, EducationCampaignBehavior, HeirSelectionCampaignBehavior, DynamicBodyCampaignBehavior, MarriageOfferCampaignBehavior, ClanMemberRolesCampaignBehavior, CrimeCampaignBehavior, RetirementCampaignBehavior, IssuesCampaignBehavior, GuardsCampaignBehavior, ArenaMasterCampaignBehavior, NPCEquipmentsCampaignBehavior, PrisonerRecruitCampaignBehavior and ~40 issue/quest behaviours (all verified as RegisterEvents-Prefix-false files).\n\nREAL TAOM IMPACT — PREGNANCY ONLY. PregnancyCampaignBehavior is the sole consumer of Campaign.Current.Models.PregnancyModel (PregnancyCampaignBehavior.cs:120 GetDailyChanceOfPregnancyForHero, :157/:194 PregnancyDurationInDays), and TAOM ships no replacement tick, so TaomPregnancyModel's race-appropriate fertility and gestation stop firing in a coop session. The downstream birth path also stops, which disables TAOM's Main/Features/RaceAge/Hooks/DeliverOffSpring_RaceAssert_Patch.cs (HeroCreator.DeliverOffSpring is reached only from PregnancyCampaignBehavior.CheckOffspringToDeliver).\n\nNOT AN IMPACT — AGING. TAOM's elven immortality and race lifespans are driven by TAOM's OWN RaceAgeBehavior (Main/Features/RaceAge/RaceAgeBehavior.cs:31 DailyTickEvent -> OnDailyTick -> ShouldDieOfOldAge -> KillByOldAge), registered unconditionally at Main/SubModule.cs:600. Coop disables only the vanilla types it enumerates and never touches third-party behaviours, so this keeps running. Moreover TaomAgeModel.cs:15-17 (BecomeOldAge => 5000, MaxAge => 10000) already makes vanilla AgingCampaignBehavior.IsItTimeOfDeath (AgingCampaignBehavior.cs:288, gated on hero.Age >= AgeModel.BecomeOldAge) unreachable in single-player. What TAOM actually loses from the aging disable is vanilla-parity only: child growth-stage transitions (OnHeroGrowsOutOfInfancy / OnHeroReachesTeenAge / OnHeroComesOfAge), death-by-illness and extra-life perk handling — the same loss every mod takes, with no TAOM-specific model going dark.\n\nTAOM ACTION: for coop parity, optionally add a TAOM-owned pregnancy tick (a CampaignBehaviorBase daily-tick mirroring PregnancyCampaignBehavior that consults TaomPregnancyModel), gated on coop-session detection so it does not double-fire in single-player. No action needed for race lifespans.",
  "corrected_severity": "MEDIUM"
 },
 {
  "refuted": true,
  "reason": "The quote is verbatim and correctly located (CoopNetworkBase.cs lines 268-271), and the receive half is real: MessagePacketHandler.cs:80-88 builds a per-type compiled invoker (\"MethodInfo method = Publish.MakeGenericMethod(messageType);\" ... \"return Expression.Lambda<Action<IMessageBroker, NetPeer, IMessage>>(body, ...).Compile();\") cached in \"publishFunctionCache\", and MessageBroker.Publish<T> applies no type filter. ContainerProvider.TryResolve<T> (GameInterface/GameInterface/ContainerProvider.cs:51) plus \"void SendAll(IMessage message);\" on Common/Common.Network/INetwork.cs make the extension point genuinely reachable from a third-party module, and SerializableTypeMapper does auto-register by FNV-1a hash of Type.FullName (\"int num = StableId(fullName);\") with no hand-maintained table — so \"no Coop-side routing table entry\" holds.\n\nBut the claim as WORDED is refuted on two counts, both in the send path:\n\n(1) NOT \"any IMessage\". Common/Common.PacketHandlers/MessagePacket.cs:28-35 hard-gates on protobuf definition: \"if (!RuntimeTypeModel.Default.IsDefined(message.GetType())) { throw new ArgumentException(\"Type \" + message.GetType().Name + \" is not serializable.\"); }\". Reinforced in Common/Common.Serialization/ProtoBufSerializer.cs:51-54: \"if (!typeMapper.TryGetId(obj.GetType(), out var id)) { throw new InvalidOperationException(\"Type \" + obj.GetType().FullName + \" is not registered with the serialization type mapper\"); }\". A plain IMessage without [ProtoContract]/[ProtoMember] throws at SendAll, it is not sent.\n\n(2) A hardcoded two-entry message-TYPE allowlist DOES exist on the server send path, which is exactly what the claim denies. CoopServer.cs:186-192 overrides Send: \"if (!connectionMessageQueue.TryHandleBroadcast(netPeer, packet)) { base.Send(netPeer, packet); }\", and Coop.Core.Server.Connections/ConnectionMessageQueue.cs:85-92 is the gate: \"private static bool ShouldBypassLoadingQueue(IPacket packet) { if (packet.PacketType == PacketType.CampaignTime) { return true; } return packet is MessagePacket messagePacket && messagePacket.MessageType == typeof(NetworkSessionLobbyChanged); }\". Anything not on that list falls into the per-peer phase switch (lines 69-82): \"case Phase.Dropping: return true;\" — silently discarded, no log. PeerChannel defaults to that phase (\"public volatile Phase Phase = Phase.Dropping;\", line 30) and Handle_PlayerConnected creates the channel with that default (\"channels.TryAdd(payload.What.PlayerPeer, new PeerChannel());\"). So server SendAll(IMessage) of a TAOM message is dropped or deferred for the whole join window; only the two allowlisted types get through.\n\nTwo further omissions make the claimed TAOM impact an overreach even where it holds: (a) receipt of an unregistered type is not benign — ProtoBufSerializer.Deserialize returns null (\"if (!typeMapper.TryGetType(protoMessageWrapper.TypeId, out var type)) { return null; }\") and PublishEvent then throws (\"if (message == null) { throw new ArgumentNullException(\"message\"); }\"); CoopClient.OnNetworkReceive wraps this in \"catch (Exception exception) { Logger.Error(exception, \"Failed to process packet\"); }\" but CoopServer.OnNetworkReceive (CoopServer.cs:113-128) has NO try/catch, so an asymmetric TAOM install throws out of the server poll loop. (b) \"just messageBroker.Subscribe<NetworkTaomX>(...)\" is not sufficient: MessageBroker stores subscribers as WeakDelegate over \"new WeakReference(@delegate.Target)\" and prunes them mid-publish (\"if (!weakDelegate.IsAlive) { list.RemoveAt(i--); continue; }\"), so TAOM must hold a strong reference to the subscribing instance or delivery silently stops.",
  "corrected_claim": "The transport is type-agnostic in the sense that matters — there is no hand-maintained Coop-side routing table, and receipt dispatches through a compiled generic Publish invoker (MessagePacketHandler.CreatePublishInvoker) onto MessageBroker, with SerializableTypeMapper auto-registering any [ProtoContract] type by FNV-1a hash of its FullName. TAOM can therefore define its own IMessage types and call ContainerProvider.TryResolve<INetwork>(out var net); net.SendAll(msg) without patching Coop. But it is NOT true that \"any IMessage is serialized and sent\" or that there is \"no message-type allowlist\". TAOM must satisfy four preconditions: (1) every co-op message type must carry [ProtoContract]/[ProtoMember] — MessagePacket.Create throws ArgumentException and ProtoBufSerializer.Serialize throws InvalidOperationException otherwise; (2) server-side broadcasts are gated by ConnectionMessageQueue.ShouldBypassLoadingQueue, a hardcoded allowlist of exactly PacketType.CampaignTime and typeof(NetworkSessionLobbyChanged) — every other type, including TAOM's, is silently dropped while a peer is in Phase.Dropping and deferred while in Phase.Queueing, so TAOM must not rely on broadcasts landing during the join window (or must re-send after the peer opens); (3) the type must be present on BOTH ends — an unknown type id deserializes to null and PublishEvent throws ArgumentNullException, which CoopClient catches but CoopServer.OnNetworkReceive does not, so a TAOM-asymmetric session throws out of the server poll loop; (4) subscribers are held as WeakDelegate over the delegate's Target, so TAOM must keep a strong reference to the subscribing object or delivery silently stops.",
  "corrected_severity": "MEDIUM"
 },
 {
  "refuted": true,
  "reason": "The headline claim is accurate and better-supported than stated, but the CLAIMED TAOM IMPACT misidentifies the host code path, and the misidentification hides a real engineering requirement — so under the verdict rule \"refuted=true if the impact is overstated/wrong\", this is refuted on the impact, not on the quote.\n\nWHAT HOLDS (verified):\n1. Quote is VERBATIM at E:/Steam/.../DedicatedServer/release-info.txt lines 8-12. Line 53-54 adds: \"Exit codes: 0 clean stop | 2 load failure/timeout | 3 fatal while serving | 4 Coop module verification failed (wrong or modified Modules\\Coop).\"\n2. \"only 5 modules\" CONFIRMED by directory listing of DedicatedServer/engine/Modules/: Coop, DedicatedServer.Windows, Native, SandBox, SandBoxCore.\n3. The SHA-256 pin table is REAL, not decoration. I hashed the shipped DLLs under DedicatedServer\\engine\\Modules\\Coop\\bin\\Win64_Shipping_Server\\ and all four match release-info exactly: Coop.Core.dll a905511960924bd2bf4881082b8d88c8fc2d4bd9a3279f4b03fe9cda609076b4; GameInterface.dll 3908757dc0694dc419f7eca328b050d49790140c7051970e4751213c301c2787; Common.dll d25e4b85f2c040c210c652919b3af1fd337523f7c4d8efcabd2b5fab5160c6ec; Coop.Steam.dll 133479b34e81089cef86c3e0e729e37a207dd103e1ce9ac2fb2fc85287fcb53c.\n4. server-config.json exposes exactly 15 keys (saveName, autosaveMinutes, password, logFile, steam, 9 difficulty keys, autoAllocateClanMemberPerks) plus 3 diagnostics booleans. NO module list key. \"not user-configurable via server-config.json\" holds.\n5. TAOM's CustomBattle hard-dep CONFIRMED: e:/repos/TAOM/Main/_Module/SubModule.xml has \"<DependedModule Id=\\\"CustomBattle\\\" />\". Understated corroboration the analyst missed: the Coop CLIENT SubModule.xml itself declares \"<DependedModule Id=\\\"CustomBattle\\\" DependentVersion=\\\"v1.4.7\\\"/>\" and \"<DependedModule Id=\\\"StoryMode\\\" DependentVersion=\\\"v1.4.7\\\"/>\", NEITHER of which is in the appliance's 5-module tree — independent proof the appliance runs a deliberately stripped, different module set. The appliance's own Coop/SubModule.xml has an EMPTY <SubModules></SubModules> and no <DependedModules> at all.\n\nWHAT IS WRONG (the refutation):\n6. The claimed route \"HostSaveGame -> CoopartiveMultiplayerExperience.StartAsServer\" is a DEAD PATH. Grepping the entire decompiled tree for HostSaveGame returns only three hits: the record definition (GameInterface/GameInterface.Services.UI.Messages/HostSaveGame.cs:5), one subscription (CoopartiveMultiplayerExperience.cs:87 \"messageBroker.Subscribe<HostSaveGame>(Handle);\") and one handler (line 318). There is NO publisher anywhere. Nothing sends HostSaveGame.\n7. The REAL in-game host route is AttemptHost, and its default branch is NOT a listen server — it SPAWNS A SECOND Bannerlord.exe. GameInterface/Coop.UI.LoadGameUI/SelectedGameVM.cs:83: \"MessageBroker.Instance.Publish(this, new AttemptHost(((SavedGameVM)this).Save.Name, password ?? string.Empty, visibility));\". Handler CoopartiveMultiplayerExperience.cs:153-159: \"if (!serverProcessManager.IsRunning) { hostedSession = true; ... serverProcessManager.Start(obj.What.SaveName, text, visibility); }\", then the client joins itself at \"Address = \\\"127.0.0.1\\\"\" (line 170). ServerProcessManager.cs:59-60: \"string engineExecutablePath = ManagedServerLauncher.GetEngineExecutablePath(); string arguments = ServerLaunchArguments.BuildManagedServerArguments(ManagedServerLauncher.GetActiveModuleIds(), saveName, currentProcess.Id, password, visibility);\". ManagedServerLauncher.cs:12-13 resolves that exe from the CLIENT's own dir: \"string directoryName = Path.GetDirectoryName(Process.GetCurrentProcess().MainModule.FileName); return Path.Combine(directoryName, \\\"Bannerlord.exe\\\");\" and line 18-19 takes the module set from the live client: \"return (from m in ModuleHelper.GetActiveModules() select m.Id).ToArray();\". ServerLaunchArguments.cs:47-55 builds \"/singleplayer\", \"/server\", BuildModuleList(moduleIds), \"/coopsave\", saveName, \"/coopowner\", ownerProcessId.\n8. True in-process (listen-server) hosting occurs ONLY on two fallbacks: standaloneServerProcess (line 142-147, \"Standalone server process hosting save '{SaveName}' in-process\") and !SessionDiscovery.SteamAvailable (line 148-152). With Steam running — the documented default (\"steam\": true) — the host path is two processes.\n\nWHY THIS MATTERS FOR TAOM (what the analyst's wording hides): TAOM will be loaded TWICE on the host machine as two independent full Bannerlord processes (two Harmony patch sets, two MCM reads, two log sinks). The spawned instance boots under /server with isServer=true and auto-hosts from InitialState with no user interaction (Coop/CoopMod.cs:264-280 TryManagedServerAutoStart -> \"Coop.StartAsServer(ManagedServerConfig.SaveName, ...)\"), so TAOM's startup must not require a rendered UI or block on user input. TAOM code that asks \"am I the host\" must use ManagedServerConfig.IsManagedServer (=> OwnerProcessId > 0), not a listen-server assumption.\n\nONE HONEST GAP: I could NOT confirm the exit-4 SHA enforcement in executable code. BannerlordCoopServer.exe is a compressed single-file bundle — control strings that provably exist (\"SERVING\", \"coop server up\", \"saveName\", \"autosaveMinutes\", \"Coop.Core.dll\") all return 0 hits on ASCII and UTF-16 byte scan, so the absence of \"SHA256\" is uninformative. Enforcement rests on the mod's own release notes plus the verified-real pin table, not on read code. This is a caveat, not a refutation of the headline.",
  "corrected_claim": "The shipped dedicated server is a locked appliance: its engine\\Modules tree contains exactly 5 modules (Coop, DedicatedServer.Windows, Native, SandBox, SandBoxCore — no CustomBattle, no StoryMode, no TAOM), server-config.json exposes no module-list key, and release-info.txt states the four Coop assemblies are SHA-256 verified at boot and again before the listen socket opens, with exit code 4 on mismatch. The pin table is real — all four shipped DLLs hash exactly to the published values — though the enforcement code itself is unverifiable because the server exe is a compressed single-file bundle. TAOM must therefore ignore the dedicated-server appliance. CORRECTION TO THE IMPACT: TAOM's co-op target is the in-game host path, but that path is NOT a listen server and does NOT route through HostSaveGame (which has zero publishers in the entire tree — it is dead code). The real route is SelectedGameVM.cs:83 publishing AttemptHost, whose default Steam-available branch calls ServerProcessManager.Start, which SPAWNS A SECOND Bannerlord.exe from the client's own bin directory with \"/singleplayer /server\" plus ManagedServerLauncher.GetActiveModuleIds() (the host's live module set, TAOM included), then rejoins it at 127.0.0.1. True in-process hosting via StartAsServer happens only when SessionDiscovery.SteamAvailable is false or standaloneServerProcess is set. Consequence for TAOM: TAOM is loaded twice on the host machine as two full processes; the spawned server instance must boot headless under /server and auto-host from InitialState (CoopMod.TryManagedServerAutoStart) without user interaction; and TAOM host-detection must key on ManagedServerConfig.IsManagedServer (OwnerProcessId > 0), not on a listen-server assumption.",
  "corrected_severity": "HIGH"
 },
 {
  "refuted": true,
  "reason": "QUOTE: verbatim and accurate. `CastleRecruitmentBehavior.cs:98-102` matches the citation exactly, and `RegisterEvents` line 42 confirms `CampaignEvents.OnGameLoadedEvent.AddNonSerializedListener(this, OnGameLoaded)`. The downstream call really does create Heroes: `CastleNotableMaintainer.cs:112` — `if (HeroCreator.CreateNotable(occupation, castle) == null)`.\n\nCOOP ORDERING: also verbatim-correct. `GameInterface.Services.GameState.Patches/GameLoadedPatch.cs:12-18` — `[HarmonyPatch(typeof(MapScreen), \"OnInitialize\")] [HarmonyPostfix] private static void OnGameLoaded() { ...; MessageBroker.Instance.Publish(null, new CampaignReady()); }`. `Coop.Core.Client.States/LoadingState.cs:39,52-55` — `messageBroker.Subscribe<CampaignReady>(Handle_CampaignLoaded)` … `registryManager.RegisterAllGameObjects();`. So registration is indeed strictly after `OnGameLoadedEvent`.\n\nWHY IT IS STILL REFUTED — the causal chain breaks at the step that matters. The claim asserts the client \"ends up with notables the server does not have\" as a CONSEQUENCE of the ordering. It does not, because (a) the client's authoritative campaign IS the host's save, and (b) TAOM's creation is deficit-based.\n\n(a) Both client paths converge on loading the host save. `ReceivingSavedDataState.cs:40-50`: `gameStateInterface.LoadSaveData(gameSaveData); Logic.LoadSavedData();` → `GameStateInterface.cs:99-114` `InteralLoadSaveGame`: `ISaveDriver driver = new CoopInMemSaveDriver(saveData); LoadResult loadResult = SaveManager.Load(\"\", driver, loadAsLateInitialize: true); MBGameManager.StartNewGame((MBGameManager)new SandBoxGameManager(loadResult));`. The `CharacterCreationState` fresh-sandbox path is NOT a counterexample — `CharacterCreationState.cs:65-69` registers, packages the hero, then `registryManager.ClearAllRegistries();` and `Handle_NetworkHeroRecieved` (line 72-76) calls `Logic.LoadSavedData()` → `ReceivingSavedDataState` → host save. The throwaway sandbox is discarded.\n\n(b) `EnsureCastleNotables` only spawns the DEFICIT: `CastleNotableMaintainer.cs:104-105` — `int existing = CountNotables(castle, occupation); for (int i = existing; i < target.Value; i++)`, documented at lines 65-67: \"Only ever ADDS notables (never moves or mutates existing heroes), so it is safe in every entity state and idempotent across repeated loads/ticks — counts the live notables first and spawns only the deficit.\" The host's save already contains the host's castle notables (TAOM's `SyncData` comment, lines 49-50: \"castle notables are real Hero objects persisted by the engine's save system\"). With matching settings the loop body executes ZERO times and ZERO Heroes are created. The ordering is real; the divergence is not.\n\nWHAT THE REAL RISK IS (narrower, different root cause): per-machine MCM settings, not load ordering. `CastleRecruitmentSettingsProvider.cs:20,24-25` — `IsEnabled => TaomSettings.Instance?.EnableCastleRecruitment ?? _defaults.Enabled` and `NotablesPerCastle => SettingClamp.Clamp(TaomSettings.Instance?.CastleNotablesPerCastle, _defaults.NotablesPerCastle, 1, 5)`. A client with the feature ON against a host with it OFF (or client 5 vs host 2) produces a real deficit and real phantom notables. NOT FOUND: any coop code that synchronises MCM/mod settings between host and client.\n\nSEVERITY OVERSTATED — no crash path exists. (1) The Hero-constructor lifetime prefix only logs: `GameInterface.Registry.Auto/LifetimePatches.cs:13-25` — `if (ModInformation.IsClient) { Logger.Error(\"Client created managed {name}\", __instance.GetType()); }`. It neither throws nor blocks — and it is not even installed at TAOM's `OnGameLoaded`, since `PatchLifetimes()` runs AFTER `RegisterAllGameObjects()` (`LoadingState.cs:55-57`). (2) An extra hero registers cleanly: `HeroRegistry.cs:32-41` iterates `Hero.AllAliveHeroes` / `Hero.DeadOrDisabledHeroes` calling `RegisterExistingObject(allAliveHero.StringId, allAliveHero)`. (3) Mismatches degrade to log lines: `ObjectManager.cs:46` `logger.Error(\"Duplicate id: {id}\", id); return false;` and `ObjectManager.cs:216` `logger.Error(\"[{ClassName}] Failed to get {name} using {id}\", ...)`.\n\nCLAIM'S ID CHARACTERISATION IS ALSO WRONG. \"registered under locally-derived ids\" — ids are `typeof(T).Name + \"_\" + id` (`AutoRegistryBase.cs:69`) over the engine's `Hero.StringId`, i.e. StringId-derived and deterministic, not coop-locally-minted. Coop moreover ships explicit id reconciliation for exactly this class of mismatch: `AttachmentIdMapper.cs:23` `autoRegistryFactory.BuildIdRemap(dictionary);` and `AttachmentIdMapInitializationHandler.cs:31` `autoRegistryFactory.SetJoinIdRemap(payload.What.AttachmentIdMap?.DerivedToServerId);`.\n\nFinally, the prescribed fix (\"gate on am-I-a-coop-client and skip\") is the wrong shape: blanket-skipping `OnGameLoaded` on the client is harmless-but-unnecessary in the matched-settings case, and does nothing about the actual divergence source (the daily `OnDailyTickSettlement` → `TickCastle` → `EnsureCastleNotables` + `FillCastleVolunteers` at line 104-108, which runs on the client every day regardless of load ordering).",
  "corrected_claim": "TAOM's `CastleRecruitmentBehavior.OnGameLoaded` → `CastleNotableMaintainer.EnsureAllCastles` → `HeroCreator.CreateNotable` does run on the coop client before coop's `CampaignReady`/`RegisterAllGameObjects()`, but it creates ZERO heroes in the normal case: the client loads the host's save (`ReceivingSavedDataState` → `GameStateInterface.InteralLoadSaveGame` → `SaveManager.Load`), and `EnsureCastleNotables` spawns only the deficit above the notables already present (`for (int i = existing; i < target.Value; i++)`, documented idempotent). The genuine divergence risk is per-machine MCM settings, not load ordering: `EnableCastleRecruitment` / `CastleNotablesPerCastle` are read from `TaomSettings.Instance` on each machine and coop synchronises no mod settings, so a client with the feature enabled (or a higher notable target) than the host will spawn phantom castle notables the server has never seen — via `OnGameLoaded` AND, continuously thereafter, via `OnDailyTickSettlement` → `TickCastle`. The failure mode is state divergence plus `ObjectManager` \"Failed to get {name} using {id}\" / \"Client created managed Hero\" log spam, not a crash; coop's Hero-ctor lifetime prefix only logs, and isn't even installed until after TAOM's `OnGameLoaded`. TAOM's fix is to make the feature host-authoritative — suppress notable CREATION (both the load-time and the daily-tick call sites) when running as a coop client, rather than gating only `OnGameLoaded`.",
  "corrected_severity": "MEDIUM"
 },
 {
  "refuted": true,
  "reason": "The TAOM quote is verbatim (MumakilMissionBehavior.cs:66) and BT attachment is genuinely ownership-blind, but the claim's load-bearing coop assertion — \"coop applies no attacker gate on Mission.RegisterBlow\" — is flatly contradicted by the decompiled source. Missions/Missions.Agents.Patches/AgentDamagePatch.cs:9-14 is `[HarmonyPatch(typeof(Mission), \"RegisterBlow\")]` whose Prefix opens with `if (!attacker.IsLocallyControlled()) { return false; }` — an attacker gate on exactly that method. That is precisely TAOM's emission path: CustomAttacksUtils.cs:60/69 binds a delegate to Mission's \"RegisterBlow\" and line 92 invokes `_registerBlow(Mission.Current, attacker, victim, realHitEntity, b, ref collisionData, in attackerWeapon, ref combatLogData)`, so every mumakil/spider/warg/elephant blow routes through the patched method. The gate is also fail-closed: NetworkAgentRegistry.cs:202-212 returns `false` when the agent is absent from AgentToInfo, else `value.CurrentAuthority == controllerIdProvider.ControllerId`. Both branches defeat the claimed mechanism — registered agents let exactly one authority-holding peer through (one blow, not N), unregistered agents are suppressed on every peer (zero blows). Damage cannot multiply by peer count. Coop further stacks a victim-side gate (RegisterBlowPatch.cs:18, `return __instance.IsLocallyControlled();`) plus attacker gates in MeleeHitCallbackPatch, ChargeDamageCallbackPatch, FallDamageCallbackPatch, MissileHitCallbackPatch and MissileAreaDamageCallbackPatch. The claim's PuppetSpawner premise is accurate (PuppetSpawner.cs:173 assigns AgentControllerType.None to non-owned agents) but moot, since coop already enforces the equivalent at the blow boundary; the proposed TAOM ownership gate would redundantly double-gate an already-gated path. The real residual risk is the inverse of the claim and much lower severity: fail-closed gating means any TAOM creature agent missing from NetworkAgentRegistry deals zero damage on all peers. Mount agents are registered on both sides (OwnedAgentReplicator.cs:182-187, PuppetSpawner.cs:209-215), so the mumakil mount-slot agent is likely covered.",
  "corrected_claim": "LOW — TAOM's BT-driven creature attacks are ownership-blind (MumakilMissionBehavior.cs:66 attaches trees to all agents), but this does NOT multiply damage. All TAOM creature blows funnel through CustomAttacksUtils.RegisterBlow (CustomAttacksUtils.cs:92) into Mission.RegisterBlow, which coop patches with an attacker-locality gate (AgentDamagePatch.cs:14, `if (!attacker.IsLocallyControlled()) return false;`). Because NetworkAgentRegistry.IsLocallyControlled returns false for unregistered agents (NetworkAgentRegistry.cs:202-212), the gate is fail-closed: at most one peer's blow lands, never N. TAOM needs NO ownership gate before BT attack tasks — adding one would double-gate an already-gated path. The actual residual risk is the opposite failure: if a TAOM creature agent (mumakil mount-slot, spider, warg) is not registered in coop's NetworkAgentRegistry, its attacks deal zero damage on every peer. Mount agents are registered by both OwnedAgentReplicator.cs:182-187 and PuppetSpawner.cs:209-215, so this is probably covered; verify in-game rather than preemptively patching. Separately, ungated BT side effects (animation, sound, target selection) will diverge cosmetically across peers — a polish item, not a blocker.",
  "corrected_severity": "LOW"
 }
]