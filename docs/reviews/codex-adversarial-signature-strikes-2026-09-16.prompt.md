ADVERSARIAL CODE REVIEW: SignatureStrikes (#605), TAOM Bannerlord v1.5.3 mod, uncommitted working tree on branch bannerlord-1.5.x.

You are the reviewer. Assume the code has bugs and prove it. Do NOT edit files, do NOT run the build, do NOT write your report to any path: return the full report as your FINAL MESSAGE (the dispatcher redirects stdout into docs/reviews/raw/codex-adversarial-signature-strikes-2026-09-16.md). Read AGENTS.md and .ai/roles/reviewer.md first.

FEATURE. A configured hero (shipped: Sauron, hero id lord_1_17 / FaceGen race sauron) gets effects keyed on the swing direction of any melee hit with any weapon except thrown or ranged items. Overhead = Slam: the struck agent is always knocked down (a GameModel verdict), enemies within outerRadius of the impact take damageFraction of the hit's damage with the engine's boulder falloff and are knocked down (synthetic blows), a one-shot morale drain (fear) hits the ring; also on an overhead into the ground. Left/Right = Sweep: the struck agent gets BlowFlags.KnockBack (GameModel verdict) and enemies in the ring get a synthetic blow carrying KnockBack. Thrust = plain hit. No Harmony patch: a MissionLogic on MissionBehavior.OnMeleeHit that only ENQUEUES a request, drained on the next OnMissionTick; two thin overrides on the already-registered TaomCombatMechanicsModel. Long per-attacker per-kind cooldowns (Slam 20 s, Sweep 12 s) plus a live MCM multiplier. Design doc: docs/features/signature-strikes.md. Deep-review RCA already applied: docs/reviews/rca-signature-strikes-2026-09-16.md (read it so you do not re-report those seven).

TAOM ID CHEATSHEET.
Kingdom IDs: empire_w=Gondor, empire_s=Mordor, empire=Dunland, vlandia=Rohan, battania=Khand, aserai=Harad, khuzait=Easterlings, sturgia=Dale/North, erebor, rivendell, lothlorien, mirkwood, isengard, gundabad, dolguldur, umbar, shaghana, abanissa.
Culture IDs (custom): gondor, mordor, erebor, rivendell, lothlorien, mirkwood, isengard, gundabad, dolguldur, umbar.
Culture IDs (XSLT/vanilla): vlandia=Rohan, empire=Dunland, empire_w=Gondor, empire_s=Mordor, battania=Khand, aserai=Harad, khuzait=Easterlings, sturgia=Dale.
NOTE: "rohan" is NOT a valid ID (Rohan uses "vlandia"). "dol_guldur" is NOT valid (use "dolguldur"). Sauron is the vanilla lord lord_1_17 retargeted by Main/_Module/ModuleData/lords.xslt (race="sauron"); his mace is the CraftedItem wm_sauron_mace in the live LOTRLOME_Armory.

READ FIRST.
docs/features/signature-strikes.md
docs/reviews/rca-signature-strikes-2026-09-16.md
Main/_Module/ModuleData/signature_strikes/signature_strikes_config.json
docs/features/combat-mechanics.md (the model this feature extends)
docs/features/dread-aura.md (the tracker pattern and morale pieces it reuses)
.claude/rules/csharp-architecture.md sections "Mission-scope agent handles and the engine's threads" and "Engine-Float Decision Gates"

FILES UNDER REVIEW.
New feature: Main/Features/SignatureStrikes/ (Domain/SignatureStrikesConfig.cs, Domain/StrikeContext.cs, Domain/StrikeEnums.cs, Domain/StrikeNames.cs, Hooks/ISignatureAgentRoster.cs, Hooks/SignatureAgentRoster.cs, Hooks/SignatureMissionGate.cs, Hooks/SignatureStrikeRunner.cs, Hooks/SignatureStrikesMissionLogic.cs, Hooks/StrikeContextFactory.cs, Hooks/StrikeRequest.cs, Hooks/StrikeRequestBuffer.cs, ISignatureStrikeRegistry.cs, ISignatureStrikeService.cs, ISignatureStrikesConfigProvider.cs, ISignatureStrikesSettingsProvider.cs, SignatureStrikeFalloff.cs, SignatureStrikeRegistry.cs, SignatureStrikeService.cs, SignatureStrikesConfigProvider.cs, SignatureStrikesIoC.cs, SignatureStrikesSettingsProvider.cs).
Modified: Main/Features/AdvancedCombat/CustomAttacksUtils.cs (TakeDamage gained a trailing BlowFlags extraFlags; ComposeBlowFlags extracted), Main/Features/CombatMechanics/Models/TaomCombatMechanicsModel.cs (two optional ctor params, DecideAgentKnockedDownByBlow non-charge branch asks the service first, new DecideAgentKnockedBackByBlow override, private SignatureVerdict), Main/Features/TaomSettings.cs (EnableSignatureStrikes, SignatureStrikeCooldownMultiplier), Main/IoC.cs, Main/SubModule.cs (registration; use git diff).
Tests: TAOM.Tests/Features/SignatureStrikes/*.cs, TAOM.Tests/Features/AdvancedCombat/CustomAttacksUtilsBlowFlagsTests.cs, the moved pins in TAOM.Tests/Features/CombatMechanics/CombatMechanicsModelInvariantsTests.cs and TAOM.Tests/Features/CoopInterop/SettingsFingerprintTests.cs.
Use `git status` and `git diff HEAD` for the exact changeset.

KNOWN SUSPECTS. For each one, CONFIRM or DISPUTE with decompiled evidence (installed v1.5.3 DLLs via ilspycmd or the ilspy MCP; E:\Decompiled_Bannerlord\_categories_v1.5.3\ is the same version and fine for reading logic).

S1. Native may not honour BlowFlags.KnockBack on a SYNTHETIC blow. The sweep ring sets KnockBack on a Blow whose WeaponRecord is FillAsMeleeBlow(null, null, -1, -1), DamageCalculated = true, registered through the private Mission.RegisterBlow by cached delegate (CustomAttacksUtils). Trace Agent.HandleBlow -> HandleBlowAux (native) and anything managed that reads BlowFlags.KnockBack for the stagger animation. Is the flag honoured with no weapon record, or does the reaction need a WeaponClass / HasWeapon? If it is not honoured, say what IS honoured (the elephant tree's KnockDown on the same path is battle-proven).

S2. The primary-victim verdicts are asked from inside Mission.CreateMeleeBlow for EVERY melee collision on an unmounted human, including blocked and parried ones (cancelDamage = true). SignatureStrikeService.DecideVerdict requires Collision == StrikeAgent, IsColliderAgent, !AttackBlockedWithShield, InflictedDamage > 0. Enumerate every CombatCollisionResult / cancel combination the engine can hand CreateMeleeBlow and confirm no blocked, parried, chamber-blocked, friendly-fire-cancelled or invulnerable-victim hit can produce a true. Also: DecideAgentKnockedBackByBlow is asked BEFORE DecideAgentKnockedDownByBlow (Mission.cs:5634 then :5638); both now consult the same cooldown stamps without writing them; confirm nothing between the two calls can change the roster entry.

S3. One-package-per-swing. The mission logic stamps LastSlamTime at enqueue in OnMeleeHit; the model reads it in CreateMeleeBlow, which runs BEFORE OnMeleeHit for the same collision. A cleaving swing hits A then B: for B, both the model and the ring should see A's stamp. Confirm from Mission.MeleeHitCallback that B's CreateMeleeBlow cannot run before A's OnMeleeHit dispatch (same native callback sequence, main thread). Then the opposite hazard: for hit A itself the model grants the knockdown, then Evaluate in OnMeleeHit may REJECT the ring for a reason the model did not check (list them: HasMeleeWeapon is checked in both; isCanceled only in the logic; anything else?), leaving a granted knockdown with no ring and no stamp, so the NEXT overhead also gets a granted knockdown. Is that reachable? Rate it.

S4. Passive-usage hits. A couched lance or a braced polearm is a melee collision with IsAlternativeAttack false and an AttackDirection. A cavalry hero added to the config (not Sauron, who is on foot) would slam with a couch. Does AttackCollisionData expose the passive-usage state (Agent.IsDoingPassiveAttack, StrikeType, AttackProgress) and should the gate exclude it? Also a mounted ATTACKER: does anything in the design assume the attacker is on foot (GetNearbyEnemyAgents centre = impact, fine; the roster's IsHuman/!IsMount filter passes a mounted human rider).

S5. StrikeContextFactory.HasMeleeWeapon reads attacker.Equipment[collisionData.AffectorWeaponSlotOrMissileIndex]. Confirm that for a melee collision this index is the wielded weapon's equipment slot (Mission.MeleeHitCallback line 5356 uses it that way), what it holds for a shield bash (the shield's slot?) and a kick (-1?), and that ItemObject.ItemType for a CraftedItem mace resolves to OneHandedWeapon at runtime (WeaponDesign-derived), so the shipped weapon passes the gate.

S6. Custom Battle. SignatureMissionGate does NOT require Campaign.Current, but TaomCombatMechanicsModel is registered only in the campaign starter (SubModule.cs, campaignStarter.AddModel). In a Custom Battle what AgentApplyDamageModel is registered, does GetNearbyEnemyAgents / Team / IsEnemyOf behave, and does the race axis actually find Sauron there (agent.Character is a BasicCharacterObject; is Character.Race the FaceGen race id that SignatureStrikeRegistry compares against IRaceManager.GetRaceIdFromName("sauron")?). Check custom_battle_commanders.json for a Sauron entry.

S7. Thread and re-entrancy. OnMeleeHit runs inside the native MeleeHitCallback. The design deferred the ring to OnMissionTick because registering blows there would re-enter the hit pipeline with `ref inOutMomentumRemaining` live. Confirm the thread (main) from the [MBCallback] caller chain and confirm that Mission.RegisterBlow from OnMissionTick raises OnAgentHit / OnRegisterBlow into OTHER TAOM behaviors (BlowDiagnostics Patch63, CombatMechanics, career passives) in a way that is correct for a synthetic blow with MissionWeapon.Invalid and no weapon record (e.g. does any TAOM postfix dereference the weapon?).

S8. Fear burst sign and units. SignatureStrikeRunner calls moraleModel.CalculateMoraleChangeToCharacter(victim, effect.FearMorale) then ComputeFearDrain and victim.ChangeMorale(-drain). Confirm the model returns a positive magnitude for a positive input under SandboxBattleMoraleModel / DefaultBattleMoraleModel (whichever is registered in a campaign) and that ChangeMorale(-x) lowers morale; confirm DreadAgentGate.CanAffect excludes the player and mounts as its comment claims (CommonAIComponent presence).

S9. Config validation gaps. Every float is FiniteFloatValidator-checked; ints are range-checked. Look for a field the provider forgets: Kind is a string (validated by name); knockDown / knockBack are bools. Is there any path where a profile with knockDown = false and knockBack = false and fearMorale = 0 and damageFraction = 0 still enqueues rings (damage 0 -> runner skips, so the log line fires per swing with ring=0). Is the log line a per-swing INFO write in a big battle worth a rate limit?

S10. Lifecycle. The roster is a process singleton cleared in OnBehaviorInitialize and OnEndMission and evicted in OnAgentDeleted only (OnAgentRemoved was dropped as redundant). Confirm OnAgentDeleted fires for every agent that leaves a mission (killed, despawned, fadeout, mission end teardown) and that a SECOND MissionLogic instance in the same process (next battle) cannot observe stale entries. Also: TaomCombatMechanicsModel is a campaign-lifetime object holding the roster; between missions the roster is empty; any hit outside a mission? No. Confirm.

REQUIRED SECTIONS.
1. VANILLA CODE: paste the decompiled bodies (v1.5.3) of Mission.MeleeHitCallback (the CreateMeleeBlow / RegisterBlow / OnMeleeHit ordering), Mission.CreateMeleeBlow (the knock-back / knock-down calls), Agent.HandleBlow (through HandleBlowAux), MissionCombatMechanicsHelper.DecideAgentKnockedBackByBlow, and whatever managed code reads BlowFlags.KnockBack after HandleBlow, as code blocks with line numbers.
2. KNOWN SUSPECTS: S1..S10 each CONFIRMED / DISPUTED / UNVERIFIED with the evidence.
3. PARALLEL-PATH CONSISTENCY: the model path (SignatureVerdict -> StrikeContextFactory.FromMeleeCollision with isCanceled:false, blow.BlowFlag, Mission.Current?.CurrentTime ?? NaN) versus the logic path (OnMeleeHit -> same factory with the engine's isCanceled, BlowFlags.None, Mission.CurrentTime). Any input that can differ between the two for the SAME collision, and what it does.
4. NAN AND CAST AUDIT: every comparison on an engine float and every float-to-int cast in the changeset, with its polarity (csharp-architecture.md "Engine-Float Decision Gates").
5. CONFIG CROSS-REFERENCE: the JSON against the C# defaults field by field; lord_1_17 against lords.xslt; race sauron against the live Armory skins.xml (E:\Steam\steamapps\common\Mount & Blade II Bannerlord\Modules\LOTRLOME_Armory\ModuleData\skins.xml) and against IRaceManager's name-to-id resolution.
6. FINDINGS OR OBSERVATIONS: numbered, each with severity P1/P2/P3, file:line, the failure scenario as concrete inputs -> wrong outcome, and the minimal fix. Try to refute each of your own findings before listing it. Unverifiable engine or native behaviour stays UNVERIFIED, never a verdict.

QUALITY GATES. Every claim about engine behaviour cites a decompiled line. Every "missing" claim was grepped. No finding on code that matches vanilla. Do not report the seven RCA items again unless the fix is wrong. No em or en dashes in the report (use "--"). Do not write files.

PRIOR REVIEW LESSONS. SUCCESSES: config ID cross-ref caught rohan/dol_guldur mismatches; vanilla decompilation caught missing gates; lifecycle tracing caught stale caches; review 62 caught cross-entity GameModel propagation by opening the engine consumer; review 72 caught a hook-level gate bypassing a service-layer fix. FAILURES: Codex assumed empire=Rohan (it is Dunland); Codex flagged vanilla-matching code as bugs; Codex skipped hard sections; Codex once assembled its report into the transcript file.
