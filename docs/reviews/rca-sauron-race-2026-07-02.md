# RCA — Sauron race + grounding (#321) deep review, 2026-07-02

**Top-line:** 6-agent deep review of the sauron-race-and-grounding changeset (equipment de-horse, `sauron` race as verbatim elf clone, race_age immortal entry, lords.xslt race switch, CombatMechanics parity row). Four agents PASS clean (standards, efficiency, completeness, clone-fidelity — the last independently re-derived the clone from backups). The data-flow agent found 1 HIGH (fixed in-session) + 1 LOW (recorded, pre-existing); the engine-compat agent corrected one documented mechanism (docs fixed), surfaced one design question (deferred to owner, recorded), and confirmed one pre-existing deployment-coupling crash class (documented).

## Findings

| # | Sev | Bug | Category | Why Missed | Preventive Action |
|---|-----|-----|----------|------------|-------------------|
| 1 | HIGH | `TaomPregnancyModel` never gates on the FATHER's race: the engine calls `GetDailyChanceOfPregnancyForHero` only for the FEMALE (`PregnancyCampaignBehavior.DailyTickHero:92` gates on `hero.IsFemale`; `RefreshSpouseVisit:120` passes her). Sauron's `immortal: true` entry therefore never blocked conception — Morgha (`lord_1_18`, no `race=` attr → human, `fertilityMod 1.0`) rolled normally. The CHANGELOG's "blocks any FUTURE children" claim was false as shipped. **FIXED:** spouse-immortality guard added to `TaomPregnancyModel` (returns 0 when `hero.Spouse`'s race is immortal), mirroring the existing hero-side check. | Campaign Mechanics / GameModels | The race_age entry was authored by copying the saruman precedent and trusting the `immortal` flag's *documented* semantics ("blocks all fertility") without tracing WHO the engine passes to the model. The feature doc itself described the model's internal steps ("if the hero's race is immortal → 0") — accurate about the code, silent about the engine's calling convention. The hole was invisible on the two prior immortal races: nazghul spouses are stripped by NazgulFamily, saruman has no spouse — Sauron is the first immortal-race hero WITH a fertile consort. | LESSONS-LEARNED entry (below): for any per-hero GameModel, decompile the engine CALLER and identify which hero is the subject before claiming couple-/group-level semantics. `race-age-system.md` updated with step 2b documenting the female-only calling convention. |
| 2 | LOW | Sauron's race_age entry (`comesOfAge: 18 ≥ fertilityEnd: 0`) trips `RaceAgeConfigProvider.ValidateConfig`'s ordering guard, logging a warning and resetting both fields to defaults at every load. Behaviorally harmless — `IsImmortal` short-circuits before either field is read. **RECORDED, not fixed:** identical pre-existing behavior on `nazghul` and `saruman` (the entry was a verbatim saruman copy); sauron adds a third instance of established log noise, not a new defect. | Config/Validation | The entry deliberately copied saruman verbatim for consistency; the validator's ordering rule predates the immortal flag and doesn't exempt immortal entries whose fertility fields are dead. | Suggested follow-up (own tiny change + test, not #321): `RaceAgeConfigProvider` skips the comesOfAge/fertilityEnd ordering check when `immortal: true` — kills 3 recurring load warnings with zero behavior change. |
| 3 | DOC | The snapshot README/CHANGELOG explained sauron's zero-action-set footprint as "Monster references `as_human_warrior` directly" — true for battles/conversations but incomplete: settlement scenes and the campaign map GENERATE suffixed names from the base-monster id (`as_sauron_lord`, `as_sauron_map`) that don't exist. The real safety mechanism there is the engine's **native silent fallback** on missing action-set ids — proven by `as_elf_map` firing for every elf lord's party icon (~40 lords, months of campaigns, zero errors; `MBGlobals.GetActionSet`'s managed throw branch never fires). "Sauron is exactly as safe as elf" remains true; the stated WHY was wrong. **FIXED:** README, CHANGELOG, and memory data-reference amended with both mechanisms + the optional `base_monster="human"` hardening for `sauron_settlement`. | Docs/Engine knowledge | The mechanism was inferred from the existence of the HeroRace `ActionSetCode` patch + the elf precedent, without decompiling the suffix-generation CONSUMERS (`HeroAgentSpawnCampaignBehavior`, `MobilePartyVisual.AddCharacterToPartyIcon`). Evidence-over-claims §C violation in miniature: a plausible mechanism written into a doc without reading the proving decompile. | The compat agent's decompile is now IN the docs. Rule reinforcement: when documenting "why X is safe," the mechanism claim needs the same decompile evidence as a signature claim. |
| 4 | DESIGN (RESOLVED same-day) | A 1.40-scale Sauron keeps the elf Monster `weight="80"`, so TAOM's new ChargeKnockdown weight-ratio formula treats him as elf-mass — horses knock him down like any infantryman. Vanilla knockdown ignores weight entirely (decompiled: `DecideAgentKnockedDownByBlow` uses health/damage/Athletics only), and the engine's own scaled-mass convention where it needs one is `Weight × scale²` (fall damage, `MissionCombatMechanicsHelper.cs:724`). **RESOLVED 2026-07-02 by user decision via the recommended config lever:** `["sauron"]` gained `KnockdownResistanceMultiplier 3.0` (above dwarf 2.5) + the full offensive set — `SwingEnergyBonusFactor 0.20`, membership in `MonsterCrushMonsterIds`, `OrcShieldCrushRaces`, `CleaveMonsterIds` — in both config surfaces, pinned by `GetConfig_MissingFile_SauronOffenseAndKnockdownDefaults`. The Monster clone stays verbatim (weight untouched). | Balance/Design | Not missed — the verbatim-clone decision consciously deferred all stat tuning ("tune later only if the 1.40 giant ragdolls oddly", plan §Part 2.2). The compat agent quantified what "later" means; the user pulled "later" to same-day. | Resolved via the config surface as recommended; no clone deviation. |
| 5 | WARN (pre-existing class) | `FaceGen.GetRaceOrDefault` is a raw dictionary indexer — it **throws `KeyNotFoundException`** on an unknown race name, despite the name. If `lords.xslt` `race="sauron"` ever loads without the Armory skins block (partial deploy, LOTRLOME update overwriting the live file, stale snapshot restore), campaign load crashes at character deserialize. **Same coupling class as every existing `race="elf"`/`"uruk"`/`"dwarf"` assignment** — sauron adds one instance, not a new class. Mitigated: the snapshot now contains the sauron block and the README restore procedure covers it. | Deployment/Data | Known coupling accepted since the race system was built; the misleading `GetRaceOrDefault` name makes it easy to assume a safe fallback exists. | Documented here + README. When authoring any future `race=` assignment, confirm the target race exists in the CURRENT live Armory skins.xml in the same session (this changeset did — the block was appended before the XSLT switch). |

## Root-cause pattern

Findings 1 and 3 share one theme: **a claim about engine behavior was derived from TAOM-side reading (the model's code, a patch's existence, a precedent race) instead of decompiling the engine's calling side.** Finding 1 trusted the model's internal check without asking who the engine passes in; finding 3 trusted a mechanism story without opening the suffix-lookup consumers. The clone/data work — where every claim was mechanically verified against backups — came back flawless; the two misses were both on the *engine-interaction narrative*.

## Why each agent missed / caught these

- **Standards, Efficiency, Completeness (PASS):** findings 1/3/4 are cross-system engine-interaction issues, structurally outside their per-file rule sets — correctly so; this is why Agent 5 and the compat agent exist.
- **Data-flow agent (caught #1, #2):** did exactly what its charter says — decompiled the engine CALLER (`PregnancyCampaignBehavior`) instead of stopping at TAOM's model. The prompt's explicit "whose race gates it?" question (written because mixed-race couples are TAOM-normal) is what surfaced it.
- **Clone-fidelity agent (0 findings, and correctly disputed its own prompt):** re-derived the race count from backups (13→14, not the prompt's claimed 15) — the independent-re-derivation discipline working as intended.
- **Compat agent (caught #3, #4, #5):** the only agent tasked with decompiling engine consumers; all three of its findings live exactly there.
- **The session author (finding 1):** wrote the "blocks future children" CHANGELOG claim from the immortal flag's doc semantics. The AskUserQuestion option text made the same claim to the user — the fix makes the promised behavior real.

## Feedback memories to codify

One durable lesson (appended to `docs/reviews/LESSONS-LEARNED.md` → Campaign Mechanics):

**Per-hero GameModel overrides: identify the engine's SUBJECT hero before claiming couple/group semantics.** A model that receives one `Hero` and returns a per-hero answer implements couple-level behavior ONLY for the hero the engine actually passes. Decompile the caller and check which entity is the subject (pregnancy = the female; age death = the aging hero; wages = the payer). Any rule of the form "X's flag prevents a couple/family/party outcome" must be traced to the subject-selection site, or implemented symmetrically (check both partners), before it goes in a doc or CHANGELOG.

No new memory FILE needed — the lesson goes to LESSONS-LEARNED (canonical record); the corrected action-set mechanism went into the existing `topics/data-reference.md` races table.
