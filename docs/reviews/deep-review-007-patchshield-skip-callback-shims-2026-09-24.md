# Deep review: plan 007, PatchShield skips the callback shims (2026-09-24)

```
DEEP REVIEW REPORT
===================
Feature: plan 007, "Stop PatchShield re-shielding the 247 callback shims at the first game start"
         branch improve/007-patchshield-skip-callback-shims, diff 7f02fc8d..0ad253d5
Date: 2026-09-24

Scope:   C# (TAOM.Dependencies: PatchShield, PatchShieldPolicy, SubModule doc comment and one log
         label; TAOM.Tests PatchShieldPolicyTests), docs (CHANGELOG, dr3-maintenance.md,
         v1.5.2-impact.md). No XML, no scripts, no harness.
Waves:   one wave: Agents 1 (standards), 2 (engine compatibility), 3 (efficiency),
         4 (completeness), 5 (data flow), 6 (design). Codex adversarial pass in parallel.

STANDARDS:     FAIL (docs only): 6 violations (0 HIGH, 1 MEDIUM, 5 LOW); code checks 1 to 10 pass
COMPATIBILITY: PASS: 0 incompatible, 1 unverified (namespace on v1.4.8, matters only for a port);
               4 wrong engine or measurement claims in text (LOW)
EFFICIENCY:    PASS: 0 issues in code (1 LOW comment claim, 1 environment follow-up)
COMPLETENESS:  INCOMPLETE: GitHub issue missing; exclusion reach undisclosed
DATA FLOW:     FAIL (LOW): 2 gaps (Traces 4, 11), 7 inconsistencies (Traces 2, 3, 5, 9, 10, 12, 13)
DESIGN:        4 KEEP proposals (3 apply, 1 follow-up)
XML:           NOT IN SCOPE
TOOLING:       NOT IN SCOPE
```

Every finding below was re-verified in the worktree before it was acted on: the namespace census
with `ilspycmd -l c` on the three installed v1.5.3 AutoGenerated DLLs (31 + 47 + 10 = 88 classes in
`ManagedCallbacks`: 3 `*CallbacksGenerated`, 79 `ScriptingInterfaceOf*`, 3 `CallbackManager`, 3
`ScriptingInterfaceObjects`); the ButterLib claim by decompiling
`Bannerlord.ButterLib.ExceptionHandler.BEWPatch` from the vendored DLL (lines 72 to 74 put
`BlankTranspiler` on three shims); the timing claims by grouping every pass-2 run in `diag.log` by
date; the fallback paths by reading `CrashReportPatchHelper.HandleAndSwallow` and the non-void
`Native2ManagedBridge.Finalizer`; the game-start timing from `diag.log` lines 31414 to 31425 and
35463 to 35501; the callers of `OnGameInitializationFinished` from the taom-src v1.5.3 cache
(`Campaign.cs`, `CustomGame.cs`, `EditorGame.cs`).

## Details

### Agent 1: Standards

| # | Sev | Finding | Verdict | Action |
|---|---|---|---|---|
| 1.1 | MED | No GitHub issue; CHANGELOG heading has no `#N` | CONFIRMED | NEEDS MIKE (filing is public) |
| 1.2 | LOW | `PatchShield.cs:168-170` loop comment says "UI-layer" over a non-UI entry | CONFIRMED | Fixed |
| 1.3 | LOW | `dr3-maintenance.md` log sample contradicts its new heading | CONFIRMED | Fixed |
| 1.4 | LOW | Stale test class summary, policy summary, section header style | CONFIRMED | Fixed |
| 1.5 | LOW | `ExcludedTargetNamespacePrefixes` public with no outside reader | Not a defect | NOT APPLIED: mirrors `CompiledProtectedOwnerPrefixes`; Agent 6 weighed and rejected it |
| 1.6 | LOW | CHANGELOG reads as if only the first game start runs pass 2 | CONFIRMED | Fixed |

### Agent 2: Engine compatibility

All API usage verified (hook signature, dispatch, `Campaign.OnInitialize` and `CustomGame` raise
sites, loading-screen timing, the 247 count, Harmony 2.4.2 calls, per-call `__originalMethod`).

| # | Sev | Finding | Verdict | Action |
|---|---|---|---|---|
| 2.1 | LOW | `"ManagedCallbacks"` excludes 88 classes, not 3 | CONFIRMED (raised to MED with 4.1) | Documented; narrowing NEEDS MIKE |
| 2.2 | LOW | `v1.5.2-impact.md` row: pass 2 "ran later" is false | CONFIRMED | Row rewritten from `diag.log` |
| 2.3 | LOW | "shims carry only finalizers" is false (ButterLib BEW transpilers) | CONFIRMED | Comment reworded |
| 2.4 | LOW | "30x between machines" is one machine over time | CONFIRMED | Comment reworded |
| 2.5 | LOW | Nothing pins the namespace to the engine | CONFIRMED | New `BindingVerification` test |
| 2.6 | INFO | Caller list omits `EditorGame` (and NavalDLC custom battle, not re-verified) | CONFIRMED for EditorGame | Comment fixed; log label NOT APPLIED (log text change) |
| 2.7 | INFO | CHANGELOG "the first game start" | CONFIRMED | Same fix as 1.6 |

### Agent 3: Efficiency

No performance issue in the changed code (loading-screen path only, stopwatch and formatter once per
pass, the enumerator allocation once per method per process).

| # | Sev | Finding | Verdict | Action |
|---|---|---|---|---|
| F1 | LOW, APPLY | Cost comment misdescribes its evidence; the test comment's "46 s" holds only in slow mode | CONFIRMED | Both comments reworded |
| F2 | LOW, FOLLOW-UP | This desktop's `Harmony.Patch` switches between about 7 ms and about 187 ms per attach at dates `diag.log` pins (2026-06-12, 2026-09-01, 2026-09-04) | CONFIRMED (re-derived: fast 2026-09-01 to 09-03, slow from 09-04) | FOLLOW-UP (environment, Mike's machine) |
| X1 | note | Namespace reach | Same as 2.1 | |
| X2 | note | Plan 006 makes "247" wording stale when it narrows the sweep | CONFIRMED as a sequencing note | Whichever plan lands second rewords `PatchShieldPolicy.cs`, the test comment and `dr3-maintenance.md` |

### Agent 4: Completeness

| # | Sev | Finding | Verdict | Action |
|---|---|---|---|---|
| 4.1 | MED | Whole-namespace exclusion undisclosed in four places and in the plan's coverage note | CONFIRMED | Disclosed in comment, dr3, CHANGELOG "Known limitation"; choice NEEDS MIKE |
| 4.2 | LOW | No automated guard against a namespace move | CONFIRMED | New test |
| 4.3 | LOW | dr3 sample | CONFIRMED | Fixed |
| 4.4 | LOW | `arena.md:10`, `lessons/harmony-il.md:8` name the old symbol | CONFIRMED | Fixed (RCA of 2026-07-06 left as history) |
| 4.5 | NIT | NoAttaches test lacks the suffix assertion and uses an unlogged input | CONFIRMED | Fixed |
| info | | Codex prompt file untracked | CONFIRMED | Committed with this report |

### Agent 5: Data flow

| Trace | Status | Verdict | Action |
|---|---|---|---|
| 1, 6, 7, 8 | CONNECTED | | |
| 2 | INCONSISTENT: exclusion covers more than Native2Managed's selector | CONFIRMED | As 2.1 |
| 3 | INCONSISTENT: ButterLib BEW transpilers on three shims | CONFIRMED | As 2.3 |
| 4 | GAP: on HandleAndSwallow's fallback paths the stack is reset and the trinity no longer swallowed | CONFIRMED | Disclosed; code fix NEEDS MIKE (see NOT APPLIED) |
| 5 | INCONSISTENT: dr3 "swallow-everything" unconditional | CONFIRMED | Reworded with the coverage given up |
| 9 | INCONSISTENT: old symbol in two docs | CONFIRMED | As 4.4 |
| 10 | INCONSISTENT: `RethrowStackPreserver.cs:35-39` ordering sentence now moot | CONFIRMED, pre-existing file | FOLLOW-UP |
| 11 | GAP: no engine pin | CONFIRMED | New test |
| 12 | INCONSISTENT: dr3 sample (`total`, order, missing pass 2) | CONFIRMED | Fixed |
| 13 | INCONSISTENT: "between machines" | CONFIRMED | As 2.4 |

### Agent 6: Design and elegance

| # | Proposal | Behaviour | Scope | Action |
|---|---|---|---|---|
| P1 | Say what the entry really excludes (comment, loop comment, dr3, CHANGELOG) | PRESERVING | APPLY | APPLIED |
| P2 | Test the entry against the installed engine | PRESERVING | APPLY | APPLIED, RED proven |
| P3 | Name `EditorGame` in the comment; widen the log label | comment PRESERVING, label CHANGING | APPLY | Comment APPLIED; label NOT APPLIED (needs Mike) |
| P4 | Split `_shielded` into seen and attached so the summary reports real coverage | CHANGING | FOLLOW-UP | NOT APPLIED (pre-existing code, needs Mike) |

## Action items

1. **NEEDS MIKE:** file the GitHub issue for plan 007, add `(#N)` to the CHANGELOG heading, close
   it with `triage-needs-ingame` (the pass-2 attach count and timing are unsmoked).
2. **NEEDS MIKE:** keep the whole-namespace prefix (now documented) or narrow it to the
   `*CallbacksGenerated` types (needs a type-name input to the policy).
3. **NEEDS MIKE:** restore stack preservation on the Native2Managed fallback paths with
   `RethrowStackPreserver.PreserveForRethrow` in `CrashReportPatchHelper.HandleAndSwallow`'s fallback
   returns, or accept the documented loss. The file belongs to plan 006's scope.
4. Owed in game: start a custom battle; the second `shield pass` line should read about `+125 new`
   with the `ms/attach` suffix and `skipped` up by about 247.

## Improvements (Step 4)

**APPLIED:**
- `TAOM.Tests/Infrastructure/Dependencies/PatchShieldPolicyTests.cs`: new
  `IsExcludedTargetNamespace_InstalledCallbackShimTypes_ReturnsTrue` (`BindingVerification`). RED
  with the entry changed to `"ManagedCallbacksX"` (2 failed, including this test with
  "ManagedCallbacks.CoreCallbacksGenerated would be re-shielded"), GREEN restored.
- Same file: `FormatShieldPassSummary_NoAttaches_DoesNotDivideByZero` asserts the
  `(no new attaches)` suffix with a reachable first-pass input; class summary and section headers.
- `Dependencies/Foundation/PatchShieldPolicy.cs:86-104`: the entry's comment states the real reach
  (88 classes), the fallback paths on which nothing swallows or preserves, and ButterLib's blank
  transpilers; `:9-11` summary names the log line; `:119-124` timing wording.
- `Dependencies/Foundation/PatchShield.cs:168-171`: loop comment names both excluded layers.
- `Dependencies/SubModule.cs:267-275`: caller list adds `EditorGame`; says pass 2 reruns at every
  game start.
- Docs: `dr3-maintenance.md` (coverage given up, reach, pin test, marker creation point, log sample
  in write order), `v1.5.2-impact.md` timing row, `arena.md:10`, `lessons/harmony-il.md:8`,
  CHANGELOG entry and "Known limitation".
- Proof: the characterisation suite `PatchShieldPolicyTests` was 23/23 green before; 24/24 after in
  the full run below. Comments and docs are behaviour-preserving; the build compiles them.

**NOT APPLIED:**
- `Dependencies/SubModule.cs:280` log label widened to "campaign, custom battle or editor":
  changes diag.log text; needs Mike.
- `PatchShield.cs:40,413` seen/attached split (Agent 6 P4): behaviour-changing, pre-existing code;
  needs Mike.
- `Main/Features/CrashReport/Hooks/CrashReportPatchHelper.cs:32,38-39,47,51` preserve the stack on
  the fallback returns: behaviour-changing, outside the changed code, and plan 006 owns the file;
  needs Mike.
- Narrowing `"ManagedCallbacks"` to `*CallbacksGenerated`: behaviour-changing design choice; needs
  Mike.
- `PatchShieldPolicy.cs:77` make the list private (Agent 1.5): same result, but it breaks symmetry
  with `CompiledProtectedOwnerPrefixes` and Agent 6 rejected it on simplicity.
- Step 4.6 convergence `deep-reviewer` pass on the applied diff: this delegate cannot spawn agents;
  owed by the orchestrator.

**FOLLOW-UP** (pre-existing code; no issues filed, because filing is public and needs Mike):
- `IncompatibleModDetector.cs:11,17,69,87,117` still say "main menu", two of them in text written
  to `diag.log` and `last-good-modlist.txt` (plan 007 deferred follow-up 3, widened).
- `PatchShield.cs:30-32` class summary names `OnSubModuleLoad` or `OnBeforeInitialModuleScreenSetAsRoot`;
  pass 2 runs in `OnGameInitializationFinished`.
- `Native2ManagedPatcher.cs:16-17`, `CrashReportSettings.cs:31` hint and `docs/features/crash-report.md:278`
  say finalizers cost nothing unless an exception is thrown; Harmony 2.4.2 calls them on every call
  (plan 006).
- `RethrowStackPreserver.cs:35-39` ordering sentence and the RCA follow-up to pin the bridge at
  priority 800 are moot now that the two finalizers share no method.
- `PatchShieldPolicy.cs:194-197` ("before any session can exist") contradicts `:178-180`.
- `docs/features/crash-report.md` never says PatchShield's exclusion depends on the Native2Managed
  sweep.
- This desktop's slow `Harmony.Patch` mode (Agent 3 F2): compare the 2026-06-12, 2026-09-01 and
  2026-09-04 switch points with Windows Update, Defender platform and tool install history.
- `lessons/harmony-il.md:190-209` recommends origin-based gating over a growing namespace list;
  plan 007's deferred follow-ups 1 and 2 carry it.
- The namespace on v1.4.8 is UNVERIFIED (not installed); re-check before any port to
  `bannerlord-1.4.5`.

## Codex review

`docs/reviews/raw/codex-adversarial-007-patchshield-skip-callback-shims-2026-09-24.md` (complete,
"END OF CODEX REVIEW"); gpt-6-astra at ultra, 177,288 tokens. Prompt:
`docs/reviews/codex-adversarial-007-patchshield-skip-callback-shims-2026-09-24.prompt.md`.
Verdict: one P3 documentation finding, no P1 or P2. It quoted the three shim types, the raise sites
and the Harmony finalizer contract from the installed DLLs, and disputed all ten Known Suspects
with line evidence (suspects 2 and 6 partly UNVERIFIED because it ran nothing).

### Phase 3d assessment

| # | Codex severity | My severity | Agree? | Reason |
|---|---|---|---|---|
| 1 | P3 (docs): the comment, dr3 and CHANGELOG say the Native2Managed finalizer swallows every exception while capture is on; it returns the exception on re-entry, unresolved service and handler failure | LOW | Yes | Verified in `CrashReportPatchHelper.cs:32,38-39,47,51`. Same finding as Agent 5 Trace 4/5. Docs reworded; Agent 5 adds that the stack is also reset on those paths (non-void bridge) |
| KS 1-10 | DISPUTED or UNVERIFIED | | Yes | Spot-checked 3, 4, 7 and 10 against the worktree: the sweep still selects `*CallbacksGenerated` (`Native2ManagedPatcher.cs:72`), the symbol appears only in the permitted files, the MCM defaults are untouched, the formatter is called at `PatchShield.cs:211` |

- **Confirmed bugs:** 1 (above), fixed in docs.
- **False positives:** none.
- **Design questions:** Codex framed the coverage loss as the plan's accepted trade and left the
  risk acceptance to the maintainer; recorded as NEEDS MIKE item 3.
- **Things Codex missed:** the namespace reach (88 classes, finding 2 of the RCA), ButterLib's
  transpilers on three shims, the "between machines" timing claim, the `v1.5.2-impact.md` row it
  could not re-check (the same lines are in `diag.log`), the missing engine pin, and the stack-trace
  half of its own finding.

### Phase 3e root cause

| # | Bug | Category | Why missed | Preventive action |
|---|---|---|---|---|
| 1 | Coverage claim reasoned from the normal path of the finalizer that stays | Other: stale coverage claim | Did not trace every return path of the remaining layer against what the removed layer did | Lesson in `lessons/harmony-il.md`: "Removing one of two finalizers on a method..." |

Full table for all findings: `docs/reviews/rca-patchshield-skip-callback-shims-2026-09-24.md`.

### AGENTS.md lessons (pending, Phase 3h consolidated later)

- **Bugs Codex typically misses:** the reach of a prefix match over installed assemblies (list the
  namespace with `ilspycmd -l c` before accepting "excludes N types"); other mods' patches on a
  target (grep vendored DLLs for the namespace string).
- **What Codex does well:** quoting raise sites and the Harmony finalizer contract from the
  installed DLLs, and answering every Known Suspect with line evidence while refusing to run
  cache-writing helpers in a read-only review.

## Verification

- `dotnet test TAOM.Tests -p:DisableModuleCopy=true -p:ModuleId= --filter "FullyQualifiedName~PatchShieldPolicyTests"`
  before the edits: Passed 23, Failed 0.
- RED check (entry misspelt): Failed 2, Passed 22, Total 24.
- Full suite after all edits: `Failed: 2, Passed: 10243, Skipped: 2, Total: 10247`. The two failures
  are the known live-Armory tests `TheElkItem_DeclaresTheScaleTheReachIsTunedFor` and
  `AnimaliaActionSets_BindOnlyHorseActions_ToClipsThatExist`.

VERDICT: READY FOR COMMIT (the three NEEDS MIKE items are disclosed in the CHANGELOG and docs and
change no code on this branch; the convergence pass and the GitHub issue are owed by the
orchestrator and Mike).
