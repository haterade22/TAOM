You are performing an adversarial code review of a Bannerlord v1.4.8 mod (TAOM, Tales From the Age of Men, .NET Framework 4.7.2). Return the full report as your FINAL MESSAGE. Do NOT write any file under docs/reviews/raw/ yourself; the dispatcher redirects your stdout there.

FEATURE UNDER REVIEW: the fix for two player freezes (issues #592 and #595). Two mechanisms, both TAOM's: (1) a deleted TaleWorlds Agent keeps native pointers on its recycled engine slot, so any store keyed by Agent.Index or any Agent/IAgentAdapter handle held across frames aliases whoever inherits the index; (2) TAOM creature behavior trees ran inside the engine's asynchronous agent tick and registered blows from it, racing the main thread's agent-removed callbacks over unsynchronised dictionaries. The changeset moves the trees to the main-thread mission tick, keys the adapter cache by object, adds a slot-identity check (Mission.FindAgentWithIndex(agent.Index) == agent) to every held handle, adds a thread tripwire, locks two shared stores, evicts a formation slot map on deletion, and guards callback bodies that run inside the engine's unguarded loops.

TAOM ID CHEATSHEET (not central here, included for any config cross-reference):
Kingdom IDs: empire_w=Gondor, empire_s=Mordor, empire=Dunland, vlandia=Rohan, battania=Khand, aserai=Harad, khuzait=Easterlings, sturgia=Dale/North, erebor=Erebor, rivendell=Rivendell, lothlorien=Lothlorien, mirkwood=Mirkwood, isengard=Isengard, gundabad=Gundabad, dolguldur=DolGuldur, umbar=Umbar, shaghana=Shaghana, abanissa=Abanissa
Culture IDs (custom): gondor, mordor, erebor, rivendell, lothlorien, mirkwood, isengard, gundabad, dolguldur, umbar
Culture IDs (XSLT/vanilla): vlandia=Rohan, empire=Dunland, empire_w=Gondor, empire_s=Mordor, battania=Khand, aserai=Harad, khuzait=Easterlings, sturgia=Dale
NOTE: "rohan" is NOT a valid ID. Rohan uses "vlandia". "dol_guldur" is NOT valid -- use "dolguldur".

READ FIRST:
- docs/reviews/rca-warg-clip-on-horse-2026-09-13.md (the RCA: evidence from two player sessions, both mechanisms, the audit table, prevention)
- .claude/rules/harmony-patches.md section "Which thread runs your target" (the verified thread map)
- .claude/rules/csharp-architecture.md section "Mission-scope agent handles and the engine's threads"
- docs/features/warg-combat.md, docs/features/mixed-formations.md, docs/features/career-system.md, docs/features/elephant.md, docs/features/dread-aura.md, docs/features/companion-tactics.md (each has a dated 2026-09-13 note)
- The staged diff: run `git diff --cached` in the repo root. The worktree CHANGELOG.md also carries another session's unstaged map-texture entry; ignore it.

VANILLA DECOMPILE TARGETS (installed v1.4.8; use the ilspy MCP server or the cached decompile under C:\Users\mikew\.taom-src\v1.4.8\ or E:\Decompiled_Bannerlord\_categories_v1.4.8\; paste the relevant bodies as code blocks in your report):
- TaleWorlds.MountAndBlade.MissionState.TickMission (the asyncAITick argument, ~lines 132-201)
- TaleWorlds.MountAndBlade.Mission.OnTick, TickAgentsAndTeamsAsync, TickAgentsAndTeams ([MBCallback]), TickAgentsAndTeamsImp, AgentTickMT, OnPreTick, WaitTickCompletion
- TaleWorlds.MountAndBlade.Agent.Tick, TickParallel, TickAsAI, ParallelUpdateCachedAndFormationValuesForAIAgent, GetBaseFormationFrame, State, IsActive, SetActionChannel, GetCurrentAction, FadeOut, OnRemove, OnDelete, GetHashCode
- TaleWorlds.MountAndBlade.Mission.OnAgentDeleted, OnAgentRemoved, OnAgentHit, RegisterBlow, SpawnAgent (the OnAgentBuild loop), FindAgentWithIndex
- TaleWorlds.MountAndBlade.Team.Tick (retreat branch), Formation.Tick (substitute orders), Formation.SetMovementOrder, Formation.GetOrderPositionOfUnit, HumanAIComponent.ParallelUpdateFormationMovement, HumanAIComponent.GetFormationFrame
- TaleWorlds.MountAndBlade.AgentComponent (OnTick, OnTickParallel, OnAgentRemoved), MissionBehavior (OnAgentDeleted, OnAgentBuild, OnMissionTick)
- TaleWorlds.MountAndBlade.MBActionSet.CheckActionAnimationClipExists, ActionIndexCache

KNOWN SUSPECTS (CONFIRM or DISPUTE each with evidence; these are hypotheses, not verdicts):
1. Bone-check timing regression. Behaviors run in reverse registration order (Mission.OnTick iterates MissionBehaviors from the end). Main/SubModule.cs registers AdvancedCombatBehavior at line ~1784 and BehaviorTreeMissionLogic at ~1785, so the tree tick (which calls AgentAdapter.CustomAttack -> Agent.SetActionChannel then registers a BoneCheckDuringAnimation) now runs BEFORE AdvancedCombatBehavior.TickBoneChecks in the same frame. BoneCheckDuringAnimation.Tick expires the check when Agent.GetCurrentAction(0) != the requested action. If native applies SetActionChannel lazily (on the next animation update) rather than synchronously, every warg bone check would expire on its first tick and warg bites would silently stop connecting. Before this changeset the tree ticked at the end of the frame (async) and the bone check first ticked the next frame. Decide from the native-facing managed code (Agent.SetActionChannel writes through IMBAgent.SetActionChannel; GetCurrentAction reads _channel0CurrentActionPointer) whether the action is visible immediately, and state whether the registration order should be swapped (a one-line change in Main/SubModule.cs) or the bone check should tolerate a first frame.
2. Scheduler ghost, now mitigated in the working tree. BehaviorTreeAgentComponent schedules itself with BehaviorTreeMissionLogic inside its constructor, BEFORE the caller does agent.AddComponent(comp). CreatureTreeTracker.TryAttach and WargMissionBehavior.OnAgentBuild now hoist the component above the try and, if the attach throws, Unschedule it and DisposeTree the agent (mirroring OnAgentRemoved). WargMissionBehavior's first-tick scan (the _treesAdded block in OnMissionTick) still constructs and attaches with no per-agent catch. Confirm whether Agent.AddComponent can throw at all in 1.4.8 (it reads as a List.Add plus type checks) and whether the remaining window matters.
3. FindAgentWithIndex on a freed-but-not-reused slot. AgentSlotIdentity.IsCurrentOccupant relies on Mission.FindAgentWithIndex returning null (or a different object) for a deleted agent's index. If the native lookup keeps returning the OLD managed object until the slot is reused, IsCurrentOccupant would report true for a deleted agent; the State check (Deleted, and the mount-despawn doc says pointers read zeroed memory) is what would save it. Find the native binding and any managed evidence of what it returns for a deleted index.
4. Restore closure after mission end. MissionAbilityExecutionContext schedules ally-buff restores by duration. If a restore fires after the mission ended, IsCurrentOccupant returns false (Mission.Current null or a different mission) and RemoveAllyContribution is skipped. Confirm CareerPerkMissionBehavior drains or cancels _activeContexts and calls CareerAbilityBuffTracker.ClearAll at mission end, or show the leak.
5. ForgetAgent capacity. After FormationLayoutService.ForgetAgent(index), the next holder of the index gets LayoutPositioner.AssignNextSlot, which advances NextMeleeIndex/NextRangedIndex. Over a long battle with many deaths and reinforcements the counters could exceed the layout grid. Read LayoutPositioner.AssignNextSlot and say what happens past capacity (wraps, clamps, or places units outside the formation).
6. Lock coverage. AgentAdapterCache, TroopStanceManager and CareerAbilityBuffTracker now lock. MissionAdapterFactory.GetAgentAdapter does TryGet then Add outside a single critical section: two threads can both miss and both Add, the second replacing the first, so two adapters briefly wrap one agent. State whether any consumer can observe the difference. Also: is any TAOM code still reachable from the async tick or the TWParallel workers that touches these stores or SpatialGrid or BehaviorTreeMissionLogic (grep Main/ for Harmony patches on Formation/Team/HumanAIComponent/Agent tick paths; the known ones are Patch30 on GetOrderPositionOfUnit, Patch31/31b/35 on SetMovementOrder/SetTargetFormation)?
7. Caster restore closure. MissionAbilityExecutionContext's hero restore now calls UpdateAgentProperties only when _agent.IsActive() and AgentSlotIdentity.IsCurrentOccupant(_agent), while CareerAbilityBuffTracker.RemoveContribution(HeroStringId, ...) always runs. The ally closure returns early, before RemoveAllyContribution, when the captured index no longer belongs to that ally. Confirm both bookkeeping choices leave CareerAbilityBuffTracker consistent (no entry left with a positive count after the last restore fires) for: ally deleted mid-window, ally slot recycled mid-window, caster killed mid-window, mission ended mid-window.
8. Howdah seat release. TaomHowdahStandingPoint.ReleaseAgent now teleports the seated agent to elephantAgent.Position only if that handle still owns its index, otherwise to the agent's own position (howdah height). TaomHowdahMachine's dead-elephant branch releases seats before clearing the seat refs. Confirm the order is right for a dead elephant that is still the occupant (the corpse position is wanted) and harmless for a recycled one, and whether a seat's own OnTick can teleport its agent to a recycled slot's position in the frame before the machine clears the refs (tick order between a UsableMachine and its StandingPoints).
9. Registration order. The bone-check ordering in suspect 1 could be restored by swapping the two AddTaomBehavior lines, but that would also swap the OnAgentDeleted order between AdvancedCombatBehavior (evicts the adapter cache) and BehaviorTreeMissionLogic. The swap was NOT made. Say whether either order of those two OnAgentDeleted overrides matters.

FILES (staged):
Adapters: Main/Adapters/AgentAdapterCache.cs (new), Main/Adapters/MissionAdapterFactory.cs, Main/Adapters/IMissionAdapterFactory.cs, Main/Adapters/AgentAdapter.cs, Main/Adapters/HeroCommissionAdapter.cs
AdvancedCombat: Main/Features/AdvancedCombat/AgentSlotIdentity.cs (new), MissionThreadGuard.cs (new), AdvancedCombatBehavior.cs, CustomAttacksUtils.cs, CreatureTreeTracker.cs
Behavior tree wrapper: Main/BehaviorTreeWrapper/BehaviorTreeAgentComponent.cs, BehaviorTreeMissionLogic.cs
Creatures: Main/Features/Warg/WargMissionBehavior.cs, Main/Features/Spider/SpiderBehaviorTree.cs, Main/Features/Elephant/TaomHowdahMachine.cs, Main/Features/Elephant/TaomHowdahStandingPoint.cs, Main/Features/Elephant/ElephantMissionBehavior.cs
Other features: Main/Features/MixedFormations/FormationLayoutService.cs, IFormationLayoutService.cs, Hooks/MixedFormationsMissionBehavior.cs, Models/FormationLayoutLabels.cs (new); Main/Features/CareerSystem/Abilities/MissionAbilityExecutionContext.cs, Abilities/CareerAbilityBuffTracker.cs, CareerPerkMissionBehavior.cs; Main/Features/DreadAura/Hooks/DreadSourceTracker.cs; Main/Features/CompanionTactics/BattleActionBar/TroopStanceManager.cs
Tests: TAOM.Tests/Adapters/AgentAdapterCacheTests.cs, MissionAdapterFactoryTests.cs; TAOM.Tests/Features/AdvancedCombat/MissionThreadGuardTests.cs; TAOM.Tests/Features/MixedFormations/FormationLayoutServiceTests.cs; TAOM.Tests/Features/CompanionTactics/BattleActionBar/TroopStanceManagerTests.cs; TAOM.Tests/Features/CareerSystem/CareerAbilityBuffTrackerTests.cs; TAOM.Tests/Features/MixedFormations/FormationLayoutLabelsTests.cs
Docs and rules: CHANGELOG.md, CLAUDE.md (one trap row), .claude/rules/csharp-architecture.md, harmony-patches.md, adapters.md, .claude/skills/deep-review/SKILL.md, docs/features/*.md listed above, docs/reference/harmony-patch-registry.md, docs/audits/cluster-cross-feature.md, cluster-harmony-patches.md, docs/reviews/lessons/adapters-taleworlds-api.md, docs/reviews/rca-warg-clip-on-horse-2026-09-13.md

REQUIRED SECTIONS OF YOUR REPORT:
1. VANILLA CODE: the decompiled bodies you relied on, as code blocks with file and line references. State for each engine entry point which thread runs it, and how you know.
2. THREADING ANALYSIS: walk one frame of single-player play with a spider tree, a warg tree, a player order and an AI order, and mark every TAOM call on a timeline with its thread. Confirm or refute that no TAOM code registers a blow, plays an action, spawns or fades an agent, or touches BehaviorTreeMissionLogic/SpatialGrid/AgentAdapterCache off the main thread after this changeset.
3. HANDLE ANALYSIS: for every place in the changed files that holds an Agent, IAgentAdapter or Agent.Index past the frame it was obtained, state its eviction path and its identity gate, and name any holder that has neither.
4. KNOWN SUSPECTS: CONFIRMED or DISPUTED, each with evidence and the minimal fix if confirmed.
5. CONFIG CROSS-REFERENCE: none expected; say so if none.
6. FINDINGS OR OBSERVATIONS: ranked by severity (P1 ship-blocking, P2 real defect, P3 improvement), each with file:line, the failing scenario, and the minimal fix. Include test gaps.
7. QUALITY GATES: build and test commands you ran (`dotnet build Main -p:DisableModuleCopy=true -p:ModuleId=`, `dotnet test TAOM.Tests -p:DisableModuleCopy=true -p:ModuleId=`) and their exact results.

PRIOR REVIEW LESSONS:
SUCCESSES: decompiling the vanilla caller chain caught thread placement the file-level review missed; config ID cross-ref caught rohan/dol_guldur mismatches; lifecycle tracing caught stale caches.
FAILURES: assuming empire=Rohan (it is Dunland); flagging vanilla-matching code as bugs; skipping the hard sections; asserting an engine method's thread from its name instead of its caller.

Try to refute your own findings before reporting them. Report UNVERIFIED rather than guessing. Use `--` not em dashes in prose.
