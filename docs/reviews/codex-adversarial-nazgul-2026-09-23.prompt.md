ADVERSARIAL CODE REVIEW: the Nine become race nazghul (#644, commit 9804f67b) and the Nine's SCREAM, SignatureStrikes with N signatures (#645, commit b90fd3a4). TAOM Bannerlord v1.5.3 mod, branch bannerlord-1.5.x.

You are the reviewer. Assume the code has bugs and prove it. Do NOT edit files, do NOT run the build, do NOT write your report to any path: return the full report as your FINAL MESSAGE (the dispatcher redirects stdout into docs/reviews/raw/codex-adversarial-nazgul-2026-09-23.md). Read AGENTS.md and .ai/roles/reviewer.md first.

SCOPE. Exactly the two commits: `git show 9804f67b` and `git show b90fd3a4`. The working tree holds other sessions' uncommitted work (elk, PatchShield, CustomAttacksUtils, IoC.cs, SubModule.cs and more): it is NOT in scope, so do not use `git diff HEAD` or `git status` to find the changeset, and do not report on those files except where a commit's code calls into them.

FEATURE, #644. The Nine (hero set `nazgul_nine`: lord_1_15 Witch-king, lord_1_155, lord_1_16, lord_1_28, lord_1_38, lord_1_48 Khamul, lord_1_48_1, lord_1_48_2, lord_1_48_3) were human (six templates in Main/_Module/ModuleData/lords.xslt had no race) or `race="uruk"` (three rows in Main/_Module/ModuleData/characters/lords.xml). All nine templates in lords.xslt now emit race nazghul (a skin in the live LOTRLOME_Armory skins.xml). The three characters/lords.xml rows were deleted: those three ids are vanilla lords (SandBox/ModuleData/lords.xml :6069, :6111, :6153 on the installed v1.5.3), so the XSLT is their one definition, and the values only the rows carried (age 20, BodyProperties age 22.19) moved into their templates. RacePersistenceService (captures every hero's race at save, restores it at session launch, because vanilla never saves BasicCharacterObject.Race) now skips the Nine on restore so an existing save picks up the XML race. `nazghul` joins CombatMechanics' OrcShieldCrushRaces. A new test gate checks every NPCCharacter race attribute in ModuleData and every race emitted by an XSLT against the races the installed Native and live LOTRLOME_Armory skins.xml files register (TAOM.Tests/Core/CultureRaceConsistencyTests.cs).

FEATURE, #645. SignatureStrikes (#605) gave Sauron direction-mapped mace effects from the engine's own melee hit; its config and code held one signature. signature_strikes_config.json is now a `signatures` list (`sauron` rows unchanged; `nazgul` keyed on hero set nazgul_nine and race nazghul). The Nine's Overhead, Left and Right are a Scream: StrikeOrigin.Self (the ring is centred on the attacker, not the impact), outer 6 m, inner 2.5 m, damageFraction 0.3, worldHitBaseDamage 0 (a ground hit does nothing), knockBack true, knockDown false, fearMorale 25, one 15 s cooldown per KIND shared by the three directions, and a module sound `LOTR/Mordor/Nazgul/nazgul_scream` (three ElevenLabs-generated Ogg takes, category mission_voice_shout) played once per strike through Mission.MakeSound at the attacker's eye, falling back to Agent.MakeVoice(Yell) when the name does not resolve. The runner now fears the struck foe too (before #645 it skipped it outright) but never deals it the ring's blow. Per-kind stamps live in StrikeKindTimes (a readonly struct over a float array; With copies). The registry resolves a signature INDEX at agent spawn (hero ids and hero sets first, then race, first listed wins). The roster falls back to the character's own id when the agent has no campaign hero (Custom Battle).

TAOM ID CHEATSHEET.
Kingdom IDs: empire_w=Gondor, empire_s=Mordor, empire=Dunland, vlandia=Rohan, battania=Khand, aserai=Harad, khuzait=Easterlings, sturgia=Dale/North, erebor, rivendell, lothlorien, mirkwood, isengard, gundabad, dolguldur, umbar, shaghana, abanissa.
Culture IDs (custom): gondor, mordor, erebor, rivendell, lothlorien, mirkwood, isengard, gundabad, dolguldur, umbar.
Culture IDs (XSLT/vanilla): vlandia=Rohan, empire=Dunland, empire_w=Gondor, empire_s=Mordor, battania=Khand, aserai=Harad, khuzait=Easterlings, sturgia=Dale.
NOTE: "rohan" is NOT a valid ID (Rohan uses "vlandia"). "dol_guldur" is NOT valid (use "dolguldur"). Sauron is lord_1_17 (race sauron). Eight of the Nine are Culture.mordor; Khamul (lord_1_48) is Culture.dolguldur.

READ FIRST.
docs/features/signature-strikes.md
docs/features/nazgul-family.md
docs/features/hero-race.md
docs/features/lord-identity-reconciliation.md
docs/reviews/rca-nazgul-race-2026-09-23.md and docs/reviews/rca-nazgul-scream-2026-09-23.md (the deep-review findings already fixed; do not re-report them unless the fix is wrong)
docs/reviews/rca-signature-strikes-2026-09-16.md and docs/reviews/raw/codex-adversarial-signature-strikes-2026-09-16.md (the #605 reviews)
.claude/rules/csharp-architecture.md sections "Mission-scope agent handles and the engine's threads", "Engine-Float Decision Gates" and "Config Providers MUST Validate"

KNOWN SUSPECTS. For each one, CONFIRM or DISPUTE with decompiled evidence (installed v1.5.3 DLLs via ilspycmd or the ilspy MCP; E:\Decompiled_Bannerlord\_categories_v1.5.3\ is the same version and fine for reading logic).

S1. The restore skip's premise. It assumes that on a save LOAD the engine re-reads the XML onto each existing CharacterObject (BasicCharacterObject.Deserialize sets Race to 0, then FaceGen.GetRaceOrDefault on the XML value) BEFORE CampaignEvents.OnSessionLaunched, so skipping the restore leaves the XML race. Trace the load sequence and confirm or refute. If the premise is wrong, say what race the Nine have on an old save after #644.

S2. Race is written by other code at runtime. RacePersistenceService's skip discards any runtime race edit on a wraith at the next load (deliberate: a PlayerSwitcher player who re-races a wraith at the barber loses it, a maintainer decision). Enumerate every managed writer of BasicCharacterObject.Race / CharacterObject.Race reachable in a TAOM campaign (vanilla face editor CharacterObject.UpdatePlayerCharacterBodyProperties, TAOM HeroRace services, PlayerSwitcher, culture conversion, anything else) and say whether any of them now fights the XML race on the Nine in a way the skip does not cover (for example a writer that runs AFTER OnSessionLaunched every load).

S3. Every race-keyed consumer that now sees the Nine as nazghul. Grep Main/ for race names and race ids (IRaceManager, GetRaceIdFromName, Races lists in config JSON under Main/_Module/ModuleData) and list each consumer the Nine now reach or leave: race_age_config (immortality), banner bearers, field commission, race fertility (#628), marriage alignment, culture conversion, troop recruitment, avatar and portrait patches, DreadAura and UncapturableHeroes (keyed on the hero set, not race), CombatMechanics OrcShieldCrushRaces. Flag any consumer whose new behaviour for the Nine is harmful or crashes (for example a race-to-culture lookup that throws or falls back to a wrong culture for nazghul).

S4. The three deleted rows. After the delete, confirm lord_1_48_1/2/3 are defined only by vanilla SandBox lords.xml plus the TAOM lords.xslt templates, that the templates emit every attribute and child the deleted rows carried that the game needs (kit, face, age, culture, occupation, skills, traits, voice), and that the Equipments union trap (Equipments is AlwaysPreferMerge, EquipmentSet unique on id + civilian + stealth, Hero.SetInitialValuesFromCharacter picks a random set) cannot leave a second kit on any of the Nine. Also confirm no other module still defines them (the live TAOM_Map and LOTRLOME_Armory ModuleData under E:\Steam\steamapps\common\Mount & Blade II Bannerlord\Modules\).

S5. Custom Battle identity. The roster computes the id as (character as CharacterObject)?.HeroObject?.StringId ?? character.StringId. In a Custom Battle, what type is Agent.Character for a commander picked from custom_battle_commanders.json, what is its StringId, does NazgulRegistry.IsWraith need a campaign, and does the race axis find nazghul there (Character.Race vs IRaceManager.GetRaceIdFromName)? Then the opposite direction: in a campaign, can a NON-hero troop's character StringId equal one of the Nine's hero ids and so register as a wraith?

S6. Cooldown stamps and the two paths. The model (TaomCombatMechanicsModel, campaign only) reads the roster's StrikeKindTimes inside Mission.CreateMeleeBlow for the knock-back verdict; the MissionLogic evaluates in OnMeleeHit and stamps `entry.Times = entry.Times.With(kind, now)`. Confirm the ordering in Mission.MeleeHitCallback for a cleaving swing that hits A then B, confirm which thread each runs on, and confirm a granted knock-back on A cannot be followed by an Evaluate rejection that leaves no stamp (so the next swing inside 15 s also gets a verdict). Also confirm StrikeKindTimes' value semantics: can two readers observe a torn or shared array after With?

S7. The module sound. Confirm from managed code (and name what stays native and UNVERIFIED) that a `module_sound` declared in a module's ModuleData/module_sounds.xml is addressable by SoundEvent.GetEventIdFromString(name), what that returns for an unknown name, and that Mission.MakeSound(id, position, false, true, -1, -1) is the right one-shot call for a positional sound in single player. Check the mission_voice_shout category's limits (Native's module_sounds.xml header) against the three Ogg files, and whether a relatedAgent of -1 changes anything (for example attenuation or the agent-voice channel).

S8. Origin Self geometry. The ring is centred on attacker.Position. Enumerate: a mounted Nazgul (the rider's mount is excluded? the enemy's mounts are excluded by victim.IsMount), a Nazgul on a slope or a wall (2D distance only), the struck foe outside the 6 m ring (then it is neither feared nor hit), a Nazgul surrounded by allies (GetNearbyEnemyAgents filters native side). Is anything here wrong for a 6 m ring centred on the attacker?

S9. Config validation. SignatureStrikesConfigProvider now validates a list: null reverts to the compiled default, an entry with no id, a repeated id (case-insensitive) or no identity axis is dropped, a bad field reverts to that signature's compiled default by id, cooldown keys are parsed as kind names and range-checked 0.5 to 120, a strike whose kind has no cooldown is dropped, origin reverts, sound is checked against ^[A-Za-z0-9_:/.\-]+$. Find a parseable but semantically wrong config that survives validation and changes mechanics (for example an id that matches the compiled default of the OTHER signature, a cooldown for a kind no strike uses, Scream placed on Thrust, a signature whose heroSets names an unknown set). An old-shape file (top-level heroIds / races / strikes, no signatures key) is a known decision: the JSON ships with the module and is overwritten on install; say what it does, not whether it should.

S10. NaN and native sinks. The deep review caught one engine float handed straight to a native call (the eye position into MakeSound, now gated). Audit every engine float in both commits that reaches a native call or a decision gate (the ring centre, distance, falloff, morale values, MissionTime, the cooldown multiplier) for polarity and finiteness.

REQUIRED SECTIONS.
1. VANILLA CODE: paste the decompiled v1.5.3 bodies you rely on, as code blocks with line numbers: the campaign load sequence that re-reads XML onto existing objects (for S1), BasicCharacterObject.Deserialize's race lines, Mission.MeleeHitCallback's CreateMeleeBlow / OnMeleeHit ordering, Mission.MakeSound, SoundEvent.GetEventIdFromString, and SandboxBattleMoraleModel.CalculateMoraleChangeToCharacter with both GetMoraleResistance overrides.
2. KNOWN SUSPECTS: S1 to S10 each CONFIRMED / DISPUTED / UNVERIFIED with the evidence.
3. CONFIG CROSS-REFERENCE: signature_strikes_config.json against the C# compiled defaults field by field for both signatures; the nine hero ids against lords.xslt, NazgulRegistry and custom_battle_commanders.json; race nazghul against the live Armory skins.xml; the module sound name against module_sounds.xml and the files on disk.
4. FINDINGS OR OBSERVATIONS: numbered, each with severity P1/P2/P3, file:line, the failure scenario as concrete inputs to wrong outcome, and the minimal fix. Try to refute each of your own findings before listing it. Unverifiable engine or native behaviour stays UNVERIFIED, never a verdict.

QUALITY GATES. Every claim about engine behaviour cites a decompiled line. Every "missing" claim was grepped. No finding on code that matches vanilla. Do not report the RCA items again unless the fix is wrong. No em or en dashes in the report (use "--"). Do not write files.

PRIOR REVIEW LESSONS. SUCCESSES: config ID cross-ref caught rohan/dol_guldur mismatches; vanilla decompilation caught missing gates; lifecycle tracing caught stale caches; review 62 caught cross-entity GameModel propagation by opening the engine consumer; review 72 caught a hook-level gate bypassing a service-layer fix. FAILURES: Codex assumed empire=Rohan (it is Dunland); Codex flagged vanilla-matching code as bugs; Codex skipped hard sections; Codex once assembled its report into the transcript file.
