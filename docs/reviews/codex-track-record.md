# Codex Reviewer — Track Record & Worked-Example Catalog

> Moved out of `AGENTS.md` 2026-07-18 so the actual TAOM review *rules* fit under Codex's
> `project_doc_max_bytes` cap. This is the full historical catch-log: every rolling essay plus
> the complete "bugs Codex misses / false positives Codex produces / what Codex does well /
> run-mode caveats" catalog across 84 reviews / 186+ bugs. `AGENTS.md` keeps a distilled
> operative summary and points here. Convention (unchanged): after each review, add the new
> essay to the top here, archive the 6th-oldest to `agents-md-review-lessons-archive.md`, and
> harvest durable patterns into `docs/reviews/lessons/<category>.md`.

### Lessons From Prior Reviews (84 reviews, 186+ bugs found)

**Rolling essay log (newest 5; full history: `docs/reviews/agents-md-review-lessons-archive.md`).** Convention (2026-07-12): after each review cycle, add the new `Last updated:` essay at the top of this list, move the 6th-oldest to the archive file, and harvest any durable pattern into the catalog below (+ `docs/reviews/lessons/<category>.md`). The catalog is the operative reviewer guidance; essays are worked examples.

Last updated: 2026-09-23 (Nazgul review 130 -- #644 made the Nine race nazghul and #645 gave them a Scream signature, a second SignatureStrikes signature with a generated sound; two seven-lens deep reviews and their convergence passes had already fixed a pre-existing HIGH, two MEDs and a string of LOWs. Codex gpt-6-astra at ultra, one run over both commits: 0 P1-P2, 2 P3 observations, both confirmed, zero false positives. Its best work was proof by construction: it showed the new race gate's blind spot by transforming a probe stylesheet in memory (a literal `NPCCharacter` in XSLT, invisible to the gate's regex) and showed the provider gap with a one-signature JSON input, rather than arguing from the code. It confirmed the restore skip's load-order premise from the engine's own load sequence, audited every race consumer the Nine now reach, and measured the three Ogg takes from their granule positions. Its qualifications carried two more text defects: a third runtime race writer (co-op join reconciliation, found by walking TAOM's own `SetHeroRace` callers where the deep review had walked only the engine's writers) and the kit sentence that holds for a new campaign only. Lesson for the prompt writer: ask the reviewer to construct the failing input for every gate a change adds; a gate reviewed against the files it passes is reviewed against nothing.)

Last updated: 2026-09-13 (Gondor volunteer pools review 110 -- a data retune: four fiefs moved to the vale they stand in, six castles made exclusive to their household line, towns 60/40 instead of 80/20; five Claude agents passed the runtime and Codex gpt-6-astra at ultra found no defect. Its value was refutation: it disputed all eight handed suspects with lines decompiled from the installed DLLs, and two of those refutations were of the PROMPT's premises, not the code's. KS3 called Morlad's pool bowman-only (the file holds bowman 50 / scout 50) and KS7 said the clan and culture pools fire only for settlements with no pool (a converted fief resolves its settlement culture's `CultureMap` entry before the standard cascade, `VolunteerRecruitmentService.cs:92`); Codex reopened both files, corrected the premise, then answered the corrected question with a Markov estimate of in-slot maturation from the real roots (about 27.5% of untouched slots at tier 5 or 6 after 30 days at notable Power 100, capped at the level 31 veteran archer by `MaxVolunteerTier` 6) and a per-case conversion table. It also re-counted the live map (97 EW settlements after #597, not the doc's 93), recomputed every mirror share by hand (largest error 6.7e-7 against a 1e-4 tolerance), re-ran the reachability flood from the production JSON roots alone, and said it had not run the unit suite instead of adopting the RCA's numbers. Lesson for the prompt writer: a Known Suspect restates the author's assumption, so ask the reviewer to refute the premise as well as the conclusion, and read a disputed premise as a finding about the prompt.)

Last updated: 2026-09-13 (creature handles and threads review 109 -- three player freezes in one evening on v2.0.27, two TAOM mechanisms: a deleted `Agent` is a live handle to whoever inherits its index, and the creature trees ticked on the engine's asynchronous agent tick. Two five-agent deep reviews and a nine-site audit preceded Codex; Codex still found the one regression in the audit (`ForgetAgent` evicted a slot and left the allocator's counters growing, so replacements stood a row deeper and on the other class's rows), by compiling the production allocator into a PowerShell harness and printing the collision, and refuted the review's thread map by opening the caller of `Mission.OnAgentPanicked`: `CommonAIComponent.OnTick`, on the asynchronous tick. It disputed five of nine suspects with decompiled lines, including the deletion-order objection that had held a registration swap back, and reported that its sandbox could not evaluate MSBuild rather than claiming a run. Lesson: a fix that edits one side of an allocator is judged by driving the other, and a callback's thread is its caller's, never its name's.)

Last updated: 2026-09-13 (SmartCavalryAI v2 review 107 -- #586 replaced a cavalry line-charge machine that had shipped four months without ever forming a line, and Codex found what five Claude agents, their fix re-check and the author all passed: 3 P1s that share one shape, the state an engine call leaves BEHIND. `Formation.SetMovementOrder` ends by clearing the native target its own `ChargeToTarget` had just set (`Formation.cs:714`), so the feature's charge was a free charge at anyone; `Formation.Tick` re-applies the retained `FacingOrder` through `SetPositioning` every tick (`:2311-2314`), so a line's direction written through positioning lasted exactly one tick; and the player's "charge THAT formation" is `SetOrderWithFormation(Charge, B)`, a plain Charge FOLLOWED by `SetTargetFormation(B)` (`OrderController.cs:812-817`), so a postfix on the order alone charged the nearest enemy instead of the chosen one. Every one of those signatures had been verified on the installed DLL by two agents and the author; none of the three post-conditions had. What made the pass good: it decompiled every cited member fresh rather than trusting the dump; it compiled and executed a Harmony probe to settle whether a struct `__state` reaches the postfix and what value a reassigned `input` shows, instead of asserting either; it disputed three of the seven handed suspects with quoted lines (the Harmony pairing, the Stop/Follow re-issue variants, the navmesh fallback for a placed rider) and was right each time; it reported that its sandbox denied MSBuild SDK discovery and labelled the supplied 8817-green result as supplied evidence rather than its own. It also caught a proper noun copied into five docs, `FormationAI.SetCurrentOrder`, which does not exist. Two process notes: a Codex prompt must tell it to return the report as its final message, because the dispatcher redirects stdout into the raw file and Codex will otherwise assemble its report into that same path and lose to the transcript; and when the shared tree does not compile because another session is mid-TDD, hand Codex an isolated worktree path for builds and reads, and it will honour it. The durable pattern, harvested into `lessons/harmony-il.md` and `lessons/adapters-taleworlds-api.md`: when a design says "we issue X", read what the engine does right after X returns, and who calls X right before.)

Last updated: 2026-09-13 (kingdom-cap armour curve review 104 -- #583 replaced the armour item curve with a chest cap per kingdom (Erebor 70 to Thenn 35, slot ratios .9/.6/.6/.5, bands .40/.64/.84/1.0, an item banded by its lowest battle-set wearer), applied to 2,510 live Armory items, with the 12-file ladder repair it exposed and #581's cross-kingdom overview and gate; Python tooling and data, no C#. Ran AFTER a 6-agent deep-review that found two real ones (the tier map keyword first while the writer was anchor first; the gate scaling a troop by its culture's cap while items carry their line's). Codex gpt-6-astra ultra: **0 P1 / 4 P2 / 3 P3, all confirmed, zero false positives; 3 of 8 Known Suspects disputed with executed arithmetic.** It reproduced everything before it looked: derive then dry run (Changed 0), the ladder repair (0 edges), the validator (0 errors, the same 7 cells), 88 XML and 81 backups identical across both trees, and hashed all of it into a snapshot file. The catches: (1) the two-tier invariant is a pure function over the curve constants and proves the PRIMARY only, while its docstring, the feature doc and the CHANGELOG said no legendary roll passes the elite piece; Codex found the live Isengard pauldron pair (medium arm 5 + 5 cloth against elite 7) and showed the invariant returns [] on it, then checked the backup to show the inversion was inherited, not introduced. (2) the reserve list judged an unworn item on its folder's cap alone, one call below the routing the deep review had just repaired, so 62 Dol Guldur items in the rhun folder read as Rhun heavy and vanished (Rhun's reserve 114, really 176); it swept the whole Armory to count them. (3) `DefaultItemCategorySelector` files armour by the display tier and `WorkshopsCampaignBehavior` produces by category, so a doc sentence saying nothing filters on the tier was wrong; it named the Erebor chest that changed workshop. (4) four stale How-To rows, each proven by an in-memory calculation (Gondor at protection 99 still writes 57). P3s: an unlabelled benchmark, two missing observation tables, a one-point rounding history it reconstructed pass by pass. **What Codex does well (this review): reading a proof's prose against what the function actually iterates over, and taking a routing fix one call further than the fix went.** It warned, correctly, that a file-default rule for the new observation table would mislabel the Mordor sub-line troops; the table routes `mordor_num_`/`mordor_uruk_` troops to their own caps as `ranged_ladders.json` does. Its sandbox could not run the full Python suite (no pytest for one file, a PermissionError on a fixture); local suite green bar one other session's in-flight test. 2 lessons in `lessons/build-tooling-workflow.md` (a proof over constants states what it proves; a new routing key is grepped to every caller). RCA: docs/reviews/rca-kingdom-cap-curve-2026-09-13.md, section "Codex adversarial pass".)

Last updated: 2026-09-13 (supply search review 106 -- #587 put a live search box on the supply order screen, TAOM's first `EditableTextWidget`, over every trade good in every orderable market, and a five-agent deep review plus a design-time stress test had already removed one MED and four plan defects. Codex, GPT-6-Astra at ultra against an isolated worktree, disputed seven of nine handed suspects with decompiled evidence and found the half of the goods-scroll reset the in-house pass had missed: the VM reset the scrollbar VALUE through a two-way `ValueFloat` binding, but `ScrollablePanel` keeps its wheel MOMENTUM across a repopulate whenever both lists overflow (`:588-598`), so a notch followed at once by a hit pick coasts the pane off the promoted row. It executed the panel's own update equations to show 115 px of travel against a 150 px pane, named the one public reset (`ResetTweenSpeed`) and the vanilla site that pairs it with a scrollbar write (`ChatLogWidget:298-302`). It also caught a doc claim written from memory of a research conclusion rather than the quote: the feature doc said vanilla's inventory closes on Escape while its search box has focus, when `GauntletInventoryScreen.OnFrameTick` returns on `IsFocusedOnInput()` before any poll; TAOM now takes that rule. Lesson: when a fix targets the state a binding CAN see, ask what private state the same widget carries that it cannot, and read the widget's update loop for every field it consults after the bound one. Second lesson: a doc sentence that names vanilla behaviour is a claim to quote, not to recall.)

Last updated: 2026-09-13 (Black Numenorean confinement + level-41 weight band review 105 -- #584 removed the 4% `mordor_num_*` sprinkle from 14 Mordor lord templates and the level-3 patrol (183 stacks) and rescaled the maxes back to 260; #585 moved 41 `troop_weights.xml` rows to 3.0 under a new level rule. Ran AFTER a 6-agent deep-review whose data-flow agent found the one real defect (the heraldry tool's id-only refusal guard went blind once the ids it keyed on were deleted; widened to the max sum, 19 of 21 specs now refuse). Codex gpt-6-astra ultra, 183k tokens: **0 P1 / 0 P2 in the changeset, 4 P3 wrong numbers, 1 PRE-EXISTING P2, zero false positives, all nine Known Suspects answered with executed arithmetic.** The P2 is the catch: `SubtractResultFramePenalty` promises an exact integer-slot subtraction, and Codex loaded the installed `ExplainedNumber` struct under PowerShell with Gondor's real 0.025 feat to show `(B - p/s) * s` landing on 69.99999 for an intended 70 (the engine reads `(int)ResultNumber`, so 69) and on 0.99999 for the promised floor of 1 (so 0). The 2026-07-17 tests asserted the float within 0.01 with a round 0.25 factor; the contract lives on the other side of a truncation the tests never performed. Fixed with a probe-and-lift, tests now cast. The four P3s were all numbers I wrote from an agent's summary instead of the data (a band comment said level 11 for a level-36 troop; the culture default never had the `0/2` initiate; a "was 1.91" copied from a pre-retarget doc is 1.93; a generator comment still named the 2.0 band above a 3.0 row), and Codex proved each with a pasted command. It also disputed two of this session's own statements (the 40 to 203 base-limit bound is settings-dependent; the spider IS an `NPCCharacter`, level 20, in `characters/`, outside the troop glob). **What Codex does well (this review): executing the shipped constants through the installed struct instead of reasoning about the algebra, and checking every number a doc quotes against a command it ran.** Suite 8,784 green. 2 lessons: `lessons/build-tooling-workflow.md` (a guard keyed on deleted data passes by construction), `lessons/testing-qa.md` (assert in the consumer's frame on shipped values). RCA: docs/reviews/rca-black-numenorean-confinement-2026-09-13.md.)

Last updated: 2026-09-12 (camp wait menu review 99 -- #567: a wait menu's condition delegate hides EVERY option when false, and refuge founding broke the camp the condition guarded, so players sat in a panel with no options; fix `4d2ea82b` made the condition `true` and exited the menu before the deposit screen. Codex at ultra: 0 P1 / 2 P2 / 1 P3, zero false positives, eight suspects answered from the installed DLLs, three disputed correctly. F1 was on the fix itself: the warden picker runs unpaused by default, the query layer (order 19501) stays usable over the conversation an incoming enemy opens, and the new exit would then destroy the ENEMY's `encounter_meeting`, leaving a live encounter with no menu transition; fixed by pausing the picker (vanilla AlleyHelper) and refusing in the callback when the current menu or encounter state has changed. F2 refuted the in-house data-flow agent's "every discharge exits the menu": the load-time discharge runs before `MapState` exists, so the runtime check sees no context and the PERSISTED menu id re-enters with a false condition (#573). O1 executed the new source pins in memory against mutated sources and showed `args => true && false` passed. Durable pattern: a modal opened from a game menu must pause, and its callback must revalidate the menu it came from; and a "which menu" fact lives in two places (live context, persisted id), so a guard that reads one is half a guard.)

Last updated: 2026-09-12 (ReturnToArmy review 98 -- one prefix, `Patch87_ReturnToArmy`, gives an army member who is in the army but NOT merged into it a way out of a town or castle; vanilla Return to Army leaves only a village, hides Leave for members, and its wait menu bounces the unattached case into "You are waiting in X". Codex at ultra: 0 P1 / 0 P2 / 2 P3, zero false positives, eight Known Suspects all answered from the installed DLLs. What it did that five in-house agents did not: it opened the INSTALLED co-op mod (CoopNightly) to test a veto rationale that said "peers agree on replicated fields" and found the membership handler applies through `GameThread.RunSafe(blocking: false)`, so the honest claim is "same engine calls as vanilla Leave, same co-op interception", never agreement; and it PROVED the IL drift guard blind spot by running `IlCallScanner` over a synthetic IsVillage-or-IsCastle body and pasting the output, instead of agreeing with the prompt, then refuted the prompt own double-leave consequence (the prefix returns false). It also disputed the in-house S2 trace with a line the five agents missed: the leader `CurrentSettlement` setter propagates null to every attached party (`MobileParty.cs:609-611`). Durable pattern: when a rationale names another mod, Codex reads that mod, so write it to survive the read.)

Last updated: 2026-09-11 (ShaderPrecompilation re-enable review 96 -- the parked main-menu shader walk returned for a public build with roster batching, a one-character player party against each batch so the engine sets the player agent and deployment auto-finishes, a renamed scene-pass MCM property so its off default reaches existing installs, and 1.4.8-correct text. Ran AFTER a 5-agent deep-review whose data-flow agent had already caught a start-timeout that discarded the whole character phase while reporting COMPLETE. Codex gpt-6-astra ultra: **2 P1 / 2 P2 / 3 P3, DO NOT SHIP, zero false positives.** C1 P1: `MBGameManager.EndGame()` is `async void` and dereferences `Game.Current` once no manager is current, so a Ctrl+Shift+K in the 1.5-second settle window after the game had gone (or an end after `StartNewGame` threw) crashed past the runner's catch. C3 P2: a teardown TIMEOUT started the next game while the old one could still be loading. C4 P2: a player who exits the shader battle from the scoreboard keeps the game, can start a normal custom battle, and that battle received the walk's guard and was ended by the runner. All three share one shape, the runner treating its own state as the truth about the engine, and all three fixes add an engine-side check (`Game.Current` before `EndGame`, a resolved teardown before advancing, `TryClaimMission` before acting on a mission). C2 P1: "every troop, lord and piece of equipment" over-promised; Campaign-only companion nodes, civilian sets and race skins are outside the preload. C5: `{newline}` is bound by `Game.Initialize`, so at the cold main menu it renders as nothing; Codex reproduced it in a fresh process. **C6 is the calibration story: Codex disputed a claim made in ITS OWN favour.** Our docs said the old force-finish path would have written a junk Order of Battle profile; Codex read `CustomBattleViews` (`new OrderOfBattleVM()`) and the base class's EMPTY `SaveConfiguration()` and showed the write never happens in a custom battle. The claim had been relayed from a research agent that read the campaign subclass. **What Codex does well (this review): opening the engine bodies the runner's comments describe instead of the runner, and reading the concrete subclass a mission constructs before attributing a side effect.** Suite 8453 green with the one pre-existing banner-bearer data failure. 3 lessons across state-lifecycle-save / adapters-taleworlds-api / localization-ui. RCA: docs/reviews/rca-shader-precompile-reenable-2026-09-11.md. **Cold in-game walk UNVERIFIED; the boundary code has no other proof.**)

Last updated: 2026-09-11 (SpecialResources outflow review 95 -- players could not see what upkeep cost and reported balances "wiped after every battle"; the change made one daily breakdown feed the tick, the tooltip, the toasts, the map-bar flag and the console dump, added four outflow toasts, stopped merchant-only troops deserting, and rescaled the Black Numenorean cost rows. Ran AFTER a 5-agent deep-review (2 real defects, both from the data-flow agent: a warning gate missing the upkeep-troops term, and a storage floor `Math.Max(0f, x)` that kept a NaN through the save) and a fix-loop re-check. **First run on GPT-6-Astra at `ultra`: 0 CRITICAL / 0 HIGH / 3 MEDIUM / 0 LOW, zero false positives, every finding confirmed by re-reading the source.** F1: the spend and recruit toasts reported the NOMINAL cost through a store that floors at zero, reachable because the party screen's prisoner recruit has no `recruit_cost` gate (the volunteer screen does) -- a spider recruited at a balance of 2 would have toasted 5; fixed by returning `before - after` read from the store, the return-value tests moved off a substitute storage whose `Get` returns 0 (they proved a request, never a debit), follow-up #563 for the gate. F2: **it compiled the committed domain type and ran shipped data through the CLR** to show `(int)Math.Ceiling(balance / -Net)` overflowing to int.MinValue on a FINITE net of minus 5.96e-8 (one town at +0.7 against 0.2 + 0.2 + 0.3 of upkeep), "Depleted in -2147483648 days" for a balance the float cannot even move; the countdown now refuses a net below the balance's resolution and range-checks the cast, and the float-cast rule is widened from NaN to finite overflow. F3: the zero-balance notice claimed a loss in progress whenever upkeep troops were present, but the tick adds the net BEFORE it tests the balance, so with income covering upkeep nothing deserts; gated on the net, worded as the rule. **What Codex does well (this review):** executing arithmetic rather than estimating it; a test-sensitivity table that separates "cannot compile against the old API" (21) from "behavioural RED" (7) from "passes either way" (1) and names the tautological self-comparison; following a returned value into the layer that clamps it; tracing the tooltip renderer to PROVE the extended rows reachable instead of leaving it unverified as the compat agent had; declining to inflate the Mumakil's 500 a day into a defect. **Meta-lesson, ours:** an Explore agent's "not found" over the dump went into the issue body as "prisoners never reach OnUnitRecruitedEvent"; the installed behavior dispatches it per unit, which is exactly what makes F1 reachable. `evidence-over-claims` A.4 again. Suite 256 green on the affected suites, 8449 of 8450 overall (the one failure predates the work). RCA: docs/reviews/rca-special-resources-outflow-2026-09-11.md.)

Last updated: 2026-08-24 (SettlementEncounterInvariant review 82 -- fix for player crash bundle `d7d9f7d3` (#510): TAOM placed the main party inside a settlement with `EnterSettlementAction.ApplyForParty` alone, creating neither `PlayerEncounter` nor `LocationEncounter`, then opened a vanilla settlement menu that derefs both unguarded. Ran AFTER a 5-agent deep-review (4 findings, ALL from the data-flow agent, 3 of them regressions the fix itself had introduced). Codex gpt-5.6-sol max: **3 P1 / 5 P2 / 1 P3 -- and ALL THREE P1s collapsed under adversarial verification** (9 verifiers + a completeness critic, each told to refute rather than confirm). P1-1 claimed the chokepoint accepts a siege battle encounter and freezes the battle: DISPUTED on two independent breaks -- the pre-fix code reached the same state (`MoveIntoSettlement` short-circuits when already inside, and the untouched `EnsureMenuOpen` half switched the menu either way, so it is not a regression), and the battle is escapable via `town_leave` -> `LeaveSettlement(); Finish();` -> `FinalizeBattle` -> `LeaveBattle`. **Codex's implied fix was actively harmful**: refusing would route a siege DEFENDER into `LeaveSettlementAction`, and `MapEvent.AddInvolvedPartyInternal` rewrites a siege assault to SiegeOutside for a defender with no `CurrentSettlement` -- the feature's own code comments already documented why. P1-2 DISPUTED on a misquote: the guard it cited as `if (PlayerEncounter.Current != null) return false;` at `ServiceBattleService.cs:103` is a COMMENT line; the real guard returns true early when the encounter already matches the map event. P1-3 (#511 is a ship blocker) PARTIAL: premises held but the chain breaks at `Campaign.Tick`'s `AtMenu == false` gate. Exactly ONE finding survived at its stated severity (P2: discharge with a failed menu-open orphans the encounter it just created -- `LeaveSettlementAction` never clears `PlayerEncounter`, and `OnLoadFinished` skips both `OnLoad()` and `Finish()` when `GameMenuId` is empty). **What Codex does well (this review): the test-sensitivity table.** For every new test it stated what the test would do against the pre-fix code and caught two that passed either way -- nothing in the 5-agent panel asks that question, and a coverage count reports both as coverage. Steal this. **Third fabricated/misattributed-quote incident on record** (after CoopInterop C3 and Career-arc C1), this time quoting a comment as code -- the running rate is now roughly one per review on this axis, so quote-verification before implementing is not optional. **Meta-lesson, and it indicts us not Codex: we shipped a WRONG ENGINE FACT into four artifacts.** The claim that `town_outside`'s Leave option no-ops on a null `Current` (a soft-lock) was inherited from a pre-existing lessons entry and a code comment, then repeated into the #511 issue body, the feature doc and the CHANGELOG without decompiling. `game_menu_town_outside_on_init` derefs `PlayerEncounter.EncounterSettlement.Name` first: it is a CTD at menu init. `evidence-over-claims` C applies to our own prior prose, not only to agents and reviewers. Also: the IL ban test built to prevent recurrence was itself evadable (`SameMethod` compares `DeclaringType`, only the interface method was banned); it now bans interface + concrete + raw `EnterSettlementAction.ApplyForParty` and fails on an allow-list row never observed as a caller -- that rotted-exemption check went red on its first run against a speculative row, which is the gate working. Suite 7,494 green. RCA: docs/reviews/rca-settlement-encounter-2026-08-24.md.)

Last updated: 2026-08-05 (Career UX arc review 81 — 8-issue arc (#377–#384) adopted from a security-vetted external submission, including TAOM's first mission-screen PrefabExtension (energy bar into Native's AgentStatus). Codex gpt-5.5 xhigh, prompt seeded with 8 Known Suspects: **0 P1 / 2 P2 — and all SIX architecture suspects DISPUTED with decompiled evidence** (datasource chain, `[ViewModelMixin("Tick")]` transpiler, filename-keyed `WidgetPrefab.LoadFrom` patching, insert-index semantics, clock unity, careers-XML integrity), independently agreeing with the Claude compatibility agent's own decompile trace — two independent verifications of the render-or-not architecture bet, both favourable. **C2 (confirmed): the overlap the Claude pass proved unreachable WAS reachable** — the data-flow agent and a same-session Claude-written invariant test both summed only `CooldownReduction` mutations; Codex read the full mutation vocabulary and found olog_hai's `Duration +4/+2` push the active window to 16s against a 15s cooldown floor — a 1s recast window that double-stacks buff contributions. The "rule scoped one category narrower than the bug" motif, this time inside a freshly-written REGRESSION TEST. Fixed with a structural runtime gate (activation blocked while the window is live) and the data-invariant test deleted — a gate that makes the state unrepresentable beats a test that constrains valid data. **C1 split verdict is the calibration story:** the `isSiegeEngineHit` half was confirmed by decompiling the dispatch site (siege missiles arrive with the operating player as affector — un-filtered, the feature would print "+N from ability" on damage the agent-stat buff never touched); the mount→rider half was DISPUTED because Codex's quoted vanilla normalization does not exist in the installed `Mission.cs` — the second fabricated/misattributed-quote incident (after CoopInterop C3), and implementing it would have introduced the same false-attribution class the confirmed half fixes. **What Codex does well (this review): reading the ENTIRE mutation vocabulary where scoped passes sampled it, and disputing its own seeded suspects with evidence instead of confirming them to please the prompt.** Suite 5,509 green. 3 lessons across localization-ui / state-lifecycle-save / adapters-taleworlds-api. RCA: docs/reviews/rca-career-ux-arc-2026-08-05.md.)

Last updated: 2026-07-16 (BannerBearers review 74 -- new feature #351: TAOM formations raise their faction standard and a bearer keeps its race. Reviewed the third-party "Raise your Banner" mod (4,535 lines decompiled) as reference and did NOT port it -- the engine already ships `BannerBearerLogic` in every battle and only fails to switch on because `SetFormationBanner` needs a hero captain carrying a banner item, which TAOM's lords never do. Supplying that one call is the whole feature (~400 lines, no Harmony, no art). Ran AFTER a 5-agent deep-review that found 2 CRITICAL + 2 HIGH (4 of the 5 agents PASSED it -- only Data Flow found anything): six culture keys were dead because `spcultures.xslt` renames the vanilla cultures but never their `id` (Rohirrim is `vlandia`), and a master-toggle leak where the disabled model returned 0 bearers instead of deferring to vanilla's 1, SUPPRESSING banners vanilla's own hero-captain path had assigned. Codex gpt-5.5 xhigh: **0 CRITICAL / 0 HIGH / 2 MED / 1 LOW -- VERDICT SHIP WITH FIXES, zero disagreements.** All 6 seeded Known Suspects came back favourable, independently confirming the riskiest bet (S1: the deployment-window freeze guard is sufficient -- hideout/arena/tournament builders don't even add `BannerBearerLogic`) and every fix. **Codex found ZERO of the bugs the internal review found and TWO it structurally could not have -- both about whether a CHOICE is semantically right, not whether code is correct.** (C1 MED) the formation's culture came from `formation.GetFirstUnit()`, which is literally `Arrangement.GetAllUnits()[0]` -- an arrangement slot, not a culture owner; a mixed-culture formation (allied army, mercenary-heavy party) flies whichever standard landed in slot 0. The deep-review's API agent had verified `GetFirstUnit()`'s null-return behaviour without ever questioning its SEMANTICS. (C2 MED) **MixedFormations' `Patch30` blanket-suppresses vanilla `GetOrderPositionOfUnit` for every unit in a field battle, silently overriding the engine's banner-bearer slotting** so standards scatter through the ranks -- a KNOWN UNKNOWN named in the plan, the feature doc AND the Data Flow agent's brief, still unresolved because tracing it meant reading a DIFFERENT feature's patch; Codex, given the whole repo and no scope boundary, found it immediately. (C3 LOW) `ExcludedRaces` typos fail open -- documented in the provider's own header comment at authoring time and consciously deferred; documenting a gap is not closing it. **What Codex does well (this review): semantic-choice auditing and cross-feature interaction tracing -- precisely the two things a scoped per-feature agent panel cannot do.** Zero false positives, likely because the prompt named six real uncertainties instead of asking for a generic sweep. **Meta-lesson: the answer to the CRITICAL was already in the repo -- this very skill's ID cheatsheet says verbatim "'rohan' is NOT a valid ID. Rohan uses 'vlandia'", phrased as a correction of a prior error. It needed no research, just consulting it. It didn't fire because the knowledge lives in a REVIEW skill and the task was AUTHORING a config. The failure is not missing knowledge, it is knowledge with no trigger -- so the fix is a regression test that fires on every build regardless of what the author knew.** 61 BannerBearers tests + full suite green; 7 lessons codified across 5 category files. RCA: docs/reviews/rca-banner-bearers-2026-07-16.md.)

Last updated: 2026-07-10 (TournamentExitHang ROUND-2 review 73 — the changeset that actually fixed #331 (measured 105-109s → 9.5s): new `ExitStallSampler` (background thread photographs the frozen MAIN thread's stack mid-stall — named the real sink in one repro after 22+ static-analysis agents bounded it wrong with assumed counts) + `PatchShield.ExcludedTargetNamespacePrefixes` (never shield the Gauntlet/2D UI layer — the shield's `__originalMethod` finalizer costs ~50µs/call and turned the tournament UI's ~10^6-call template release into the 107s freeze). Codex gpt-5.5 xhigh: **0 P1 / 2 P2 / 4 P3, all confirmed + addressed.** P2-1: `Poll` had no Timer-reentrancy guard — overlapping ticks would race `_samplesTaken` and interleave Suspend/Resume pairs (fixed: `Interlocked` guard; why missed: the pattern was copied from the watchdog sibling whose callback never blocks — the sibling convention was safe for a reason that didn't transfer). P2-2: the only main-thread-suspending diagnostics component rode the master toggle (fixed: independent MCM gate). P3s: suspended-window logging (fixed — nothing allocates between Suspend/Resume now), main-thread invariant across engine bumps (deferred + documented), two drift items (fixed). The parallel deep-review's COMPAT agent separately delivered two catches Codex was told not to re-report: the reflection-invoked `StackTrace` ctor was justified by a FALSE comment (real cause: a named-argument typo `fNeedFileInfo:` vs `needFileInfo` misread as a missing ctor — it verified the ctor empirically on the live CLR), and the exclusion list had stopped at the measured incident while TAOM's own Patch38 nameplate target (~3000 calls/sec) was still paying the shield tax (fixed: third namespace prefix). **What Codex does well (this review):** threading-interleave analysis on NEW infrastructure (Timer overlap semantics, suspend-count behavior) rather than only decompile checks; correctly separating accepted-documented risks from new defects. **Meta-lesson: a fix derived from one measured incident defaults to incident-scope — sweep the CLASS (all shielded targets by call frequency) before closing.** Suite 4,177 green. RCA: docs/reviews/rca-tournament-exit-hang-2026-07-06.md (rounds 1+2, findings 1-14).)

