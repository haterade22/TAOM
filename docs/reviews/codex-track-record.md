# Codex Reviewer — Track Record & Worked-Example Catalog

> Moved out of `AGENTS.md` 2026-07-18 so the actual TAOM review *rules* fit under Codex's
> `project_doc_max_bytes` cap. This is the full historical catch-log: every rolling essay plus
> the complete "bugs Codex misses / false positives Codex produces / what Codex does well /
> run-mode caveats" catalog across 81 reviews / 165+ bugs. `AGENTS.md` keeps a distilled
> operative summary and points here. Convention (unchanged): after each review, add the new
> essay to the top here, archive the 6th-oldest to `agents-md-review-lessons-archive.md`, and
> harvest durable patterns into `docs/reviews/lessons/<category>.md`.

### Lessons From Prior Reviews (81 reviews, 165+ bugs found)

**Rolling essay log (newest 5; full history: `docs/reviews/agents-md-review-lessons-archive.md`).** Convention (2026-07-12): after each review cycle, add the new `Last updated:` essay at the top of this list, move the 6th-oldest to the archive file, and harvest any durable pattern into the catalog below (+ `docs/reviews/lessons/<category>.md`). The catalog is the operative reviewer guidance; essays are worked examples.

Last updated: 2026-08-05 (Career UX arc review 81 — 8-issue arc (#377–#384) adopted from a security-vetted external submission, including TAOM's first mission-screen PrefabExtension (energy bar into Native's AgentStatus). Codex gpt-5.5 xhigh, prompt seeded with 8 Known Suspects: **0 P1 / 2 P2 — and all SIX architecture suspects DISPUTED with decompiled evidence** (datasource chain, `[ViewModelMixin("Tick")]` transpiler, filename-keyed `WidgetPrefab.LoadFrom` patching, insert-index semantics, clock unity, careers-XML integrity), independently agreeing with the Claude compatibility agent's own decompile trace — two independent verifications of the render-or-not architecture bet, both favourable. **C2 (confirmed): the overlap the Claude pass proved unreachable WAS reachable** — the data-flow agent and a same-session Claude-written invariant test both summed only `CooldownReduction` mutations; Codex read the full mutation vocabulary and found olog_hai's `Duration +4/+2` push the active window to 16s against a 15s cooldown floor — a 1s recast window that double-stacks buff contributions. The "rule scoped one category narrower than the bug" motif, this time inside a freshly-written REGRESSION TEST. Fixed with a structural runtime gate (activation blocked while the window is live) and the data-invariant test deleted — a gate that makes the state unrepresentable beats a test that constrains valid data. **C1 split verdict is the calibration story:** the `isSiegeEngineHit` half was confirmed by decompiling the dispatch site (siege missiles arrive with the operating player as affector — un-filtered, the feature would print "+N from ability" on damage the agent-stat buff never touched); the mount→rider half was DISPUTED because Codex's quoted vanilla normalization does not exist in the installed `Mission.cs` — the second fabricated/misattributed-quote incident (after CoopInterop C3), and implementing it would have introduced the same false-attribution class the confirmed half fixes. **What Codex does well (this review): reading the ENTIRE mutation vocabulary where scoped passes sampled it, and disputing its own seeded suspects with evidence instead of confirming them to please the prompt.** Suite 5,509 green. 3 lessons across localization-ui / state-lifecycle-save / adapters-taleworlds-api. RCA: docs/reviews/rca-career-ux-arc-2026-08-05.md.)

Last updated: 2026-08-01 (CoopInterop review 79 — TAOM's interop layer for BannerlordCoop, a DIFFERENT project from the BannerlordTogether the existing layer was built for; its launcher id is the bare string `Coop`, and `CoopPresence` matches ids by EXACT equality, so every shield TAOM shipped was inert against the co-op mod players actually install. Ran AFTER a 5-agent deep-review whose Data Flow agent found 2 HIGH nobody else approached (sibling handlers reaching the SAME mutating service method the tick gate protected). Codex gpt-5.5 xhigh: **0 CRITICAL / 4 HIGH / 3 MED / 1 LOW — VERDICT NOT SAFE TO SHIP.** It DISPUTED all four Known Suspects aimed at the new layer — fail-open direction, reflection-binder double-checked locking, the five-name assembly-redirect deletion, and the detection-coupling tests — and confirmed the two aimed at its edges. **What Codex does well (this review): distinguishing WHICH SIGNAL a gate consults, across an entire feature area, when every individual call site reads correctly in isolation.** Its widest catch (C1 HIGH): eight diplomacy + time-acceleration sites gated on module PRESENCE (`ICoopPresenceProvider.IsCoopActive`), which is process-constant — true whenever the co-op module is merely ENABLED — so TAOM's War of the Ring rules silently switched off for a SOLO player who had it installed and for the co-op HOST, the one peer that must enforce them. Every one of those eight lines is locally sensible; only the aggregate is wrong. C4 HIGH audited a fix applied hours earlier in the same session: the siege shared/local split let a client keep its reward, but the local half set `RewardClaimed`, which `SnapshotForSave` serializes — so a client claiming its own reward wrote per-peer state into the HOST's save record. **Codex's own C1 fix was WRONG, though, and this is the useful failure:** it proposed gating on session/role, which breaks BannerlordTogether — `IsAuthority` fails open, so both BT peers report authoritative and nothing gates at all. The file carried a comment stating exactly that, and Codex did not engage with it. The correct predicate keys on whether the host/client probe RESOLVED, satisfying all five rows including the BT one Codex missed and the solo/host rows the presence gate missed. **False positive (C3 HIGH): it quoted `_ => _service.EnforcePermanentAlliances()` as an ungated registration; the real line is `_ => OnSessionLaunched()`, and that handler gates** — the first fabricated-quote finding on record, notable because every prior review's quotes were verbatim. One further finding (C2) was already fixed concurrently. **Accuracy: 5 of 8 usable as written — 1 fabricated quote, 1 already-fixed, 1 whose proposed fix causes a different regression.** All three caught by re-reading source before implementing, per `evidence-over-claims.md`. C6 MED corrected the reviewer's own prior reasoning: `CultureConversionBehavior.OnSettlementOwnerChanged` had been left ungated and DOCUMENTED as safe because "it only queues a pending timer" — but the store is SyncData-backed and the daily processor that matures those records IS gated, so a client accumulates conversions nothing services; writing the justification into the doc had made it look considered. **Meta-lesson: when a reviewer disagrees with an in-code comment that explains a deliberate choice, the comment is evidence the reviewer has not weighed — check whether the finding's fix breaks the case the comment names before implementing it.** Suite 4,759 green. 2 lessons in `lessons/campaign-mechanics.md`. RCA: docs/reviews/rca-coop-authority-gating-2026-08-01.md. **End-to-end co-op UNVERIFIED — nothing has run in a live two-peer session.**)

Last updated: 2026-07-16 (BannerBearers review 74 -- new feature #351: TAOM formations raise their faction standard and a bearer keeps its race. Reviewed the third-party "Raise your Banner" mod (4,535 lines decompiled) as reference and did NOT port it -- the engine already ships `BannerBearerLogic` in every battle and only fails to switch on because `SetFormationBanner` needs a hero captain carrying a banner item, which TAOM's lords never do. Supplying that one call is the whole feature (~400 lines, no Harmony, no art). Ran AFTER a 5-agent deep-review that found 2 CRITICAL + 2 HIGH (4 of the 5 agents PASSED it -- only Data Flow found anything): six culture keys were dead because `spcultures.xslt` renames the vanilla cultures but never their `id` (Rohirrim is `vlandia`), and a master-toggle leak where the disabled model returned 0 bearers instead of deferring to vanilla's 1, SUPPRESSING banners vanilla's own hero-captain path had assigned. Codex gpt-5.5 xhigh: **0 CRITICAL / 0 HIGH / 2 MED / 1 LOW -- VERDICT SHIP WITH FIXES, zero disagreements.** All 6 seeded Known Suspects came back favourable, independently confirming the riskiest bet (S1: the deployment-window freeze guard is sufficient -- hideout/arena/tournament builders don't even add `BannerBearerLogic`) and every fix. **Codex found ZERO of the bugs the internal review found and TWO it structurally could not have -- both about whether a CHOICE is semantically right, not whether code is correct.** (C1 MED) the formation's culture came from `formation.GetFirstUnit()`, which is literally `Arrangement.GetAllUnits()[0]` -- an arrangement slot, not a culture owner; a mixed-culture formation (allied army, mercenary-heavy party) flies whichever standard landed in slot 0. The deep-review's API agent had verified `GetFirstUnit()`'s null-return behaviour without ever questioning its SEMANTICS. (C2 MED) **MixedFormations' `Patch30` blanket-suppresses vanilla `GetOrderPositionOfUnit` for every unit in a field battle, silently overriding the engine's banner-bearer slotting** so standards scatter through the ranks -- a KNOWN UNKNOWN named in the plan, the feature doc AND the Data Flow agent's brief, still unresolved because tracing it meant reading a DIFFERENT feature's patch; Codex, given the whole repo and no scope boundary, found it immediately. (C3 LOW) `ExcludedRaces` typos fail open -- documented in the provider's own header comment at authoring time and consciously deferred; documenting a gap is not closing it. **What Codex does well (this review): semantic-choice auditing and cross-feature interaction tracing -- precisely the two things a scoped per-feature agent panel cannot do.** Zero false positives, likely because the prompt named six real uncertainties instead of asking for a generic sweep. **Meta-lesson: the answer to the CRITICAL was already in the repo -- this very skill's ID cheatsheet says verbatim "'rohan' is NOT a valid ID. Rohan uses 'vlandia'", phrased as a correction of a prior error. It needed no research, just consulting it. It didn't fire because the knowledge lives in a REVIEW skill and the task was AUTHORING a config. The failure is not missing knowledge, it is knowledge with no trigger -- so the fix is a regression test that fires on every build regardless of what the author knew.** 61 BannerBearers tests + full suite green; 7 lessons codified across 5 category files. RCA: docs/reviews/rca-banner-bearers-2026-07-16.md.)

Last updated: 2026-07-10 (TournamentExitHang ROUND-2 review 73 — the changeset that actually fixed #331 (measured 105-109s → 9.5s): new `ExitStallSampler` (background thread photographs the frozen MAIN thread's stack mid-stall — named the real sink in one repro after 22+ static-analysis agents bounded it wrong with assumed counts) + `PatchShield.ExcludedTargetNamespacePrefixes` (never shield the Gauntlet/2D UI layer — the shield's `__originalMethod` finalizer costs ~50µs/call and turned the tournament UI's ~10^6-call template release into the 107s freeze). Codex gpt-5.5 xhigh: **0 P1 / 2 P2 / 4 P3, all confirmed + addressed.** P2-1: `Poll` had no Timer-reentrancy guard — overlapping ticks would race `_samplesTaken` and interleave Suspend/Resume pairs (fixed: `Interlocked` guard; why missed: the pattern was copied from the watchdog sibling whose callback never blocks — the sibling convention was safe for a reason that didn't transfer). P2-2: the only main-thread-suspending diagnostics component rode the master toggle (fixed: independent MCM gate). P3s: suspended-window logging (fixed — nothing allocates between Suspend/Resume now), main-thread invariant across engine bumps (deferred + documented), two drift items (fixed). The parallel deep-review's COMPAT agent separately delivered two catches Codex was told not to re-report: the reflection-invoked `StackTrace` ctor was justified by a FALSE comment (real cause: a named-argument typo `fNeedFileInfo:` vs `needFileInfo` misread as a missing ctor — it verified the ctor empirically on the live CLR), and the exclusion list had stopped at the measured incident while TAOM's own Patch38 nameplate target (~3000 calls/sec) was still paying the shield tax (fixed: third namespace prefix). **What Codex does well (this review):** threading-interleave analysis on NEW infrastructure (Timer overlap semantics, suspend-count behavior) rather than only decompile checks; correctly separating accepted-documented risks from new defects. **Meta-lesson: a fix derived from one measured incident defaults to incident-scope — sweep the CLASS (all shielded targets by call frequency) before closing.** Suite 4,177 green. RCA: docs/reviews/rca-tournament-exit-hang-2026-07-06.md (rounds 1+2, findings 1-14).)

Last updated: 2026-07-06 (TournamentExitHang review 72 — fix for issue #331: exit-phase diagnostics added to BattleLoadDiagnostics (9 phases bracketing Mission.EndMission → MapState first tick) localized a measured 108s tournament-exit hang to the engine's leaked tournament UI layer (`MissionGauntletTournamentView.OnMissionScreenFinalize` nulls `_gauntletMovie`/`_gauntletLayer` without release — the practice view releases correctly); `Patch60_TournamentExitMovieRelease` (capture-Prefix + release-Postfix) replicates the practice view's `ReleaseMovie`→`RemoveLayer` at OnEndMission time. Ran AFTER a 5-agent deep-review that confirmed + fixed 2 MED exit-window state-machine defects (toggle-gated state transitions; campaign-only closers for an any-mission opener). Codex gpt-5.5 xhigh: **0 P1 / 1 P2 / 0 P3.** The P2 was the highest-value catch and audited the DEEP-REVIEW FIX ITSELF: the service-level window-closes were made unconditional, but the two closer CALL SITES (`PlayerEncounter_Start_Patch`, `Mission_Initialize_BattleLoad_Patch`) still early-out on `!svc.IsEnabled` before calling them — so the toggle-off-mid-window latch the fix targeted remained reachable. The 3 fresh regression tests exercised the service directly and were structurally blind to hook-level early-outs. Codex explicitly distinguished this from re-reporting the fixed bug ("the service methods were fixed correctly, but the hooks still skip those methods"). Also cleanly verified the leak root-cause chain, the double-`TryLoseFocus` idempotence, `RemoveLayer`-during-view-iteration safety (S1-S6 all resolved with decompiles), and the log-name contract. **What Codex does well (this review): verifying a just-applied fix at the OUTERMOST gate — it asked "who calls the fixed method and under what guard?" where both the fixer and the regression tests stopped at the fixed layer.** Fixed in-session (closers now called before the hook toggle gates); suite 4,136 green. Codex sandbox again couldn't run dotnet tests (MSBuild probe blocked). RCA: docs/reviews/rca-tournament-exit-hang-2026-07-06.md (finding #4); LESSONS-LEARNED "State, Lifecycle & Save" (outermost-gate clause added).)

Last updated: 2026-07-04 (CaravanTrade review 71 — new feature: four Harmony postfixes on vanilla `CaravansCampaignBehavior` private methods + two `TaomCaravanModel` overrides → pure `ICaravanTradeService`, making AI/player caravans range past the local town cluster, trade across the endless war (`WarTradePolicy`), and carry fuller baskets; #329. Ran AFTER a 5-agent deep-review that itself caught + fixed 1 HIGH (the war-gate delegated to `IAlignmentService.AreEnemyAlignments`, whose Neutral-as-enemy-of-everyone semantics silently blocked ALL neutral-faction trade under the shipped default — the sibling AlignmentRecruitment feature had already documented that exact trap; the unit test masked it by mocking the buggy predicate). Codex gpt-5.5 xhigh: **0 CRITICAL / 0 HIGH / 4 MEDIUM / 1 LOW.** DISPUTED 5 of 7 Known Suspects with decompiled evidence (the reweight math compresses vanilla's 5× near-bias to ~1.53×; the CanTradeWith postfix only ever flips war-caused false→true and honors the prohibited-kingdom list during war; dropdown-index→enum mapping exact; the double-distance recompute is cache-backed via `AiHelper→DistanceHelper→DefaultMapDistanceModel._navigationCache`, not a live pathfind). **Notably Codex DISPUTED my own seeded Suspect 2 (player-detection inconsistency) correctly by decompiling the caravan-creation path — vanilla passes `Hero.MainHero` as the OWNER even for companion-LED caravans, so `GetInitialTradeGold(owner==Hero.MainHero)` is not too narrow.** The 4 MEDs, all fixed/documented: (C1) Lever 3 MUTATED the `_*VeryFarCache` fields once at session start, so an MCM master-off mid-session didn't revert the range ceiling — moved the lever to a `ref float` postfix on the read-each-time getter `GetDistanceLimitVeryFarAsDaysForNavigationType` (scale-on-read, no persistent mutation); (C2) the range lever is engine-global (getter has no per-caravan context) so `ApplyToPlayerCaravans` can't scope it — documented honestly (the MCM hint over-promised); (C3) the war-gate's `isPlayer` is faction-level while the other levers are clan-level — documented as a known limitation; (C4) a player-founded kingdom (`new_kingdom`, absent from `alignment.json`) resolves Neutral and trades across the Free/Evil line — added `GetKingdomSide→GetCultureSide` culture-fallback. **What Codex does well (this review):** (a) it DISPROVED a seeded hypothesis by decompiling the creation path rather than accepting the framing; (b) it modeled the master-off promise at the STATE level (cache-mutation vs per-call gate) that the deep-review's per-service-method toggle-coverage check structurally missed. **Meta-lesson: C4 is the SAME player-founded-kingdom-resolves-Neutral class Codex caught in review 70 (WotR momentum) — written the SAME session — and CaravanTrade didn't apply it. Reinforces: read LESSONS-LEARNED "Adapters & TaleWorlds API" (consult the sibling feature) BEFORE reusing `IAlignmentService`.** Codex sandbox again couldn't run dotnet tests; local suite 4,086 green (+ regression tests for Neutral, player-founded kingdom, and the live master-off). Build's game-deploy step was blocked by a running-game file lock (environment, not code) — compile verified clean via `-p:ModuleId=`. RCA: docs/reviews/rca-caravan-trade-2026-07-04.md (Codex-pass section); LESSONS-LEARNED "GameModels & Services" + "Testing & QA".)

