ADVERSARIAL CODE REVIEW: CultureDoctrine (#608), TAOM Bannerlord v1.5.3 mod, uncommitted working tree on branch bannerlord-1.5.x.

You are the reviewer. Assume the code has bugs and prove it. Do NOT edit files, do NOT run the build, do NOT write your report to any path: return the full report as your FINAL MESSAGE (the dispatcher redirects stdout into docs/reviews/raw/codex-adversarial-culture-doctrine-2026-09-16.md). Read AGENTS.md and .ai/roles/reviewer.md first. The maintainer asked for a broad, large review at the highest effort: take the time, decompile everything you cite, and try to refute the seven-agent deep review that already ran (docs/reviews/rca-culture-doctrine-2026-09-16.md) as well as extend it.

FEATURE. Each culture's AI fights field battles with its own doctrine. At EarlyStart (after MissionCombatantsLogic.EarlyStart has created the TeamAIGeneral per team and registered vanilla's tactic set), a MissionLogic replaces every AI team's tactic list with the list culture_doctrines.json authors for the side's majority culture: nine vanilla TacticComponent subclasses that scale GetTacticWeight by a per-culture multiplier (no Harmony; GetTacticWeight is protected internal virtual and every field tactic overrides it), plus four TAOM tactics on a shared TaomTacticBase (TaomTacticShieldWall, TaomTacticInfantryMass, TaomTacticCavalryDominance, TaomTacticArcherRing) that reproduce the vanilla tactic lifecycle (TacticDefensiveEngagement was the template) and drive vanilla BehaviorComponents through pure plan tables and pure weight functions over a TeamQuerySnapshot. The engine still picks the max-weight tactic every 5 s (TeamAIComponent.MakeDecision: MaxBy(weight * 1.5 if current)) and each formation its behaviour every 0.5 s. Sieges untouched (Mission.IsFieldBattle live read). MCM toggle OFF until the in-game A/B passes. A separate [MissionPerf] frame-time heartbeat (Main/Features/MissionPerf) is the A/B's perf instrument. Design doc: docs/features/culture-doctrine.md. Deep-review RCA already applied: docs/reviews/rca-culture-doctrine-2026-09-16.md (twelve findings, all fixed; read it so you do not re-report them unless a fix is wrong).

TAOM ID CHEATSHEET.
Kingdom IDs: empire_w=Gondor, empire_s=Mordor, empire=Dunland, vlandia=Rohan, battania=Khand, aserai=Harad, khuzait=Easterlings, sturgia=Dale/North, erebor, rivendell, lothlorien, mirkwood, isengard, gundabad, dolguldur, umbar, shaghana, abanissa.
Culture IDs (custom): gondor, mordor, erebor, rivendell, lothlorien, mirkwood, isengard, gundabad, dolguldur, umbar, lindon, goblin, mistymountainorcs, plus bandit/raider cultures erebor_warriors, gundabad_raiders, dunland_raiders, gondor_soldiers, mirkwood_stalkers.
Culture IDs (XSLT/vanilla): vlandia=Rohan, empire=Dunland, aserai=Harad, khuzait=Easterlings, sturgia=Dale, battania=Khand.
NOTE: "rohan" is NOT a valid ID (Rohan uses "vlandia"). "dunland" is NOT valid (use "empire"). The full registry is what tools/validate_moduledata.py and the taom-moduledata MCP report; culture_doctrines.json keys were checked against it.

READ FIRST.
docs/features/culture-doctrine.md
docs/reviews/rca-culture-doctrine-2026-09-16.md
Main/_Module/ModuleData/culture_doctrine/culture_doctrines.json
docs/reference/engine/formations-and-team-ai.md (the v1.5.3 tactic-layer table)
.claude/rules/harmony-patches.md section "Which thread runs your target"
.claude/rules/csharp-architecture.md sections "Mission-scope agent handles and the engine's threads" and "Engine-Float Decision Gates"

FILES UNDER REVIEW.
New feature: Main/Features/CultureDoctrine/ (Domain/DoctrineTactic.cs, Domain/Doctrine.cs, Domain/DoctrineCatalog.cs, Domain/SideProfile.cs, Domain/TacticRoster.cs, Domain/CultureDoctrineConfig.cs, Doctrines/DoctrinePlan.cs, Doctrines/DoctrinePlans.cs, Doctrines/DoctrineWeights.cs, Doctrines/TacticPhaseMachine.cs, Hooks/CultureDoctrineMissionLogic.cs, Hooks/TeamCombatantSelector.cs, Hooks/TeamTacticProbe.cs, Hooks/CaravanTacticsRule.cs, Hooks/Tactics/VanillaTacticWrappers.cs, Hooks/Tactics/TaomTacticBase.cs, Hooks/Tactics/TaomTactics.cs, Hooks/Tactics/BehaviorWeightApplier.cs, Hooks/Tactics/TeamQuerySnapshotFactory.cs, Hooks/Tactics/TacticFactory.cs, Cheats/CultureDoctrineCheats.cs, CultureDoctrineConfigProvider.cs, CultureDoctrineSettingsProvider.cs, ICultureDoctrineConfigProvider.cs, ICultureDoctrineSettingsProvider.cs, CultureDoctrineIoC.cs).
New feature: Main/Features/MissionPerf/ (FrameStats.cs, MissionPerfLine.cs, Hooks/MissionPerfHeartbeatBehavior.cs).
Modified: Main/Features/TaomSettings.cs (Battle Tactics/Culture Doctrine group), Main/Features/BattleLoadDiagnostics/BattleLoadDiagnosticsSettings.cs (EnableMissionPerfHeartbeat), Main/Features/CoopInterop/CoopSettingsRelevance.cs, Main/IoC.cs, Main/SubModule.cs (two AddTaomBehavior lines), Main/_Module/ModuleData/taom_module_strings.xml (four str_team_ai_tactic_text.TaomTactic* rows) plus the 12 seeded Languages files.
Tests: TAOM.Tests/Features/CultureDoctrine/*.cs (including VanillaTacticWeightReference.cs, ShippedDoctrineOrderingTests.cs, DoctrineSwitchInvariantTests.cs, TeamCombatantSelectorTests.cs, CultureDoctrineBindingTests.cs), TAOM.Tests/Features/MissionPerf/FrameStatsTests.cs, the census update in TAOM.Tests/Features/CoopInterop/SettingsFingerprintTests.cs, the ClassInitialize added to TAOM.Tests/Features/DevConsole/ConsoleCommandBindingTests.cs.
Use `git status` and `git diff HEAD` for the exact changeset.

KNOWN SUSPECTS. For each one, CONFIRM or DISPUTE with decompiled evidence (installed v1.5.3 DLLs via ilspycmd or the ilspy MCP; E:\Decompiled_Bannerlord\_categories_v1.5.3\ is the same version and fine for reading logic).

S1. The first decision runs on the MAIN thread during deployment. DeploymentMissionController.SetupAIOfEnemyTeam sets AllowAiTicking and ForceTickOccasionally, calls team.ResetTactic() (MakeDecision over the TAOM list, then the chosen tactic's TickOccasionally, i.e. TaomTacticBase.Tick including ManageFormationCounts -> SplitFormationClassIntoGivenNumber -> TransferUnits) and team.Tick(0f), during the deployment UI, for the enemy team only. Trace what happens to the PLAYER team's tactic (when does its first MakeDecision run: FinishDeployment? first async tick?) and whether a TAOM tactic transferring units between formations during the deployment phase differs in any way from what vanilla's tactics do at that moment (vanilla tactics run the same ManageFormationCounts there). Confirm nothing in TaomTacticBase assumes the async thread (a main-thread throw during deployment is inside the same try/catch, but is base.TickOccasionally reachable with Mission.Current.IsMissionEnding at that point?).

S2. Weight competition. TAOM.Tests/Features/CultureDoctrine/VanillaTacticWeightReference.cs reproduces the nine vanilla GetTacticWeight formulas at nominal scene scores and ShippedDoctrineOrderingTests asserts every shipped TAOM row beats every vanilla row it shares a list with by 1.5x at a canonical army. Verify each reference formula line by line against the decompiled tactics (TacticCharge.cs:59-88 with no casualties: is the first term really 0 and is LinearExtrapolation unclamped; TacticFullScaleAttack; TacticFrontalCavalryCharge; TacticRangedHarrassmentOffensive including the enemy-ranged clamp; TacticDefensiveEngagement's distance factor; TacticDefensiveLine, TacticDefensiveRing, TacticHoldChokePoint at score 1; TacticCoordinatedRetreat at Total/Remaining = 1 and the cavalry parity factor). Then attack the criterion itself: is "TAOM weight >= 1.5 x every vanilla row" the right bar given MaxBy applies the 1.5 only to the CURRENT tactic and the hysteresis branch (ResetTacticalPositions false for every field tactic) switches unconditionally otherwise? Is there a real scene score above 1 (TacticDefensiveLine.GetTacticalPositionScore, TacticDefensiveRing's insurmountable 1.5 and slope factors, HoldChokePoint) that makes a shipped vanilla row beat its culture's TAOM tactic on a scene with entities? Quantify with the shipped multipliers.

S3. Late-battle handover. TacticCharge's weight grows with casualties (num7 = own power / (own power + power lost); LinearExtrapolation(0, 1.6 num2, (1 - num7)/(1 - num3))). At what casualty share does Charge x 0.3 (erebor) or Charge x 2.0 (mordor) overtake ShieldWall / InfantryMass at the canonical armies? Is that the intended "commit when the enemy breaks" or does Mordor's InfantryMass hand over to a mob Charge too early / Erebor's wall too late? Compute the crossover.

S4. TaomTacticShieldWall (defender) uses BehaviorDefend with DefensePosition = HighGroundOf(_mainInfantry), recomputed in BeforeApply at every phase apply (Defend, then Engage, then every formation-set change). FormationQuerySystem.HighGroundCloseToForeseenBattleGround searches a square of half the distance to the enemy's median around the formation (10 s cache). Once the enemy is close that square shrinks and the high ground may sit at or beyond the enemy line: does the Engage re-apply walk the wall INTO the enemy, or leave it in place? Compare with BehaviorHoldHighGround, which locks its position once the enemy is within max(RangedAlly.MissileRangeAdjusted * 0.8, 30). Should BeforeApply freeze the position after the first Defend apply (a one-line fix) and what does vanilla TacticDefensiveEngagement do (it uses HoldHighGround, which locks)?

S5. ArcherRing geometry. BehaviorDefensiveRing moves the infantry to TacticalDefendPosition.Position and, within 10 m, forms a Circle whose FormOrderCustom width is computed from the largest ranged formation's square (BehaviorDefensiveRing.cs:40-58). BehaviorFireFromInfantryCover places the archers 0.33 * Depth BEHIND the main formation's ORDER position in a Square (BehaviorFireFromInfantryCover.CalculateCurrentOrder). With the ring centred on the position and facing the enemy, is "behind the order position" inside the circle or at its rear edge/outside? Vanilla TacticDefensiveRing pairs the same two behaviours, so state parity or the difference. Also: the runtime TacticalPosition's Width is max(10, archers.Arrangement.Width) and IsInsurmountable = true; BehaviorDefensiveRing reads only Position and Direction, but does anything else read the TacticalPosition (TeamAIComponent.TacticalPositions is NOT given this object; confirm nothing enumerates it).

S6. TeamCombatantSelector mirrors Mission.GetAgentTeam (Mission.cs:5233-5240): ally team iff !IsUnderPlayersCommand && !IsInSameArmyAsPlayer. The TAOM army test compares PartyBase.MobileParty.Army against MobileParty.MainParty.Army. Decompile the troop origin's IsInSameArmyAsPlayer (PartyGroupTroopSupplier / PartyAgentOrigin) and IsUnderPlayersCommand, and confirm the per-combatant approximation matches per-troop reality for: the player's own party; a party in the player-led army (IsUnderPlayersCommand true via DefaultEncounterModel.IsPartyUnderPlayerCommand?); a party in an AI-led army the player joined; an independent allied lord; a garrison or militia party in a village raid defence; caravans and villagers. Any case where the combatant-level answer differs from the troop-level one changes which team's doctrine a culture counts toward.

S7. CaravanTacticsRule mirrors MissionCaravanOrVillagerTacticsHandler.EarlyStart (SandBox). Confirm the mirrored condition is exact (caravan on the side; villager on the side with MapEvent.MapEventSettlement == null), that MapEvent.PlayerMapEvent is non-null at EarlyStart of a campaign field battle, and that the by-name presence test (behaviors[i].GetType().Name == "MissionCaravanOrVillagerTacticsHandler") finds the handler in the missions where it is registered (grep SandBoxMissions.cs for it). Does the ensured DefensiveLine at multiplier 1 interact badly with a doctrine that also carries DefensiveLine at another multiplier (first row wins in TacticRoster; the ensured row is appended only if absent)?

S8. Custom Battle. CustomBattleCombatant.GetTacticsSkillAmount is the roster max Tactics skill (0 for TAOM troops), BasicCulture is the UI-picked culture, there is no PlayerAllyTeam, Campaign.Current is null. Walk CultureDoctrineMissionLogic.EarlyStart -> TeamCombatantSelector -> CaravanTacticsRule -> TacticFactory for a Custom Battle and confirm no campaign dereference is reachable (MobileParty.MainParty, MapEvent.PlayerMapEvent, Campaign.Current.Models). Then: does Custom Battle register a MissionCombatantsLogic with TeamAIType FieldBattle (BannerlordMissions.cs / CustomBattleMissions), so Mission.IsFieldBattle is true at TAOM's EarlyStart?

S9. The debug status line reads TeamAIComponent._currentTactic by FieldRefAccess and formation.AI.ActiveBehavior on the main thread from OnMissionTick, and from the console command taom.tactic_status at any time. The RCA says OnMissionTick never overlaps the async tick because Mission.OnPreTick waits (WaitTickCompletion). Confirm from Mission.OnPreTick / OnTick / TickAgentsAndTeamsImp / tickCompleted, and state what CAN run concurrently with the async tick (native combat callbacks, AfterAsyncTickTick). Then: is there any path where the console command's reads could throw rather than tear (ArrangementOrder is a struct copy; FormationsIncludingEmpty is fixed at 8; AI can be null?).

S10. Failure latch. After a TAOM tactic throws, _failed makes GetTacticWeight return 0 and Tick a no-op, but the engine still holds it as CurrentTactic until the next MakeDecision (up to 5 s) and still calls its TickOccasionally (base only). During that window the formations keep the last-applied weights. Is there any formation state the plan applied that becomes harmful when frozen (a DefensePosition on the far side of the enemy, a TacticalDefendPosition, a Flank)? And when MakeDecision switches away, the next tactic's OnApply sets IsTacticReapplyNeeded and its Tick re-applies weights only for the formations ITS plan names: a formation the failed plan set (e.g. RangedCavalry with MountedSkirmish) that the new tactic does not name keeps the old weights. Is that vanilla parity (vanilla tactics also only touch their own slots) or a TAOM-specific gap because TAOM plans may name fewer roles?

S11. Multiplier zero and MaxBy ties. A row at multiplier 0 keeps the tactic registered at weight 0. TaleWorlds.Core.Extensions.MaxBy keeps the FIRST element on ties. If a doctrine sets Charge 0 and every other row is 0 at some moment (all vanilla defensives return 0 for an attacker with no scene entities; the TAOM tactic returns 0 for its no-go case), the first list entry wins: TacticRoster guarantees Charge first only when the doctrine lacks a Charge row; a doctrine that lists Charge later in its own order puts another tactic first. Is there a shipped doctrine where an attacker with no infantry (all-cavalry raid) ends on a tactic whose plan does nothing for cavalry? Walk vlandia, mordor and empire as attackers with the shipped rows.

S12. CoopSettingsRelevance: EnableCultureDoctrine is simulation-relevant and included in the fingerprint; CultureDoctrineDebug and EnableMissionPerfHeartbeat are excluded as instrumentation. Confirm no doctrine read site is missing from that classification and that a mismatch between two peers is at least reported (SettingsFingerprintLog).

S13. Lifecycle. TaomTacticBase objects hold Team and Formation references and a TacticalPosition; they live in TeamAIComponent._availableTactics for the mission. CultureDoctrineMissionLogic._taomTactics is cleared in OnCreated and OnEndMission. Is there any path (mission retreat, mission end without OnEndMission, a second mission in the same process) where a TaomTacticBase outlives its mission through a process singleton? The IoC singletons are the config provider (immutable catalog) and the settings provider (stateless). Confirm.

S14. Localisation. str_team_ai_tactic_text.TaomTacticShieldWall etc. exist in taom_module_strings.xml and the 12 language files carry the English text. Confirm GameTextManager.LoadFromXML splits the id on the first '.' and that a TAOM GameText file registered in SubModule.xml merges variations into the same "str_team_ai_tactic_text" GameText that Native's module_strings.xml populated (not a replacement that drops vanilla's nine).

REQUIRED SECTIONS.
1. VANILLA CODE: paste the decompiled bodies (v1.5.3) of TeamAIComponent.MakeDecision and Tick, TacticComponent.AreFormationsCreated and TickOccasionally, DeploymentMissionController.SetupAIOfEnemyTeam and FinishDeployment, Mission.GetAgentTeam, MissionCombatantsLogic.SupportsAllyTeamOnPlayerSide (static), FormationQuerySystem's HighGroundCloseToForeseenBattleGround query, BehaviorHoldHighGround.CalculateCurrentOrder (the lock), BehaviorFireFromInfantryCover.CalculateCurrentOrder, TacticCharge.GetTacticWeight, as code blocks with line numbers.
2. KNOWN SUSPECTS: S1..S14 each CONFIRMED / DISPUTED / UNVERIFIED with the evidence.
3. WEIGHT TABLE: for each of erebor, lindon, vlandia, mordor (and empire, gondor which have no TAOM tactic), at the canonical army the test uses AND at one realistic army you choose, the weight of every registered row at full strength with scene scores 0 and 1, and which row wins at the first decision.
4. NAN AND CAST AUDIT: every comparison on an engine float and every float-to-int cast in the changeset, with its polarity (csharp-architecture.md "Engine-Float Decision Gates"), including HasBattleBeenJoined's division by MovementSpeedMaximum, RingGeometry, FrameStats and the config provider.
5. CONFIG CROSS-REFERENCE: every culture key in culture_doctrines.json against the culture registry (Main/_Module/ModuleData/taom_spcultures.xml, spcultures.xslt, characters/clans.xml for the bandit cultures) and against what PartyBase.BasicCulture (MapFaction.Culture) actually reports for each kind of party (kingdom lord, clan-only lord, mercenary, bandit, caravan, villager, garrison); every tactic id against DoctrineTactic; every multiplier against the provider's [0, 5] gate.
6. EXTENSIBILITY: the doc promises Phase C TAOM BehaviorComponents ensured lazily by the tactic (formation.AI.GetBehavior<T>() ?? AddAiBehavior on the team-AI tick), Phase D Mission.GetAgentTroopClass_Override routing plus ManageFormationCounts overrides plus an AgentDrivenProperties aggression post-pass in TaomAgentStatCalculateModel.UpdateAgentStats, Phase E per-formation culture weights. For each, name the concrete change to the current classes and whether anything in the current shape forces a rewrite; note that TaomAgentStatCalculateModel is registered only for campaigns (SubModule.cs), so the Phase D A/B would need a Custom Battle registration.
7. FINDINGS OR OBSERVATIONS: numbered, each with severity P1/P2/P3, file:line, the failure scenario as concrete inputs -> wrong outcome, and the minimal fix. Try to refute each of your own findings before listing it. Unverifiable engine or native behaviour stays UNVERIFIED, never a verdict.

QUALITY GATES. Every claim about engine behaviour cites a decompiled line. Every "missing" claim was grepped. No finding on code that matches vanilla. Do not report the twelve RCA items again unless a fix is wrong. No em or en dashes in the report (use "--"). Do not write files.

PRIOR REVIEW LESSONS. SUCCESSES: config ID cross-ref caught rohan/dol_guldur mismatches; vanilla decompilation caught missing gates; lifecycle tracing caught stale caches; review 62 caught cross-entity GameModel propagation by opening the engine consumer; review 72 caught a hook-level gate bypassing a service-layer fix; review 109 corrected the thread map by reading CommonAIComponent.OnTick. FAILURES: Codex assumed empire=Rohan (it is Dunland); Codex flagged vanilla-matching code as bugs; Codex skipped hard sections; Codex once assembled its report into the transcript file.
