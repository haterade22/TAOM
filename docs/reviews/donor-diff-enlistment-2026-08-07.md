# Donor diff — RF_Enlistment vs TAOM Enlistment (2026-08-07)

Six subsystem agents diffed the RF_Enlistment donor against TAOM's rewrite; each finding was then
independently re-verified by a second agent instructed to REFUTE it. Source: workflow run
`wf_72d8175b-676` (the run stalled before its synthesis phase; these are its verified findings,
recovered from the journal).

**45 gaps found, 40 adversarially verified: 33 CONFIRMED, 7 REFUTED.**

Symptom keys: S1 passenger/nothing-to-do · S2 never joins battles · S3 can't interact after service
· S4 left behind · S5 unexpected desertion.

---

## Confirmed

### [CRITICAL] #2 Donor's wait-menu tick creates/joins the commander's battle every frame; TAOM's only syncs position

**Verifier:** CONFIRMED, CRITICAL. Donor `ServiceWaitMenuOnTick` runs 1179-1241; the battle branch at 1210-1224 is verbatim as quoted (TryClearStaleEncounterForCommanderBattle -> TryCreateCommanderBattleEncounter when PlayerEncounter.Current==null -> TryJoinCommanderEncounterBattle when EncounteredBattle==commanderParty.MapEvent -> return), the settlement-follow branch is at 1226-1237, the commander-gone close-out at 1188-1202, and SyncPlayerPartyToCommanderServiceMode is the last line at 1239 — so the reviewer's correction of TAOM's code comment is accurate. TAOM's OnWaitMenuTick (EnlistmentMenuBehavior.cs:100-107) is exactly the quoted five lines: authority gate, `++_tickCounter % 5`, SyncPosition. No battle branch, no settlement branch, no commander-gone branch. Cadence verified against installed 1.4.7: MapState.OnTick -> `else if (AtMenu) OnMenuModeTick(dt)` -> `MenuContext.OnTick(dt)` (MapState.cs:144-169) -> GameMenu.RunOnTick fires OnTick per frame while IsWaitActive (GameMenu.cs:224-235). So this is a per-frame pump TAOM already owns and uses for nothing but a throttled teleport. It is also the only mechanism in either mod that keys on the commander's LIVE `MapEvent` back-reference rather than on an event edge or an id-match loop, which is why it, not #1, is the direct answer to S2. Additional confirmed sub-gap the reviewer did not name: the settlement-follow branch has no TAOM counterpart at all — `IMobilePartyAttachmentAdapter.MoveIntoSettlement` (MobilePartyAttachmentAdapter.cs:160-181) has ZERO callers in Main/Features/**, so TAOM never follows the commander into a settlement.

---

### [CRITICAL] #2 No ConversationContext ownership gate — the oath Finish() destroys a SETTLEMENT encounter that is the player's own business

**Verifier:** Confirmed on both sides and mechanically sound. Donor RFEnlistmentCampaignBehavior.cs:2226-2245 has the four gates verbatim as quoted; I also found a SECOND independent gate the report understated — line 2232's `PlayerEncounter.EncounteredMobileParty == null` also catches the settlement case, because a settlement encounter's `_encounteredParty` is `settlement.Party` and `EncounteredMobileParty => EncounteredParty?.MobileParty` (PlayerEncounter.cs:162) is null for it. TAOM side confirmed: `if (_encounter.HasCurrent) _encounter.Finish(false)` is unconditional (EnlistmentService.cs:94-100); a repo-wide grep finds `ConversationContext` only in Main/Features/Messengers/MessengerCampaignBehavior.cs:392, nowhere in Enlistment or Adapters; the enlist line is on `hero_main_options` with no settlement restriction (EnlistmentDialogBehavior.cs:49-56) and `EnlistmentDialogGateService.CanEnlistWith` adds none (lines 21-46). `hero_main_options` is the standard vanilla lord token reachable from an in-town keep conversation (36 occurrences in LordConversationsCampaignBehavior.cs). Engine chain verified on installed 1.4.7: `EncounterManager.StartSettlementEncounter` does `PlayerEncounter.Start()` then `PlayerEncounter.Current.Init(attackerParty.Party, settlement.Party, settlement)` (EncounterManager.cs:153, 182, 187), so PlayerEncounter.Current IS live while the player is inside a town; `InsideSettlement` = `MainParty.IsActive && CurrentSettlement != null` (PlayerEncounter.cs:255-265) is true at that moment; `Finish(false)` skips `LeaveSettlement()` at PlayerEncounter.cs:1082-1085 and nulls `Campaign.Current.LocationEncounter` at :1061; and `EncounterManager.HandleEncounterForMobileParty` returns early for any party with `(mobileParty.CurrentSettlement != null && !mobileParty.IsGarrison)` (EncounterManager.cs:36). Nothing in TAOM ever calls `LeaveSettlementAction` for the main party, so the residue survives the whole term and past discharge. Explains S3, and plausibly S1/S4.

---

### [CRITICAL] Discharge never leaves the service wait menu — after any auto-discharge the player is frozen in a menu with zero selectable options and no Escape exit

**Explains:** S3, S4

**Donor:** `RFEnlistmentCampaignBehavior.cs:1186-1202 (ServiceWaitMenuOnTick auto-close) and :6743-6746 (RestorePostServiceCampaignContext tail)`

**TAOM:** `Main/Features/Enlistment/DischargeService.cs:32-93 (no menu interaction at all); Main/Features/Enlistment/Hooks/EnlistmentMenuBehavior.cs:122 (ExitToLast on ONE path only); Main/Features/Enlistment/EnlistmentReconciler.cs:87, :107, :121, :224; Main/Features/Enlistment/EnlistmentLoadNormalizer.cs:57`

**Verifier:** CONFIRMED, with one sub-argument corrected. Donor RF:1186-1202 quote is verbatim including the comment naming the symptom ("left the player frozen in the wait menu with an orphaned camera"), and RF:6743-6746 is the same ExitToLast fallback at the tail of RestorePostServiceCampaignContext. TAOM: `grep -rn "ExitToLast|ExitMenuMode" Main/` returns only GameMenuAdapter.cs:81-90 (the wrapper), EnlistmentMenuBehavior.cs:122, and an unrelated FiefHubCampaignBehavior.cs:86. DischargeService.Execute (DischargeService.cs:32-93) touches no menu. So CommanderDead (EnlistmentReconciler.cs:87, :121, :224), CommanderUnavailableGraceExpired (:107) and HeirSuccessionOrPossessionMismatch (EnlistmentLoadNormalizer.cs:57) all leave the wait menu open. Engine verified against installed 1.4.7 `GameMenu.cs`: `GetMenuOptionConditionsHold` L158-169 routes every wait-menu option through `RunWaitMenuCondition` (L242-254), which invokes the menu's OnCondition — TAOM's is `args => _store.Record.IsEnlisted` (EnlistmentMenuBehavior.cs:64), false the instant `Record.Reset()` runs (DischargeService.cs:68). Both options therefore evaluate false. `GameMenuManager` (installed 1.4.7, read in full) has no auto-exit on a failing wait condition, and `IsWaitActive` stays true, so time keeps fast-forwarding. CORRECTION to one sub-argument: the missing `isLeave: true` is NOT a donor/TAOM difference — I verified `CampaignGameStarter.AddGameMenuOption(..., bool isLeave = false, int index = -1, ...)` (CampaignGameStarter.cs:93) and the donor passes `false` on all three of its options too (RF:348-373). The gap is solely the missing ExitToLast on the five programmatic paths. Mechanically explains S4 ("visible on the map, stranded" — RestorePresence sets IsVisible=true behind a dead menu). S3 attribution is weak: the leave-button path does call ExitToLast (EnlistmentMenuBehavior.cs:122).

**Minimal fix:** Move the menu exit into `DischargeService.Execute` (or a subscriber on `EnlistmentEnded`): if `_gameMenu.CurrentMenuId == EnlistmentMenuService.ServiceWaitMenuId` then `_gameMenu.ExitToLast()`, so every discharge reason gets it, not just the menu-button path.

---

### [CRITICAL] Nothing in TAOM re-asserts the service wait menu, so the only continuous position-sync driver can be lost permanently

**Verifier:** CONFIRMED, and this is the strongest of the five. Every donor citation checks out verbatim: `ActivateCommanderServiceWaitMenu()` is called at RF:913 (from the maintenance tick, against WHATEVER menu is showing), 6447 (settlement attach), 6503 (park attach), 6602/6643/6657/6675 (every battle-failure re-park); the helper body at RF:6526-6541 is quoted correctly; `ShouldForceCommanderServiceMenu` (RF:6885-6916) does gate on `bool hiddenServiceMode = !mainParty.IsActive && !mainParty.IsVisible;`; and RF:1012-1014 is `PlayerEncounter.Finish(); TraceEnlistmentState(...); TryAttachPlayerToCommanderDuty(showFeedback: false);` (the report says 1007-1009 — off by five lines, code identical). The engine claim is verified against installed 1.4.7: `PlayerEncounter.Finish` (PlayerEncounter.cs:1013-1026) calls `GameMenu.ExitToLast()` unconditionally when a menu context exists, and I traced it further than the reporter did — `GameMenuManager.ExitToLast()` (GameMenuManager.cs:47-53) calls `MapState.ExitMenuMode()`, so it leaves menu mode entirely rather than popping back to a prior menu. TAOM side verified: a grep for ServiceWaitMenuId / Activate / SwitchTo / EnsureMenuOpen across Main/Features/Enlistment/ + Main/Adapters/ finds activation only in the SetNextMenu prefix, the EnterMenuMode postfix, `OnConversationEnded` (EnlistmentMenuBehavior.cs:129-132), and — added to disk DURING this review — `ServiceBattleService.ReassertServiceMenu` (:139-151, called from the join rollback :112 and OnCommanderBattleEnded :128). ParkNear never opens a menu. PARTIAL CORRECTION: that new helper closes the ServiceBattleService.cs:100-111 half the report cites. The other half is untouched and is the game-breaker: EnlistmentReconciler.cs:172 `if (!_encounter.Finish(false))` — the stranded-conversation-encounter self-heal — drops the wait menu with no re-assert, leaving the player parked (IsVisible=false, IsActive=false, camera on the commander) in free map mode. Nothing periodic recovers it; `TryRedirectMenu` only fires on an incoming push, and with the party inactive/invisible no conversation can start to trigger OnConversationEnded. That self-heal is exactly the branch that fires on the live tester's existing save. Recovery only comes at the commander's next battle — and S2 says the commander fought zero of 456 events.

---

### [HIGH] #1 Donor's ~4 Hz CampaignEvents.TickEvent recovery ladder; TAOM subscribes to no TickEvent

**Verifier:** CONFIRMED, downgraded to HIGH. Donor cites are exact: `CampaignEvents.TickEvent.AddNonSerializedListener(this, OnTick);` at RFEnlistmentCampaignBehavior.cs:129, `MaintenanceIntervalSeconds = 0.25f` at :69, OnTick body at :837-882 (three-stage ladder as quoted), and `NeedsCommanderAttachment()` at :6749-6800 does re-derive purely from live state (commanderParty/mainParty MapEvent, CurrentSettlement, AttachedTo, Army, DefaultBehavior==EscortParty) with no reference to the pending flag. TAOM side confirmed by full grep of Main/Features/Enlistment/**: the only CampaignEvents subscriptions are HourlyTick (EnlistmentBehavior.cs:59, EnlistmentDutyBehavior.cs:30), DailyTick, MapEventStarted/Ended (EnlistmentBattleBehavior.cs:42-43), OnGameLoaded/SessionLaunched/NewGameCreated, HeroPrisonerTaken/Released, ConversationEnded, SettlementEntered, MobilePartyDestroyed. No TickEvent anywhere. BUT the framing overstates the delta on the attach axis: the donor's own stage-3 budget after a FAILED attach is `_nextAttachmentRetryHour = now + 1f` (RF:882) — one campaign hour, the same cadence as TAOM's hourly reconcile — so "14400 vs 24 recovery attempts" is only true of stages 1-2, not stage 3. The genuinely lost capability is stages 1-2 (TryResolvePendingCommanderMenuEscape / TryResolvePendingCommanderEncounter), which are the only donor code that runs while the player is NOT in the wait menu (the wait-menu tick is gated on `IsWaitMenu && IsWaitActive`, GameMenu.cs:224-226). That covers S1/S4 windows TAOM has no pump for; it does not on its own establish S2.

---

### [HIGH] #3 No pending-attachment retry flag; EnlistedBattle is a latch that can pin the join gate closed

**Verifier:** CONFIRMED on the structure, downgraded to HIGH because the "permanent pin" is unproven. Verified in TAOM: `TryJoin` refuses unless `State == EnlistedAttached` (ServiceBattleService.cs:44-48); `OnCommanderBattleEnded` returns early while `_encounter.HasCurrent` (:116-122); the reconciler's demote requires `!_encounter.HasCurrent` (EnlistmentReconciler.cs:142-148); the stranded-encounter sweeper requires `EnlistedAttached` (:166-169); OnWaitMenuTick's SyncPosition (:105) and OnWaitMenuInit's EnsureParked (:95) and TryRedirectMenu (EnlistmentMenuService.cs:33) and OnConversationEnded (:129) all require EnlistedAttached. EnlistmentTransitionTable confirms EnlistedBattle -> CommanderUnavailable / DetachedOnDuty are illegal, so EnlistedBattle's only exits are EnlistedAttached, EnlistedPlayerCaptive and Discharging. Donor cites all check out (flag at :108, SyncData at :150, ten set-sites and the success-only clears at the listed lines, and TryAttachPlayerToCommanderDuty / TryResolvePendingCommanderEncounter carry no state precondition). What I could NOT confirm is the deadlock's entry: every in-TAOM path that opens an encounter also closes it on failure (`_encounter.Finish(false)` at ServiceBattleService.cs:100 before the forced demote at :105-109, and DischargeService closes at exit), so an encounter leak while in EnlistedBattle would have to come from outside the feature. The harm that IS mechanically provable without any leak: after a battle the demote is hourly-only, so state sits in EnlistedBattle for up to a campaign hour with presence already restored (`_attachment.RestorePresence()`, ServiceBattleService.cs:67) and BOTH position sync and park gated off — a visible, unparked, unsynced player while the column moves. That is S4 exactly.

---

### [HIGH] #5 A commander joining an already-running MapEvent fires no MapEventStarted

**Verifier:** CONFIRMED, HIGH. The engine claim holds against the installed 1.4.7 assemblies: `CampaignEventDispatcher.Instance.OnMapEventStarted(this, attackerParty, defenderParty)` appears exactly once in MapEvent.cs, at line 838, as the last statement of Initialize (I grepped every CampaignEventDispatcher call site in the file — the others are OnPartyAddedToMapEvent, OnPlayerPartyKnockedOrKilledTroop, loot, OnMapEventEnded, SiegeCompleted). `PartyBase.MapEventSide` is the setter at PartyBase.cs:289-327 and it propagates to attached parties at :324; the add path dispatches `OnPartyAddedToMapEvent(party)` at MapEvent.cs:681 — a DIFFERENT event. So a late-joining commander produces no MapEventStarted, and TAOM's only subscription for the join is that edge (EnlistmentBattleBehavior.cs:42/67-93). Donor cites verified: OnMapEventStarted at :514-541 is the thin advisory handler quoted (keys on `commanderParty.MapEvent != mapEvent`, sets `_pendingCommanderAttachment = true` at :532 BEFORE any attempt), and TryResolvePendingCommanderEncounter at :953-1011 re-polls off the live back-reference. TAOM's fallback (`AttachmentStatus.BattleJoinRequired` -> BattleJoinRequested -> TryJoinCommanderBattle) is hourly AND re-gated on EnlistedAttached inside TryJoin, so a late join that misses is retried at most once per campaign hour. Worth noting for the minimal fix: `OnPartyAddedToMapEvent` is a real dispatched event neither mod subscribes to — cheaper than a poll, though #2's wait-menu poll subsumes it.

---

### [HIGH] #1 Wait menu has no "Talk to commander" option — the entire enlisted dialog layer is unreachable

**Verifier:** CONFIRMED on both sides. Donor RFEnlistmentCampaignBehavior.cs:356-364 registers `rf_enlistment_wait_talk_to_commander` with condition `args => _serviceRecord.IsEnlisted && ResolveCommander()?.CharacterObject != null` and consequence `TalkToCommanderFromServiceMenu()` (body at 1162-1177, verbatim as quoted, ending in `CampaignMapConversation.OpenConversation(playerData, commanderData)`). TAOM: `grep -rn AddGameMenuOption Main/Features/Enlistment/` returns exactly two hits, EnlistmentMenuBehavior.cs:73 (`taom_enlist_menu_status`) and :81 (`taom_enlist_menu_leave`). `grep -rn CampaignMapConversation Main/ --include=*.cs` returns one hit, EliteEmissaryBehavior.cs:110 — nothing in Enlistment. I re-verified that no other route exists: LordConversationsConditionPatches.cs only SUPPRESSES four vanilla lines, it opens nothing. Enlisted content is all on `hero_main_options` (EnlistmentDialogBehavior.cs:96, EnlistmentAssignmentDialogBehavior.cs:40, EnlistmentQuartermasterBehavior.cs:57), and while parked the party is IsVisible=false/IsActive=false (MobilePartyAttachmentAdapter.cs:46-47) inside a wait menu, so the commander cannot be clicked. CORRECTION to the severity framing: the reviewer says "the entire dialog layer" is dead, but the discharge tree has a working menu equivalent (`taom_enlist_menu_leave`), so the player is not trapped — reassignment and quartermaster are the genuinely dead trees. Explains the content half of S1, not a hard break.

---

### [HIGH] #2 Wait-menu tick is a throttled position sync only — the donor's tick IS the service loop

**Verifier:** CONFIRMED. Donor `ServiceWaitMenuOnTick` at 1179-1240 has exactly the four branches quoted (commander-gone → ShowMessage + ReleasePlayerFromCommanderDuty + Clear + ExitToLast; MapEvent → TryClearStaleEncounterForCommanderBattle / TryCreateCommanderBattleEncounter / TryJoinCommanderEncounterBattle; CurrentSettlement → TryAttachPlayerToCommanderDuty; else SyncPlayerPartyToCommanderServiceMode + background + text). I read SyncPlayerPartyToCommanderServiceMode at 6543-6575: it is indeed the full re-park (AttachedTo=null, Position, IsVisible=false, IsActive=false, SetAsCameraFollowParty), not a position copy. TAOM's OnWaitMenuTick (EnlistmentMenuBehavior.cs:100-107) is verbatim as quoted and only calls `_attachment.SyncPosition`. Battle-join drivers confirmed to be only EnlistmentBattleBehavior.cs:42 (MapEventStarted) and EnlistmentReconciler.cs:195-204 (hourly). TWO CORRECTIONS: (a) the reviewer's donor quote elides `if (!commanderParty.IsActive) return;` which sits between the commander-null branch and the MapEvent branch; (b) "never re-asserts the park" is true of the TICK only — OnWaitMenuInit:95-96 calls EnsureParked and the hourly reconciler calls it on AttachRequired, so the real delta is cadence (per-frame vs per-hour) plus the missing settlement branch, not total absence. Downgraded from CRITICAL on that basis.

---

### [HIGH] #3 No non-player-initiated discharge exits the wait menu — auto-discharge strands the player in an optionless menu

**Verifier:** CONFIRMED, including the engine mechanism, which I verified independently. Donor calls GameMenu.ExitToLast() at 1197-1200 (tick, commander gone) and inside RestorePostServiceCampaignContext 6743-6746 (reached from Discharge at 1468). TAOM: `grep -rn ExitToLast Main/` shows exactly one Enlistment call site, EnlistmentMenuBehavior.cs:122, inside OnLeaveServiceSelected; DischargeService.cs:32-93 touches no menu. Engine check on installed 1.4.7 (C:\Users\mikew\.taom-src\v1.4.7\TaleWorlds.CampaignSystem.GameMenus.GameMenu.cs:158-168): `GetMenuOptionConditionsHold` returns `_menuItems[n].GetConditionsHold(...) && RunWaitMenuCondition(menuContext)` for a wait menu, and GameMenuVM.Refresh (E:\Decompiled_Bannerlord\...\GameMenuVM.cs:367-370) only builds an item when that returns true — so a failing menu OnCondition hides every option. TAOM's menu condition is `args => _store.Record.IsEnlisted` (EnlistmentMenuBehavior.cs:64) and Reset() sets State=NotEnlisted (EnlistmentRecord.cs:42-51), so IsEnlisted goes false. Neither TAOM's nor the donor's options are `isLeave:true`, so GetLeaveMenuOption returns null and there is no ESC shortcut out. IMPORTANT SYMPTOM CORRECTION: this cannot explain the OBSERVED S3/S5. The player's two test services ended in "Desertion", which is only produced by ClassifyLeaveReason on the player-chosen paths — and the wait-menu path DOES call ExitToLast. The real exposure is the reconciler's CommanderDead / GraceExpired discharges and the load normalizer, which are latent, not what the player hit.

---

### [HIGH] #4 Settlement following is absent — the adapter's MoveIntoSettlement/LeaveSettlement have zero call sites

**Verifier:** CONFIRMED. Donor TryAttachPlayerToCommanderDuty at 6433-6456 contains the EnterSettlementAction.ApplyForParty + ActivateCommanderServiceWaitMenu block verbatim, and the LeaveSettlementAction.ApplyForParty block at 6493-6498. TAOM: `grep -rn "MoveIntoSettlement|LeaveSettlement" Main/ --include=*.cs` returns only IMobilePartyAttachmentAdapter.cs:30/32 and MobilePartyAttachmentAdapter.cs:160/183 — no callers. ServiceAttachmentService.Assess (18-50) reads no settlement state on either side. Engine claims verified on installed 1.4.7: MobileParty.cs:577-623, the CurrentSettlement setter does `Position = (IsCurrentlyAtSea ? PortPosition : GatePosition)`, so syncing to a settled commander parks the player on the gate; and MapEvent.cs:640-643 does `if (IsSiegeAssault && party.MobileParty.CurrentSettlement == null && side == Defender) _mapEventType = BattleTypes.SiegeOutside` — the assault→SiegeOutside rewrite is real and ServiceBattleService.cs:76-77 guards only the Attacker case. ONE CORRECTION: the reviewer implies this leaves the player behind. It does not — SyncPosition still tracks the (pinned) gate position, so following continues. The real losses are settlement content and the siege battle-type corruption.

---

### [HIGH] #7 Leaving service via the wait menu silently classifies as Desertion — the donor has no desertion concept for discharge at all

**Verifier:** CONFIRMED on both sides and it is the one gap that directly matches an observed symptom. Donor: wait-menu option 368-373 is `rf_enlistment_wait_leave_service` / "Leave military service" → `args => Discharge()`; Discharge() at 1441-1473 has no early-vs-late branch (equipment-debt gate, ApplyServiceCareerConsequences, ReleasePlayerFromCommanderDuty, RestorePostServiceCampaignContext, Clear, message). `DefaultContractDays = 365f` at line 70 is used only by RenewContract and CanRenewContract (1425-1427). `grep -rni desert` across the donor returns only DutyMissionDeserterSweep strings — no desertion outcome exists. I also read ApplyServiceCareerConsequences (7752+): its penalties key off CommanderTrust / DutyFailures, never contract timing. TAOM: EnlistmentMenuBehavior.cs:81-87 registers "Ask to be released from service" with an unconditional-true condition, consequence OnLeaveServiceSelected (:109-123) calling `_gate.ClassifyLeaveReason(...)`; EnlistmentDialogGateService.cs:55-63 returns Desertion for any `nowDays < ContractEndDay`; ContractDays defaults to 365.0 (IEnlistmentConfigProvider.cs:8). DischargeConsequenceService.cs:29-33 confirms the Desertion branch forfeits DeferredWages while every honorable reason settles them. So a polite-sounding option silently deserts for the first year, with no warning. Explains S5 exactly.

---

### [HIGH] #3 Every TAOM Finish passes forcePlayerOutFromSettlement:false; the donor uses the engine default (true) at all three non-battle sites

**Verifier:** Every cited fact checks out. Engine: `public static void Finish(bool forcePlayerOutFromSettlement = true)` (PlayerEncounter.cs:1013) — default IS true, and the `LeaveSettlement()` at :1084 is gated on it. Donor: bare `PlayerEncounter.Finish();` at RFEnlistmentCampaignBehavior.cs:1007, :1037 and :2245 (read all three). Donor `RestorePostServiceCampaignContext` exists verbatim at :6717-6747 and is called from `Discharge()` at :1468. TAOM: `public bool Finish(bool forcePlayerOutFromSettlement = false)` (EncounterAdapter.cs:204) and every call site passes false — EncounterAdapter.cs:124, EnlistmentService.cs:96, DischargeService.cs:61, EnlistmentReconciler.cs:172, ServiceBattleService.cs:100. `IMobilePartyAttachmentAdapter.LeaveSettlement()` (MobilePartyAttachmentAdapter.cs:183-201) has ZERO callers repo-wide — grep confirms only its own declaration/definition. `DischargeService.Execute` really is RestorePresence + Finish(false) + stop (lines 50-92); there is no post-service context restore. One correction that strengthens rather than weakens the gap: the donor's non-besieged settlement exit is not only the Finish default — `TryAttachPlayerToCommanderDuty` explicitly runs `LeaveSettlementAction.ApplyForParty(mainParty)` when the player is in a settlement and the commander is not (donor:6491-6496). TAOM has no analogue at all. Explains S3 and S4; overlaps #2 but is a distinct defect (the parameter itself plus the missing discharge-side restore).

---

### [HIGH] TAOM invented a desertion classification the donor does not have, and pinned it to a 365-day contract — so EVERY voluntary exit is desertion

**Explains:** S5

**Donor:** `RFEnlistmentCampaignBehavior.cs:1441-1473 (Discharge), :70 (DefaultContractDays), :1425-1428 (CanRenewContract), :798-802 (expiry notice)`

**TAOM:** `Main/Features/Enlistment/EnlistmentDialogGateService.cs:55-63; Main/Features/Enlistment/IEnlistmentConfigProvider.cs:8; Main/Features/Enlistment/Hooks/EnlistmentMenuBehavior.cs:120-122; Main/Features/Enlistment/Hooks/EnlistmentDialogBehavior.cs:132-137`

**Verifier:** CONFIRMED on both sides. Donor: `grep -i desert RFEnlistmentCampaignBehavior.cs` returns only DutyMissionDeserterSweep hits (lines 86, 425, 2776, 3004, 3153-3154, 3445-3476, ... 8034) — no exit-classification concept exists. `Discharge()` (RF:1441-1472) is unconditionally honourable; `DefaultContractDays = 365f` (RF:70) is used only by `CanRenewContract()` (RF:1425-1428) and the expiry notice (RF:798-802, "Your enlistment contract has expired. You can renew it in any town or castle."). TAOM: `EnlistmentDialogGateService.ClassifyLeaveReason` (EnlistmentDialogGateService.cs:55-63) returns Desertion whenever `nowDays < ContractEndDay`; `ContractEndDay = nowDays + _config.GetConfig().ContractDays` (EnlistmentService.cs:83) with `ContractDays = 365.0` (IEnlistmentConfigProvider.cs:8). I confirmed `EnlistmentConfigProvider` never reads JSON (`private readonly EnlistmentCoreConfig _config = new EnlistmentCoreConfig();`, EnlistmentConfigProvider.cs:13) and that `Main/_Module/ModuleData/enlistment/enlistment_config.json` contains no `contract*` key at all (grep exit=1). Both leave paths call it: EnlistmentMenuBehavior.cs:120 and EnlistmentDialogBehavior.cs:136 — the latter behind the spoken line "You have marched well enough. Go, then — you are released." (EnlistmentDialogBehavior.cs:107), so a commander-granted release is recorded as desertion. Mechanically explains S5 exactly: DischargeService.cs:80 emits `[Enlistment] service ended: Desertion`, and DischargeConsequenceService.cs:29-33 forfeits DeferredWages on that reason while every other reason settles them (:35-46). Severity corrected CRITICAL→HIGH: it fully explains a reported symptom and silently confiscates earned wages, but leaves no corrupted state.

**Minimal fix:** Make a commander-granted release honourable regardless of the day: have both leave paths pass `DischargeReason.PlayerRequest`, and reserve `Desertion` for a genuinely unilateral walk-out (or drop it, matching the donor). If the honour split is kept, the contract length must be a handful of days, not 365.

---

### [HIGH] No "Talk to commander" wait-menu option — the entire enlisted dialog surface (discharge, reassign, quartermaster) is unreachable code

**Explains:** S1, S3, S5

**Donor:** `RFEnlistmentCampaignBehavior.cs:357-364 (menu option) and :1162-1177 (TalkToCommanderFromServiceMenu)`

**TAOM:** `Main/Features/Enlistment/Hooks/EnlistmentMenuBehavior.cs:73-88 (only two options registered); no `CampaignMapConversation.OpenConversation` exists anywhere under Main/Features/Enlistment (verified by grep across Main/ — the only callers are EliteEmissaryBehavior.cs:110 and MessengerCampaignBehavior.cs:409)`

**Verifier:** CONFIRMED. Donor RF:357-364 registers `rf_enlistment_wait_talk_to_commander` as the middle of three wait-menu options, and RF:1162-1177 `TalkToCommanderFromServiceMenu()` opens the map conversation via `CampaignMapConversation.OpenConversation(playerData, commanderData)` — quoted text is verbatim. TAOM: `grep -rn AddGameMenuOption Main/Features/Enlistment` returns exactly two hits (EnlistmentMenuBehavior.cs:73 status, :81 leave); `grep -rn OpenConversation Main/` returns only EliteEmissaryBehavior.cs:110 and MessengerCampaignBehavior.cs:409 — nothing in Enlistment. All enlisted dialog content hangs off `hero_main_options`: taom_enlist_discharge (EnlistmentDialogBehavior.cs:94-101), taom_enlist_reassign (EnlistmentAssignmentDialogBehavior.cs:38-46 + 4 picks), taom_enlist_qm_request (EnlistmentQuartermasterBehavior.cs:55-62). While EnlistedAttached the main party is `IsVisible=false; IsActive=false` (MobilePartyAttachmentAdapter.cs:46-47) and the player is held in the wait menu (activated at EnlistmentMenuBehavior.cs:132 / ServiceBattleService.cs:147), with `StartWait()` forcing UnstoppableFastForward (GameMenu.cs:285-290, installed 1.4.7) — no map click, no conversation. So three shipped behaviors never fire. CAVEAT on attribution: S3 is a POST-service symptom and this gap is entirely during-service — that attribution is wrong (S3 was already root-caused to the leaked PlayerEncounter). S5 is gap #1's classifier, not this. This gap genuinely supports S1's "never participates in anything" half. One theoretical exception the review missed: `FieldDutyRuntime.cs:96-110` transitions to EnlistedDetachedOnDuty and calls RestorePresence(), which would in principle free the player — but nothing exits the wait menu there either, so the dialogs stay unreachable in practice. Severity corrected CRITICAL→HIGH.

**Minimal fix:** Add a third wait-menu option in EnlistmentMenuBehavior.OnSessionLaunched that calls a new adapter method wrapping `CampaignMapConversation.OpenConversation(new ConversationCharacterData(CharacterObject.PlayerCharacter, PartyBase.MainParty), new ConversationCharacterData(commander.CharacterObject, commander.PartyBelongedTo.Party))`, mirroring RF:1162-1177.

---

### [HIGH] Discharge never restores the campaign context: no settlement placement and no wait-menu exit on any non-interactive discharge path

**Explains:** S3, S4

**Donor:** `RFEnlistmentCampaignBehavior.cs:1464-1468, :6717-6747`

**TAOM:** `Main/Features/Enlistment/DischargeService.cs:32-93; Main/Features/Enlistment/Hooks/EnlistmentMenuBehavior.cs:109-123`

**Verifier:** CONFIRMED, and the menu half is worse than the report argues. Donor verified: `Discharge()` at RF:1440-1471 captures `Settlement? releaseSettlement = ResolveCommander()?.PartyBelongedTo?.CurrentSettlement;` (RF:1464) then calls `RestorePostServiceCampaignContext(releaseSettlement)` (RF:1468); the helper at RF:6717-6747 reads exactly as quoted, including the terminal `if (Campaign.Current?.CurrentMenuContext?.GameMenu?.StringId == ServiceWaitMenuId) { GameMenu.ExitToLast(); }`. The donor's AUTO path does it too — `ServiceWaitMenuOnTick` (RF:1195-1201) explicitly exits the wait menu when the commander is gone. TAOM verified: `DischargeService.Execute` (:32-93) does only `RestorePresence()` (:50) and a conditional `_encounter.Finish(false)` (:58-63); it does not inject `IGameMenuAdapter` at all (ctor :16-28), so it structurally cannot exit the menu, and it never reads the commander's settlement. Menu exit exists only at `EnlistmentMenuBehavior.OnLeaveServiceSelected` (:121-122); the five non-interactive callers are confirmed at EnlistmentReconciler.cs:87, :107, :121, :224 and EnlistmentLoadNormalizer.cs:57. I verified the consequence independently against 1.4.7 rather than taking it on trust, and it is a hard soft-lock: `GameMenu.RunWaitMenuCondition` is reachable ONLY from `GetMenuOptionConditionsHold` (GameMenu.cs:158-168) — there is no path that exits a wait menu when its OnCondition flips false. So after e.g. a CommanderDead discharge on the hourly tick the player stays in `taom_enlistment_service_wait` with BOTH options ('Review your service', 'Ask to be released') condition-gated to false via `args => _store.Record.IsEnlisted` (EnlistmentMenuBehavior.cs:64), i.e. a menu with zero selectable options and no way out. That is a literal match for S3 'cannot click any lord or settlement'. The settlement-placement half is real but minor (the player pops out at the gate they were already parked at). Severity HIGH rather than CRITICAL only because it needs an auto-discharge to fire, whereas gap #2 fires on the live tester's existing save.

**Minimal fix:** In `DischargeService.Execute`, capture `_commander.GetSnapshot(record.CommanderHeroId).SettlementId` before `_store.Record.Reset()`, then after `RestorePresence()` either `MoveIntoSettlement(settlementId)` + switch to the town/castle/village menu, or `_gameMenu.ExitToLast()` when the current menu is the service wait menu — and remove the now-redundant `ExitToLast` from `OnLeaveServiceSelected`.

---

### [MED] #6 Six re-attach edges the donor arms are not subscribed at all in TAOM

**Verifier:** CONFIRMED as a code difference, downgraded to MED because the delta is latency rather than capability. Every donor cite is verbatim: OnPlayerBattleEnd ends `_pendingCommanderAttachment = true;` at :511; OnMapEventEnded's triple-guarded set at :574-579; OnPartyLeftArmy's `_pendingCommanderAttachment = true; _nextAttachmentRetryHour = 0f;` at :646-647; OnMobilePartyLeftSiegeEvent at :684; OnSiegeCompleted at :733; OnDailyTick's catch-all at :783-790; OnSettlementLeft's direct retry at :392-403; registrations at :131-141. TAOM subscribes to none of OnPlayerBattleEndEvent / OnSettlementLeftEvent / OnPartyJoinedArmyEvent / OnPartyLeftArmyEvent / OnMobilePartyJoinedToSiegeEventEvent / OnMobilePartyLeftSiegeEventEvent / SiegeCompletedEvent (verified by grep across the whole feature). But TAOM's hourly reconciler does not depend on any edge having been observed: `ReconcileAttached` re-derives from a fresh CommanderSnapshot + PlayerPresenceSnapshot every pass and routes AttachRequired -> EnsureParked (EnlistmentReconciler.cs:135-193), so all six situations self-heal within one campaign hour. The donor's edges mostly only re-arm a flag whose own tick-path budget is `now + 1f` campaign hour on failure — i.e. the same latency. The two edges that genuinely beat that are OnPartyLeftArmy's `_nextAttachmentRetryHour = 0f` and OnSettlementLeft's unbudgeted immediate retry.

---

### [MED] #5 No real-time maintenance tick — the donor retries attach/encounter/menu-escape 4x per second

**Verifier:** CONFIRMED. Donor: `MaintenanceIntervalSeconds = 0.25f` at line 68, `CampaignEvents.TickEvent.AddNonSerializedListener(this, OnTick)` at line 129, and OnTick at 836-882 with the exact body quoted (EnsureDutyMissionTargetVisibility → IsDetachedDutyMissionInProgress → RepairLegacyCommanderServiceState → TryResolvePendingCommanderMenuEscape → TryResolvePendingCommanderEncounter → attach retry with _nextAttachmentRetryHour). TAOM: I enumerated every CampaignEvents subscription under Main/Features/Enlistment — there is no TickEvent and no OnSettlementLeftEvent; the only recurring drivers are HourlyTickEvent (EnlistmentBehavior.cs:59, EnlistmentDutyBehavior.cs:30) and DailyTickEvent. CORRECTION on impact: TAOM does have a real-time loop — the engine's wait-menu OnTick — so the unique loss is retries while the wait menu is NOT active (during conversations, settlement menus, encounters). That overlaps heavily with #2, which is where the substantive delta actually lives. Downgraded from HIGH.

---

### [MED] #8 Discharge does not restore the post-service campaign context

**Verifier:** CONFIRMED. Donor RestorePostServiceCampaignContext at 6717-6747 is verbatim as quoted (EnterSettlementAction.ApplyForParty + village/castle/town menu selection + SwitchToMenu-or-ActivateGameMenu, else ExitToLast), and Discharge captures `releaseSettlement = ResolveCommander()?.PartyBelongedTo?.CurrentSettlement` at 1465 before calling it at 1468. TAOM DischargeService.cs:32-93 does RestorePresence + Finish any live PlayerEncounter + Reset + event, and never reads a settlement or picks a landing menu. CORRECTIONS: (a) the "wait menu still on screen" half of the claim is really gap #3 double-counted, and it does NOT apply to the player-initiated path, which calls ExitToLast at EnlistmentMenuBehavior.cs:122; (b) since TAOM never puts the player inside a settlement in the first place (#4), the donor's EnterSettlement-on-release is mostly a nicety here. The residual real delta is a landing-context difference, not a break.

---

### [MED] #5 No `TryClearStaleEncounterForCommanderBattle` equivalent — TAOM finishes ANY live encounter blind before seeding

**Verifier:** Donor code verified verbatim at RFEnlistmentCampaignBehavior.cs:1013-1039 — all four guards present exactly as quoted, and the failure mode really is 'leave it alone and return false'. The independent conversation guard at :993-998 inside `TryResolvePendingCommanderEncounter` is real, and `OnMapEventStarted` really does seed only when nothing is live (`if (PlayerEncounter.Current == null) { TryCreateCommanderBattleEncounter(...); }`, donor:530-537). TAOM side verified verbatim: EncounterAdapter.cs:120-124 keeps only the battle-identity guard and then finishes whatever is live. So the behavioural difference is real. Two reasons I am downgrading from CRITICAL: (a) the two damaging legs — a live settlement encounter and the player's own battle encounter — are already counted as #2 and #7, so the independent contribution here is the missing 'retry later instead of tear down' policy; (b) the conversation-guard leg is close to unreachable in TAOM's design, because the join is edge-driven from `MapEventStarted` and `Campaign.Current.TimeControlMode` is Stop during a conversation, which also short-circuits `EncounterManager.HandleEncounters` (EncounterManager.cs:26).

---

### [MED] #7 No abort when the MAIN party is already in a DIFFERENT map event — TAOM will Finish the player's own battle encounter

**Verifier:** Donor guard verified verbatim at RFEnlistmentCampaignBehavior.cs:6621-6630 (`|| (mainParty.MapEvent != null && mainParty.MapEvent != mapEvent)` -> abort). TAOM verified: `ServiceBattleService.TryJoin` gates only on state (line 44), side resolution (50), and `IsCommanderBattleJoinable` (57); neither it nor `EncounterAdapter.EnsureEncounterAgainst` (112-124) ever reads `PartyBase.MainParty.MapEvent` before finishing. `IsMainPartyInMapEvent` (EncounterAdapter.cs:62-76) is used only post-join at :174, exactly as claimed. Engine consequence confirmed: `PlayerEncounter.Finish` runs `Current.FinalizeBattle()` and `Current.FinishEncounterInternal(playerIsWinner)` at PlayerEncounter.cs:1071-1072. Two corrections. First, only the `MapEventStarted` edge is exposed — the hourly path is already guarded, because `ServiceAttachmentService.Assess` returns Attached (not BattleJoinRequired) when `player.IsInMapEvent` (ServiceAttachmentService.cs:39-41) and PlayerInForeignMapEvent at :44-45. Second, the S5 attribution is unsupported: desertion is decided solely by `EnlistmentDialogGateService.ClassifyLeaveReason` (`nowDays < ContractEndDay` -> Desertion, lines 55-63) and nothing in the battle path touches it. Reachability while parked (IsActive=false) is narrow, so MED rather than HIGH.

---

### [MED] #8 No real-time maintenance pump — the encounter lifecycle is driven by one edge plus an hourly tick

**Verifier:** Every donor citation verified: `MaintenanceIntervalSeconds = 0.25f` at RFEnlistmentCampaignBehavior.cs:69, `CampaignEvents.TickEvent.AddNonSerializedListener(this, OnTick)` at :129, the throttled pump body at :837-883 (including `if (TryResolvePendingCommanderEncounter()) return;` at :865), and `ServiceWaitMenuOnTick`'s full encounter handoff at :1209-1224 verbatim as quoted. `_pendingCommanderAttachment` is persisted (`dataStore.SyncData("rf_enlistment_pending_commander_attachment", ...)`, :150) and set at 12 sites, so intent genuinely survives retries. TAOM verified: `MapEventStarted` single edge (EnlistmentBattleBehavior.cs:42), `HourlyTickEvent` (EnlistmentBehavior.cs:59), and `OnWaitMenuTick` doing position sync only (EnlistmentMenuBehavior.cs:100-107, verbatim); no pending-intent flag exists anywhere in the feature. So the structural difference is real and is a plausible S1 contributor. Corrections: TAOM is not retry-less — the hourly reconciler routes `AttachmentStatus.BattleJoinRequired` to `TryJoinCommanderBattle` (EnlistmentReconciler.cs:195-204 -> EnlistmentBattleBehavior.cs:53-63 -> ServiceBattleService.cs:39), so a miss costs one battle rather than the whole term. And the S2 attribution is wrong: 'commander in ZERO of 456 map events' is a commander-involvement/party-id observation emitted by `FindCommanderPartyIdIn` (EnlistmentBattleBehavior.cs:116-131), which a retry-cadence gap cannot produce. Also 'the encounter lifecycle stops whenever the wait menu is not active' is a non-sequitur for TAOM, whose lifecycle is not menu-driven at all.

---

### [MED] #9 Rollback after a failed join finishes the encounter and never re-opens the service menu

**Verifier:** Donor verified: all four abort paths in `TryJoinCommanderEncounterBattle` / `TryCreateCommanderBattleEncounter` (RFEnlistmentCampaignBehavior.cs:6599-6603, :6640-6644, :6654-6658, :6672-6678) run `SyncPlayerPartyToCommanderServiceMode(commanderParty); ActivateCommanderServiceWaitMenu();` and none of them calls `PlayerEncounter.Finish` — the seeded encounter is deliberately left live for the 0.25s pump. TAOM verified verbatim at ServiceBattleService.cs:96-111: `_encounter.Finish(false)` then transition then `EnsureParked`, with no menu re-assertion. Engine confirmed: `Finish` calls `GameMenu.ExitToLast()` at PlayerEncounter.cs:1019-1022, and `MobilePartyAttachmentAdapter.ParkNear` (lines 20-58) only touches AttachedTo/MoveMode/Position/IsVisible/IsActive/camera — no menu. One correction the report missed but which does not rescue TAOM: TAOM DOES own menu-redirect machinery (`GameMenuManagerSetNextMenuPatch` + `MapStateEnterMenuModePatch` -> `EnlistmentMenuService.TryRedirectMenu`, with "encounter" in the default `RedirectMenuIds`, IEnlistmentConfigProvider.cs:18-31). It is gated on `State == EnlistedAttached` (EnlistmentMenuService.cs:33), and at the instant `Finish(false)` -> `ExitToLast()` runs the state is still `EnlistedBattle` (the transition is two statements later at ServiceBattleService.cs:105). So the rollback's own menu exit is not caught, exactly as claimed — though a later menu push or `OnConversationEnded` (EnlistmentMenuBehavior.cs:125-133) would eventually re-assert, so this is not permanent. MED rather than HIGH.

---

### [MED] No post-service campaign-context restore — the donor puts the player into the commander's settlement on release, TAOM just un-hides them wherever the park left them

**Explains:** S3, S4

**Donor:** `RFEnlistmentCampaignBehavior.cs:1465 (capture) and :6717-6747 (RestorePostServiceCampaignContext)`

**TAOM:** `absent — Main/Features/Enlistment/DischargeService.cs has no settlement handling; Main/Adapters/MobilePartyAttachmentAdapter.cs:60-85 is the whole restore`

**Verifier:** The code difference is CONFIRMED. Donor RF:1465 captures `Settlement? releaseSettlement = ResolveCommander()?.PartyBelongedTo?.CurrentSettlement;` before clearing, and RF:6717-6746 `RestorePostServiceCampaignContext` is verbatim as quoted (EnterSettlementAction.ApplyForParty + village/castle/town menu switch, ExitToLast fallback). The donor's own ServiceFlowAudit.md §4 line 78 is real ("Leaving service could restore the player into an invalid campaign context", lines 83-84). TAOM: DischargeService.cs has no settlement handling; the whole restore is MobilePartyAttachmentAdapter.RestorePresence (lines 60-85) — IsActive/IsVisible/SetMoveModeHold/camera only. `MoveIntoSettlement` (MobilePartyAttachmentAdapter.cs:160-181) exists but `grep -rn MoveIntoSettlement Main/` shows zero callers — it is dead code. BUT the symptom attribution does not hold up. The mechanically damaging half of the donor method is its ExitToLast fallback, which is gap #3, not this. Materialising at the settlement's map position with CurrentSettlement==null is a legal vanilla party state, not a stranding — nothing about it blocks clicks (S3 was root-caused to the leaked PlayerEncounter; EncounterManager.HandleEncounterForMobileParty, installed 1.4.7 L38, gates on IsActive/AttachedTo/MapEventSide/CurrentSettlement/PlayerEncounter, none of which this sets). Severity corrected HIGH→MED: a real dropped polish behaviour the donor added deliberately after a reported bug, but it does not produce S3 or S4 on its own.

**Minimal fix:** Port RF:6717-6747 into `DischargeService`: capture `_commander.GetSnapshot(record.CommanderHeroId)`'s current settlement before `Record.Reset()`, and after `RestorePresence()` call an adapter `EnterSettlement(settlementId)` + `SwitchTo(town|castle|village)` when one exists, else `ExitToLast()`.

---

### [MED] Commander-in-settlement follow was dropped entirely; snapshot fields + adapter methods are dead code

**Verifier:** CONFIRMED with two factual corrections. Donor code is real and as quoted: RF:6428-6452 handles `commanderParty.CurrentSettlement != null` FIRST (same-settlement → success; else `RestorePlayerPartyCampaignPresence()` + `EnterSettlementAction.ApplyForParty` + `ActivateCommanderServiceWaitMenu()`), `IsPlayerInCommanderServiceMode` (RF:6937-6950) does return false when either party is in a settlement so the maintenance loop keeps firing, and `ServiceWaitMenuOnTick` re-runs it at RF:1226-1237. TAOM side confirmed: `ServiceAttachmentService.Assess` (:18-50) has no settlement branch — it tests only state, IsCaptive, commander fitness, PartyIsInMapEvent, player.IsInMapEvent, player.LooksParked. `IMobilePartyAttachmentAdapter.MoveIntoSettlement` / `LeaveSettlement` (MobilePartyAttachmentAdapter.cs:160-201) genuinely have zero callers. The gate-position mechanic is verified against 1.4.7 (MobileParty.cs:598-604: `Position = (IsCurrentlyAtSea ? PortPosition : GatePosition)`), so SyncPosition does leave the player hidden outside the gate for the whole stay. CORRECTION 1: 'read by NOTHING — a grep for both across Main/ finds no consumer' is false for `SettlementId` — `Main/Features/Enlistment/Duties/FieldDutyRuntime.cs:73` reads `commanderSnapshot.SettlementId`. Only `PartyIsInSettlement` is unread. CORRECTION 2: 'not a defender if the settlement is besieged' does not follow. I checked 1.4.7 — `MobileParty.IsActive` is set false only in `RemoveParty` (MobileParty.cs:3517) and `PlayerCaptivity`, never on settlement entry, so the commander snapshot keeps `PartyIsActive=true`; when the siege map event starts, `Assess` returns BattleJoinRequired and `ServiceBattleService.TryJoin` seeds from `mapEvent.AttackerSide/DefenderSide.LeaderParty` (EncounterAdapter.cs:136-144), which resolves a settlement defender. Joining still works. Net effect is real but bounded: the follow does not break, the player is just parked at the gate instead of inside, so it contributes to S1 but not to S4 (invisible while parked) and not to S2.

---

### [LOW] #8 TAOM redirects encounter/join_encounter on state alone; donor exempts both when the commander is in battle

**Verifier:** CONFIRMED as a code difference, downgraded to LOW because no mechanical path to S1/S2 exists today. Donor `ShouldForceCommanderServiceMenu` is at :6885-6916 exactly as quoted, including the `hiddenServiceMode || _pendingCommanderAttachment` requirement at :6900-6903 and the `(menuId == "join_encounter" || menuId == "encounter") && commanderParty.MapEvent != null -> return false` carve-out at :6905-6908. TAOM's `TryRedirectMenu` (EnlistmentMenuService.cs:27-40) gates only on `State != EnlistedAttached`, and both ids are in the default RedirectMenuIds (IEnlistmentConfigProvider.cs:27-28) with no commander-MapEvent exemption. But the three routes the gap names are all closed at present: TAOM's own join flips to EnlistedBattle (ServiceBattleService.cs:66) before `EnsureMenuOpen("encounter")` at :86, so the redirect is off for it; while EnlistedAttached the main party is parked IsActive=false/IsVisible=false (MobilePartyAttachmentAdapter.cs:46-47) so vanilla encounter detection skips it; and the rollback path re-parks. The reviewer concedes this itself ("latent... once G1/G2 add pumps this becomes an active hazard"). So: real defensive gap, correct to fix alongside #2's pump, but it explains neither S1 nor S2 as shipped.

---

### [LOW] #9 Stranded-PlayerEncounter sweeper runs hourly and only in EnlistedAttached

**Verifier:** PARTLY CONFIRMED, downgraded to LOW — narrowing (3) is factually wrong. Narrowings (1) and (2) hold: TAOM's sweeper is inside ReconcileHourly and requires `record.State == EnlistedAttached` (EnlistmentReconciler.cs:166-174), so it cannot run in EnlistedBattle, CommanderUnavailable, EnlistedDetachedOnDuty or EnlistedPlayerCaptive, whereas the donor's has no state concept at all. The ConversationContext claim also holds — grep of Main/ returns exactly one `CurrentConversationContext` reference, in MessengerCampaignBehavior.cs:392, none in the Enlistment feature, versus the donor's wait-don't-abandon returns at RF:994-998 and RF:1030-1034 (both verified in place). Narrowing (3) is wrong: execution does not stop after the sweep. Control falls through to `switch (assessment.Status)` at EnlistmentReconciler.cs:176, and `AttachRequired -> _attachment.EnsureParked(...)` runs at :190-193 in the SAME pass. Closing the encounter cannot stale the assessment computed at :150 because `ServiceAttachmentService.Assess` never reads HasPlayerEncounter (it reads IsCaptive, commander fitness, PartyIsInMapEvent, player.IsInMapEvent, LooksParked — ServiceAttachmentService.cs:18-50). So TAOM does chain sweep -> re-park, and the claim that "the hour after the sweep the player is still unparked" is false. Symptom attribution is also off: S3 (cannot click lords/settlements after leaving service) is the leaked-oath-encounter bug already root-caused and fixed, not this sweeper's scope.

---

### [LOW] #6 Menu redirect fires on state alone — no party-presence gate and no battle-menu exemption

**Verifier:** Both quotes are verbatim-accurate. Donor ShouldForceCommanderServiceMenu at 6885-6915 carries `bool hiddenServiceMode = !mainParty.IsActive && !mainParty.IsVisible; if (!hiddenServiceMode && !_pendingCommanderAttachment) return false;` and `if ((menuId == "join_encounter" || menuId == "encounter") && commanderParty.MapEvent != null) return false;`; IsCommanderServiceNativeMenu at 6917-6935 is the id list as quoted. TAOM's TryRedirectMenu (EnlistmentMenuService.cs:27-50) gates on `_store.Record.State != EnlistmentState.EnlistedAttached` alone, and "encounter"/"join_encounter" are in the default RedirectMenuIds (IEnlistmentConfigProvider.cs:26-27). So the code difference is REAL. But the claimed harm is not demonstrated: the successful-join path transitions to EnlistedBattle at ServiceBattleService.cs:66 BEFORE EnsureMenuOpen("encounter") at :86, and the early-outs at :44-58 push no encounter menu at all — while parked (IsActive=false) the engine does not generate player encounters either. I could not construct a confirmed path where the missing guards actually swallow a battle. Real as a difference, speculative as a symptom cause; the S1/S2 attribution is unsupported.

---

### [LOW] #9 Wait-menu text is static — no live status, no commander activity, no background

**Verifier:** CONFIRMED as a factual difference. Donor UpdateServiceWaitMenuText (~1283-1308) builds the multi-line block with Rank / Current duty / Status / Active assignment / Days served / Service XP / Trust exactly as quoted, GetCommanderTravelStatusText (1310-1330) returns "commander engaged in battle" / `$"inside {CurrentSettlement.Name}"` / "marching toward a settlement" / "moving to engage the enemy" / "raiding hostile lands" / "sailing with the fleet", and both are called every tick from 1234-1241. TAOM: EnlistmentWaitMenuPresenter.cs:27-28 is the single static sentence, refreshed only from OnWaitMenuInit (EnlistmentMenuBehavior.cs:97); `grep -rn "SetBackgroundMeshName|BackgroundMesh" Main/Features/Enlistment/` returns nothing. But this is presentation only — it changes nothing mechanical, and "explains S1" is a subjective read of a symptom that has real mechanical causes elsewhere (#1, #2, #4). Downgraded from MED.

---

### [LOW] #10 No settlement-menu affordances while enlisted — donor adds status/renew/discharge/drill options to town and castle

**Verifier:** CONFIRMED. Donor registers matched "town"/"castle" pairs at 164-195 (rf_enlistment_join_*), 198-229 (status), 232-263 (renew, gated on CanRenewContract), 266-297 (discharge), 300-331 (training, gated on CanTrainToday → TrainWithTroops) — all as described. TAOM registers nothing on any vanilla settlement menu (the same two-hit AddGameMenuOption grep). Real, but the lowest-value item on the list: it is entirely downstream of #4 — because TAOM never moves the enlisted player into the commander's settlement, the "town"/"castle" menus are never reached while enlisted, so adding options there would change nothing until settlement-following exists. Fix #4 first; this is a follow-on.

---

### [LOW] #4 `EncountersBlocked` omits the settlement clause of EncounterManager:38, so the diagnostic cannot see the state that causes S3

**Verifier:** The code facts are exact. PlayerPresenceSnapshot.cs:35-36 is verbatim as quoted; `SettlementId` is declared at :15, populated at MobilePartyAttachmentAdapter.cs:148 (`settlementId: main.CurrentSettlement?.StringId`), printed in `Describe()` at :44, and never folded into the verdict. The engine gate it claims to mirror is `EncounterManager.HandleEncounterForMobileParty` (EncounterManager.cs:36) and does contain `(mobileParty.CurrentSettlement != null && !mobileParty.IsGarrison)`; it also uses `mobileParty.MapEventSide != null`, not `MapEvent != null`, so the `IsInMapEvent` point is real too. DischargeService.cs:85-90 is the sole consumer, as claimed. TWO corrections though. First, this is NOT a donor gap — the report itself concedes the donor has no such diagnostic, so it fails the 'behaviour the donor HAS that TAOM DROPPED' bar the task set. Second, and more important: an incomplete diagnostic predicate cannot CAUSE S3. It can only fail to report it. The stated 'claims to explain: S3' is wrong; the cause is #2/#3. Real as a verified defect, but diagnostic-only.

---

### [LOW] No settlement-menu escape hatch — the donor offers status/renew/leave from town and castle menus, TAOM offers nothing outside the wait menu

**Explains:** S3, S5

**Donor:** `RFEnlistmentCampaignBehavior.cs:198-298 (status/renew/discharge options on both "town" and "castle")`

**TAOM:** `absent — Main/Features/Enlistment/Hooks/EnlistmentMenuBehavior.cs:73-88 registers options only on `EnlistmentMenuService.ServiceWaitMenuId`; grep for `AddGameMenuOption` across Main/Features/Enlistment returns only those two lines`

**Verifier:** The absence is CONFIRMED. Donor RF:198-296 registers six options as quoted — rf_enlistment_status_town/castle (RF:198-229), rf_enlistment_renew_town/castle (RF:231-261), rf_enlistment_discharge_town/castle (RF:263-294) — all `settings.EnableRecruitmentStub && ...`, plus RenewContract at RF:1430-1439 and CanRenewContract at RF:1425-1428. TAOM registers menu options only on EnlistmentMenuService.ServiceWaitMenuId (EnlistmentMenuBehavior.cs:73, :81 — the only two AddGameMenuOption calls in the feature), and `grep -rn -i renew Main/Features/Enlistment` returns only the unused `DischargeReason.ContractNotRenewed` enum member (DischargeReason.cs:32) and its DischargeConsequenceService.cs:37 case — there is no renewal path anywhere. BUT the claimed value does not survive scrutiny. The "second route out, independent of the wait menu" argument requires the enlisted player to reach a `town`/`castle` menu, which neither mod permits: both hold the player in a wait menu under StartWait/UnstoppableFastForward, and TAOM additionally redirects town_wait/town_outside/castle_outside back to the wait menu (IEnlistmentConfigProvider.cs:18-31). And the missing renewal is harmless in TAOM's design — nothing happens at contract expiry except that ClassifyLeaveReason starts returning PlayerRequest, so an un-renewed term simply continues. S3 is post-service (not this) and S5 is gap #1's classifier (not this). Severity corrected MED→LOW: a genuine dropped surface worth restoring alongside gap #2, but it explains no reported symptom on its own.

**Minimal fix:** Register `town`/`castle` mirrors of the two wait-menu options (status + leave) in EnlistmentMenuBehavior.OnSessionLaunched, gated on `_store.Record.IsEnlisted`, following RF:198-298.

---

### [LOW] Donor runs a sub-hourly maintenance tick that re-asserts the park/follow invariant; TAOM has NO real-time tick at all

**Verifier:** CONFIRMED as a code fact, but the severity and reasoning are substantially overstated. Verified true: donor RF:69 `MaintenanceIntervalSeconds = 0.25f`, RF:129 `CampaignEvents.TickEvent.AddNonSerializedListener(this, OnTick)`, RF:837-883 body all read exactly as quoted; the 1.4.7 cadence math is right (`Campaign.TickMapTime` L840-874: `_dt = 0.25f*realDt`, `MapTimeTracker.Tick(4320f*num)`; `CampaignEventDispatcher.Instance.Tick(_dt)` L960). Verified true: a grep for AddNonSerializedListener across Main/Features/Enlistment/ returns 21 hits, none of them TickEvent or QuarterHourlyTickEvent. THREE errors in the report. (1) The title is false as written — TAOM does have a real-time tick, `EnlistmentMenuBehavior.OnWaitMenuTick`. (2) The load-bearing 'donor syncs continuously, TAOM only from the menu' contrast is backwards: the donor's OnTick does NOT sync a parked player. RF:870 `if (!_pendingCommanderAttachment && !NeedsCommanderAttachment()) return;` early-exits, and `NeedsCommanderAttachment` (RF:6749) returns false whenever `IsPlayerInCommanderServiceMode` holds (RF:6937 — hidden+inactive). The donor's ONLY continuous position sync is `ServiceWaitMenuOnTick` → `SyncPlayerPartyToCommanderServiceMode(commanderParty)` at RF:1239 — i.e. exactly where TAOM put it (EnlistmentMenuBehavior.cs:100-107). Sync is parity, not a gap. (3) The retry is further throttled by `_nextAttachmentRetryHour = now + (attached ? 0.25f : 1f)` in CAMPAIGN HOURS, so the donor's effective re-attach cadence is 15 in-game min on success and a full in-game hour on failure — identical to TAOM's hourly on the failure path, not '3.3x finer'. The residual real difference is therefore only re-park LATENCY when the player is loose: donor ~15 in-game min vs TAOM ≤60 in-game min, and TAOM's hourly path does self-heal (EnlistmentReconciler.cs:190-193 AttachRequired → EnsureParked; Assess accepts EnlistedBattle too, ServiceAttachmentService.cs:20). The 'visible on the map, stranded' framing does not follow from drift while parked — ParkNear sets IsVisible=false/IsActive=false (MobilePartyAttachmentAdapter.cs:46-47), so a drifting parked party is invisible. The one genuinely visible window they name (post-battle: OnCommanderBattleEnded returns while `_encounter.HasCurrent`) is a distinct missing-repark bug, not a tick-cadence bug, and the on-disk ServiceBattleService.cs changed during this review (now 152 lines) to add `ReassertServiceMenu` on that exact path. Does not explain S1 or S2 at all.

---

## Refuted — do NOT act on these

### #4 No retry budget or backoff — one attachment attempt per campaign hour

REFUTED — two load-bearing claims are wrong. (a) The donor's failure cadence is the SAME as TAOM's. RF:876-882 reads `_nextAttachmentRetryHour = now + (attached ? 0.25f : 1f)` in CAMPAIGN hours; on a FAILED attach the donor's tick path also waits a full campaign hour, so "a fraction of a second at map speed" describes only the post-SUCCESS re-attach, not the failure retry the gap is about. TAOM's post-success maintenance is in fact TIGHTER, not looser: the wait-menu tick runs SyncPosition every 5 frames (EnlistmentMenuBehavior.cs:103-106) versus the donor's 0.25-campaign-hour re-attach. (b) The cited trigger is factually false on the installed 1.4.7. The gap says a park fails "when the commander's party is momentarily inactive — e.g. inside a settlement". `MobileParty.IsActive` is a plain auto-property (MobileParty.cs:356) and the only `IsActive = false` in the type is inside `RemoveParty()` (MobileParty.cs:3517, party destruction); `EnterSettlementAction` never touches it (its only party mutation is `mobileParty.CurrentSettlement = settlement`, EnterSettlementAction.cs:82) and neither does the `CurrentSettlement` setter (MobileParty.cs:577-620). A commander inside a town stays IsActive==true, so `FindCommanderParty`'s `party.IsActive` filter (MobilePartyAttachmentAdapter.cs:208) does not reject it and the quoted PARK FAILED line at :37 does not fire for that reason. The only residual truth here — no immediate retry on the urgent edges — is gap #6, not a separate finding.

---

### #7 No menu-escape pump: TAOM can only rewrite a menu id as it is being pushed

REFUTED — the reviewer missed TAOM's second interception point. `Main/Features/Enlistment/Hooks/MapStateEnterMenuModePatch.cs:64-71` is a Harmony POSTFIX on `MapState.EnterMenuMode` that reads `__instance.GameMenuId` after menu mode has already been entered and calls `GameMenu.SwitchToMenu(redirected)` — its own doc comment says it exists "for menu ids that reached the map state WITHOUT passing through GameMenuManager.SetNextMenu (some native paths write MapStateData.GameMenuId directly)". That is precisely the "rescue a player already sitting in a native menu" behaviour the gap says is absent, and precisely the bypass case ("pushed by an engine path that does not go through GameMenuManager.SetNextMenu") the gap says is uncovered. The gap's supporting observation is technically true but misleading: TAOM reads `IGameMenuAdapter.CurrentMenuId` in one place, but it reads the equivalent `MapState.GameMenuId` in the patch above, and `GameMenuAdapter.EnsureMenuOpen` (GameMenuAdapter.cs:133-161) reads CurrentMenuId three more times. A narrower residual gap does exist — neither patch re-evaluates while the player REMAINS in a menu whose eligibility changes underneath them (e.g. state demoting EnlistedBattle -> EnlistedAttached while sitting in an aftermath menu) — but that is not the gap as stated, and it is a much smaller claim than "the player sits in army_wait with no pump watching".

---

### #1 Oath Finish() fires on a NON-terminal dialog line; the donor finishes only on the conversation's last line

Both code quotes are accurate (donor RFEnlistmentCampaignBehavior.cs:2050-2055 + 2058-2063; TAOM EnlistmentDialogBehavior.cs:67-74 -> EnlistmentService.cs:94-100), but the MECHANISM is refuted. ConversationManager.ProcessSentence (installed v1.4.7, C:\Users\mikew\.taom-src\v1.4.7\TaleWorlds.CampaignSystem.Conversation.ConversationManager.cs:276-294) calls `conversationSentence.RunConsequence(Game.Current)` at line 294 — at the moment the sentence is DISPLAYED, after `UpdateCurrentSentenceText()`. So the donor's `FinalizeEnlistmentConversation` also runs mid-conversation: its own line ("Then rise and take your place...") still has to render and be clicked through before `close_window`. The claim that the donor's Finish runs 'as the conversation terminates, with no further dialog line to render' is false. Worse for the gap: the donor's consequence at TAOM's EXACT dialog position — `CompleteEnlistmentWithConversationLord` (donor:2194-2213) on the 'I swear it' PLAYER line — calls `TryAttachPlayerToCommanderDuty(showFeedback:false)` (donor:6395), which in the field case reaches `SyncPlayerPartyToCommanderServiceMode(commanderParty); ActivateCommanderServiceWaitMenu();` (donor:6505-6506). The donor therefore hides/deactivates MainParty and switches menus on that same non-terminal player line. The residual difference between donor and TAOM is exactly one NPC line, and I found no mechanism by which that boundary produces S1/S3/S4. The real defect at this site is the missing ownership gate, which is gap #2 — not the line position.

---

### #6 `IsCommanderRelatedParty` (army-leader / attached-party equivalence) has no TAOM analogue

The type-absence is factually correct — donor `IsCommanderRelatedParty` is verbatim at RFEnlistmentCampaignBehavior.cs:7206-7230 and is used at :988, :1025, :2238, :6975; TAOM has no army/attached-party equivalence, and `EncounteredPartyId` (EncounterAdapter.cs:23) genuinely has zero consumers (grep over Main/ and TAOM.Tests/ returns only the interface declaration and the property). But the gap does not stand as an independent behavioural difference. Sub-claim (b) is demonstrably wrong: TAOM's reuse test is `PlayerEncounter.Current != null && PlayerEncounter.EncounteredBattle == mapEvent` (EncounterAdapter.cs:120). `EncounteredBattle` is the MapEvent object, and an encounter seeded against the commander's ARMY LEADER of that same event still satisfies `EncounteredBattle == mapEvent` — so it IS recognised as reusable and returns true at :121. The army-leader case is covered by battle identity; it is never 'torn down and re-created every attempt'. Sub-claim (a) is gap #2 restated and sub-claim (c) is gap #5 restated. With (b) refuted and (a)/(c) duplicated, nothing independently symptom-causing survives.

---

### #10 Settlement exit before joining is narrowed to besieged settlements only, so a player with residual CurrentSettlement can never join

The code quotes are accurate on both sides — donor RFEnlistmentCampaignBehavior.cs:6609-6616 applies the besieged-settlement exit with no attacker/defender split, and TAOM narrows it to `if (side.Value == PartyBattleSide.Attacker) _encounter.LeaveSettlementIfUnderSiege();` (ServiceBattleService.cs:76-77, adapter at EncounterAdapter.cs:180-202). But the stated causal chain is refuted. `PlayerEncounter.JoinBattle` -> `JoinBattleInternal` (installed v1.4.7, PlayerEncounter.cs:929-960) sets `PartyBase.MainParty.MapEventSide = EncounteredBattle.GetMapEventSide(side)` directly and never consults `EncounterManager.HandleEncounterForMobileParty`. The `(mobileParty.CurrentSettlement != null && !mobileParty.IsGarrison)` clause at EncounterManager.cs:36 therefore does NOT 'keep the party out' of the join, and the claimed link to S1/S2 does not hold. The attribution is also misplaced: the donor's actual non-besieged settlement exit is `LeaveSettlementAction.ApplyForParty(mainParty)` inside `TryAttachPlayerToCommanderDuty` (donor:6491-6496), not the `PlayerEncounter.LeaveSettlement()` at 6609-6616 — and that missing exit is already covered by gap #3. The residual-CurrentSettlement problem is real, but it belongs to #2/#3; this narrowing is not the mechanism and, as the report itself notes, is independently defensible.

---

### Release does not clear Army / AttachedTo — the donor unbinds both, TAOM's restore path unbinds neither

The donor quote is accurate (RF:6681-6714, verbatim) and TAOM's RestorePresence (MobilePartyAttachmentAdapter.cs:60-85) genuinely never touches Army or AttachedTo — but this is not a behavioural difference on any reachable path, so it fails the "concrete behavioural difference" bar. `grep -rn AttachedTo Main/ --include=*.cs` shows TAOM only ever CLEARS it (MobilePartyAttachmentAdapter.cs:42-43) or reads it (:133, :150; PlayerPresenceSnapshot.cs; EncounterAdapter.cs:131-135 comment); nothing sets it. `grep -rn "\.Army" Main/Features/Enlistment Main/Adapters` shows Army is only READ (ArmyRhythmProbeAdapter.cs:33, WarEventSnapshotAdapter.cs:139) — TAOM never joins the commander's army, and the vanilla join-army dialog is suppressed while enlisted by LordConversationsJoinArmyConditionPatch (LordConversationsConditionPatches.cs:37-48). MainParty.AttachedTo is engine-set only through army membership, which cannot be reached here. The named counter-paths do not produce it either: ServiceBattleService.cs:66-93 joins a MapEvent, not an army, and its rollback re-parks through ParkNear which clears AttachedTo; EnlistmentLoadNormalizer.cs:31-77 only calls RestorePresence/ParkNear via the reconciler. The engine claim itself checks out (EncounterManager.HandleEncounterForMobileParty L38 in installed 1.4.7 does gate on `mobileParty.AttachedTo != null`), so this is worth adding as two cheap defensive lines — but it is hardening, not the shipped defect, and it does not explain S3, which was already root-caused to the leaked PlayerEncounter.

---

### CORRECTION: EnforceCommanderServiceMode is dead code AND degenerate — do not port it

NOT A GAP — and correctly so; this entry reports the absence of a gap, so there is no dropped donor behaviour to confirm. Its factual claims all check out. `grep -rn EnforceCommanderServiceMode` over the whole donor tree returns exactly one hit, RFEnlistmentCampaignBehavior.cs:6330, the definition; it is on no tick and no call site. The body at RF:6330-6357 is quoted verbatim and all four branches do terminate in the same `RestorePlayerPartyCampaignPresence()` call, so wiring it would unpark the player unconditionally. The distributed live version is also described accurately: `RestorePlayerPartyCampaignPresence` (RF:6359-6381) and `PreparePlayerPartyForCommanderBattle` (RF:6383-6390) are called at the top of `TryJoinCommanderEncounterBattle` (RF:6589-6593) and `TryCreateCommanderBattleEncounter` (RF:6630-6634), with `wasHiddenServiceMode` re-parking via `SyncPlayerPartyToCommanderServiceMode` at RF:6601/6642/6656/6674. TAOM's `ServiceBattleService.TryJoin` (:66-68 state → RestorePresence → SyncPosition; :99-112 rollback with Finish + forced transition + EnsureParked) does reproduce that contract. Recording real=false because the schema asks whether a gap genuinely exists, and this entry's own conclusion — correctly — is that it does not.

---

