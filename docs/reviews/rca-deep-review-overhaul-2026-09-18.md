# RCA: /deep-review overhaul and the XSD gate (deep review, 2026-09-18)

## Top-line

The work (#621) rebuilt `/deep-review` around a read-only senior `deep-reviewer` agent, a Design & Elegance lens whose proposals Step 4 applies, an XML lens, and a new XSD gate, `tools/validate_xml_schemas.py`. Its own deep review ran on the new agents in two waves (engine claims, tool correctness, cross-document data flow and design, then standards plus completeness). Every finding below was re-read in source or reproduced before it was fixed; the tool fixes each went RED then GREEN in `tools/tests/test_validate_xml_schemas.py` (23 tests became 47). Three findings sit in pre-existing data or code outside the change and were filed instead of fixed (#619, #620, #622), per the changed-code-only rule.

The dominant pattern: **a change to a contract was checked against the shapes its author had in mind, not against every consumer and every input.** The launch table, the trigger sweep, the tool's input boundary and the first docstring's glob rule all failed that way.

## Findings + Root Cause Table

| # | Sev | Bug | Category | Why Missed | Preventive Action |
|---|---|---|---|---|---|
| F1 | MED | The new launch table had rows for C#, XML and both, and none for a harness-, docs- or tooling-only change. This very changeset matched no row, and the Step 2c tooling agent fired only for scripts that write, so a read-only gate had no lens. | Closed enumeration | The table was written from the two shapes under discussion and never tested against the change it belonged to. | Per-lens launch predicates (every shape defined, a lens with nothing to do reports NOT IN SCOPE); a `tooling.md` lens for every script, read-only included. |
| F2 | MED | `/ship` lines 3, 13 and 14 still told sessions to skip XML-only changes, and five other files carried stale counts or facts (`new-culture-authoring.md` "5 parallel agents", `mcp-servers.md` "C# review", the operating manual's "v1.5.2" dump, the stocktake effort list without `xhigh`, the ports rule's April field list). | Incomplete consumer sweep | The update pass edited the lines already known to mention `/deep-review` (ship line 20) instead of grepping every consumer of the changed rule. | Lens 5 gained a harness mode: trace every changed declaration (agent names, trigger rules, counts, flags) to all consumers repo-wide. Same shape as the #618 "grep the old VALUE" lesson. |
| F3 | MED | The XSD gate could report PASS while checking nothing or the wrong thing: a nonexistent or directory path, a DOCTYPE (the engine loads such a file as nothing), an empty folder registration, a file from an unscanned module, three SubModule.xml shapes the engine throws on, a malformed SubModule.xml (traceback), an unreadable file (traceback). | Gate reports clean on bad input (repeat offender) | The tests covered the engine-mirroring logic, not the input boundary. The repo already carries this lesson several times (`UPGRADE_INDEX_EMPTY`, "a gate that inspected nothing must fail"); the tooling agent that would have asked only fired for writers. | Each case now has a two-direction test; `tooling.md` asks "silent clean" and "exit-code contract" of every script. |
| F4 | MED | `lxml` imported at module top, unguarded. CI's tools-tests job installs nothing, so the new test module errored at import, and the CLI died with a traceback (exit 1, which the tool defines as "a file failed"). | Optional dependency | `tools/audit_polearm_shield_parity.py` does the same, and CI's "Python Tool Tests" job already fails on it (run 35005853072, 2026-09-15: `No module named 'lxml'`, `FAILED (errors=5)`); the sibling copied was the wrong one (`check_external_xslt.py` imports lxml lazily). | Guarded import, exit 2, `skipUnless` on the lxml test classes; `tooling.md` item 11. |
| F5 | MED | Agent 7's command `validate_xml_schemas.py [--module <live dir>] <files>` replaced the repo module, so a mixed file list printed NOT REGISTERED for every repo file and still ended PASS. | Instruction encodes a trap | The instruction was written against the flag's name, not its semantics (`--module` replaces the default). | The tool infers each file's own module; the gate table lives once in `.claude/rules/moduledata-validation.md` "Gate per file kind". |
| F6 | MED | Every lens moved to Fable at max effort while the skill still said "launch ALL in a SINGLE message" and "NO LIMIT". A memory note and today's CHANGELOG both record fan-out width exhausting the usage limit, and a subagent that hits it returns nothing. | Cost multiplied, width unbounded | The seniority change was made without revisiting the launch width it multiplies. | Waves of at most four, defect lenses first; Step 2c expansions follow in fours and prefer an existing gate script. |
| F7 | LOW | The first docstring stated ".NET's `*.xml` matches an extension starting with xml, so `foo.xmlbak` loads" as universal. It holds only on volumes with 8.3 short names: reproduced on the .NET Framework CLR, C: returns `foo.xml, foo.xmlbak`, E: (game, Armory, repo) returns `foo.xml`. | Unmeasured environment claim | Taken from the documented .NET quirk without measuring on the drive the game runs from. | Docstring corrected (the tool keeps the superset on purpose); lesson in `lessons/xslt-moduledata.md`. |
| F8 | LOW | `check-deep-review.sh`'s mute grepped for lens names `log-agent.sh` never writes: 0 matches in both log generations, so it never muted. | Consumer of another hook's output, untested | Written against expected log content, never against a line the writer produced. | Pattern now `agent_type=deep-reviewer`; `tools/test_hooks.sh` section 7 drives the real writer and reader in both directions; lesson in `lessons/build-tooling-workflow.md`. |
| F9 | LOW | `docs/features/moduledata-validation.md` told readers to keep `family_type` on `<Horse>` as real-but-undeclared, while the #364 lesson already said `HorseComponent` never reads it. | Doc contradicts a lesson | The #364 fix corrected one code comment and recorded the lesson without searching docs for the claim. | Doc corrected with the three read sites and vanilla's usage; lesson "grep `docs/` for a refuted claim". |
| F10 | LOW | Replacing the em dash in `/ship`'s `description:` with a colon made its YAML frontmatter invalid (`: ` starts a mapping); `tools/test_hooks.sh` 3b failed. | Style fix changed syntax | The output-style rule offers a colon as the first replacement for a dash, and frontmatter is YAML. | Reworded with parentheses; `output-style.md` now says so; 3b caught it, so frontmatter edits are followed by `bash tools/test_hooks.sh`. |
| F11 | LOW | Standards: the agent description summarised instead of saying when to use it; Step 4 restated the §B cadence; `harness-facts.md`'s "Last verified" stayed 2026-08-05; three rewritten SKILL.md lines kept long dashes; the feature doc cited lxml's line numbers (698/727/767) for attributes on 697/726/766 and overstated "mirrors exactly". | Authoring hygiene | Written quickly around the load-bearing parts. | Fixed; the standards lens now has a harness checklist (H1 to H6). |
| F12 | LOW | Tool edges: `strip()` also stripped NBSP for the `_replaceWhileMerging` exemption; an XSD include by URL would be fetched; `--live` ignored `BANNERLORD_GAME_MODULES`; an uncompilable XSD compiled once per file; several behaviours untested. | Edge handling | Not asked. | Fixed with tests. |
| F13 | HIGH, pre-existing | `MISSING_COLLISION_BODY` has never fired: `validate_moduledata.py` defines `missing_collision_body_issues` but `main()` never calls it, so the hook's `--code` line blocks nothing. It guards the #352 infinite-load class. | Gate wired into docs and hook, not into the code path | `CommitGateCoverageTests` checks that a code string appears in source, not that `main()` reaches it. Found by the builder adding `SCHEMA_INVALID` beside it; no lens was scoped to it. | Filed #622 with the fix and an end-to-end test through `main()`; measured 0 issues today, so wiring it blocks nothing. |
| F14 | LOW, process | No GitHub issue existed for the work. | Process | Built before an issue was filed. | #621 filed retroactively. |

## Root Cause Pattern: a contract checked against its author's shapes

F1, F2, F3, F5 and F7 share one mechanism. Each change defined or described a contract (which lenses run, when `/deep-review` triggers, which files a gate reads, what a flag does, which files a glob matches) and was checked only against the cases its author was thinking about. The consumers and inputs outside that picture (a harness-only changeset, a sibling doc with an old count, a nonexistent path, a mixed file list, a volume without short names) were never exercised. The fix is the same each time: enumerate the consumers or inputs mechanically (grep every consumer, test every input class, measure on the target volume) instead of reasoning from the design.

## Why the lenses missed or caught these

This was the work's first review, so "missed" here means what each lens did not see.

- **Engine claims (Lens 2, engine-claims mode):** caught F7 and the "mirrors exactly" overstatement, and settled the open question by reading `rgl_log`: the engine does validate on a campaign load. Not scoped to tool inputs (F3) or to docs consistency (F2).
- **Tool correctness (tooling plus Lens 3):** caught F3, F4, F12 and the Agent 7 trap. Did not see F13: `validate_moduledata.py` was not in its scope.
- **Data flow (Lens 5, harness mode improvised for this review):** caught F1, F2, F5, F6 and F8's mechanism, the widest yield, as in every earlier review. It did not check YAML validity (F10 came later, during the fixes).
- **Design (Lens 6):** proposed the lens files, the waves, the predicates, the `SCHEMA_INVALID` pass and module inference, all applied.
- **Standards plus completeness (Lenses 1 and 4, adapted):** caught F9's lesson link, F11, F14, the missing catalog rows and the missing tests.
- **F13** was found by the builder during the fixes, outside every lens's scope; **F10** was introduced during the fixes and caught by `tools/test_hooks.sh` before the convergence pass.

## Feedback memories to codify

None beyond the three lessons written (`build-tooling-workflow`: the dead mute; `xslt-moduledata`: a refuted claim still in a doc, and the 8.3 glob) and the output-style note for YAML frontmatter.

## Second review: the #622 and #619 fixes (same day)

Mike asked for #622 and #619 to be fixed rather than filed. The fixes (#622: `main()` calls the collision-body pass; #619: seven dead registrations removed from the live `TAOM_Map/SubModule.xml`; a crash found while testing) got their own `/deep-review`, the first run that spawned reviewers from the lens files: wave 1 Agents 2, 5, 7 and 3, wave 2 Agents 4, 6 and the tooling lens. The live edit was confirmed byte-faithful and engine-neutral (every removed id is still registered by SandBoxCore or SandBox, so no merged document changes), and Mike confirmed the live install loads. Findings, all fixed or filed:

| # | Sev | Bug | Category | Why Missed | Preventive Action |
|---|---|---|---|---|---|
| S1 | MED | The #619 test asserted only that registrations resolve; the mirror (`lotraom-assets/v1.5/TAOM_Map`) and the release still ship all eight over 197-245 byte stub files, which resolve, so a resync passed it. | Gate tests the wrong property | Written against the live install's shape (stubs gone since 2026-09-15) without checking the source the edit would be reverted from. | The test pins the registered id set to `Settlements`; proven to fail on the mirror's shape. Mirror left as is by Mike's choice; #619 stays open for that half. |
| S2 | MED | With one unparsable pack and one missing body, the pass byte-scanned all 4,611 packs (22.6 GiB, 110-119 s), past the hook's 45 s bound, so the gate would answer "ask: UNCHECKED" in the #599 case it exists for. | Cost on the gated path | The pass was written and tested in isolation; wiring it into the hook changed its time budget and nobody measured the fallback. | Only unparsed packs are byte-scanned (a parsed pack's TOC already lists its bodies, #352); tested with a spy on the scan. The CLI's same default is #623. |
| S3 | MED | "The validator still takes 9.5 s / run time unchanged" (CHANGELOG, feature doc): it went from about 5.9 s to about 9 s. `armory-ref-audit.md`'s "4.8 s" was measured with the pass never running. | Unmeasured claim | One post-change timing was compared with a remembered number instead of an A/B. | Corrected from the lenses' A/B runs. |
| S4 | MED | Five places said the MCP tool and `/verify` see `MISSING_COLLISION_BODY`; only the commit hook runs it (`taom_query.validate` runs the schema engine alone, `/verify` runs no validator). | Docs describe intended wiring | Written when the pass was added, against the plan. | The two current-tense code docstrings, `/armory-audit` and the wiring doc now say the hook only; parity filed as #623. |
| S5 | LOW | An exception inside the new pass would exit 1 and the hook would block the commit with no findings listed. | Error path of a gate | The pass had no guard, unlike the generator pass beside it. | A scan that raises becomes one ERROR saying the bodies were NOT verified and why; tested. The general crash exit code for the hook is #623. |
| S6 | LOW | The crash fix wrapped a string in `Path` at the one use site; `missing_ref_roots` held strings while `extra_ref_roots` beside it held Paths. | Fix at the symptom | The minimal fix was applied where it crashed. | `Validator.missing_ref_roots` now holds Paths; the existing test asserts it directly. |
| S7 | LOW | New `main()` tests asserted "non-zero" where the hook distinguishes 1 (block) from 2 (allow), and ran real generator subprocesses. | Test precision, hermeticity | Copied a looser pattern. | Exact exit codes; the generator pass is mocked; one shared `_run_main` helper replaces three copies. |
| S8 | LOW | Step 1's live sweep searched `ModuleData/` only, so the edit to `TAOM_Map/SubModule.xml` at the module root was invisible to it. | Sweep scope | The sweep was written for data files. | Step 1 now names `TAOM_Map/SubModule.xml` beside the sweep. Another session rewrote the same sweep that afternoon (the Armory's root `SubModule.xml`, both `Assets/` trees, and a start time taken from the change's own last review, because the audit log's latest `deep-reviewer` line can be another session's review). |

Pattern: S1 to S4 each checked a claim against the state in front of the author (the live install, the pass on its own, one timing, the plan) instead of the state that decides it (the source a resync restores, the hook's budget, an A/B, the call sites). #622 itself was the same mistake, which is why `CommitGateCoverageTests.test_every_issue_pass_is_reached_from_main` now pins the call graph and the lesson is in `lessons/testing-qa.md`.

Why the lenses caught or missed these: Agent 5 (data flow) and Agent 7 (XML) both found S1 from the mirror; Agent 2 (engine) verified the engine neutrality and S3 but called the #619 test sufficient, because it reasoned about the live state only (resolved in the report by re-running the mirror check). Agent 3 and the tooling lens found S2, S5 and S7, and the tooling lens corrected wave 1's assumption that a crash lets the commit through (it blocks it with no findings). Agent 6 proposed S6, the shared helper, the reach test and the pinned id set, all applied. Agent 4 found the owed lesson, issue records and handbook rows; the handbook still describes the eight-row manifest the mirror ships, which stays accurate while the mirror is unchanged. S8 was found by the orchestrator running Step 1.
