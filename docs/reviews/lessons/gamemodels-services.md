# Lessons — GameModels & Services

> Category file of the master lessons record — index + house shape: [LESSONS-LEARNED.md](../LESSONS-LEARNED.md). **Append new GameModels & Services lessons HERE** (`### rule` → `**Why missed:**` → `**Prevent:**` → `**Source:**`).

### A single SyncData string must stay under 32,767 UTF-8 bytes — chunk any unbounded/growing string payload
The engine's `ArchiveSerializer.SerializeEntry` (v1.4.6) writes each save-archive entry's byte length as `WriteShort((short)Data.Length)` — a signed int16 truncation — then writes the data in full. So **any single `SyncData` string whose UTF-8 payload exceeds 32,767 bytes is written with a wrong length and CORRUPTS THE SAVE at write time** (unloadable next launch: `ArgumentException: Source array was not long enough` inside `ArchiveDeserializer.LoadFrom` for >65,535 B, or `OverflowException` for the 32,768–65,535 B negative-int16 range). WarOfTheRingMomentum (#327) serialized its whole event log — up to 100 events/type × 6 types × 2 sides, each with a full localized description — as ONE `SyncData` string; it crossed 32 KB around day ~50 and every subsequent save was bricked. The fix (`MomentumSyncChunker`) splits the JSON across N chunk keys ≤10,000 chars each; the per-chunk cap makes corruption impossible regardless of log size, with no data or gameplay change.
- **Why missed:** the #327 deep-review + Codex passes verified persistence *round-trip fidelity* (does the state survive save/load?) and never checked the *byte size* of any single synced string against the archive-entry limit. The single-string transport was itself a fix for a `Dictionary<string,string>` container that didn't round-trip `IDataStore` at ~1,000 entries — it was reviewed as a fix, not as a new risk, and traded a data-loss bug for a worse save-corruption bug. The limit is undocumented and only bites at scale, so nothing in the suite surfaced it.
- **Prevent:** never sync an unbounded or growing string via a single `SyncData` key. Chunk it (count key + `_{i}` chunk keys, each capped so worst-case UTF-8 ≤ 32,763 B: 10,000 UTF-16 chars × 3 B/char = 30,000 B), or use proper saveable containers of small entries (the engine handles many small entries fine — only a *single* entry >32,767 B is fatal). Add a test that builds the realistic MAX state, asserts it exceeds the limit as one string, and asserts every chunk stays under it. `SaveLoadDiagnostics` (Patch61) stamps `[SaveLoad] GraphFault kind=archiveParse` at this exact site; `tools/inspect_sav.py` / `repair_sav_strings.py` triage + recover already-bricked saves offline.
- **Source:** docs/reviews/rca-momentum-save-corruption-2026-07-07.md (multi-user v2.0.9 "A problem occured while trying to load the saved game."; engine `ArchiveSerializer.cs:27`).

### An MCM master-off / player-scope promise must be verified per ENGINE SEAM, not per service method
A feature whose "off = exact vanilla" and "applies to player caravans only" promises are spread across multiple Harmony/GameModel seams can hold the promise in every SERVICE method (each folds `Enabled` / the player flag) yet still break it, because the SEAMS differ in what they can do. CaravanTrade (Codex 2026-07-04) had three distinct failures the per-method toggle-coverage check missed: (C1) one lever MUTATED persistent engine state (`_*VeryFarCache`) once at session start, so a mid-session master-off did not revert it — "folds the toggle in its service call" ≠ "reverts live" when the effect is a cached write; (C2) the range-envelope lever is engine-GLOBAL (the ceiling getter has no per-caravan context) so `ApplyToPlayerCaravans` cannot scope it, yet the MCM hint promised "off = only NPC caravans change"; (C3) the war-gate's player detection is faction-level (`caravanFaction == Hero.MainHero.MapFaction`, matching vanilla's own marker) while the other levers are clan-level, so the same toggle means different things per lever.
- **Why missed:** the deep-review MCM-toggle-coverage agent enumerated every service method and confirmed each reads `Enabled` — a per-method check. It never asked (a) does the effect PERSIST past the toggle read (mutation vs per-call gate), or (b) can each engine SEAM actually honor the scope the MCM HINT advertises. A user-facing hint is a spec and needs the same coverage as behavior.
- **Prevent:** for any multi-seam feature with a master-off or per-entity-scope promise, build a per-SEAM table: {seam, gated-live-or-mutates-state, scope-granularity-it-can-support (global / faction / clan / per-entity), what the MCM hint claims}. Fix the seam to gate live (prefer patching a read-each-time getter over a write-once cache), or make the hint honest about what that seam can't scope. Live-toggle rule of thumb: scale on READ, never mutate the cached field.
- **Source:** docs/reviews/rca-caravan-trade-2026-07-04.md (Codex MED C1/C2/C3). Sibling: the culture-fallback for player-founded kingdoms (C4) reuses the "consult the sibling feature" lesson under Adapters & TaleWorlds API.

### Never dereference `Campaign.Current` in editor-mode code paths
In code that runs in editor mode (where `Campaign.Current` may be null), avoid TaleWorlds struct properties whose getters lazy-resolve via a global singleton — `CampaignVec2.Face` calls `Campaign.Current.MapSceneWrapper.GetFaceIndex(this)` and NREs when `Campaign.Current` is null. Prefer the raw scalar accessors (`ToVec2()`, `AsVec2`, `GetGroundVec3()`); for face/navmesh resolution route through the `Scene` reference the editor script holds, not through `Campaign.Current.MapSceneWrapper`.
- **Why missed:** EditorCacheRebuild Codex review #38 caught the P2 — `SettlementSnapshotStore.Save` read `s.GatePosition.Face.FaceIndex` for diff comparison, inheriting the pattern blindly from runtime-mode code. Vanilla's own editor cache builder (`SettlementPositionScriptNavigationCache`) never touches `.Face` on `CampaignVec2` — it uses `Scene` directly.
- **Prevent:** When accessing a TaleWorlds struct in editor-mode code, decompile the property getter body (not just the signature) before relying on it; if it dereferences `Campaign.Current` / `Mission.Current` / `Game.Current` or any global singleton, treat the property as runtime-only. Known offender: `CampaignVec2.Face`. Add new offenders to this list when found during a port.
- **Source:** memory/feedback_campaign_coupled_property_in_editor.md

### A "fall back to default" fail-safe must cover runtime-unresolvable inputs, not just load-invalid ones
When a config value is validated at the load boundary (reject empty/dup/unknown) but its *resolvability* is only knowable later (against the live `MBObjectManager` / engine), a value that passes load validation can still resolve to nothing at runtime. Trace the consumer's behavior for that case: an empty *resolved* result must hit the intended default, not whatever stale/global state the consumer held before. Validation-at-load ≠ produces-a-usable-result.
- **Why missed:** Codex review 2026-06-27 (custom-battle curated commanders). The "fall back to default" fail-safe was implemented only at the provider/load layer (empty/whitespace ids → faction not registered → default). A curated faction whose ids are all syntactically valid but unresolvable (typo / removed lord) kept `HasCuratedEntry == true`, `SideCommanderFilter` skipped every null, and the UI patch's `count==0` early-return left the dropdown on the global unfiltered list. The data-flow agent traced "unresolvable ids are warn+skipped" and marked it CONNECTED without asking what the consumer shows when *every* id is skipped.
- **Prevent:** For any "curated overrides default" feature, (1) filter the curated values by existence/resolvability before returning them and fall through to the default path when none survive; (2) add a test for the all-unresolvable case asserting the default path is taken; (3) add a shipped-data regression test cross-checking the real config against real id sources. Extend Agent-5 rule 2c reasoning from "is the collection populated?" to "does the collection's *resolved* form produce a usable result, and if not, is the fallback correct?"
- **Source:** docs/reviews/rca-custom-battle-lords-2026-06-27.md

### Push custom GameStates via `GameStateManager.CreateState<T>()`, never `new`
When pushing a custom `GameState` subclass onto `Game.Current.GameStateManager`, always use `manager.CreateState<MyState>()` then `manager.PushState(state)` — never `new MyState() + PushState`. `CreateState<T>` runs `HandleCreateState`, which assigns `state.GameStateManager = this` (else the state is orphaned) and notifies listeners including `GameStateScreenManager.OnCreateState` → `CreateScreen(state)`. `PushState` alone only activates an already-existing screen, so `new + PushState` skips screen creation entirely (and any `CreateScreen`-prefix screen substitution never fires) — the feature is a silent no-op.
- **Why missed:** Codex review #36 (FiefManagement port, 2026-05-06) — Claude wrote `Game.Current.GameStateManager.PushState(new FiefManagementGameState(fief))`; the entire feature would have been a silent no-op because the Patch36 screen-substitution prefix only fires through `CreateState`.
- **Prevent:** Give the GameState a parameterless ctor (required by the `new()` constraint on `CreateState<T>`) plus an `Initialize(...)` method; create via `CreateState<T>()`, call `Initialize`, then `PushState`. Vanilla precedents: `InventoryScreenHelper.OpenInventory` (`CreateState<InventoryState>()`), `CraftingHelper`. Before writing the consequence callback, decompile `GameStateManager.PushState`, `HandleCreateState`, and `OnCreateState` to confirm the creation pathway.
- **Source:** memory/feedback_gamestate_creation_pattern.md

### Validate input BEFORE a fallback-returning lookup, never after
When a lookup function MAY return a "default"/"fallback" value for invalid input (sentinel + warning log), the caller MUST validate the input's validity BEFORE the lookup whenever the result feeds an allow-list / security / correctness decision. The fallback is for logging-and-survival, not for acceptance — validating the lookup's OUTPUT (`is the returned name in the allowed list?`) doesn't catch the case where the allow-list happens to include the fallback default.
- **Why missed:** Codex review #33 (CharacterCreation race-filter, 2026-05-06) — `RaceManager.GetRaceNameFromId(int)` (`RaceManager.cs:111-132`) silently returns `"human"` for unknown IDs with only a warning. `SetPlayerRace` checked that fallback name against the culture's allow-list; for cultures allowing `"human"` (Mordor, Isengard, Gundabad, Dol Guldur, all vanilla cultures) it preserved the original junk integer. Claude trusted `GetRaceNameFromId` to return a meaningful string per ID without reading the fallback path.
- **Prevent:** For every `GetXxxFromId` / `LookupXxx` / `Resolve` call, read the body; if it logs-and-returns for invalid input, that value is fallback not validation. Pair every lookup with a validator on the same interface (`IRaceManager.IsValidRaceId(int)`, `IMBObjectManager.Contains*`) and run the validator BEFORE the lookup. For new services, expose both `Get*FromId` and `IsValid*Id`. Regression test: lookup returns the fallback → assert the caller rejects (`SetPlayerRace_InvalidFaceGenRaceId_DoesNotPreserve_FallsBackToCultureDefault`). Rule: `.claude/rules/csharp-architecture.md` "Lookup Functions With Fallbacks: Validate Before Lookup (MANDATORY)".
- **Source:** memory/feedback_validate_before_lookup_with_fallback.md (TAOM.Tests/Features/CharacterCreation/CharacterCreationContentServiceTests.cs)

### Settings/MCM Clamp helpers must NaN/Infinity-guard
The simple clamp ternary `value < min ? min : value > max ? max : value` returns NaN when `value=NaN` (both `NaN < min` and `NaN > max` are false). NaN then propagates through downstream math (`5f * (1f - NaN) = NaN`) and `comparisonValue < NaN` is always false, freezing whatever comparison gates the next state transition. Every settings-provider clamp must defensive-default on NaN/Infinity.
- **Why missed:** Codex adversarial review of SmartCavalryAI (2026-05-06). The 5-agent /deep-review mechanically verified the `[0..1]` range for `ChargeFormationStrictness` but never enumerated NaN as an input class. A corrupted MCM save / community config producing NaN would freeze cavalry in `Forming` or `Reforming` for the entire battle — no log, no recovery short of mission end. Loader-side validation doesn't help: `TaomSettings.Instance` is populated by MCM at runtime, not from a JSON file.
- **Prevent:** Replace `Clamp(float v, float min, float max)` with `SafeClamp(float? v, float defaultValue, float min, float max)`: `var x = v ?? defaultValue; if (float.IsNaN(x) || float.IsInfinity(x)) return defaultValue; return x < min ? min : x > max ? max : x;`. MCM/SettingsSingleton wrappers are providers too. Reference fix: `Main/Features/SmartCavalryAI/SmartCavalryAISettingsProvider.cs` `SafeClamp`.
- **Source:** memory/feedback_clamp_nan_infinity_propagates.md (docs/reviews/codex-adversarial-smartcavalryai-2026-05-06.md)

### Editor-visible fields on engine-discovered classes ARE config — validate them
Public user-tunable fields (with or without `[EditableScriptComponentVariable]`) on classes inheriting `ScriptComponentBehavior` / `GameModel` / `CampaignBehaviorBase` are functionally identical to JSON/XML config: user-editable, untrusted, flowing into comparisons and native engine calls. Apply the same validation — `TAOM.Core.Validation.FiniteFloatValidator.IsFinite*` BEFORE any range check, sign-flip rejection on directional fields, log + fallback on failure.
- **Why missed:** Codex review (scene-scripts CS_Road, 2026-05-13) caught `Width <= 0f` not rejecting NaN — NaN comparisons return false in IEEE-754, so NaN sneaks past every range check; same `ElevationOffset`/`RepeatU`/`RepeatV` flowed to native `Mesh.AddTriangle`. The `csharp-architecture.md` "Config Providers MUST Validate" rule had the countermeasure but its documented scope was JSON/XML providers only — editor-visible fields weren't classified as config. This was the THIRD shipment of the same NaN bug.
- **Prevent:** For every editor-visible `public float`/`double` field add `FiniteFloatValidator.IsFinite(field)` at the consumer entry point; for `int`/`bool` fields apply range/value-set validation if used as an index, enum lookup, or FSM selector; apply the same gates to `TaomSettings.*Action` boundary lambdas. Bug history: Career cooldown review #31, EditorCacheRebuild review #38, scene-scripts CS_Road 2026-05-13.
- **Source:** memory/feedback_editor_fields_are_config.md (docs/reviews/rca-scene-scripts-cs-road-2026-05-13.md)

### Every player-affecting feature needs an independent player MCM opt-out
Every feature that changes the player's experience must expose an MCM toggle the player can use to turn it off. When the feature gates both player and AI, ship INDEPENDENT "Apply To Player" and "Apply To AI Lords" toggles — not just a single AI toggle plus an all-or-nothing master. The player must be able to exempt themselves while leaving AI gated (and vice versa).
- **Why missed:** Standing instruction from the user, 2026-06-17, on AlignmentRecruitment — *"There should always be an option to turn this off in the MCM for the players also."* The feature had shipped with a master toggle (`EnableAlignmentRecruitment`) + an AI toggle (`EnableAlignmentRecruitmentAi`) but no player-only disable.
- **Prevent:** Default toggle set = a master enable plus per-side `ApplyToPlayer` / `ApplyToAi` bools (each default true). Wire each through the settings provider (`TaomSettings.Instance?.X ?? jsonDefault`) and gate independently in the pure service (`if (isPlayer && !ApplyToPlayer) return inert; if (!isPlayer && !ApplyToAi) return inert;`). The config-provider's field-by-field `sanitized` copy must include the new bool or the JSON value is silently dropped to default — pin it with a valid-JSON parse test. Mirror the committed AlignmentRecruitment pattern.
- **Source:** memory/feedback_player_mcm_optout_toggle.md

### The code path that DISABLES a feature must not sit behind the disable guard
A master toggle promising "off = feature gone" must be able to RETRACT already-applied state (a spawned UI layer, an injected model, a running effect), not just skip NEW work. The classic trap: the retract call lives inside the same tick/handler that early-returns on `!Enabled`, so once the toggle flips off the handler short-circuits before ever reaching the retract — the applied state persists forever. Every runtime read that reflects the feature's visibility/activeness must ALSO fold the master toggle, not just the feature-specific sub-toggle.
- **Why missed:** WotR-momentum (#327, 2026-07-03). `MomentumEnabled` off should hide the on-map meter, but the only `RemoveMapMeter()` caller (`RefreshMapMeter`) sat AFTER `OnDailyTick`'s `if (!MomentumEnabled) return`, so a mid-war disable never reached it; and the indicator VM's `IsIndicatorVisible` folded only `ShowMapMeter`, not `MomentumEnabled`. The meter stayed on screen. Same family as CombatMechanics 2026-07-02 (`GetHorseChargePenetration` returned the tuned value with the master off) — there an unfolded read, here an unreachable retract.
- **Prevent:** For any master toggle whose hint promises "off = no feature," (1) run the retract/teardown path BEFORE the enabled-guard (or on a handler the guard doesn't gate), so flipping off actively removes applied state; (2) fold the master toggle into EVERY runtime visibility/active read, not just the feature's own sub-toggles; (3) add a test/trace: "toggle off mid-run → applied state removed." Agent-5 master-toggle-fold check (rule 2b) must extend from "is the value read folded?" to "can the DISABLE actually execute, or is it behind the disable guard?"
- **Source:** docs/reviews/rca-wotr-momentum-2026-07-03.md finding #2

### No aspirational enum values or state fields
When designing an enum, status report, or per-element outcome type, do NOT add values "for future extensibility" unless there is a concrete caller in the same PR that produces or consumes them. Reserved-for-future names become dead code that either misleads (UI built around an always-zero count) or gets deleted, breaking the unstated future-extension promise. Both outcomes are bad.
- **Why missed:** Deep-review #5 (EquipPresets, 2026-05-06) flagged the data-flow inconsistency — `SlotApplyOutcome.SlotLocked` was declared, had a `case` arm in the service switch, and `PresetLoadResult.SkippedLockedSlots` was a result field, but across the entire codebase nothing returned `SlotLocked` (the adapter only returned Equipped/ItemMissing/ModifierMissing/HeroNotFound/Failed) and `SkippedLockedSlots` was always empty. Design-time reasoning was "future-extension hook for IsLocked enforcement." Caught by deep-review Data Flow Tracing (Agent 5).
- **Prevent:** Every enum value needs ≥1 producer AND ≥1 consumer in the same PR; every status/outcome field must be populated by some path AND read by some consumer; delete `// reserved for X` comments rather than writing them. Test is the gate — if you can't write a test exercising the value/field, it's not real yet. The OK counter-case: a future-tense value with a same-PR producer even if the consumer is just `_logger.LogDebug`. Siblings: `feedback_user_facing_promise_must_match_code.md` (MCM hint drift), `feedback_dont_defer_high_review_findings.md`.
- **Source:** memory/feedback_no_aspirational_enum_values.md

### TaleWorlds computed getters NRE before your `!= null` guard — route through one null-safe chokepoint
A TaleWorlds computed getter (`X => A.B`) dereferences the nested object inside its own body, so `if (party.Culture != null)` is useless — the getter throws the NRE before the comparison runs. `PartyBase.Culture` is `MapFaction.Culture` with no null guard (`PartyBase.cs:255`) and `MapFaction` returns null for a faction-less party. Guard the INNER object: write `party.MapFaction?.Culture`, never `party.Culture` then null-check. `Hero.Culture` and `Settlement.Culture` are plain `[SaveableField]` fields (safe once parent non-null); `PartyBase.Culture` / `PartyBase.BasicCulture` are computed (unsafe) — decompile the getter, don't assume.
- **Why missed:** RCA `docs/reviews/rca-culturefeat-partyculture-nre-2026-06-15.md` — campaign-map hard crash, faction-less Gordan party during `Army.OnSiegeStarted` strength calc. Three compounding factors: (1) TAOM copied vanilla `PartyBaseHelper.HasFeat`'s precedence verbatim, inheriting the unsafe `party.Culture` call; vanilla calls `HasFeat` rarely while TAOM calls party-culture resolution on EVERY party-size/morale/speed/food/upgrade calc (5+ GameModels), reaching faction-less parties vanilla never touches. (2) Fixing TAOM's own boundary does NOT protect the vanilla `base.X()` calls — `DefaultArmyManagementCalculationModel.DailyBeingAtArmyInfluenceAward`, `DefaultBattleRewardModel.CalculateRenownGain`, `DefaultPartySpeedCalculatingModel.CalculateFinalSpeed`, `DefaultPartyTroopUpgradeModel.GetGoldCostForUpgrade` all call the same throwing `PartyBaseHelper.HasFeat`, so the NRE fires inside the base call before TAOM code runs. A data-flow sweep scoped to `Main\` is blind to this. Codex caught the base-call exposure; the fix author + 7 review agents all missed it.
- **Prevent:** One null-safe chokepoint every model routes through — `CultureFeatAdapter.ResolvePartyCulture(PartyBase)` = `party.LeaderHero?.Culture ?? party.MapFaction?.Culture ?? party.Owner?.Culture ?? party.Settlement?.Culture` (preserves vanilla precedence, every step `?.`). When you copy a vanilla helper, list the entities vanilla calls it on vs the entities TAOM will. When you override a GameModel that calls `base`, decompile the base method and audit what it dereferences. For the vanilla-base-call exposure the root fix is a Harmony Prefix on the shared helper itself (`PartyBaseHelper_HasFeat_Patch` in `Patch18_CulturalFeats`: `__result = ResolvePartyCulture(party)?.HasFeat(feat) ?? false; return false;`) — one patch fixes every base + future caller instead of reimplementing N base methods. Deep-review Agent 2 (API compat) and Agent 5 (data flow) are positioned to catch this. In-game confirmed 2026-06-15, committed 0046eaf, #281. Related: `feedback_widening_settlement_type_gate_audit.md`, `feedback_ported_data_upstream_bugs_vanilla_baseline.md`.
- **Source:** memory/feedback_taleworlds_computed_getter_nre_route_through_chokepoint.md (docs/reviews/rca-culturefeat-partyculture-nre-2026-06-15.md)

### Cross-feature handshake when two features share an adapter and one writes back state
When feature A and feature B both patch the same `TaleWorlds.MountAndBlade.X` class via the same shared `IXxxAdapter` AND at least one writes back state (`SetX` / `SetPositioning` / `SetMovementOrder`), they silently fight each other every frame. /deep-review reviews features in isolation, so this cross-feature data-flow gap is invisible to per-feature audits. Default handshake rule: more-specific feature wins over more-general.
- **Why missed:** Codex adversarial review of SmartCavalryAI (2026-05-06). MixedFormations (Patch30) and SmartCavalryAI (Patch31) both attach to `Formation`. SmartCavalryAI's `ApplyChargeLine` calls `Formation.SetPositioning` for a wide cavalry line; MixedFormations' Prefix on `GetOrderPositionOfUnit` then runs per-unit and overrides those positions if the formation has a layout. Horse-archer cavalry (≥10 units, ≥20% ranged: Khuzait, Easterlings, Rohirrim) satisfies MixedFormations' is-mixed heuristic, so the cavalry line is silently undone for exactly the factions where it matters most. Tick order doesn't help — Patch30 fires per-unit on every position query, not at tick-start.
- **Prevent:** Before writing the second feature, inventory which TaleWorlds types it patches, which OTHER TAOM features patch them, which of those WRITE back state (read-only Postfixes don't conflict), and for each writer/other pair design the handshake — the "loser" excludes the "winner"'s domain (MixedFormations added `if (formation.RepresentativeIsCavalry) return false;` to its mixed-formation detector AND `ComputeUnitPlanePosition`). Document the handshake in both features' docs. Test: stub the adapter to return the second feature's state (`formation.RepresentativeIsCavalry.Returns(true)`) and verify the first feature excludes it. Reference fix: `Main/Features/MixedFormations/FormationLayoutService.cs`.
- **Source:** memory/feedback_cross_feature_handshake_via_shared_adapter.md (docs/reviews/codex-adversarial-smartcavalryai-2026-05-06.md)

### Overriding a per-party capability GameModel must mirror the engine's cross-party propagation
A per-party capability `GameModel` is NOT a per-party-isolated decision — the engine propagates and recomputes the result across the army attachment tree and the on-land/at-sea lifecycle, so a naive gate desyncs armies. For naval: `MobileParty.IsCurrentlyAtSea` setter loops `_attachedParties` (force-propagates), `NavigationCapability` getter recomputes each party from its OWN `HasNavalNavigationCapability`, and on transition `FinishNavigationTransitionInternal` re-paths each attached party via `ComputePath(MoveTargetPoint, NavigationCapability, ...)`. Mirror the donor's attached-to inheritance and encode the lifecycle (already-at-sea ⇒ capable regardless of toggles, to reach land).
- **Why missed:** Codex review 2026-06-24 (NavalTravel #296). `TaomPartyNavigationModel.HasNavalNavigationCapability` first keyed only on `mobileParty.IsMainParty`; with `ApplyToAi=false` a player-led army's attached AI parties reported non-naval, but the engine forced them to sea with `Default`-only navigation → couldn't path on water → stranded/desync. The donor (NavalDLC `NavalPartyNavigationModel`) encoded attached-to inheritance (a non-leader inherits its army leader's capability); the TAOM port dropped that limb when it swapped the ship-gate for a config-gate. Rewriting ONE method of an otherwise-faithful engine-model port consumed the audit budget and the method's OTHER behavioral limbs flew through unaudited (same shape as `feedback_native_port_hot_path_audit`). /deep-review missed it; Codex caught it.
- **Prevent:** Before overriding a per-party capability/navigation GameModel, decompile the engine consumer of the result and trace how it PROPAGATES (army/attached parties) and RECOMPUTES (per-party getters). Mirror the donor's attached-party inheritance (non-leader inherits `AttachedTo.HasNavalNavigationCapability`). Encode the lifecycle: unified rule `if (isAtSea) return true; if (!enabled) return false; if (CanSail) return true; return !isMain && attachedLeaderCanSail;`. When you keep N-1 limbs of a faithful port and rewrite limb N, re-audit limb N's ENTIRE behavior. Extract the decision to a pure service method (`HasNavalCapability(isMain, isAtSea, attachedLeaderCanSail)`) and pin with a decision-matrix test (GameModel boundary is in-game-only per ADR-008). RCA: `docs/reviews/rca-navaltravel-2026-06-24.md`.
- **Source:** memory/feedback_gamemodel_capability_engine_propagation.md (docs/reviews/rca-navaltravel-2026-06-24.md)

### Research the engine SCALE before modifying any `AgentDrivenProperties`
Before writing any `agentDrivenProperties.X += value` / `*= value`, decompile the vanilla code that CONSUMES that property to understand its scale, range, and gameplay effect. API existence checks pass while the modification fails at the semantic level.
- **Why missed:** Phase IV career abilities shipped with 3 critical unit-scale bugs — cavalry charge added raw 25 to a normalized 0.001-scale property (+25000% damage), infantry damage reduction modified `ArmorEncumbrance` (affects movement weight, not incoming damage), and tuning XML mixed decimal fractions with whole-number percentages. All passed API existence checks but failed semantically because the engine's use of the values was never checked.
- **Prevent:** After verifying a property EXISTS via `ilspycmd`, also decompile the vanilla GameModel/Mission code that READS it; inspect the baseline value range in-game before adding buffs; for damage-path effects trace through `ApplyDamageReductions` / `ApplyDamageAmplifications`, not stat properties; standardize all tuning XML as whole-number percentages and have executors always divide by 100.
- **Source:** memory/feedback_engine_scale_research.md

### Decompile the SETTER/METHOD BODY before mutating TaleWorlds VM or guarded engine state
Before assigning to any TaleWorlds VM property post-construction (`SelectedIndex`, `SelectedItem`, `Index`, `Current*`), decompile the setter body — not just the signature. Surface API verification is insufficient and vanilla-pattern mirroring is dangerous when the lifecycle context differs. The same extends to METHODS whose entire body is wrapped in a guard, and to every vanilla method that mutates the same state on the same code path (decompile the call chain, patch the LAST writer).
- **Why missed:** Three compounding instances. (1) Codex review #30 (CustomBattles filter+cap, 2026-05-04): `ItemList.Clear() + AddItem(*N) + SelectedIndex = 0`, but `SelectorVM<T>.SelectedIndex` setter has `if (value != _selectedIndex)` — vanilla inits player side `_selectedIndex = 0`, so the assignment was a no-op; `SelectedItem` kept pointing at a removed `CharacterItemVM` and the battle launched with the wrong commander. Vanilla `RefreshValues()` uses the same sequence but at construction time when `_selectedIndex == -1`. (2) CCBodyProperties 2026-05-06: `CharacterObject.UpdatePlayerCharacterBodyProperties` wraps its entire body in `if (IsPlayerCharacter && IsHero)` and doesn't call base, so a /deep-review agent quoted what was INSIDE the guard while omitting the guard itself — accurate snippet, wrong behavior. (3) Second CCBodyProperties iteration: `OnCultureSelection → InitializePlayersFaceKeyAccordingToCultureSelection` is a SECOND vanilla writer firing moments after ours, four hops downstream of the patched entry point, clobbering our body with the culture default.
- **Prevent:** Decompile the setter/method body and look for `if (value != _backingField)` early-returns, `_onChange?.Invoke`/`OnPropertyChanged` fired only inside the guard, and wrapping `if`/`try`/`lock`. Identify the lifecycle context — what value does the backing field hold when your code runs? Use the type's `Refresh()` escape hatch, or cache `AccessTools.Field` once and `field.SetValue(vm, sentinel)` before reassignment (never intermediate-value `prop = -1; prop = 0;` — many setters crash on the sentinel). For guarded engine methods, write the underlying state directly AS WELL as calling the high-level method. Map the entire call chain mutating the same state and patch the LAST writer (postfix the highest-level template method, e.g. `OnCultureSelection`). Cross-reference 1.2 vs 1.3 hook locations for refactored CC code (sealed `CharacterCreationContent` moved overwrite logic to stage VMs). Rule: `.claude/rules/gui-ui.md` "TaleWorlds VM property setters: verify no-op early returns". Reference: `Main/Features/CustomBattles/Hooks/CommanderSelectorRebuilder.cs`, `Main/Features/CharacterCreation/Hooks/CharacterCreationCultureStageVM_OnCultureSelection_Patch.cs`.
- **Source:** memory/feedback_taleworlds_vm_setter_decompile.md

### Overriding one getter in an engine derived=A op B family breaks every combination/read site
When you Harmony-patch / override ONE getter that is an operand of a TaleWorlds derived-value relationship (`derived = A op B`), grep every call site of the sibling operands AND every direct reader of the getter, and audit each — they live in unrelated files and a per-file review of your patch never sees them. The override looks correct in isolation; the breakage only appears at the combination/read site.
- **Why missed:** TroopWeight phantom-wounded bug (2026-06-07). The feature Postfix-weighted `PartyBase.NumberOfAllMembers` (correct for party-size budgeting) but left the sibling `PartyBase.NumberOfHealthyMembers` unweighted (it feeds gameplay — battle supply, casualty tracking, sacrifice limits). FOUR vanilla display surfaces compute `wounded = NumberOfAllMembers − NumberOfHealthyMembers` in three unrelated files (`CampaignUIHelper`, `GameMenuPartyItemVM`, `PartyBaseHelper`); the weight surplus rendered as phantom wounds ("62 troops / 16 wounded", no battle). The engine never stores "displayed wounded" — every consumer recomputes `all − healthy`. SECOND leak (SettlementFood, 2026-06-18): `DefaultSettlementFoodModel` reads `GarrisonParty.Party.NumberOfAllMembers / 20` DIRECTLY for garrison food (a direct read, not a combination); weighting is wrong for food (food is headcount) → elite garrisons ate 2-3× intended → chronic starvation. Same family as `feedback_review_blindspots` (files-in-isolation) and `feedback_cross_feature_handshake_via_shared_adapter`.
- **Prevent:** Before overriding a getter in a family like `{NumberOfAllMembers, NumberOfHealthyMembers, NumberOfWoundedTotalMembers}` (or any `total = healthy + wounded` / `net = gross − x` triple), decompile and grep the OTHER operands' call sites AND `grep -r "\.NumberOfAllMembers"` across decompiled vanilla for ALL direct readers; classify each (party-size-budget = intended vs food/AI-strength/capacity/wages = probably not). When display-only, add a display-only Postfix that rewrites the combined output — do NOT weight the gameplay-feeding sibling globally. Fix shape: `TaomSettlementFoodModel : DefaultSettlementFoodModel` adds back `(weighted − raw)/divisor` so the food term uses the RAW count while the global getter stays weighted. Cache note: prefer `ConditionalWeakTable<EngineObj, box>` over `Dictionary<int hashcode, ...>` for per-engine-object caches (reference-keyed, auto-evicting). RCAs: `docs/reviews/rca-troopweight-phantom-wounded-2026-06-07.md`, `docs/reviews/rca-settlement-food-2026-06-18.md`; feature: `docs/features/settlement-food.md`.
- **Source:** memory/feedback_weighted_getter_in_derived_family.md (docs/reviews/rca-troopweight-phantom-wounded-2026-06-07.md, docs/reviews/rca-settlement-food-2026-06-18.md)

---

### GameModel override bodies allow no inline decision logic — extract to a service
A GameModel override body is binary: either a single constant/expression (`MaxCharacterTier => 10`) OR boundary conversion (perk check, adapter wrap) plus a direct delegate to an injected service. Any `if` / `foreach` / `switch` / branching `yield return` / multi-line computation inline is a violation regardless of line count — the <150-line cap is a ceiling, not the test.
- **Why missed:** Codex flagged `TaomSiegeEventModel` (defender trebuchets, 2026-04-20 review) — a 6-line `yield return` with one conditional perk branch, rationalized as "trivial enough to skip the service" by pointing to `TaomCharacterStatsModel` (`MaxCharacterTier => 10`) as precedent. But that model is a literal constant, not decision logic; the deep-review Standards agent made the same error and passed the file.
- **Prevent:** If the override body has `if`/`foreach`/`switch`/branching `yield`/>1 computed expression, extract to a service — don't negotiate "trivial length." The only legal inline ops are perk checks, adapter wrapping, and a direct service call. A Standards reviewer must inspect the body, not accept "PASS — under 150 lines." Rule: `.claude/rules/gamemodels.md` Rule 4.
- **Source:** memory/feedback_gamemodel_inline_logic.md

### No service locator inside services / VMs / engines — constructor-inject
`IoC.Resolve<T>()` / `ResolveAll<T>()` inside a service, ViewModel, engine, helper, mixin, or hook violates the constructor-injection rule. Resolve only at boundaries: Harmony patch static methods, `ScreenBase` subclasses, `CampaignBehaviorBase` ctors, `GameModel` ctors, `SubModule.cs`, and static `OpenXxx()` helpers. A `try { IoC.Resolve<T>() } catch {}` guard is worse than an exception — it silently returns null in production and hides test-time failures.
- **Why missed:** Review #26 (2026-04) — `CareerScreenVM` resolved `ICareerConfigProvider` inline and guarded `IModLogger` with try/catch; DryIoc isn't configured in unit tests, so 8 `CareerScreenVMTests` threw NRE past the "no career set" guard. The try/catch had been seeded as a "minor convenience," normalizing the anti-pattern. The rule existed in `csharp-architecture.md` but wasn't mechanized in the standards agent.
- **Prevent:** Add every dependency as a constructor parameter; resolve at the caller and pass down. Standards agent greps `IoC.Resolve<` outside boundary classes (Agent 1 rule #9). Never treat pre-existing test failures as "background noise" — investigate immediately.
- **Source:** memory/feedback_no_service_locator_in_services.md

### Don't mirror an existing standards violation into net-new code
When a sibling method already breaks a rule (inline `IoC.Resolve` + branch, inline decision logic, a wrong fallback order), copying its shape into a NEW method propagates the defect — and rule-4 / service-locator fire on the fresh code regardless of the pre-existing one. Treat an adjacent method as a *pattern to audit*, not a *pattern to clone*.
- **Why missed:** deep-review (2026-06-26, career phantom-passive wiring) — `TaomPartyHealingModel.GetDailyHealingHpForHeroes` was written by mirroring the file's pre-existing `GetSurvivalChance`, inheriting its inline `IoC.Resolve<ICareerPassiveService>()` + branch (a service-locator + rule-4 violation) AND a reversed `LeaderHero ?? Owner` vs `Owner ?? LeaderHero` resolution order. The sibling "looked canonical" because it shipped.
- **Prevent:** before copying an adjacent method, grep it against `gamemodels.md` rule 4 + the no-service-locator rule. If the sibling violates, write the new method to the *correct* pattern (ctor-inject + delegate to the service's `ApplyFactor`/`ApplyFlat`) and, when cheap, fix the sibling in the same pass to remove the inconsistency. Constructor-inject the dependency even if the registration is one extra `IoC.Resolve` at the `SubModule.cs` call site.
- **Source:** docs/reviews/rca-career-phantom-passives-2026-06-26.md

### A boundary class reading `TaomSettings.Instance` directly is NOT a service-locator/ADR-008 defect
The `*SettingsProvider`-injection pattern (~30 TAOM features wrap MCM reads in one) is for SERVICES and unit-tested classes. A boundary — GameModel, Harmony patch, runner/orchestrator, static IL-call target — reading `TaomSettings.Instance?.X ?? default` directly is fine and well-precedented: `TroopWeight/Hooks/*` (×8), `TaomSettlementFoodModel`, `PartyIconScaleConfig` all do it. Don't flag it as a violation — the consumer isn't unit-tested, so a provider adds an interface + class + IoC registration for **zero testability win** (`simplicity-criterion.md` reject: tiny win + added complexity).
- **Why missed:** deep-review Standards Agent 1 (2026-06-25) flagged `ShaderPrecompileRunner.Begin()`'s direct read as an ADR-008 violation, over-generalizing the service-only convention to a boundary. Claude disputed with grep evidence; Codex independently disputed the same suspect. The flag burned two reviewers' effort on a non-defect.
- **Prevent:** before flagging a direct `TaomSettings.Instance` read, classify the reader. Service / VM / engine / tested class → provider required (the existing rule). Boundary (GameModel / Harmony patch / runner / static IL target, per ADR-008, game-only) → direct read is acceptable; the runner declares its boundary status in its own header comment.
- **Source:** docs/reviews/rca-shader-precompile-phase0-2026-06-25.md

### `MissionBehavior` subclass must not declare `BehaviorType => Logic` unless it inherits `MissionLogic`
A class inheriting `MissionBehavior` (not `MissionLogic`) MUST return `MissionBehaviorType.Other`, never `Logic`. `Mission.AddMissionBehavior` does `MissionLogics.Add(behavior as MissionLogic)`; the cast yields null for a non-`MissionLogic` type, the null is appended, and `CheckMissionEnded` (10s cadence) NREs on the null slot ~10s into every battle. Only return `Logic` if the class actually inherits `MissionLogic`.
- **Why missed:** The original DLLs of the MixedFormations / SmartCavalryAI / SiegeDismount ports all declared `(MissionBehaviorType)1` = `Other` (correct); the 2026-05-07 port commits flipped them to `Logic` by cargo-culting a sibling template without checking the base class. A second wave shipped from the vendored `BehaviorTreeWrapper.dll`. RCA: `docs/reviews/rca-looter-battle-nre-2026-05-24.md`.
- **Prevent:** Default any `MissionBehavior` subclass to `BehaviorType => Other`; only `Logic` when it `: MissionLogic`. When porting, copy the original `BehaviorType` getter literally and resolve the numeric cast to a name by decompiling the **installed** enum (`Logic=0, Other=1` on both 1.3.15 and 1.4.5) — never infer the value from the bug story. Audit a source-grep of `BehaviorType` AND every vendored DLL under `Main/_Module/bin/...`. Regression test asserts `typeof(MissionLogic).IsAssignableFrom(typeof(BehaviorTreeMissionLogic))`.
- **Source:** memory/feedback_missionbehaviortype_logic_requires_missionlogic_inheritance.md

### Reconstructing a clamped value from its clamped result is impossible — cache the pre-clamp original
When a computation clamps/floors a value and a SECOND consumer later needs the pre-clamp original, it cannot be reverse-engineered from the clamped result — the clamp is lossy (many originals map to the same floored output). Cache the original at compute time and hand it to the second consumer.
- **Why missed:** the TroopWeight count→limit rework deflated the party-size limit by `ComputeSizePenalty = clamp(weighted−raw, 0, base−1)`, then the shed hook reconstructed `trueBase = deflated + surplus`. Correct in the UNCLAMPED branch; but when clamped, `deflated=1` regardless of the true base, so the reconstruction overshot (`121` vs true `100`) and the shed UNDER-trimmed exactly the elite-heavy post-upgrade parties it exists to police. The author reasoned the algebra from the unclamped case and never re-checked it against the clamp branch written 20 lines earlier. Data-flow deep-review agent caught it (2026-07-11).
- **Prevent:** any time a value is clamped/floored/saturated and something downstream needs the raw pre-clamp input, cache the input (a `ConditionalWeakTable<Entity,int>` for per-entity state) — do NOT recover it from the output. Fix: `ApplyPartySizeWeightPenalty` caches the pre-penalty base; the shed reads `GetTrueBaseSizeLimit`.
- **Source:** `docs/reviews/rca-troopweight-count-to-limit-2026-07-11.md` finding #1.

### Gating a feature OFF requires path enumeration, not layer gating
When adding a kill-switch, mission-type gate, or master toggle that must make a feature **inert**, enumerate every entry point the feature owns (Harmony patches, `MissionBehavior`s, `CampaignBehavior`s, GameModel overrides, UI handlers) and every path from each to an engine write. Gate at a point that dominates all of them, or gate each one. Gating the *service* and inferring the *feature* is covered is the trap: a boundary class doing "per-agent boundary work" with no service abstraction — a legitimate, documented TAOM pattern — is exactly the path that gets missed.
- **Why missed:** the siege-guard changeset (2026-07-16) gated `CavalryChargeService.HandleChargeOrder`/`Tick` on `IsFieldBattle`, because that was the path the crash hypothesis ran through (Patch31 postfix → native `SetPositioning`). But `SmartCavalryAIMissionBehavior.OnMissionTick` also calls `ApplyCollisionAvoidance`, which writes `agent.SetMovementDirection` per mounted unit per frame **bypassing the service entirely** — so with `AvoidFriendlies` (default `true`) the feature kept manipulating cavalry every frame in a siege, which is precisely what the change claimed to stop. An earlier research pass had even surfaced `ApplyCollisionAvoidance`, but only to refute a null-mount NRE theory; it was filed as "not a crash risk" and never re-examined as a *suppression* path. Only deep-review Agent 5 (Data Flow) caught it — Agents 1/3 are scoped to **changed files**, and the bypass lived in a file that was not in the diff.
- **Prevent:** grep the whole feature folder for direct engine writes (`Set*`/`Add*`/`Remove*` on `Agent`/`Formation`/`Team`) and confirm each is downstream of the gate. **Corollary: for a "turn this feature off" change, the diff is the wrong review scope** — brief the reviewer on the feature folder, not the changed files. This generalizes the deep-review Agent 5 "master-toggle fold check", whose scope covered *GameModel overrides* but not *behavior-class boundary work*.
- **Source:** `docs/reviews/rca-siege-guards-2026-07-16.md` finding #1.
### A GameModel override's disabled path must be `return base.<Method>(...)`, never a computed "off" value
Folding a master toggle inside the SERVICE is not the same as restoring vanilla. A `GameModel` is not "our feature" -- it is a policy oracle the ENGINE consults for every entity, including ones our feature never touched. BannerBearers (2026-07-16) folded `Enabled` correctly at the service layer (`GetDesiredBearerCount` returned 0 when off) and the model passed that 0 straight to the engine. Vanilla returns `1`. So with the feature "disabled", a formation bannered by vanilla's own hero-captain path (`GeneralsAndCaptainsAssignmentLogic`) or by the player's Order-of-Battle screen -- neither of which TAOM intercepts -- kept its banner item and got **zero bearers**: strictly worse than vanilla, not equal to it. Three sibling overrides leaked the same way (a tuned troop-count threshold the engine's own `CanFormationDeployBannerBearers` reads, a race gate narrowing vanilla's eligible set, an equipment backstop).
- **Why missed:** the reviewer reasoned "our feature does nothing when off" and treated that as identical to "the engine behaves as it would without us". The existing master-toggle-fold rule's worked example (CombatMechanics `GetHorseChargePenetration`, 2026-07-02) is about a config value read WITHOUT folding the toggle -- so a reviewer checking "does this override read `IsEnabled`?" ticks the box. This instance reads the toggle and still leaks, by answering with a toggle-aware `0` instead of deferring. "Folded the toggle" and "restored vanilla" are different properties; only the second is what the hint promises. Also: an override shaped like `=> _service.Value` LOOKS like the rule's legal "single expression" form, so it draws no scrutiny.
- **Prevent:** for every `GameModel` override, write the disabled path as literally `return base.<Method>(...)`. If an override cannot express its disabled path as a base call, it does not belong in that slot. At review time, check the *disabled return value* against vanilla's *actual* return value -- not merely that `IsEnabled` is read somewhere on the path.
- **Source:** docs/reviews/rca-banner-bearers-2026-07-16.md (findings 2/3/5/6). Second instance of the master-toggle-fold class after docs/reviews/rca-combat-mechanics-2026-07-02.md.

### An absolute count subtracted from an `ExplainedNumber` must be scaled into the BASE frame first
`ExplainedNumber.Add(v)` does `BaseNumber += v`; `AddFactor(v)` does `SumOfFactors += v`; the result is `BaseNumber * (1 + SumOfFactors)`. So **an `Add` is always multiplied by `(1 + SumOfFactors)` no matter when it runs** — factors are summed, not sequenced, and call ORDER cannot clamp or scope an `Add`. Any value you derived from `limit.ResultNumber` (or that is an absolute body/gold/item count) is in the RESULT frame; subtracting it raw over-applies it by `value × SumOfFactors` for every factor-boosted entity. Divide by `(1 + SumOfFactors)` before the `Add` — gate the scale as a positive requirement (`if (!(scale > eps)) return;`) so NaN and factor-cancelled (`SumOfFactors <= -1`) frames skip rather than poison. Corollary: a result-frame clamp (`penalty <= baseLimit - 1` to keep the limit ≥ 1) only holds if the subtraction is frame-converted; applied raw, the floor silently breaks for factor-boosted entities.
- **Why missed:** TroopWeight's count→limit rework (2026-07-11) shipped `limit.Add(-penalty)` with a comment reasoning that it was "applied last so the surplus is clamped against the feat/career-boosted limit" — reasoning about call ORDER without reading `ExplainedNumber`'s arithmetic. Ordering is irrelevant; the comment was actively teaching the wrong model. The 2026-07-11 5-agent review + Codex pass both read the method and neither opened the struct. Every factor-boosted culture (Mordor +10% … Goblin +40%) was over-taxed for 6 days.
- **Prevent:** when mutating an `ExplainedNumber` you did not construct, state in a comment which FRAME your value is in before calling `Add`/`AddFactor`. Unit-test the factor case explicitly — construct `new ExplainedNumber(base)`, `AddFactor(f)`, apply, assert the exact `ResultNumber` (a no-factor-only test passes on the buggy code). Known live sibling still unfixed: `CareerPassiveService.ApplyFlat` amplifies flat "+N party size" passives the same way (+6 reads as +8.4 at Goblin +40%) — deliberately out of scope, its own issue.
- **Source:** docs/reviews/rca-troopweight-result-frame-2026-07-17.md (finding 1). Signatures verified against installed v1.4.7 `TaleWorlds.CampaignSystem.ExplainedNumber` (`Add`:227, `AddFactor`:242, `_unclampedResultNumber`:149, `ResultNumber`:115).

### Guard the float→int CAST, not the integer arithmetic downstream of it
`(int)float.NaN` **and** `(int)float.PositiveInfinity` are both `int.MinValue` on net472/x64, and `int.MinValue - 1` underflows (unchecked — TAOM does not set `CheckForOverflowUnderflow`) to `int.MaxValue`. So a guard written as `int head = value - 1; if (head <= 0) return 0;` — intended to reject a degenerate value — reads a poisoned input as the LARGEST possible budget and returns a plausible-looking result. Verified on net472: `ComputeSizePenalty(10, 200, (int)float.NaN)` returned **190**, not 0. Validate finiteness at the cast (`FiniteFloatValidator.IsFinite`), and write the integer gate as a positive requirement on the value itself (`if (baseLimit < 2) return 0;`) so the subtraction is unreachable for degenerate inputs.
- **Why missed:** **5th instance of the NaN-gate class** (Career cooldown #31, EditorCacheRebuild #38, CS_Road 2026-05-13 — all CONFIG floats; CombatMechanics 2026-07-02 — ENGINE floats at decision gates; this — a float→int CAST). Each recurrence happened because the rule's scope was exactly one category narrower than the bug. Here the author applied the engine-float rule rigorously to the gate being *written* (`!(scale > MinFactorScale)`) and not at all to the pre-existing cast two lines above it in the same method. 4 of 5 deep-review agents structurally couldn't see it; the data-flow agent found it by tracing the float→int→guard path end to end.
- **Prevent:** the NaN-gate rule now covers three categories — config floats at load, engine floats at runtime decision gates, **and float→int casts feeding integer guards**. When touching ANY method, gate every float→decision path in it, not only the lines you added. Ask: "what int does this cast produce for NaN/±Inf, and does that value defeat any guard downstream?" Add one regression test per cast asserting the degenerate input yields the safe result. If a 6th instance appears in a category this list doesn't name, widen again rather than patch the instance.
- **Source:** docs/reviews/rca-troopweight-result-frame-2026-07-17.md (finding 3). Rule file: `.claude/rules/csharp-architecture.md` "Engine-Float Decision Gates".

---

### A cache key must capture everything the cached answer depends on
A memo keyed on a service method's *input* is only correct if that input determines the output. `MenuLinkStyleRewriter` cached (menu text -> rewritten text), but the rewrite depends on the linked objects' **culture**, reachable only through the lookup the cache exists to skip. Menu text is byte-identical before and after a culture conversion, and across a load of a different save — so the cache could return a stale faction colour with nothing to indicate it.
- **Why missed:** the key was chosen because it is what the function receives, not because it is what the answer depends on. The test asserted the cache *hit* (`ResolvesLookupOnlyOnce`), which pinned the defect as intended behaviour — a test written from the mechanism can only confirm the mechanism runs.
- **Prevent:** before adding a cache, write down what the output depends on and confirm the key covers all of it. A dependency outside the key needs an invalidation signal; with no cheap signal, do not cache. Then ask what the cache actually saves — if the guarded path runs once per UI interaction, delete it rather than fixing it. Any regression test must vary a *dependency* while holding the *key* constant.
- **Source:** MenuLinkColors deep review 2026-07-26 (HIGH). RCA: `docs/reviews/rca-menu-link-colors-2026-07-26.md`

---

### Clamp a cross-field invariant where the sources converge, not once per source
`WarOfTheRingService.GetEffectivePhaseDays` selects `(phase1Day, phase2Day)` from four sources — MCM sliders, JSON, JSON test-mode, compiled defaults. Only the JSON pair was validated, so an equal or inverted pair from any other source reached `CheckPhaseTransition`, whose two guards are sequential `if`s over an in-place `CurrentPhase` mutation: both transitions ran in one call and `IsengardWar` was never observable to the map meter, momentum service, or save. Per-source validation scales linearly with sources and drifts — N sources means N chances to forget the N+1th. Clamp once at the fan-in (`phase1 = max(1, phase1); phase2 = max(phase1+1, phase2)`), and keep per-source validation only for the job a silent clamp cannot do: **warning the author their input was wrong**.
- **Why missed:** **2nd instance of the dual-surface class** (CombatMechanics 2026-07-02 — JSON enforced `autoKnockdownWeightRatio >= neutralWeightRatio`, the MCM slider clamped to a bare `[2,30]` and recreated the exact state the JSON invariant prevented). Here the change was a four-constant retune, so it did not *present* as config-provider work and the validation rule never came to mind — even though all four constants are user-editable config and one is a live in-game slider. The validator's `<` also reads as correct in isolation; it is only wrong because of control flow in a different file. And every existing test pinned `IsAvailable = false` in `Setup()`, leaving the MCM branch at **zero** coverage behind a healthy-looking count of 25 tests.
- **Prevent:** (1) the config-validation rule is triggered by the **nature of the value**, not the size or shape of the diff — a one-line constant edit to a user-editable value carries it. (2) A guard whose correctness depends on control flow elsewhere must be reviewed with that file open. (3) Verify MCM min/max actually clamp — in MCM 5.12.1 `[SettingPropertyInteger]` bounds are UI-only metadata and `BaseSettingsJsonConverter.ReadJson` assigns with no range check, so a stale settings file bypasses them. (4) When a service selects among N sources, assert a test exercises each branch; test counts do not measure branch coverage.
- **Source:** WotR phase-day deep review 2026-07-30 (2 HIGH). RCA: `docs/reviews/rca-wotr-phase-ordering-2026-07-30.md`

---

### A doc comment citing a precedent is a claim, not evidence — open the file
`MomentumSettingsProvider` re-clamps every MCM value it exposes and documents itself as following the "TaomSettingsProvider precedent". `TaomSettingsProvider` implements no such thing — it is four raw pass-through properties. An author reading that comment would conclude the pattern was already established repo-wide and skip the check.
- **Why missed:** the comment was written by an author who assumed the sibling did what they were doing rather than reading it, and it then sat in the codebase asserting a false fact with the authority of a code comment. Nothing verifies comment claims.
- **Prevent:** when a comment, doc, or agent report cites a precedent, sibling, or "existing pattern" as justification, open the cited file before relying on it — the same evidence-over-claims discipline applied to review findings. When writing such a comment, cite the specific file and member you actually read.
- **Source:** WotR phase-day deep review 2026-07-30. RCA: `docs/reviews/rca-wotr-phase-ordering-2026-07-30.md`

<!-- backlinks-start auto-generated; edit lint_docs.py / build_backlinks.py to change -->

## Referenced by

- [docs/reviews/LESSONS-LEARNED.md](../LESSONS-LEARNED.md)

<!-- backlinks-end -->

### Gate the rule, not the call site

When gating a behaviour on an environment flag (co-op presence, feature toggle), grep every caller
of the underlying service method and every input path that reaches it BEFORE deciding where the
gate goes. Hand-gating N sites is only correct if you proved N.

**Why missed:** #370 gated TAOM's diplomacy vetoes at the Harmony prefixes. The same three rules
(`IsWarAllowed` / `ShouldBlockPeace` / `IsAllianceDecisionAllowed`) were also consumed by
`TaomKingdomDecisionPermissionModel` (3 overrides, reached from `DeclareWarDecision.IsAllowed()`)
and `TaomDiplomacyModel.IsAtConstantWar` — both left enforcing. The deep-review data-flow agent
found the first and missed the second, because `TaomDiplomacyModel` was not in the changeset and the
agent's scope was the diff. Same changeset: the time-acceleration UI was suppressed while the
E / Space / Ctrl+Space keybinds driving the same service stayed live.

**Prevent:** a caller-enumerating test that scans the whole source tree rather than the diff —
`CoopVetoClassificationTests.EveryConsumerOfADivergenceProneDiplomacyRule_AlsoReadsTheCoopFlag`.
Verify it is non-vacuous by confirming it detects the known consumers. Corollary: suppressing a
widget is not disabling a feature — gate the service, since the UI is one input path among several.
Corollary 2: when a changeset gates a shared rule, scope at least one review agent to *the rule*,
not to the changed files.

**Source:** `docs/reviews/rca-coop-veto-surface-2026-08-01.md` (#370, 3 HIGH).
