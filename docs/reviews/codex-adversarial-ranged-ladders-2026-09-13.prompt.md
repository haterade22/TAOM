You are an adversarial reviewer for TAOM, a Lord of the Rings total conversion of Mount and Blade II: Bannerlord v1.4.8. Assume this changeset has real bugs and find them. Every claim you make must be backed by a file path and line you read, or a decompiled engine body you read. Try to refute your own findings before reporting them. Report "UNVERIFIED" when you could not read the evidence; never guess.

FEATURE: Ranged range ladders (#582) plus the veteran militia step. Seven commits on branch bannerlord-1.4.5: d20838e4 (library, spec, both tools, tests), f46abb62 (16 troop files re-rostered: 258 launcher slot edits over 227 troops), 1083a8b0 (validator gate RANGED_LADDER_INVERSION), 29291bbd (docs), b2e058e8 (HTML report + tracked copy), d5e43caf (veteran militia +15 on every skill, 30 troops in 15 files), 02666e32 (review fixes: crossbow spread factor, HTML skeleton and no clock, ammo from the launcher's set, militia cache guard). This is Python tooling and XML data. There is NO C# in scope except a comment in TAOM.Tests/Features/TroopProgression/TroopUpgradeSkillMonotonicityTests.cs.

THE DESIGN IN ONE PARAGRAPH: an archer's reach is its launcher's missile_speed and nothing else (Mission.OnAgentShootMissile launches at the wielded bow's GetModifiedMissileSpeedForCurrentUsage; SandboxAgentStatCalculateModel pins MissileSpeedMultiplier at 1 for bows; Bow skill feeds accuracy, cadence and AI error only). Two rules per launcher class (Bow, Crossbow, never compared with each other): inside a LINE (a troop file, or an id prefix inside one, in kingdom rank order) a lower engine tier is never faster than a higher one; inside a BAND (engine tier E 0-2, R 3-4, V 5-6, X 7-8, C 9-10) a better-ranked line is never slower than a worse-ranked one. Both come from one grid in tools/ranged_ladders.json: speed = band_base[band] + rank_step * (18 - rank), band_base E58 R62 V66 X70 C74, rank_step 2, 18 lines. Every cell is a generated item ladder_<line>_<bow|xbow>_<band> (130 items, 13 folders of the LIVE LOTRLOME_Armory plus the lotraom-assets mirror), a verbatim clone of the line's donor bow with only id, name, missile_speed and is_merchandise=false changed; English name rows are inserted in the Armory's Languages/loc_<folder>.xml between TAOM-RANGED-LADDER markers. rebalance_ranged_ladders.py moved every Item0..Item3 bow/crossbow slot of every battle set onto its cell (ammo untouched). Separately, rebalance_troops.py now adds MILITIA_ELITE_BONUS=15 to every skill of the troops a culture binds to *_elite_militia_troop, and 30 troops were restatted.

TAOM ID CHEATSHEET:
Kingdom IDs: empire_w=Gondor, empire_s=Mordor, empire=Dunland, vlandia=Rohan, battania=Khand, aserai=Harad, khuzait=Easterlings, sturgia=Dale/North, erebor=Erebor, rivendell=Rivendell, lothlorien=Lothlorien, mirkwood=Mirkwood, isengard=Isengard, gundabad=Gundabad, dolguldur=DolGuldur, umbar=Umbar, shaghana=Shaghana, abanissa=Abanissa
Culture IDs (custom): gondor, mordor, erebor, rivendell, lothlorien, mirkwood, isengard, gundabad, dolguldur, umbar
Culture IDs (XSLT/vanilla): vlandia=Rohan, empire=Dunland, empire_w=Gondor, empire_s=Mordor, battania=Khand, aserai=Harad, khuzait=Easterlings, sturgia=Dale
Troop FILE tokens (the spec's `files` keys, NOT culture ids): dale, dolguldur, dunland, erebor, goblin, gondor, gundabad, harad, isengard, lindon, mirkwood, mordor, rhun_new, rivendell, rohan, umbar. NOTE: "rohan" is a valid FILE token (troops_rohan.xml) even though the culture id is vlandia; "rhun_new" is the file, "rhun" the Armory folder. "dol_guldur" is the Armory FOLDER, "dolguldur" the file and culture.

READ FIRST:
docs/features/ranged-ladders.md
docs/reviews/rca-ranged-ladders-2026-09-12.md
docs/reviews/rca-ranged-troops-report-2026-09-13.md
tools/ranged_ladders.json
tools/README.md "XML I/O convention" section
docs/features/troop-skill-balance.md (militia section)

KNOWN SUSPECTS (CONFIRM or DISPUTE each, with evidence):
1. line_of() precedence. A prefix claim wins over a file claim only when the prefix line also lists the file in `files` (ranged_ladder.line_of: `if not line.get("files") or troop.culture in line["files"]`). gondor_special lists files ["gondor"] and prefixes gondor_ith_/gondor_ithilien_/gondor_brv_. Is there any troop id in ANY file that matches a prefix of one line but sits in a file that line does not list, and would therefore fall through to the whole-file owner silently? Also: mordor_num and mordor_uruk both list files ["mordor"] with prefixes; the base `mordor` line lists ["mordor"] without prefixes; validate_spec's "claimed whole by both" check skips lines that carry prefixes. Can two prefix-carrying lines claim overlapping ids? (validate_spec now checks prefix overlap by startswith; does that catch `gondor_ith_` vs `gondor_ithilien_`? They do NOT overlap by startswith and target different troops; confirm no id matches both.)
2. troop_speed() takes the MAX missile_speed over a troop's battle sets, and planned_edits() rewrites every launcher slot in every set to the SAME cell, so after --apply every set is identical. But the engine draws each battle set independently at spawn (see the INCONSISTENT_ARMOUR_SLOT rule): is there any troop whose sets carried a bow in one set and a crossbow in another, and did the tool handle a troop that carries BOTH classes (a bow line and a crossbow line each get their own cell; does that troop's `sets` after rewrite still hold arrows for the bow and bolts for the crossbow in the right sets)? Enumerate such troops from the committed files.
3. The generated items are clones of donors with is_merchandise="false" and the donor's `culture=`, `difficulty=`, `value=`, `item_usage=` and weapon flags kept verbatim. The donor `wm_ithilien_bow` carries difficulty="100". Decompile the installed 1.4.8 DLLs and find EVERY reader of ItemObject.Difficulty, ItemObject.Culture and ItemObject.NotMerchandise (IsMerchandise): does any of them affect (a) what a regular troop spawns with, (b) the AI's weapon selection in mission (Agent.WieldInitialWeapons / EquipItemsFromSpawnEquipment / MissionEquipment), (c) loot after battle (LootCollector / MapEvent), (d) the recruitment or upgrade cost of the troop (DefaultPartyWageModel, DefaultPartyTroopUpgradeModel: do they read item VALUE? If value is copied verbatim from a 900-value elf bow onto a 58-speed clone, does anything price the troop by its equipment?), (e) `CharacterObject.GetBattlePower` / `Level` / `Tier` computed from equipment value (BasicCharacterObject.GetPower? Equipment.GetTotalValue?). Read the bodies.
4. Ammo class compatibility. A bow clone keeps ammo_class="Arrow"; the troop's other slots keep their arrows. But the ladder's crossbow line for a culture may have been rostered onto a troop whose ammo is bolts from a DIFFERENT usage, or the reverse. Also `item_usage` rides along: an Isengard crossbow clone keeps item_usage="crossbow" while a vanilla crossbow_c clone keeps its own; any troop whose new launcher's item_usage requires a reload animation the troop's skeleton/race lacks (dwarves, orcs, elves with vanilla usage strings)? Are there troops whose PREVIOUS launcher had a different item_usage than the new cell's donor (e.g. long_bow vs bow) and whether that changes anything at spawn (AI holster, two-hand idle, mounted use: a `long_bow` usage cannot be fired from horseback in vanilla; did any HORSE archer receive a cell whose donor is a long_bow-usage item? Enumerate: for each mounted troop (default_group HorseArcher, or a Horse slot in its sets), read the item_usage of its new ladder item and say whether vanilla allows shooting it mounted (WeaponFlags / item_usage "long_bow" + the CantReloadOnHorseback / bow-on-horse rules in Agent / MissionWeapon / ItemObject).
5. The loc rows: the generator inserts `<string id="ladder_..." text="..."/>` into Languages/loc_<folder>.xml of the live Armory. The engine loads `Languages/language_data.xml` LanguageFile entries: are the per-folder loc files the ENGLISH source, and does the engine read the English row for the id when the language is English, or does it read the inline {=id}text from the item XML? If both, which wins, and are the two texts identical (the generator writes the same string to both)? If language_data.xml does not list a file the generator wrote to, the rows are dead but harmless; confirm.
6. rebalance_troops.MILITIA_ELITE_BONUS is applied BEFORE cultural modifiers and specialization: `baseline = {k: v + 15}` then `+ mods` then apply_specialization (which swaps Bow/Crossbow for crossbow-armed militia and may floor). For isengard_militia_veteran_archer (crossbow-armed) confirm the committed values equal max(0, baseline21+15+mod) after the swap, and that the Bow/Crossbow swap did not put +15 on the wrong skill. Also check whether ANY caller of militia_troop_ids relies on the old single-cache early return semantics that the guard changed (taom_schema has its own regex and cache; fix_upgrade_armour_regressions and analyze_* call the function).
7. taom_schema._ranged_ladder_inversions reads the spec with rl.load_spec() (default path), loads troops with rl.load_ranged_troops(self.moduledata) and launchers from Registries.launchers built over item_roots. The MCP server tools/taom_mcp_server.py and the CLI both build registries through build_registries; but Validator tests construct Registries(...) directly with launchers omitted (defaulted {}), so the gate silently skips in every existing validator test: is there ANY test that exercises the gate through Validator.run() with launchers present? (tools/tests/test_ranged_ladder.py ValidatorGateTests call _ranged_ladder_inversions directly.) Is skipping when launchers is {} distinguishable from the install being absent, i.e. does the CLI print that the gate was skipped, as it does for other install-dependent checks?
8. generate_ranged_ladder_items.py --revert strips the loc block by regex and removes ranged_ladder.xml files; --apply on a folder whose loc template has CRLF and a block written LF (or vice versa): is the block's newline the file's DOMINANT newline (gsk.dominant_newline) and does `_LOC_BLOCK_RE` (`[ \t]*START.*?END[ \t]*\r?\n?`, DOTALL) leave a stray blank line or eat the line before `</strings>` on revert? Test with a temp file of each newline style.

FILES (read all):
tools/ranged_ladder.py
tools/ranged_ladders.json
tools/generate_ranged_ladder_items.py
tools/rebalance_ranged_ladders.py
tools/rebalance_troops.py (MILITIA_BINDING_RE, MILITIA_ELITE_BONUS, militia_troop_ids, elite_militia_troop_ids, calculate_skills, clamp_upgrade_monotonicity, write path)
tools/taom_schema.py (Registries.launchers, build_launchers, Validator._ranged_ladder_inversions, _RANGED_LADDER_EXEMPT; ignore the uncommitted CROSS_CULTURE_ARMOUR_INVERSION work from another session in the same file)
tools/generate_starter_kit.py (imported helpers only: read_xml, write_xml, checked_write, dominant_newline, index_items, registered_item_folders, _serialize)
tools/fix_upgrade_armour_regressions.py (write_changes, _rewrite_block only)
tools/tests/test_ranged_ladder.py
tools/tests/test_militia_elite_bonus.py
Main/_Module/ModuleData/troops/troops_*.xml (the 16 files; use `git show f46abb62 --stat` and `git show d5e43caf --stat`)
Main/_Module/ModuleData/taom_spcultures.xml and spcultures.xslt (militia bindings)
docs/reference/ranged-troops.html (generated; check it is a valid document and that its numbers match the troop XML for 3 troops of your choice)
The LIVE Armory: E:/Steam/steamapps/common/Mount & Blade II Bannerlord/Modules/LOTRLOME_Armory/ModuleData/LOTRLOME_items/<folder>/ranged_ladder.xml (13 files) and Languages/loc_<folder>.xml (the marker blocks), SubModule.xml (registered Items paths), Languages/language_data.xml

VANILLA CODE (installed 1.4.8; decompile with ilspycmd on "E:/Steam/steamapps/common/Mount & Blade II Bannerlord/bin/Win64_Shipping_Client/<Assembly>.dll" -t <Type>, or read E:/Decompiled_Bannerlord/_categories_v1.4.8/ which matches; the installed DLL is authoritative). Paste the relevant bodies as code blocks:
TaleWorlds.MountAndBlade.Mission.OnAgentShootMissile (missile speed source, damage bonus)
SandBox.GameComponents.SandboxAgentStatCalculateModel.GetWeaponInaccuracy and the MissileSpeedMultiplier writes in UpdateHumanStats
TaleWorlds.MountAndBlade.AgentStatCalculateModel.SetAiRelatedProperties (AiRangedHorsebackMissileRange, AiShootFreq)
TaleWorlds.Core.ItemObject (Difficulty, Culture, NotMerchandise readers), TaleWorlds.Core.WeaponComponentData (item_usage, ammo_class, MaxDataValue)
TaleWorlds.MountAndBlade.Agent.WieldInitialWeapons / Agent.EquipItemsFromSpawnEquipment, TaleWorlds.MountAndBlade.MissionEquipment (how the AI picks which launcher to draw when several are carried), and the mounted-use rules for long_bow item_usage
TaleWorlds.CampaignSystem.CultureObject.Deserialize (militia attributes), TaleWorlds.CampaignSystem.Settlements.Settlement militia spawn (AddTroopToMilitiaParty), DefaultSettlementMilitiaModel
TaleWorlds.CampaignSystem.GameComponents.DefaultCharacterStatsModel.GetTier and TAOM's Main/Features/TroopProgression/Models/TaomCharacterStatsModel.cs
TaleWorlds.Localization / MBTextManager: how {=id}text resolves against a loaded language file when the language is English

DEEP ANALYSIS (concrete scenarios, answer each):
A. Take dale_royal_crossbowman, mordor_uruk_archer and rider_of_himring. For each: the committed XML launcher id, the generated item's missile_speed, ammo_class and item_usage, the ammo the troop carries in the same set, and whether the pair is valid for the engine (arrow to bow, bolt to crossbow). Then compute the page's Speed, Reach, Spread and Total dmg for that troop from the formulas and compare with the row in docs/reference/ranged-troops.html.
B. Run `python tools/rebalance_ranged_ladders.py` (read-only by default; it writes only reports and the tracked HTML) and `python tools/generate_ranged_ladder_items.py --verify` and `python tools/validate_moduledata.py --code RANGED_LADDER_INVERSION` and report the output lines. Do NOT run --apply or --revert on anything.
C. Run `python -m unittest tools.tests.test_ranged_ladder tools.tests.test_militia_elite_bonus` and report the Ran line.
D. The spec's rank list encodes a design decision (Mirkwood best, Dunland last, Gondor split into a special line, Mordor into three). Do not review the ranking itself. DO check whether the grid produces any speed outside what the engine or vanilla data treats as sane: vanilla bows run 64 to 90, crossbows 55 to 97; the grid's top is 108. Is there any engine clamp, animation timing or physics assumption keyed to missile_speed (e.g. missile simulation step, hit detection, the shield-block or dodge logic) that a 108-speed arrow could break? Read the managed side; say UNVERIFIED where it is native.
E. The 30 militia: confirm `git show d5e43caf` changed only <skill value> lines, +15 each (three troops +10 on one skill floored at 0), no other attribute; and that no basic militia troop upgrades into anything other than its own veteran (an edge from a basic militia into a LINE troop whose skills are now below the militia's would be a UPGRADE_SKILL_REGRESSION).

CONFIG CROSS-REFERENCE:
For every `files` token in tools/ranged_ladders.json confirm a troops_<token>.xml exists; for every `folder` confirm the Armory SubModule.xml registers LOTRLOME_items/<folder>; for every donor id confirm it is defined exactly once across the Armory and vanilla items and is of the declared class; for every ladder_* id referenced in the 16 troop files confirm exactly one definition in exactly one ranged_ladder.xml. Report counts, not "looks fine".

FINDINGS OR OBSERVATIONS:
For each finding: severity (P1 ship-blocking, P2 real defect, P3 minor), file:line, what happens in game or in the tool, the evidence you read, the minimal fix. For each Known Suspect: CONFIRMED or DISPUTED with evidence. Then a section "What I could not verify" naming every claim you had to leave UNVERIFIED and why.

QUALITY GATES: no finding without a file:line or decompiled body; no "should" without a scenario; refute yourself before reporting; report counts you computed, not estimates; do not flag the ranking design, the +15 magnitude, or the choice to generate 130 items, those are user decisions; do not flag em dashes or prose; do not propose refactors without a defect.

PRIOR REVIEW LESSONS:
SUCCESSES: config ID cross-reference caught rohan/dol_guldur mismatches; decompiling the engine consumer caught cross-entity propagation the per-file reviews missed; asking what a second run produces caught non-reproducible generated output.
FAILURES: Codex assumed empire=Rohan (it is Dunland); flagged vanilla-matching code as bugs; skipped the hard decompile sections and reported "looks fine"; reported estimates as counts.

Output your review as markdown to stdout.
