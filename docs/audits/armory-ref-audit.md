# Armory reference audit

GENERATED by `tools/audit_armory_refs.py`, do not hand-edit. Re-run after any Armory sync; commit the result so the next diff is the change log. Feature doc: [armory-ref-audit.md](../features/armory-ref-audit.md).

- Run: 2026-09-19 at repo `83bdad85`
- Verdict: **CLEAN**
- Inventory: 4,611 tpacs, 16,442 metameshes, 8,259 physics shapes

## Broken mesh and body refs (a missing BODY is the #352 infinite load)

None. Every `mesh`, `holster_mesh`, `body_name` and collision body in the item XML resolves to a shipped asset.

## Catalogue drift since the committed `docs/reference/armory-catalogue/catalogue.tsv`

- RENAME: 0, MOVE: 0, MOVED same name: 0, DELETE: 0, NEW: 0
- Nothing the live XML names was deleted or renamed.

## New art nothing uses yet

None.

## Dead `Item.<id>` refs in TAOM ModuleData

None. Every item a troop, lord, roster or config names is defined.

## Generators vs shipped XML

- `generate_ranged_ladder_items.py --verify`: OK (exit 0) OK: all 123 ladder items present with their speed, damage and accuracy in 2 tree(s)
- `generate_starter_kit.py --verify --quiet`: OK (exit 0) OK: no drift

## How to read this

- A `MISSING_BODY` row hangs every mission that preloads a carrier: repoint the item to the art that ships (never restore a tpac beside its replacement), then re-run with `--regen-catalogue`.
- A `MISSING_MESH` row is an invisible item; same fix.
- A deleted or renamed mesh that is still named is tomorrow's row: fix it today.
- New art nothing uses is an audit signal for the roster authors, not a fault.

