# Baseline: TAOM-owned {=key} literals present in C# but absent from every
# ModuleData strings XML. An unregistered key renders correctly in English --
# MBTextManager.GetLocalizedText short-circuits and returns the inline default --
# and is untranslatable in the other eleven languages. See #434.
#
# This list may only SHRINK. UnregisteredLocalizationKeyBaselineTests asserts the
# live set is a subset of it, so a new unregistered key fails the build, and a
# fixed one must be deleted from here.
#
# EMPTY as of 2026-08-09. The 317 keys this file was created to hold were all
# registered by tools/harvest_literal_loc_keys.py and translated into the twelve
# languages, so the ratchet is now a hard gate: any literal {=taom_*} key without
# a ModuleData row fails the build. Re-run the harvester rather than adding a line
# here -- an entry is an admission that a string ships untranslatable, and needs an
# issue saying why.
